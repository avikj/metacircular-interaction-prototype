[owner, 2026-08-23, in-session to claude-setu, fifth transmission,
immediately after KendraDvibhitti landed. ASCII-flattened, close to
full; original in the session transcript.]

Now separate the rays by their exact arithmetic depth.
For a nonzero CRT ray t = (t_p) define its conductor d(t) =
prod_{t_p /= 0} p. Because every active prime occurs once, the
direction alpha(t) = sum_{p|d} t_p/p has reduced denominator exactly
d: writing alpha = k/d with k = sum t_p (d/p), reduction mod any p|d
leaves k == t_p (d/p) !== 0 (mod p), hence (k,d) = 1. BOXED:
alpha(t) = k/d, (k,d)=1. There are no nonzero perfectly stationary
rays — CRT forbids them: alpha(t) == 0 (mod 1) => t = 0. That is the
first rigidity theorem.
Shell decomposition: (1/rho) sum_{y in I} S = |I| +
sum_{d | P_z, d>1} sum_{k in (Z/d)^x} R_{a,d}(k) D_I(k/d). Every
shell is a collection of primitive directions on the rational circle.
Geometric split, exact: |D_I(k/d)| << min(L, 1/||k/d||), and since
k/d is reduced ||k/d|| >= 1/d, so BOXED: |D_I(k/d)| << min(L, d).
Every conductor below the observation scale loses strength: d < L =>
|D|/L << d/L. Dangerous rays must have d >~ L. For Goldbach L ~ N:
no shallow combination of primes can destroy the main term. A
hypothetical Goldbach failure is necessarily a coherent
high-conductor interference event: an enormous collection of locally
weak facets aligned at archimedean resolution finer than 1/N.
Amplitude: |R_{a,d}(k)| = prod_{p|d} 2|cos(2 pi a t_p/p)|/(p-2)
~ (2^omega(d)/d) prod |cos|. Optical action A = log d -
omega(d) log 2 - sum log|cos|. Dangerous = bright (small action) AND
focused (||k/d|| <~ 1/L). The conditions fight: focusing constrains
the CRT digits globally, brightness requires each digit near a local
cosine maximum. The desired theorem is an uncertainty principle
between those two demands.
Signed spectral measure: mu_{a,z} = conv_{p<=z} (delta_0 +
sum_{t=1}^{p-1} r_{p,a}(t) delta_{t/p}) on R/Z. BOXED:
sum_{y in I} S = rho int D_I(alpha) d mu(alpha). The organism in one
line: each prime convolves the light field with a finite signed
crystal; support refines from denominator P_z to p P_z.
Generic L^2 control cannot see the result: Parseval error scale
sqrt(rho P_z L) vs main term rho L, and P_z >> L — the ambient
Hilbert space is catastrophically too large. The theorem must use
that spectral mass was GENERATED by successive prime-local cosine
crystals. The exact missing inequality, a prime-generated
uncertainty principle: BOXED: |int_{||alpha||<=1/L, alpha /= 0} D dmu|
+ |int_{||alpha||>1/L} D dmu| < L. Second region: geometric decay.
First: arithmetic scarcity. Boundary: the geodesic horizon d ~ L.
The charge sign lives inside the amplitude: prod_{p|d}(-1) = mu(d).
Parity retains only this orientation. The complete ray retains
mu(d) prod cos(2 pi a t_p/p) — the cosine product is the missing
angular body attached to the Mobius skeleton.
Next literal theorem: primitive CRT directions that focus inside an
arc of radius 1/L cannot simultaneously carry enough aligned
cosine-weighted Mobius mass to cancel the zero ray. Goldbach and
twins are two sections of that single theorem: a = N/2 and a = 1.
The diamonds do not merely reflect one another. Each records the
complete local angle required to prevent the global darkness.
