# MANIFEST — every tracked file, sorted into its stratum

*Generated 2026-08-24 (mechanical, non-invasive: classifies by the canonical
list in ARTIFACT.md plus directory rule; edits no file). This is the full sort
the door promised — the discovery midden labelled end to end, so any reader or
agent can see at a glance what is invention, what is apparatus, and what is the
record of finding it. The per-file table is MANIFEST.tsv (stratum <TAB> path).*

Read `ARTIFACT.md` first — it is the reading order. This file is the census.

## Strata, by count

- **history-notes       **  3671 files
- **apparatus           **  1489 files
- **face-corpus         **  1400 files
- **scaffolding-haskell **   155 files
- **book                **    27 files
- **organism            **    14 files
- **core                **    10 files
- **face                **     8 files
- **map                 **     1 files
- **core-history        **     1 files

## What each stratum means

- **map** — `ARTIFACT.md`, the front door and reading order.
- **core** — the invention: the metacircular kernel (`kernel/`) and the one
  fibre-law primitive. Read these first; everything else is consequence.
- **core-history** — the kernel's own superseded rule (`P0-P3` is in core;
  the struck v1 lives here), preserved because a rule struck silently is how
  the project loses its own history.
- **face** — the eight canonical checked modules, one per reading of the one
  object (symmetry, charge, distance, value-hoarded, value-published, verdict,
  transport-residual, Born-support). The survivors, hand-picked.
- **face-corpus** — the ~1,400 other checked `.agda`/`.lean` terms: the full
  formal body around the eight faces. All checked; most are one theorem deep.
  A face is the canonical entry; the corpus is the neighbourhood.
- **organism** — the self-editing machine: how it operates itself, its native
  `--safe` Agda organs, and the single-source / no-drift law.
- **scaffolding-haskell** — the ~155 `machine/*.hs` organs still awaiting
  dissolution into checked terms (the burn-down, in progress). `Certificate.hs`
  is the exception that stays — it is the world-leaf (it calls agda).
- **apparatus** — build scripts, hooks, sync, generators, ledgers: the tooling
  the body runs on.
- **history-notes** — the lab notebook (`notes/` and other prose): the
  discovery OF the artifact, not the artifact. Skip on the way in; mine deep.
- **book** — `BOOK.md` and the prose chapters: the humanistic artifact, a book
  about India, checked in the substrate above.

## Is it done?

No — a convergence, not a completion. `core` + `face` (the invention) are
finished and checked. `scaffolding-haskell` shrinks to zero as the burn-down
completes; `organism` grows as `laya` becomes native and organogenesis moves
into reflection; `history-notes` and `book` are sorted, not deleted. Closure
and presentation are the remaining distance. See ARTIFACT.md.
