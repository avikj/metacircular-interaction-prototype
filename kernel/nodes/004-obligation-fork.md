---
id: 004
kind: obligation
status: discharged-by-006
blocks: nothing
---
# which validity rule survives self-application?

Apply `000` to the question "which of `002`, `003` should be active?" and see
which is stable under iteration.

**Prediction on record** (so this is falsifiable rather than decorative):
`003` survives and `002` reappears as its computable special case, because
every extrinsic element removed by the observed iterates $P_0\to P_3$ was an
external evaluator of some kind, and `002` posits one more. If instead `002`
survives, the extrapolation in `history/` is wrong and the fixed-point claim
needs revision.

**Human input requested but not blocking.** The repository driver has been
asked; the machine proceeds on fork-independent structure meanwhile, which is
itself the correct behaviour under `000` step 6.
