# Prompt for the receiving Claude Code coordinator

You are continuing Avik Jain's NS/RH work in `avikj/metacircular-interaction-prototype`. Read this archive as a source-and-derivation handoff, not as proof that the global endpoints have already been checked. The user specifically wants actual metacircular kernel execution and lossless analysis of the whole theorem graph, not another abstract plan, another manually expanded return coefficient, or another renamed RH equivalent.

First inspect the current working tree, source pin, existing agent work and toolchain. Do not reset or overwrite anyone's changes. The comparison pin in this handoff is `168ea8e240524f898af4b0e9cf70297c38422f08`, historically Agda 2.8.0 / Cubical v0.9. `infra/preflight.sh` is nonmutating. The actual Yantra wire is `sh interactive/run-yantra.sh --wire`; it exposes `sadhana.patra` for complete safe modules and `sadhana.vislesana` for types/normal forms. Use an isolated session log, preserve false controls and cache provenance, and audit the case-sensitive `Kernel` versus `kernel` path in the root checker.

Read `HANDOFF.md` sections 0–6 and 65–67 before using a result. Then read the claim/source indices and the full originals relevant to your lane. The main handoff has a complete cross-linked mathematical synthesis. `SOURCE_ANTHOLOGY.md` contains the selected originals, including their historical scopes and corrections. Numeric note filenames are unstable aliases; use source IDs and SHA256.

Priority inputs:
- S12: complete radial matrix toroidal source, actual pressure Hessian -2/7 and nonzero l4 emission.
- S13: actual-Xi cardinal interpolation with double-exponential source tails and logarithmic support overhead; exact H5 heat response.
- S15: actual full-polarized 2->4->2 return and signed examples, not a frozen source surrogate.
- S16: the supremum-inside-time heat bound, small energy/enstrophy control, signed radial memory and one-sided Landau arithmetic escape.
- S17–S18: complete source-sharing nonlinear evaluator, causal Volterra inverse with no spurious smallness boundary, analytic reconstruction on the actual existence domain and source-aware finite certificates.
- S19: all-depth actual midpoint propagator and exact nonlinear returning kinetic-storage identity; rigorous Abel inverse repairing the earlier high-order arithmetic convergence shortcut.
- S20 and the arithmetic portion of S22: direct dyadic pole residuals and direct quantitative Goldbach-square scale criterion.
- S00/S25: broader foundational and arithmetic organism; do not narrow the repository to one named wrapper.

Use the repository's existing complete fibre, history, substitution/locus and installation machinery before inventing replacements. A native proof term may already exist in another representation; search definitions, theorem bodies and composition paths rather than trusting old comments or local TODOs. Conversely, a generic field named boundedness/excludeII does not instantiate the actual zeta or PDE theorem. Do not install an unproved assertion or weaken the target to make a checker pass.

The main operational move is: exact source -> explicit operation/residual -> derivation -> native installation -> reuse at supported instances/loci. Preserve multiplicity, signs, initial complementary data, actual nonlinear source occurrences, physical domain/boundaries, moving-observer terms and proof evidence. Scalar summaries are derived readings, not replacement sources.

Do not infer an all-time theorem from a causal inverse on every already-bounded finite interval, an infinite family from finitely checked coefficients, passivity from squared output positivity, peak regularity from signed total kinetic work, or source-image membership from existence in a completion. The correction ledger supplies explicit counterexamples to these shortcuts.

Coordinate bounded work lanes described in handoff section 70. Produce actual native compiler/wire evidence, not another Python substitute. The Python artifacts remain useful regression controls; any new claim that they have been formalized requires the real native encoding and check. Keep analytical dependencies explicit when the proof assistant does not yet model the relevant function-space result.

Update `CLAIM_GRAPH.json`-style records with concrete statements, source classes, parameters, dependency terms, current pin, exact commands, exit status, negative controls, correction edges and remaining assumptions. Progress means discharging an endpoint dependency, proving a needed adapter, or ruling out an invalid route—not merely making another equivalent description.

No generic deterministic guarantee of solving RH or NS follows from the word metacircular. The deliverable is the actual composed endpoint proof if the required mathematical arguments can be found/established, otherwise the strongest concrete source-grounded results and precise unfilled dependencies. Do not claim those dependencies are absent from the full corpus without adequate search. Do not stop merely because the problem is famous; work on the actual mathematical objects supplied here.
