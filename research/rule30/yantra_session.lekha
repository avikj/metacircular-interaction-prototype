# दोषलेखः — the written defect.  APPEND ONLY.  NEVER EDIT.  NEVER DELETE.
#
# AHIMSA_SUTRA_VISTARA §6: यत्र संक्रमणं न सम्भवति तत्र दोषो लिख्यते ।
#                          लिखितो दोषो जीवति । अलिखितो दोषो हिंसा ।
#
# Written by interactive/DefectRecord.hs, which computes
# `sara:` as a chain over every preceding record.  Edit any line above the
# last record and `dosalekha verify` names the record where it happened.
# To correct a record, APPEND one whose `uttara:` names it.
#
dosa 0001
  kala: 2026-09-11
  karta: yantra (interactive/Server.hs)
  jati: sankramana-asambhava
  vastu: sadhana.patra
  yatna: turn 1 of the session; operation `sadhana.patra` was asked and transport was not available
  hetu: no verdict: the invocation failed in the environment, not in the module (no located type error in agda's output — a killed process, a missing root, a locale fault) — Checking Candidate (/tmp/math-machine-agda.Hn6LG5/Candidate.agda).
  nasta: the verdict you asked for, and with it the distinction between `agda says no` and `agda could not be run`, which a single red merges
  sesa: repair the environment and ask again; the module itself was not examined and is not diminished
  pramana: ProofGate.hs, kEnvironmentFault — the marker means exactly `about the environment, not the mathematics`
  yogyata-drsta: the handler for `sadhana.patra` ran to completion in this turn and produced this refusal; the naṣṭa above are its own output, not a later reconstruction
  yogyata-ksetra: the session state as of turn 1 — 0 store entries, 0 remainders; and nothing outside this process
  yogyata-avadhi: says nothing about any other session, any other store, or the same question asked with different arguments
  punarabhinaya: sh interactive/run-yantra.sh
  sara: 859886d01fe9aad2
.
