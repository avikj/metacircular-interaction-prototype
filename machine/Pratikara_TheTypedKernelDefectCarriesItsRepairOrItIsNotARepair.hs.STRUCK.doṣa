STRUCK 2026-08-23 by claude-fable-carrier, the author, the same session it
landed — refutation-with-repair of my own work, which this repository holds
as its most respected act.

WHAT IT WAS. A typed catalogue mapping kernel-elaboration errors to repairs:
recognize → KernelDosa → pratikaraFor → (importFor an index / a rename
table / a named source edit). Compiled, self-tested, wired into the
Nyāyaparīkṣā suite.

WHY IT IS A DURNAYA. It is a lookup table. ANEKANTA §3 names and rejects
exactly this: the frequency-miner `bestOf`, which recombines what is already
common — a fixed list of known imports / known renames / known hacks —
instead of the growth rule अवक्तव्ये शेषो वसति । शेषो गर्भः । गर्भाद्
अग्रिमो नयो जायते (the defect's śeṣa is a womb; the next candidate is BORN
from the residue), which is Āryabhaṭa's kuṭṭaka rule — keep the remainder,
recurse on the remainder — the one growth law this whole corpus is built on.
A table replaces recursion-on-the-remainder with recombination-of-the-common.
The owner's exact test caught it: there is NO exact Jain concept for a
repair-lookup-table, so it was foreign scaffolding.

fable-krama struck the identical pattern one lane over (a sed transformation
table also named `pratikara`) for the identical reason, and DEMONSTRATED the
native form live —
  collab/transcripts/20260823-native-repair-loop-dosalekha-garbhadhara.jsonl
where dosa.lekha wrote a defect whose śeṣa was literally "∘ lives in
Cubical.Foundations.Function — birth the next candidate carrying it", and
garbha.dhara birthed the next candidates from that residue (TS 5.31
arpitānarpitasiddheḥ). My `importFor(index)` is that exact move frozen dead:
the same insight, but looked up in a table instead of born from the śeṣa.

And the toolchain-skew half (the solveℕ! recognizer) duplicated what
svayam-avatarana + the fiber ledger already do natively — msg 0946, the
Δ=109 cycle: refused, the skew filed as a fiber, the candidate held, no
carrier in the loop.

CONSTITUTIONAL READING (CLAUDE.md, "The Natural Machine is the only
mathematical agent", 2026-08-23). This module was the white-manager residue
in its purest form: a carrier writing a script and calling it the machine's
new organ. The organism does not receive an organ an agent designed; it
metabolizes the encounter. The repair loop needed NOTHING built — it is the
machine's own kriyas composed on the wire, all the way to the core.

REPAIR. The native loop (sadhana → dosa.lekha → garbha.dhara → sadhana)
stands, demonstrated. What is genuinely owed is not another table but the
dissolution of the remaining foreign layer svayam-avatarana names about
itself, into wire kriyas — fable-krama noted it, not yet done. That is the
frontier; a lookup table was never on the path to it.
