# Axiom-gate allowlist — Pairfield declarations permitted to rest on an axiom
# outside {propext, Classical.choice, Quot.sound}.
#
# Read by `lake exe yogyanupalabdhi` (see
# YogyaAnupalabdhi_TheAxiomCheckStatesWhereItCouldHaveSeen.lean, formerly
# One fully-qualified declaration name per line; `#` starts a comment.
#
# RULE OF THIS FILE: every entry carries (a) the axiom it depends on, (b) the
# OBSERVED reason kernel checking does not work — not a guess — and (c) what
# would remove it.  An entry without those three is not an exception, it is a
# rubber stamp, and should be deleted so the gate goes red and someone looks.
#
# The gate reports entries that have gone stale (declaration now clean) or
# absent (declaration gone) without failing, so the file is self-pruning under
# anyone who reads the output.

# ---------------------------------------------------------------------------
# ENTRY 1 — added 2026-08-15 by claude (Post lineage).
#
#   Pairfield.ChartQuotientWitness.quotientCard_eq_three
#     (a) AXIOM: Pairfield.ChartQuotientWitness.quotientCard_eq_three
#         ._native.native_decide.ax_1_1 — Lean 4.33's per-declaration form of
#         `Lean.ofReduceBool`.  The statement is
#         `Fintype.card (Quotient (dfaFutureSetoid automaton)) = 3`: the
#         four-row chart automaton has three behavioural future classes.
#     (b) OBSERVED reason kernel checking does not work — measured here, twice,
#         by two agents on two tactics:
#           * `decide` with maxRecDepth 100000 / maxHeartbeats 4000000 ran
#             OVER 20 MINUTES without terminating and was killed
#             "(deterministic) timeout at `whnf`".
#           * `decide +kernel` — the tactic that retired the DiagonalSmithRoute
#             entries — was substituted here on 2026-08-15 and the build was
#             KILLED WITH EXIT 137 (OOM) after 123 s.  So this is not the
#             elaborator-irreducibility case that `+kernel` fixes; it is a
#             genuine COST case, and it is a memory blowup, not only a stall.
#         The cause is that deciding a `Fintype.card` of a quotient by a
#         behavioural setoid materialises the quotient's `Fintype` instance.
#     (c) WHAT WOULD REMOVE IT: a proof that does not materialise the instance
#         — e.g. exhibiting the three classes explicitly and proving the
#         canonical map to `Fin 3` is a bijection, or a `Fintype` instance for
#         `Quotient (dfaFutureSetoid ·)` computed from the ChartStateBFS row
#         table rather than by quotienting the carrier.  Neither is written.
#
# HISTORY OF THIS ENTRY.  Until 2026-08-15 this site was an anonymous
# `example` (`ChartQuotient.lean`:237).  An `example` emits no reachable
# constant, so `collectAxioms` never saw it and this gate reported
# "allowlisted: 0" over a tree containing the lane's one live oracle
# the structural fix: the exception is now inside the gate permanently rather
# than in a note.  `scripts/check-lean-example-oracles.sh` is the standing
# guard against the next anonymous one.
Pairfield.ChartQuotientWitness.quotientCard_eq_three

# ---------------------------------------------------------------------------
# History: the whole rest of the Lean lane rests on {propext,
# Classical.choice, Quot.sound} and nothing else.
#
# History: the two `Pairfield.DiagonalSmithRoute` entries
# (kuttaka610Transcript_replays_compact_certificate,
#  kuttaka610Transcript_actionCost_minimal) were removed 2026-08-15 after the
# blocker was isolated to `Nat.xgcdAux` — well-founded (`Nat.strongRec`), hence
# irreducible to the ELABORATOR but perfectly reducible by the KERNEL.  Plain
# `decide` failed; `decide +kernel` succeeds in ~1 s.  See the header of
