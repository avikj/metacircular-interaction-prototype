{-# OPTIONS --cubical --safe --no-import-sorts #-}

-- GENERATED by machine/TraceLibrary.hs from machine/replay.traces.
-- Do not hand-edit; regenerate.  Every lemma below is the machine's
-- own trace-replay output, which the kernel had already accepted and
-- the machine had already discarded.

module TraceCited where

open import Cubical.Foundations.Prelude
open import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)
addZero : (a : ℕ) → (a + zero) ≡ a
addZero zero = refl
addZero (suc a) = cong suc (addZero a)
addSuc : (a b : ℕ) → (a + suc b) ≡ suc (a + b)
addSuc zero b = refl
addSuc (suc a) b = cong suc (addSuc a b)
addAssoc : (a b c : ℕ) → ((a + b) + c) ≡ (a + (b + c))
addAssoc zero b c = refl
addAssoc (suc a) b c = cong suc (addAssoc a b c)
mulZero : (a : ℕ) → (a · zero) ≡ zero
mulZero zero = refl
mulZero (suc a) = mulZero a
mulSuc : (a b : ℕ) → (a · suc b) ≡ ((a · b) + a)
mulSuc zero b = refl
mulSuc (suc a) b =
  cong (λ h → suc (b + h)) (mulSuc a b)
  ∙ cong suc (sym (addAssoc b (a · b) a))
  ∙ sym (addSuc (b + (a · b)) a)

-- from the trace for  x = +(0,x)
trace0_ : (x : ℕ) → x ≡ (zero + x)
trace0_ zero = sym (addZero (zero))
trace0_ (suc x) = sym (cong (λ h → suc (h)) (sym (trace0_ (x)))) ∙ sym (addSuc (zero) (x))

-- from the trace for  0 = *(0,x)
trace1_lem0 : (x : ℕ) → x ≡ (zero + x)
trace1_lem0 x = refl
trace1_ : (x : ℕ) → zero ≡ (zero · x)
trace1_ zero = sym (mulZero (zero))
trace1_ (suc x) = sym (addZero (zero)) ∙ sym (cong (λ h → (h + zero)) (sym (trace1_ (x)))) ∙ sym (mulSuc (zero) (x))

-- from the trace for  x = *(s(0),x)
trace2_lem0 : (x : ℕ) → x ≡ (zero + x)
trace2_lem0 x = refl
trace2_lem2 : (x : ℕ) → zero ≡ (zero · x)
trace2_lem2 x = refl
trace2_ : (x : ℕ) → x ≡ (suc (zero) · x)
trace2_ zero = sym (mulZero (suc (zero)))
trace2_ (suc x) = sym (cong (λ h → suc (h)) (addZero (x))) ∙ sym (addSuc (x) (zero)) ∙ sym (cong (λ h → (h + suc (zero))) (sym (trace2_ (x)))) ∙ sym (mulSuc (suc (zero)) (x))

-- the flagship residual, discharged by a lemma the machine proved and discarded
flagshipResidual : (x : ℕ) → x ≡ x + zero · x
flagshipResidual x = sym (addZero x)
