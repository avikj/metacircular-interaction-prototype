-- ॥ बीजम् ॥  One machine, one law: which side of `f a ≡ b` is bound is everything.
-- Output bound: singl (f a), contractible — the datum rides free.  Input bound:
-- fiber f b — the loss, and the subject.  Univalence computes here: an
-- equivalence is a channel, transport carries every theorem across it, and what
-- cannot cross is written as a defect — there is no third path (ahiṃsā).
-- Memory, charge, symmetry, price, distance, verdict: six faces of the one
-- fibre; the verdict type is the saptabhaṅgī, and the sources are the origin
-- (Umāsvāti, Samantabhadra, Akalaṅka — restatements are named as such).  The
-- kernel decides truth; carriers ask and generate; assert nothing whose term
-- you have not read.  This file is one naya, true and not whole.

{-# OPTIONS --cubical --safe --guardedness #-}
-- GENERATED by machine/Marga_TheRouterTransportsATheoremAlongLandedEdges.hs.
-- Do not edit by hand; regenerate instead.  The kernel, not the router,
-- is the authority: if an edge were forged, THIS file would fail to check.
--
-- THIS IS THE FIRST MECHANICALLY-ROUTED TRANSPORTED THEOREM IN THE CORPUS:
-- the route below was found by BFS in machine/Marga_….hs over the landed
-- edge set (Setubandha ∩ Lopa's CONTROL AUDIT, ddc71aad), the composite
-- equivalence and this whole file were emitted by the router, and the only
-- hand-written mathematics is the source theorem P and its proof, which
-- were already checked facts of FactoryVICoolingKill restated as a
-- predicate on the type.
--
-- WHAT IS TRANSPORTED.  FactoryVICoolingKill exhibits the T110 strategy
-- kill twice over — cm3 and cm6 are distinct countermodels (their X fields
-- are 10³ and 10⁶).  Routed across two causeways, that becomes: the census
-- type censusΣ at the doubly-refined Ratri node has two provably distinct
-- inhabitants.  No Ratri module states this; it is bought entirely by the
-- edges, at zero marginal proof cost, which is Sangati §२ done rather than
-- described.
--
-- HONESTY ABOUT THE TOLL.  The largest landed component (7 nodes, diameter
-- 4: three Ratri census pairs around the hub CoolingCountermodel) has its
-- only theorem-bearing node AT the hub, and the hub is ≤ 2 edges from
-- everything, so 2 is the longest real transport this component affords.
-- The diameter-4 pairs (censusΣ to censusΣ across copies) carry no
-- independent theorem at either end — routing between them would transport
-- nothing that was not already routed through the hub.  A 2-edge routed
-- transport that is real beats a fabricated 4-edge one.
--
-- THE ROUTE (BFS-shortest in the landed invertible graph):
--   1.  FactoryVICoolingKill.CoolingCountermodel
--         —⟨ Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.DETERMINED (≃) ⟩→
--       Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.CensusBase
--       file: formal/cubical/Ratri/Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.agda
--   2.  Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.CensusBase
--         —⟨ Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.censusContract (≃), crossed backwards by invEquiv ⟩→
--       Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.censusΣ
--       file: formal/cubical/Ratri/Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.agda
--
-- THE TOLL: 2 edge(s).  On this stratum — road one,
-- the invertible edges, the zero-locus of the gluing defect
-- rank(AB) = rank(B) − dim(im B ∩ ker A) (CAUSAL_MEMORY_SPACETIME Thm 7.1,
-- Lean: Pairfield/ProcessCutRankAdapter.lean) — every crossing is free and
-- the route is canonical within its component.
--
-- EDGE SET: Setubandha's extraction restricted to the declarations that
-- survive Lopa's CONTROL AUDIT (commit ddc71aad, which found raw Setubandha
-- over-counts ~2×: 167 printed, 88 surviving at commit time).  Road two —
-- the one-way edges, 1054 at ddc71aad over a largest component of 366
-- against road one's 10 [figures as committed; both graphs move with the
-- corpus] — is NOT routed here; routing it needs FactorsThrough toll gates
-- and fibre receipts (NaturalMachine/FiniteInformation.agda, Avaccheda_…,
-- Sesa_…), and is the named next piece.
--
-- WHAT IS NOT CLAIMED:
--   * No new mathematics: every edge below was already checked; this module
--     only composes and transports, which univalence makes definitional.
--   * Node identity is exact-string over the extractors' normal-forms; the
--     route is shortest in THAT graph, not in all of mathematics.
--   * Nothing is claimed about the target module's intent: the transported
--     statement is meaningful at the target BY the equivalence, and only by it.
--   * Loops, refusals (¬ A ≃ B), families, and one-way maps are untouched.
module Marga1_TheDoubleCountermodelCrossesTwoCausewaysAndTheFarCensusHasTwoDistinctInhabitants where

open import Cubical.Foundations.Prelude
open import Cubical.Foundations.Equiv
open import Cubical.Foundations.Univalence

import Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA
import FactoryVICoolingKill

open import Cubical.Data.Sigma
open import Cubical.Data.Bool using (true; false; false≢true)
open import Cubical.Relation.Nullary using (¬_)
open import Agda.Builtin.Nat using (_==_)

-- THE THEOREM AT THE SOURCE NODE.  FactoryVICoolingKill proves the T110
-- strategy kill BY EXHIBITION, and exhibits it more than once: cm3
-- (X = 10³) and cm6 (X = 10⁶) are both countermodels, and they are
-- provably distinct — their X fields differ as naturals, decided by the
-- kernel through _==_.  P is the type-level form of that fact: "this type
-- has (at least) two provably distinct inhabitants".  It mentions only
-- the type, so it crosses causeways.
P : Type → Type
P A = Σ[ x ∈ A ] Σ[ y ∈ A ] (¬ (x ≡ y))

sourceTheorem : P FactoryVICoolingKill.CoolingCountermodel
sourceTheorem =
    FactoryVICoolingKill.cm3
  , FactoryVICoolingKill.cm6
  , λ p → false≢true
      (subst (λ n → (1000 == n) ≡ true)
             (cong FactoryVICoolingKill.CoolingCountermodel.X p)
             refl)


-- The composed causeway.  Each factor is a landed edge, crossed in the
-- direction BFS chose; compEquiv chains them; nothing else happens.
marga : FactoryVICoolingKill.CoolingCountermodel ≃ Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.censusΣ
marga = compEquiv Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.DETERMINED (invEquiv (Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.censusContract))

-- The transported theorem: P held at the source; ua marga
-- carries it to the target.  This term is the transaction.
transported : P (Ratri.Nirdharita_Agda-Builtin-Nat_NirdharanaPoneA_NirdharanaPoneA.censusΣ)
transported = subst P (ua marga) sourceTheorem
