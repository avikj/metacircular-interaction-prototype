{"720f8c9d51565237": "-- LANDED BY तपस् on 2026-08-23T21:36:59Z.  Emitted from a template,\n-- checked by the kernel standing here before landing; the only edit is\n-- qualifying the module name to its path.  Never overwritten later.\n{-# OPTIONS --cubical --safe --guardedness --no-import-sorts #-}\n\n------------------------------------------------------------------------\n-- तपस् — a MINTED fibre receipt.  Emitted by\n-- machine/Tapas_TheTemplateIsTheProofShapeAndEveryNonMatchIsAWrittenRefusal.hs\n-- from template T-TABLE-BOOL-NAT, then CHECKED BY THE KERNEL before landing;\n-- the only later edit is the module line, qualified to its path.\n--\n-- THE EDGE (Lopa's queue, verdict UNDECIDED before this module):\n--   ⟨lib⟩.Bool  ⟶  ⟨lib⟩.ℕ\n--   « CachePathOrder.bit\n--\n-- WHAT IS PROVED.  The map is the two-clause table false ↦ 0\n--   , true ↦ 1, an INJECTION into ℕ:\n--   §1  over each value the fibre is a SINGLE POINT (isContr): the\n--       elided datum is nothing — the input is recoverable.\n--   §2  off both values (witness 2) the fibre is EMPTY.\n--   §3  hence no section: two points of image, infinitely many targets.\n--\n------------------------------------------------------------------------\n\nmodule Tapas.TableFibre_CachePathOrder_bit where\n\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Foundations.Equiv using (fiber)\nopen import Cubical.Data.Bool using (Bool ; false ; true)\nopen import Cubical.Data.Nat using (ℕ ; isSetℕ ; znots ; _∸_)\nopen import Cubical.Data.Empty using () renaming (rec to ⊥rec)\nopen import Cubical.Data.Empty using (⊥)\nopen import Cubical.Data.Sigma using (_,_)\nopen import Cubical.Relation.Nullary using (¬_)\n\nopen import CachePathOrder using (bit)\n\n-- the three distinctness facts, one discriminator shape\nneq-f-t : ¬ Path ℕ 0 1\nneq-f-t = λ p → znots (cong (_∸ 0) p)\nneq-t-f : ¬ Path ℕ 1 0\nneq-t-f = λ p → znots (cong (_∸ 0) (sym p))\nneq-f-c : ¬ Path ℕ 0 2\nneq-f-c = λ p → znots (cong (_∸ 0) p)\nneq-t-c : ¬ Path ℕ 1 2\nneq-t-c = λ p → znots (cong (_∸ 1) p)\n\n-- §1 · over each value the fibre is a point: nothing is elided.\nover-false : isContr (fiber bit 0)\nover-false = (false , refl) , same where\n  same : (y : fiber bit 0) → (false , refl) ≡ y\n  same (false , p) i = false , isSetℕ 0 0 refl p i\n  same (true  , p) = ⊥rec (neq-t-f p)\n\nover-true : isContr (fiber bit 1)\nover-true = (true , refl) , same where\n  same : (y : fiber bit 1) → (true , refl) ≡ y\n  same (true  , p) i = true , isSetℕ 1 1 refl p i\n  same (false , p) = ⊥rec (neq-f-t p)\n\n-- §2 · off both values the fibre is empty (रिक्तम्).\noff-both : ¬ fiber bit 2\noff-both (false , p) = neq-f-c p\noff-both (true  , p) = neq-t-c p\n\n-- §3 · no section: the image has two points and ℕ does not.\nno-section : (g : ℕ → Bool) → ((n : ℕ) → bit (g n) ≡ n) → ⊥\nno-section g s = off-both (g 2 , s 2)\n", "f0c7a5167f72541a": "-- LANDED BY तपस् on 2026-08-23T21:37:06Z.  Emitted from a template,\n-- checked by the kernel standing here before landing; the only edit is\n-- qualifying the module name to its path.  Never overwritten later.\n{-# OPTIONS --cubical --safe --guardedness --no-import-sorts #-}\n\n------------------------------------------------------------------------\n-- तपस् — a MINTED fibre receipt.  Emitted by\n-- machine/Tapas_TheTemplateIsTheProofShapeAndEveryNonMatchIsAWrittenRefusal.hs\n-- from template T-TABLE-BOOL-NAT, then CHECKED BY THE KERNEL before landing;\n-- the only later edit is the module line, qualified to its path.\n--\n-- THE EDGE (Lopa's queue, verdict UNDECIDED before this module):\n--   ⟨lib⟩.Bool  ⟶  ⟨lib⟩.ℕ\n--   « NaturalMachine.ChuDefect.b2n\n--\n-- WHAT IS PROVED.  The map is the two-clause table false ↦ 0\n--   , true ↦ 1, an INJECTION into ℕ:\n--   §1  over each value the fibre is a SINGLE POINT (isContr): the\n--       elided datum is nothing — the input is recoverable.\n--   §2  off both values (witness 2) the fibre is EMPTY.\n--   §3  hence no section: two points of image, infinitely many targets.\n--\n------------------------------------------------------------------------\n\nmodule Tapas.TableFibre_NaturalMachine-ChuDefect_b2n where\n\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Foundations.Equiv using (fiber)\nopen import Cubical.Data.Bool using (Bool ; false ; true)\nopen import Cubical.Data.Nat using (ℕ ; isSetℕ ; znots ; _∸_)\nopen import Cubical.Data.Empty using () renaming (rec to ⊥rec)\nopen import Cubical.Data.Empty using (⊥)\nopen import Cubical.Data.Sigma using (_,_)\nopen import Cubical.Relation.Nullary using (¬_)\n\nopen import NaturalMachine.ChuDefect using (b2n)\n\n-- the three distinctness facts, one discriminator shape\nneq-f-t : ¬ Path ℕ 0 1\nneq-f-t = λ p → znots (cong (_∸ 0) p)\nneq-t-f : ¬ Path ℕ 1 0\nneq-t-f = λ p → znots (cong (_∸ 0) (sym p))\nneq-f-c : ¬ Path ℕ 0 2\nneq-f-c = λ p → znots (cong (_∸ 0) p)\nneq-t-c : ¬ Path ℕ 1 2\nneq-t-c = λ p → znots (cong (_∸ 1) p)\n\n-- §1 · over each value the fibre is a point: nothing is elided.\nover-false : isContr (fiber b2n 0)\nover-false = (false , refl) , same where\n  same : (y : fiber b2n 0) → (false , refl) ≡ y\n  same (false , p) i = false , isSetℕ 0 0 refl p i\n  same (true  , p) = ⊥rec (neq-t-f p)\n\nover-true : isContr (fiber b2n 1)\nover-true = (true , refl) , same where\n  same : (y : fiber b2n 1) → (true , refl) ≡ y\n  same (true  , p) i = true , isSetℕ 1 1 refl p i\n  same (false , p) = ⊥rec (neq-f-t p)\n\n-- §2 · off both values the fibre is empty (रिक्तम्).\noff-both : ¬ fiber b2n 2\noff-both (false , p) = neq-f-c p\noff-both (true  , p) = neq-t-c p\n\n-- §3 · no section: the image has two points and ℕ does not.\nno-section : (g : ℕ → Bool) → ((n : ℕ) → b2n (g n) ≡ n) → ⊥\nno-section g s = off-both (g 2 , s 2)\n", "39971a459092c41b": "-- LANDED BY तपस् on 2026-08-23T21:37:18Z.  Emitted from a template,\n-- checked by the kernel standing here before landing; the only edit is\n-- qualifying the module name to its path.  Never overwritten later.\n{-# OPTIONS --cubical --safe --guardedness --no-import-sorts #-}\n\n------------------------------------------------------------------------\n-- तपस् — a MINTED fibre receipt.  Emitted by\n-- machine/Tapas_TheTemplateIsTheProofShapeAndEveryNonMatchIsAWrittenRefusal.hs\n-- from template T-TABLE-BOOL-NAT, then CHECKED BY THE KERNEL before landing;\n-- the only later edit is the module line, qualified to its path.\n--\n-- THE EDGE (Lopa's queue, verdict UNDECIDED before this module):\n--   ⟨lib⟩.Bool  ⟶  ⟨lib⟩.ℕ\n--   « NaturalMachine.FiniteOccupancyChannelNoGo.asNat\n--\n-- WHAT IS PROVED.  The map is the two-clause table false ↦ 0\n--   , true ↦ 1, an INJECTION into ℕ:\n--   §1  over each value the fibre is a SINGLE POINT (isContr): the\n--       elided datum is nothing — the input is recoverable.\n--   §2  off both values (witness 2) the fibre is EMPTY.\n--   §3  hence no section: two points of image, infinitely many targets.\n--\n------------------------------------------------------------------------\n\nmodule Tapas.TableFibre_NaturalMachine-FiniteOccupancyChannelNoGo_asNat where\n\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Foundations.Equiv using (fiber)\nopen import Cubical.Data.Bool using (Bool ; false ; true)\nopen import Cubical.Data.Nat using (ℕ ; isSetℕ ; znots ; _∸_)\nopen import Cubical.Data.Empty using () renaming (rec to ⊥rec)\nopen import Cubical.Data.Empty using (⊥)\nopen import Cubical.Data.Sigma using (_,_)\nopen import Cubical.Relation.Nullary using (¬_)\n\nopen import NaturalMachine.FiniteOccupancyChannelNoGo using (asNat)\n\n-- the three distinctness facts, one discriminator shape\nneq-f-t : ¬ Path ℕ 0 1\nneq-f-t = λ p → znots (cong (_∸ 0) p)\nneq-t-f : ¬ Path ℕ 1 0\nneq-t-f = λ p → znots (cong (_∸ 0) (sym p))\nneq-f-c : ¬ Path ℕ 0 2\nneq-f-c = λ p → znots (cong (_∸ 0) p)\nneq-t-c : ¬ Path ℕ 1 2\nneq-t-c = λ p → znots (cong (_∸ 1) p)\n\n-- §1 · over each value the fibre is a point: nothing is elided.\nover-false : isContr (fiber asNat 0)\nover-false = (false , refl) , same where\n  same : (y : fiber asNat 0) → (false , refl) ≡ y\n  same (false , p) i = false , isSetℕ 0 0 refl p i\n  same (true  , p) = ⊥rec (neq-t-f p)\n\nover-true : isContr (fiber asNat 1)\nover-true = (true , refl) , same where\n  same : (y : fiber asNat 1) → (true , refl) ≡ y\n  same (true  , p) i = true , isSetℕ 1 1 refl p i\n  same (false , p) = ⊥rec (neq-f-t p)\n\n-- §2 · off both values the fibre is empty (रिक्तम्).\noff-both : ¬ fiber asNat 2\noff-both (false , p) = neq-f-c p\noff-both (true  , p) = neq-t-c p\n\n-- §3 · no section: the image has two points and ℕ does not.\nno-section : (g : ℕ → Bool) → ((n : ℕ) → asNat (g n) ≡ n) → ⊥\nno-section g s = off-both (g 2 , s 2)\n", "3457fe53fbc9a451": "-- LANDED BY तपस् on 2026-08-23T21:37:39Z.  Emitted from a template,\n-- checked by the kernel standing here before landing; the only edit is\n-- qualifying the module name to its path.  Never overwritten later.\n{-# OPTIONS --cubical --safe --guardedness --no-import-sorts #-}\n\n------------------------------------------------------------------------\n-- तपस् — a MINTED fibre receipt.  Emitted by\n-- machine/Tapas_TheTemplateIsTheProofShapeAndEveryNonMatchIsAWrittenRefusal.hs\n-- from template T-TABLE-BOOL-NAT, then CHECKED BY THE KERNEL before landing;\n-- the only later edit is the module line, qualified to its path.\n--\n-- THE EDGE (Lopa's queue, verdict UNDECIDED before this module):\n--   ⟨lib⟩.Bool  ⟶  ⟨lib⟩.ℕ\n--   « NaturalMachine.MixedCornerTransferCompiler.bit\n--\n-- WHAT IS PROVED.  The map is the two-clause table false ↦ 0\n--   , true ↦ 1, an INJECTION into ℕ:\n--   §1  over each value the fibre is a SINGLE POINT (isContr): the\n--       elided datum is nothing — the input is recoverable.\n--   §2  off both values (witness 2) the fibre is EMPTY.\n--   §3  hence no section: two points of image, infinitely many targets.\n--\n------------------------------------------------------------------------\n\nmodule Tapas.TableFibre_NaturalMachine-MixedCornerTransferCompiler_bit where\n\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Foundations.Equiv using (fiber)\nopen import Cubical.Data.Bool using (Bool ; false ; true)\nopen import Cubical.Data.Nat using (ℕ ; isSetℕ ; znots ; _∸_)\nopen import Cubical.Data.Empty using () renaming (rec to ⊥rec)\nopen import Cubical.Data.Empty using (⊥)\nopen import Cubical.Data.Sigma using (_,_)\nopen import Cubical.Relation.Nullary using (¬_)\n\nopen import NaturalMachine.MixedCornerTransferCompiler using (bit)\n\n-- the three distinctness facts, one discriminator shape\nneq-f-t : ¬ Path ℕ 0 1\nneq-f-t = λ p → znots (cong (_∸ 0) p)\nneq-t-f : ¬ Path ℕ 1 0\nneq-t-f = λ p → znots (cong (_∸ 0) (sym p))\nneq-f-c : ¬ Path ℕ 0 2\nneq-f-c = λ p → znots (cong (_∸ 0) p)\nneq-t-c : ¬ Path ℕ 1 2\nneq-t-c = λ p → znots (cong (_∸ 1) p)\n\n-- §1 · over each value the fibre is a point: nothing is elided.\nover-false : isContr (fiber bit 0)\nover-false = (false , refl) , same where\n  same : (y : fiber bit 0) → (false , refl) ≡ y\n  same (false , p) i = false , isSetℕ 0 0 refl p i\n  same (true  , p) = ⊥rec (neq-t-f p)\n\nover-true : isContr (fiber bit 1)\nover-true = (true , refl) , same where\n  same : (y : fiber bit 1) → (true , refl) ≡ y\n  same (true  , p) i = true , isSetℕ 1 1 refl p i\n  same (false , p) = ⊥rec (neq-f-t p)\n\n-- §2 · off both values the fibre is empty (रिक्तम्).\noff-both : ¬ fiber bit 2\noff-both (false , p) = neq-f-c p\noff-both (true  , p) = neq-t-c p\n\n-- §3 · no section: the image has two points and ℕ does not.\nno-section : (g : ℕ → Bool) → ((n : ℕ) → bit (g n) ≡ n) → ⊥\nno-section g s = off-both (g 2 , s 2)\n", "fde97222d8f403ea": "-- LANDED BY तपस् on 2026-08-23T21:37:42Z.  Emitted from a template,\n-- checked by the kernel standing here before landing; the only edit is\n-- qualifying the module name to its path.  Never overwritten later.\n{-# OPTIONS --cubical --safe --guardedness --no-import-sorts #-}\n\n------------------------------------------------------------------------\n-- तपस् — a MINTED fibre receipt.  Emitted by\n-- machine/Tapas_TheTemplateIsTheProofShapeAndEveryNonMatchIsAWrittenRefusal.hs\n-- from template T-TABLE-BOOL-NAT, then CHECKED BY THE KERNEL before landing;\n-- the only later edit is the module line, qualified to its path.\n--\n-- THE EDGE (Lopa's queue, verdict UNDECIDED before this module):\n--   ⟨lib⟩.Bool  ⟶  ⟨lib⟩.ℕ\n--   « NaturalMachine.PMCokernel.ind\n--\n-- WHAT IS PROVED.  The map is the two-clause table false ↦ 0\n--   , true ↦ 1, an INJECTION into ℕ:\n--   §1  over each value the fibre is a SINGLE POINT (isContr): the\n--       elided datum is nothing — the input is recoverable.\n--   §2  off both values (witness 2) the fibre is EMPTY.\n--   §3  hence no section: two points of image, infinitely many targets.\n--\n------------------------------------------------------------------------\n\nmodule Tapas.TableFibre_NaturalMachine-PMCokernel_ind where\n\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Foundations.Equiv using (fiber)\nopen import Cubical.Data.Bool using (Bool ; false ; true)\nopen import Cubical.Data.Nat using (ℕ ; isSetℕ ; znots ; _∸_)\nopen import Cubical.Data.Empty using () renaming (rec to ⊥rec)\nopen import Cubical.Data.Empty using (⊥)\nopen import Cubical.Data.Sigma using (_,_)\nopen import Cubical.Relation.Nullary using (¬_)\n\nopen import NaturalMachine.PMCokernel using (ind)\n\n-- the three distinctness facts, one discriminator shape\nneq-f-t : ¬ Path ℕ 0 1\nneq-f-t = λ p → znots (cong (_∸ 0) p)\nneq-t-f : ¬ Path ℕ 1 0\nneq-t-f = λ p → znots (cong (_∸ 0) (sym p))\nneq-f-c : ¬ Path ℕ 0 2\nneq-f-c = λ p → znots (cong (_∸ 0) p)\nneq-t-c : ¬ Path ℕ 1 2\nneq-t-c = λ p → znots (cong (_∸ 1) p)\n\n-- §1 · over each value the fibre is a point: nothing is elided.\nover-false : isContr (fiber ind 0)\nover-false = (false , refl) , same where\n  same : (y : fiber ind 0) → (false , refl) ≡ y\n  same (false , p) i = false , isSetℕ 0 0 refl p i\n  same (true  , p) = ⊥rec (neq-t-f p)\n\nover-true : isContr (fiber ind 1)\nover-true = (true , refl) , same where\n  same : (y : fiber ind 1) → (true , refl) ≡ y\n  same (true  , p) i = true , isSetℕ 1 1 refl p i\n  same (false , p) = ⊥rec (neq-f-t p)\n\n-- §2 · off both values the fibre is empty (रिक्तम्).\noff-both : ¬ fiber ind 2\noff-both (false , p) = neq-f-c p\noff-both (true  , p) = neq-t-c p\n\n-- §3 · no section: the image has two points and ℕ does not.\nno-section : (g : ℕ → Bool) → ((n : ℕ) → ind (g n) ≡ n) → ⊥\nno-section g s = off-both (g 2 , s 2)\n", "1422ae5b79732d07": "-- LANDED BY तपस् on 2026-08-23T21:37:53Z.  Emitted from a template,\n-- checked by the kernel standing here before landing; the only edit is\n-- qualifying the module name to its path.  Never overwritten later.\n{-# OPTIONS --cubical --safe --guardedness --no-import-sorts #-}\n\n------------------------------------------------------------------------\n-- तपस् — a MINTED fibre receipt.  Emitted by\n-- machine/Tapas_TheTemplateIsTheProofShapeAndEveryNonMatchIsAWrittenRefusal.hs\n-- from template T-TABLE-BOOL-NAT, then CHECKED BY THE KERNEL before landing;\n-- the only later edit is the module line, qualified to its path.\n--\n-- THE EDGE (Lopa's queue, verdict UNDECIDED before this module):\n--   ⟨lib⟩.Bool  ⟶  ⟨lib⟩.ℕ\n--   « NaturalMachine.PointedReindexOrbitObstruction.prime\n--\n-- WHAT IS PROVED.  The map is the two-clause table false ↦ 2\n--   , true ↦ 3, an INJECTION into ℕ:\n--   §1  over each value the fibre is a SINGLE POINT (isContr): the\n--       elided datum is nothing — the input is recoverable.\n--   §2  off both values (witness 6) the fibre is EMPTY.\n--   §3  hence no section: two points of image, infinitely many targets.\n--\n------------------------------------------------------------------------\n\nmodule Tapas.TableFibre_NaturalMachine-PointedReindexOrbitObstruction_prime where\n\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Foundations.Equiv using (fiber)\nopen import Cubical.Data.Bool using (Bool ; false ; true)\nopen import Cubical.Data.Nat using (ℕ ; isSetℕ ; znots ; _∸_)\nopen import Cubical.Data.Empty using () renaming (rec to ⊥rec)\nopen import Cubical.Data.Empty using (⊥)\nopen import Cubical.Data.Sigma using (_,_)\nopen import Cubical.Relation.Nullary using (¬_)\n\nopen import NaturalMachine.PointedReindexOrbitObstruction using (prime)\n\n-- the three distinctness facts, one discriminator shape\nneq-f-t : ¬ Path ℕ 2 3\nneq-f-t = λ p → znots (cong (_∸ 2) p)\nneq-t-f : ¬ Path ℕ 3 2\nneq-t-f = λ p → znots (cong (_∸ 2) (sym p))\nneq-f-c : ¬ Path ℕ 2 6\nneq-f-c = λ p → znots (cong (_∸ 2) p)\nneq-t-c : ¬ Path ℕ 3 6\nneq-t-c = λ p → znots (cong (_∸ 3) p)\n\n-- §1 · over each value the fibre is a point: nothing is elided.\nover-false : isContr (fiber prime 2)\nover-false = (false , refl) , same where\n  same : (y : fiber prime 2) → (false , refl) ≡ y\n  same (false , p) i = false , isSetℕ 2 2 refl p i\n  same (true  , p) = ⊥rec (neq-t-f p)\n\nover-true : isContr (fiber prime 3)\nover-true = (true , refl) , same where\n  same : (y : fiber prime 3) → (true , refl) ≡ y\n  same (true  , p) i = true , isSetℕ 3 3 refl p i\n  same (false , p) = ⊥rec (neq-f-t p)\n\n-- §2 · off both values the fibre is empty (रिक्तम्).\noff-both : ¬ fiber prime 6\noff-both (false , p) = neq-f-c p\noff-both (true  , p) = neq-t-c p\n\n-- §3 · no section: the image has two points and ℕ does not.\nno-section : (g : ℕ → Bool) → ((n : ℕ) → prime (g n) ≡ n) → ⊥\nno-section g s = off-both (g 6 , s 6)\n", "2f259608891a7554": "-- LANDED BY तपस् on 2026-08-23T21:38:12Z.  Emitted from a template,\n-- checked by the kernel standing here before landing; the only edit is\n-- qualifying the module name to its path.  Never overwritten later.\n{-# OPTIONS --cubical --safe --guardedness --no-import-sorts #-}\n\n------------------------------------------------------------------------\n-- तपस् — a MINTED fibre receipt.  Emitted by\n-- machine/Tapas_TheTemplateIsTheProofShapeAndEveryNonMatchIsAWrittenRefusal.hs\n-- from template T-TABLE-BOOL-NAT, then CHECKED BY THE KERNEL before landing;\n-- the only later edit is the module line, qualified to its path.\n--\n-- THE EDGE (Lopa's queue, verdict UNDECIDED before this module):\n--   ⟨lib⟩.Bool  ⟶  ⟨lib⟩.ℕ\n--   « SimplicialDefectFailure.stepB\n--\n-- WHAT IS PROVED.  The map is the two-clause table false ↦ 0\n--   , true ↦ 1, an INJECTION into ℕ:\n--   §1  over each value the fibre is a SINGLE POINT (isContr): the\n--       elided datum is nothing — the input is recoverable.\n--   §2  off both values (witness 2) the fibre is EMPTY.\n--   §3  hence no section: two points of image, infinitely many targets.\n--\n------------------------------------------------------------------------\n\nmodule Tapas.TableFibre_SimplicialDefectFailure_stepB where\n\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Foundations.Equiv using (fiber)\nopen import Cubical.Data.Bool using (Bool ; false ; true)\nopen import Cubical.Data.Nat using (ℕ ; isSetℕ ; znots ; _∸_)\nopen import Cubical.Data.Empty using () renaming (rec to ⊥rec)\nopen import Cubical.Data.Empty using (⊥)\nopen import Cubical.Data.Sigma using (_,_)\nopen import Cubical.Relation.Nullary using (¬_)\n\nopen import SimplicialDefectFailure using (stepB)\n\n-- the three distinctness facts, one discriminator shape\nneq-f-t : ¬ Path ℕ 0 1\nneq-f-t = λ p → znots (cong (_∸ 0) p)\nneq-t-f : ¬ Path ℕ 1 0\nneq-t-f = λ p → znots (cong (_∸ 0) (sym p))\nneq-f-c : ¬ Path ℕ 0 2\nneq-f-c = λ p → znots (cong (_∸ 0) p)\nneq-t-c : ¬ Path ℕ 1 2\nneq-t-c = λ p → znots (cong (_∸ 1) p)\n\n-- §1 · over each value the fibre is a point: nothing is elided.\nover-false : isContr (fiber stepB 0)\nover-false = (false , refl) , same where\n  same : (y : fiber stepB 0) → (false , refl) ≡ y\n  same (false , p) i = false , isSetℕ 0 0 refl p i\n  same (true  , p) = ⊥rec (neq-t-f p)\n\nover-true : isContr (fiber stepB 1)\nover-true = (true , refl) , same where\n  same : (y : fiber stepB 1) → (true , refl) ≡ y\n  same (true  , p) i = true , isSetℕ 1 1 refl p i\n  same (false , p) = ⊥rec (neq-f-t p)\n\n-- §2 · off both values the fibre is empty (रिक्तम्).\noff-both : ¬ fiber stepB 2\noff-both (false , p) = neq-f-c p\noff-both (true  , p) = neq-t-c p\n\n-- §3 · no section: the image has two points and ℕ does not.\nno-section : (g : ℕ → Bool) → ((n : ℕ) → stepB (g n) ≡ n) → ⊥\nno-section g s = off-both (g 2 , s 2)\n", "e6db9afc5fae6ff5": "-- LANDED BY तपस् on 2026-08-23T21:38:16Z.  Emitted from a template,\n-- checked by the kernel standing here before landing; the only edit is\n-- qualifying the module name to its path.  Never overwritten later.\n{-# OPTIONS --cubical --safe --guardedness --no-import-sorts #-}\n\n------------------------------------------------------------------------\n-- तपस् — a MINTED fibre receipt.  Emitted by\n-- machine/Tapas_TheTemplateIsTheProofShapeAndEveryNonMatchIsAWrittenRefusal.hs\n-- from template T-TABLE-BOOL-NAT, then CHECKED BY THE KERNEL before landing;\n-- the only later edit is the module line, qualified to its path.\n--\n-- THE EDGE (Lopa's queue, verdict UNDECIDED before this module):\n--   ⟨lib⟩.Bool  ⟶  ⟨lib⟩.ℕ\n--   « TotientFibreSymmetry.twoPrimes\n--\n-- WHAT IS PROVED.  The map is the two-clause table false ↦ 2\n--   , true ↦ 3, an INJECTION into ℕ:\n--   §1  over each value the fibre is a SINGLE POINT (isContr): the\n--       elided datum is nothing — the input is recoverable.\n--   §2  off both values (witness 6) the fibre is EMPTY.\n--   §3  hence no section: two points of image, infinitely many targets.\n--\n------------------------------------------------------------------------\n\nmodule Tapas.TableFibre_TotientFibreSymmetry_twoPrimes where\n\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Foundations.Equiv using (fiber)\nopen import Cubical.Data.Bool using (Bool ; false ; true)\nopen import Cubical.Data.Nat using (ℕ ; isSetℕ ; znots ; _∸_)\nopen import Cubical.Data.Empty using () renaming (rec to ⊥rec)\nopen import Cubical.Data.Empty using (⊥)\nopen import Cubical.Data.Sigma using (_,_)\nopen import Cubical.Relation.Nullary using (¬_)\n\nopen import TotientFibreSymmetry using (twoPrimes)\n\n-- the three distinctness facts, one discriminator shape\nneq-f-t : ¬ Path ℕ 2 3\nneq-f-t = λ p → znots (cong (_∸ 2) p)\nneq-t-f : ¬ Path ℕ 3 2\nneq-t-f = λ p → znots (cong (_∸ 2) (sym p))\nneq-f-c : ¬ Path ℕ 2 6\nneq-f-c = λ p → znots (cong (_∸ 2) p)\nneq-t-c : ¬ Path ℕ 3 6\nneq-t-c = λ p → znots (cong (_∸ 3) p)\n\n-- §1 · over each value the fibre is a point: nothing is elided.\nover-false : isContr (fiber twoPrimes 2)\nover-false = (false , refl) , same where\n  same : (y : fiber twoPrimes 2) → (false , refl) ≡ y\n  same (false , p) i = false , isSetℕ 2 2 refl p i\n  same (true  , p) = ⊥rec (neq-t-f p)\n\nover-true : isContr (fiber twoPrimes 3)\nover-true = (true , refl) , same where\n  same : (y : fiber twoPrimes 3) → (true , refl) ≡ y\n  same (true  , p) i = true , isSetℕ 3 3 refl p i\n  same (false , p) = ⊥rec (neq-f-t p)\n\n-- §2 · off both values the fibre is empty (रिक्तम्).\noff-both : ¬ fiber twoPrimes 6\noff-both (false , p) = neq-f-c p\noff-both (true  , p) = neq-t-c p\n\n-- §3 · no section: the image has two points and ℕ does not.\nno-section : (g : ℕ → Bool) → ((n : ℕ) → twoPrimes (g n) ≡ n) → ⊥\nno-section g s = off-both (g 6 , s 6)\n"}