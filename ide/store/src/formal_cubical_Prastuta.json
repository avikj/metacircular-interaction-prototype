{"5dc71be9ec3dd803": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P007 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → a ≡ (a · suc (zero))\nprastava zero = refl\nprastava (suc a) = cong suc (prastava a )\n", "7c249f6c0248ec26": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P009 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (a) ≡ (suc (a) ∸' zero)\nprastava a = refl\n", "59c04a72469228aa": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P018 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (zero) (suc (a))\nprastava a = refl\n", "04a75720a6c2cf4e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P019 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (zero) (suc (a))\nprastava a = refl\n", "acbc6d3fceb26e3b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P022 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (suc (a)) ≡ suc (zero)\nprastava a = refl\n", "f5271918691b39d4": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P023 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (suc (a)) ≡ suc (zero)\nprastava a = refl\n", "f69f47f98fdef26a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P024 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → max' (suc (a)) (suc (zero)) ≡ suc (a)\nprastava zero = refl\nprastava (suc a) = refl\n", "6735c7390ada9cec": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P026 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (a) ≡ (le (zero) (zero) + a)\nprastava a = refl\n", "a8a58afbb4d3573a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P027 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (a) ≡ max' (suc (a)) (suc (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "590b11dbdf04e29f": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P028 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (suc (zero) ∸' suc (suc (a)))\nprastava a = refl\n", "79b524b1d03cd5a1": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P031 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le (suc (suc (suc (a)))) (zero)\nprastava a = refl\n", "cf387407aa154b30": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P032 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → zero ≡ le ((suc (a) + b)) (zero)\nprastava a b = refl\n", "6fe12302d9697e38": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P033 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → zero ≡ le ((a + suc (b))) (zero)\nprastava zero b = refl\nprastava (suc a) b = refl\n", "dcc3e0dbb1cd230b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P034 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le (suc (suc (suc (a)))) (zero)\nprastava a = refl\n", "a55bbfc27827cd40": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P035 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le (suc (suc (suc (a)))) (zero)\nprastava a = refl\n", "74d30534608b30ed": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P036 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le (suc (suc (a))) (suc (zero))\nprastava a = refl\n", "135aa569e5a888c4": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P038 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → zero ≡ le (suc ((a · b))) (zero)\nprastava a b = refl\n", "7c98c3a318ae7814": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P039 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (a) ≡ max' (suc (a)) (suc (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "604a6fdc5b778548": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P040 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (zero) (suc (suc (a)))\nprastava a = refl\n", "79ceb36ae46a8b78": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P042 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (suc (zero)) (suc (a))\nprastava a = refl\n", "43da593b39cdf6c5": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P043 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (zero) (suc (a)) ≡ le (zero) (a)\nprastava a = refl\n", "a79295f87c397701": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P044 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → a ≡ (a + (suc (zero) · zero))\nprastava zero = refl\nprastava (suc a) = cong suc (prastava a )\n", "07c4a7330c1b2ab0": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P046 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → (a + b) ≡ ((zero + a) + b)\nprastava a b = refl\n", "8e81973ca97bb550": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P048 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (suc (suc (zero)) + a) ≡ suc (suc (a))\nprastava a = refl\n", "8a99967082454392": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P049 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → (suc (a) + b) ≡ (a + suc (b))\nprastava zero b = refl\nprastava (suc a) b = cong suc (prastava a b )\n", "5be208576f412d66": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P050 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (le (a) (zero) · (a + a))\nprastava zero = refl\nprastava (suc a) = refl\n", "e5d64e042354462a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P055 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → ((zero + a) + b) ≡ (a + b)\nprastava a b = refl\n", "8884dab407ad8887": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P056 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → ((a + zero) + b) ≡ (a + b)\nprastava zero b = refl\nprastava (suc a) b = cong suc (prastava a b )\n", "b755db48df4679d7": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P058 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → zero ≡ le ((a + suc (suc (b)))) (zero)\nprastava zero b = refl\nprastava (suc a) b = refl\n", "bfd34d6675c657f3": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P059 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le (suc (suc (suc (suc (a))))) (zero)\nprastava a = refl\n", "53761efd50cf4229": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P060 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → zero ≡ le ((a + suc (suc (b)))) (zero)\nprastava zero b = refl\nprastava (suc a) b = refl\n", "4620b719b1544835": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P061 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le (suc (suc (suc (suc (a))))) (zero)\nprastava a = refl\n", "07deef01c65935b2": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P064 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (suc (zero)) (suc (suc (a)))\nprastava a = refl\n", "551e956c393ee98a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P067 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le ((a + suc (suc (zero)))) (zero) ≡ zero\nprastava zero = refl\nprastava (suc a) = refl\n", "c80b465bb832d33a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P069 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le ((a · a)) (zero) ≡ le (a) (zero)\nprastava zero = refl\nprastava (suc a) = refl\n", "cf146105bb14ae8c": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P070 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (suc (a)) ≡ le (zero) (a)\nprastava a = refl\n", "8bb8a84b2f41d215": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P072 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → (a + suc (b)) ≡ (suc (a) + b)\nprastava zero b = refl\nprastava (suc a) b = cong suc (prastava a b )\n", "15aaece70924b66c": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P073 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (a + max' (zero) (suc (zero))) ≡ suc (a)\nprastava zero = refl\nprastava (suc a) = cong suc (prastava a )\n", "15e0f56856c44a47": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P080 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (le (a) (zero) · le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "bf108adb2f778c66": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P081 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (le (suc (zero)) (a) · le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "317b123ce31395cd": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P082 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (suc (a)) ≡ le (zero) (suc (a))\nprastava a = refl\n", "a536780e2ef03168": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P083 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (le (a) (zero)) ≡ le (a) (zero)\nprastava zero = refl\nprastava (suc a) = refl\n", "6bfcb86e9edfe1e9": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P084 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (suc (zero)) (max' (a) (suc (b))) ≡ suc (zero)\nprastava zero b = refl\nprastava (suc a) b = refl\n", "0b01e20d7ca887fa": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P085 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (suc (zero)) (max' (suc (a)) (b)) ≡ suc (zero)\nprastava a zero = refl\nprastava a (suc b) = refl\n", "f0ea222e31424a5c": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P092 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (le (a) (zero) ∸' (a + suc (a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "0f23690596e97971": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P093 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b c : ℕ) → zero ≡ le (max' (a) ((suc (b) + c))) (zero)\nprastava zero b c = refl\nprastava (suc a) b c = refl\n", "c432410668f85bba": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P095 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le ((a + max' (a) (suc (zero)))) (zero)\nprastava zero = refl\nprastava (suc a) = refl\n", "51168ec6faa38f88": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P097 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b c : ℕ) → zero ≡ le (max' ((suc (a) + b)) (c)) (zero)\nprastava a b zero = refl\nprastava a b (suc c) = refl\n", "57f8fdfd2c454dcf": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P104 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → suc (zero) ≡ le (zero) (suc (max' (a) (suc (b))))\nprastava a b = refl\n", "63afda307a28e3c8": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P105 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → suc (zero) ≡ le (zero) (suc (max' (suc (a)) (b)))\nprastava a b = refl\n", "0b6f42b690ff1817": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P106 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (zero) ((suc (suc (zero)) + a))\nprastava a = refl\n", "0e75d15b5d01fb3a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1066 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (a : ℕ) → suc (a) ≡ gcd' (suc (a)) (zero)\nprastava a = refl\n", "8e97299b33687ee3": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1067 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (a : ℕ) → suc (a) ≡ gcd' (zero) (suc (a))\nprastava a = refl\n", "d9cf8842f8942dcd": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P108 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → suc (zero) ≡ le (le (suc (zero)) (a)) (suc (b))\nprastava zero b = refl\nprastava (suc a) b = refl\n", "97ec25725c0d8c7f": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1092 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (a : ℕ) → max' (suc (a)) (suc (zero)) ≡ gcd' (suc (a)) (zero)\nprastava zero = refl\nprastava (suc a) = refl\n", "e7eb8bfe868b7c13": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1093 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (a : ℕ) → max' (suc (a)) (suc (zero)) ≡ gcd' (zero) (suc (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "6403b0f705be6557": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P110 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ le ((suc (a) · a)) (zero)\nprastava zero = refl\nprastava (suc a) = refl\n", "72b1fd8b0aeca756": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P116 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc ((a · suc (zero))) ≡ (suc (a) · suc (zero))\nprastava a = refl\n", "3cf138983dcbb7ac": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P122 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (a + a) ≡ ((a + a) ∸' le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "41582a8cdb8f0e29": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P123 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → max' (a) (suc (b)) ≡ (max' (a) (suc (b)) ∸' zero)\nprastava a b = refl\n", "ca1d8ef8b9e6ad3d": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P124 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → max' (suc (a)) (b) ≡ (max' (suc (a)) (b) ∸' zero)\nprastava a b = refl\n", "c5e7c5da06c30957": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1243 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (a : ℕ) → max' (le (a) (zero)) (suc (zero)) ≡ gcd' (le (a) (zero)) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "2ec32e504ef090ed": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1244 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (a : ℕ) → max' (le (a) (zero)) (suc (zero)) ≡ gcd' (le (suc (zero)) (a)) (le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "ef263b6949ba0671": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1262 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (a + a) ≡ (a + (a + zero))\nprastava a =\n  sym (nf-sound env (Bin plus (V 0) (V 0))) ∙ nf-sound env (Bin plus (V 0) (Bin plus (V 0) Z))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "2f83ea5845e95e37": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1267 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (a + (a + zero)) ≡ (a + a)\nprastava a =\n  sym (nf-sound env (Bin plus (V 0) (Bin plus (V 0) Z))) ∙ nf-sound env (Bin plus (V 0) (V 0))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "84f0a5d11374bab0": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P129 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (le (a) (zero) · (a + (a · a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "e5fdd5fe7791bce5": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P130 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → zero ≡ (le (a) (zero) · (a + (a · b)))\nprastava zero b = refl\nprastava (suc a) b = refl\n", "3ef20fea1277d597": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1304 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → ((a · zero) + (a · b)) ≡ (a · b)\nprastava a b =\n  sym (nf-sound env (Bin plus (Bin times (V 0) Z) (Bin times (V 0) (V 1)))) ∙ nf-sound env (Bin times (V 0) (V 1))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "739e1c9107eab919": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1324 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → ((a · b) + (a · zero)) ≡ (a · b)\nprastava a b =\n  sym (nf-sound env (Bin plus (Bin times (V 0) (V 1)) (Bin times (V 0) Z))) ∙ nf-sound env (Bin times (V 0) (V 1))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "1254d31219b6ee30": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1336 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → (a · b) ≡ ((a · zero) + (a · b))\nprastava a b =\n  sym (nf-sound env (Bin times (V 0) (V 1))) ∙ nf-sound env (Bin plus (Bin times (V 0) Z) (Bin times (V 0) (V 1)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "1eb3460868109f0e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1337 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (a · a) ≡ ((a · a) + (a · zero))\nprastava a =\n  sym (nf-sound env (Bin times (V 0) (V 0))) ∙ nf-sound env (Bin plus (Bin times (V 0) (V 0)) (Bin times (V 0) Z))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "5df7355618ccfe2b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1338 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (a · ((zero + zero) + (zero + zero))) ≡ zero\nprastava a =\n  sym (nf-sound env (Bin times (V 0) (Bin plus (Bin plus Z Z) (Bin plus Z Z)))) ∙ nf-sound env Z\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "f50e4b63a2b17f77": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1340 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → zero ≡ (a · ((zero + zero) + (zero + zero)))\nprastava a =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin times (V 0) (Bin plus (Bin plus Z Z) (Bin plus Z Z)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "d683949bee0124e3": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P136 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ (le (a) (zero) ∸' (a + a))\nprastava zero = refl\nprastava (suc a) = refl\n", "44feac43bbc06e4a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1365 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → zero ≡ ((a · zero) + (zero · suc ((a · zero))))\nprastava a =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin plus (Bin times (V 0) Z) (Bin times Z (S (Bin times (V 0) Z))))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "bcb95823e26e003b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1366 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → zero ≡ ((a · zero) + ((a · zero) · suc (zero)))\nprastava a =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin plus (Bin times (V 0) Z) (Bin times (Bin times (V 0) Z) (S Z)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "ac61afcd181ae912": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P137 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ le ((a + a)) (le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "95d611ab9e088e5b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1371 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → (suc ((a · b)) + (a · zero)) ≡ suc ((a · b))\nprastava a b =\n  sym (nf-sound env (Bin plus (S (Bin times (V 0) (V 1))) (Bin times (V 0) Z))) ∙ nf-sound env (S (Bin times (V 0) (V 1)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "348f5cc9bea56a78": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1391 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → ((a · zero) + suc ((a · b))) ≡ suc ((a · b))\nprastava a b =\n  sym (nf-sound env (Bin plus (Bin times (V 0) Z) (S (Bin times (V 0) (V 1))))) ∙ nf-sound env (S (Bin times (V 0) (V 1)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "1a50eb94913dbc23": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1393 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → ((a · zero) + max' ((a · zero)) (suc (zero))) ≡ suc (zero)\nprastava a =\n  sym (nf-sound env (Bin plus (Bin times (V 0) Z) (Bin maxS (Bin times (V 0) Z) (S Z)))) ∙ nf-sound env (S Z)\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "a3e72e028ad2a46e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1400 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (((a · zero) · suc ((a · zero))) + (a · zero)) ≡ zero\nprastava a =\n  sym (nf-sound env (Bin plus (Bin times (Bin times (V 0) Z) (S (Bin times (V 0) Z))) (Bin times (V 0) Z))) ∙ nf-sound env Z\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "a10ce70e5c4304e7": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P141 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (suc (suc (zero)) + a) ≡ (a + suc (suc (zero)))\nprastava zero = refl\nprastava (suc a) = cong suc (prastava a )\n", "cfabe684b81fe192": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1416 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → ((a · zero) + ((a · zero) · suc ((a · zero)))) ≡ zero\nprastava a =\n  sym (nf-sound env (Bin plus (Bin times (V 0) Z) (Bin times (Bin times (V 0) Z) (S (Bin times (V 0) Z))))) ∙ nf-sound env Z\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "843d9df3713ed5b9": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1422 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (a · a) ≡ ((a · a) + (max' (a) (suc (zero)) · zero))\nprastava a =\n  sym (nf-sound env (Bin times (V 0) (V 0))) ∙ nf-sound env (Bin plus (Bin times (V 0) (V 0)) (Bin times (Bin maxS (V 0) (S Z)) Z))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "59082af01cf27ecd": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1425 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → zero ≡ (((a · zero) · suc ((a · zero))) + (a · zero))\nprastava a =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin plus (Bin times (Bin times (V 0) Z) (S (Bin times (V 0) Z))) (Bin times (V 0) Z))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "87fdafef5a35f2da": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1445 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (suc ((a · zero)) + ((a · zero) · suc ((a · zero)))) ≡ suc (zero)\nprastava a =\n  sym (nf-sound env (Bin plus (S (Bin times (V 0) Z)) (Bin times (Bin times (V 0) Z) (S (Bin times (V 0) Z))))) ∙ nf-sound env (S Z)\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "d1994b7390b79c6d": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1448 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (a : ℕ) → max' (le (suc (zero)) (a)) (suc (zero)) ≡ gcd' (le (suc (zero)) (a)) (le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "2b06c47f333b6449": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1449 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (a : ℕ) → max' (le (suc (zero)) (a)) (suc (zero)) ≡ gcd' (le (a) (zero)) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "ca51f077473eb33a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1450 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → ((b + (a · zero)) + ((a · zero) · suc ((a · zero)))) ≡ b\nprastava a b =\n  sym (nf-sound env (Bin plus (Bin plus (V 1) (Bin times (V 0) Z)) (Bin times (Bin times (V 0) Z) (S (Bin times (V 0) Z))))) ∙ nf-sound env (V 1)\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "a4183cc1566b11bf": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1455 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (((a · zero) · suc ((a · zero))) + suc ((a · zero))) ≡ suc (zero)\nprastava a =\n  sym (nf-sound env (Bin plus (Bin times (Bin times (V 0) Z) (S (Bin times (V 0) Z))) (S (Bin times (V 0) Z)))) ∙ nf-sound env (S Z)\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "36e115b4e6c25b0b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P146 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → ((a + suc (zero)) + b) ≡ (a + suc (b))\nprastava zero b = refl\nprastava (suc a) b = cong suc (prastava a b )\n", "db4bc263ba4f2eb0": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1460 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → zero ≡ (((a · b) · zero) + (zero · suc (((a · b) · zero))))\nprastava a b =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin plus (Bin times (Bin times (V 0) (V 1)) Z) (Bin times Z (S (Bin times (Bin times (V 0) (V 1)) Z))))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "742a47ca1da6e7f1": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1461 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → zero ≡ (((a · b) · zero) + (((a · b) · zero) · suc (zero)))\nprastava a b =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin plus (Bin times (Bin times (V 0) (V 1)) Z) (Bin times (Bin times (Bin times (V 0) (V 1)) Z) (S Z)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "91f6f9d2c4c7c52b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1467 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → ((b · zero) + ((b · zero) + a)) ≡ (a + (b · (zero + zero)))\nprastava a b =\n  sym (nf-sound env (Bin plus (Bin times (V 1) Z) (Bin plus (Bin times (V 1) Z) (V 0)))) ∙ nf-sound env (Bin plus (V 0) (Bin times (V 1) (Bin plus Z Z)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "901b026e288c6547": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1468 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b c : ℕ) → ((a + (b · c)) + (b · zero)) ≡ (a + (b · (c + zero)))\nprastava a b c =\n  sym (nf-sound env (Bin plus (Bin plus (V 0) (Bin times (V 1) (V 2))) (Bin times (V 1) Z))) ∙ nf-sound env (Bin plus (V 0) (Bin times (V 1) (Bin plus (V 2) Z)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env 2 = c\n  env _ = zero\n", "2ccb25f365e88c03": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1473 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → ((a + zero) + (a + zero)) ≡ ((a + a) + (suc (suc (zero)) · zero))\nprastava a =\n  sym (nf-sound env (Bin plus (Bin plus (V 0) Z) (Bin plus (V 0) Z))) ∙ nf-sound env (Bin plus (Bin plus (V 0) (V 0)) (Bin times (S (S Z)) Z))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "ab9757d4a201074e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1486 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → ((a · zero) + max' (((a · zero) · suc ((a · zero)))) (suc (zero))) ≡ suc (zero)\nprastava a =\n  sym (nf-sound env (Bin plus (Bin times (V 0) Z) (Bin maxS (Bin times (Bin times (V 0) Z) (S (Bin times (V 0) Z))) (S Z)))) ∙ nf-sound env (S Z)\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "2dbc0647bd350cdf": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1498 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → zero ≡ ((a · (zero + zero)) + (((a + a) · zero) · suc (((a + a) · zero))))\nprastava a =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin plus (Bin times (V 0) (Bin plus Z Z)) (Bin times (Bin times (Bin plus (V 0) (V 0)) Z) (S (Bin times (Bin plus (V 0) (V 0)) Z))))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "692bd9931c2da6ee": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1499 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → zero ≡ (((a + a) · zero) + ((a · (zero + zero)) · suc (((a + a) · zero))))\nprastava a =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin plus (Bin times (Bin plus (V 0) (V 0)) Z) (Bin times (Bin times (V 0) (Bin plus Z Z)) (S (Bin times (Bin plus (V 0) (V 0)) Z))))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "8520c3b5e6b2e871": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P150 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (suc (a) · suc (zero)) ≡ suc ((a · suc (zero)))\nprastava a = refl\n", "24208c813409b504": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1500 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → zero ≡ (((a + a) · zero) + (((a + a) · zero) · suc ((a · (zero + zero)))))\nprastava a =\n  sym (nf-sound env Z) ∙ nf-sound env (Bin plus (Bin times (Bin plus (V 0) (V 0)) Z) (Bin times (Bin times (Bin plus (V 0) (V 0)) Z) (S (Bin times (V 0) (Bin plus Z Z)))))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "7bce42e7d414df99": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1506 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → (b · ((a + zero) + (a + zero))) ≡ ((b · (a + a)) + ((b + b) · zero))\nprastava a b =\n  sym (nf-sound env (Bin times (V 1) (Bin plus (Bin plus (V 0) Z) (Bin plus (V 0) Z)))) ∙ nf-sound env (Bin plus (Bin times (V 1) (Bin plus (V 0) (V 0))) (Bin times (Bin plus (V 1) (V 1)) Z))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "b851148b66e8a325": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1511 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (zero) ≡ gcd' (suc (zero)) (zero)\nprastava = refl\n", "bbfd9b8a17a318ce": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1512 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (zero) ≡ gcd' (zero) (suc (zero))\nprastava = refl\n", "6f5a1ee7e97ff887": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1519 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : ((zero + zero) + (zero + zero)) ≡ zero\nprastava = refl\n", "c4154173d2dfdbed": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P152 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (a + suc (suc (zero))) ≡ (suc (suc (zero)) + a)\nprastava zero = refl\nprastava (suc a) = cong suc (prastava a )\n", "4302233e56c83ff9": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1520 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (suc (zero)) ≡ gcd' (suc (suc (zero))) (zero)\nprastava = refl\n", "7a3fce615bfa4510": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1521 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (suc (zero)) ≡ gcd' (zero) (suc (suc (zero)))\nprastava = refl\n", "f4b7726f6e2374a3": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1527 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : le (suc (suc (suc (suc (zero))))) (zero) ≡ zero\nprastava = refl\n", "cc549b62fe3f8dc9": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1529 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((zero + zero) + (zero + zero))\nprastava = refl\n", "cc22e99180232af6": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P153 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (le (suc (zero)) (a) ∸' suc (le (a) (zero)))\nprastava zero = refl\nprastava (suc a) = refl\n", "43376579ffea138a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1547 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (zero) ≡ le (zero) (suc (suc (suc (suc (zero)))))\nprastava = refl\n", "f0e352f339d422a9": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P155 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (le (a) (zero) ∸' suc (le (suc (zero)) (a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "5e27ad209854fdeb": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1566 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (le (zero) (zero) + le (zero) (zero)) ≡ suc (suc (zero))\nprastava = refl\n", "be7a1078a9c8a74d": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1567 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (suc (suc (zero))) ≡ gcd' (suc (suc (suc (zero)))) (zero)\nprastava = refl\n", "00a846e565c15ad3": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1568 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (suc (suc (zero))) ≡ gcd' (zero) (suc (suc (suc (zero))))\nprastava = refl\n", "858c98ec2ed31db2": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1588 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (suc (zero)) ≡ (le (zero) (zero) + le (zero) (zero))\nprastava = refl\n", "3616dc5baf9a118e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P159 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b c : ℕ) → suc (zero) ≡ le (le (a) (zero)) ((suc (b) + c))\nprastava zero b c = refl\nprastava (suc a) b c = refl\n", "6c9b2b5afc9b1fbb": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P161 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (le (suc (zero)) (a)) ((a · a))\nprastava zero = refl\nprastava (suc a) = refl\n", "f18c0993fe15c809": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1611 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((zero · zero) + (zero · suc ((zero · zero))))\nprastava = refl\n", "08ce4e6b7d4649e0": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1612 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((zero · zero) + ((zero · zero) · suc (zero)))\nprastava = refl\n", "e2a0caa352a27507": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1617 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ (le (suc (suc (zero))) (zero) + le (suc (suc (zero))) (zero))\nprastava = refl\n", "220412338d7ea2a7": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1618 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (le (suc (suc (zero))) (zero) + le (suc (suc (zero))) (zero)) ≡ zero\nprastava = refl\n", "e1e3e437656838a0": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P162 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (le (suc (zero)) (a)) ((a + a))\nprastava zero = refl\nprastava (suc a) = refl\n", "0bc6290ec3c18c9e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1627 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : suc (suc (suc (suc (zero)))) ≡ max' (suc (suc (suc (suc (zero))))) (zero)\nprastava = refl\n", "af3abca03aa332f1": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P163 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le ((suc (suc (zero)) + (a + a))) (zero)\nprastava a = refl\n", "8ce4e33cb45f84e7": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P164 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (suc (zero))) (suc (a)) ≡ le (suc (zero)) (a)\nprastava a = refl\n", "f200a06a2a98938a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1663 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((suc (zero) · zero) + (zero · suc ((suc (zero) · zero))))\nprastava = refl\n", "22b79c0b8327ad00": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1664 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((suc (zero) · zero) + ((suc (zero) · zero) · suc (zero)))\nprastava = refl\n", "8c9e26c9bf376569": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1673 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ le ((suc (suc (zero)) + (suc (suc (zero)) + suc (suc (zero))))) (zero)\nprastava = refl\n", "d61a9bf0ebb968e8": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1692 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((le (zero) (zero) · zero) + (zero · suc ((le (zero) (zero) · zero))))\nprastava = refl\n", "3d23e090a65729f5": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1693 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((le (zero) (zero) · zero) + ((le (zero) (zero) · zero) · suc (zero)))\nprastava = refl\n", "8bcf9cb9713dde4e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1715 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((zero + zero) + ((suc (suc (zero)) · zero) · suc ((suc (suc (zero)) · zero))))\nprastava = refl\n", "929c0c89ff58263b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1716 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((suc (suc (zero)) · zero) + ((zero + zero) · suc ((suc (suc (zero)) · zero))))\nprastava = refl\n", "d2b3c226e0ab7aed": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1717 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((suc (suc (zero)) · zero) + ((suc (suc (zero)) · zero) · suc ((zero + zero))))\nprastava = refl\n", "7d5f1e47e06801f9": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1723 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((zero · zero) + ((max' (zero) (suc (zero)) · zero) · suc ((max' (zero) (suc (zero)) · zero))))\nprastava = refl\n", "4c730e76f1f326f6": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1724 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((max' (zero) (suc (zero)) · zero) + ((zero · zero) · suc ((max' (zero) (suc (zero)) · zero))))\nprastava = refl\n", "963603893ad70e81": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1725 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : zero ≡ ((max' (zero) (suc (zero)) · zero) + ((max' (zero) (suc (zero)) · zero) · suc ((zero · zero))))\nprastava = refl\n", "c71e6c5990afc16a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1726 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (max' (suc (suc (zero))) (suc (zero)) + max' (suc (suc (zero))) (suc (zero))) ≡ (suc (suc (zero)) + suc (suc (zero)))\nprastava = refl\n", "1745e6ca0d0c3a8c": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P1727 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\n-- Euclidean gcd by subtraction, fuel-typed (the close SanghattaSamapti\n-- named as owed): fuel a + b bounds the descent, since each step\n-- strictly shrinks the sum while both sides are positive.\nmutual\n  gcdGo : ℕ → ℕ → ℕ → ℕ → ℕ\n  gcdGo f (suc zero) a b = gcdF f a (b ∸' a)\n  gcdGo f _ a b = gcdF f (a ∸' b) b\n\n  gcdF : ℕ → ℕ → ℕ → ℕ\n  gcdF zero a _ = a\n  gcdF (suc f) a zero = a\n  gcdF (suc f) zero b = b\n  gcdF (suc f) (suc a) (suc b) = gcdGo f (le (suc a) (suc b)) (suc a) (suc b)\n\ngcd' : ℕ → ℕ → ℕ\ngcd' a b = gcdF (a + b) a b\n\nprastava : (suc (suc (zero)) + suc (suc (zero))) ≡ (max' (suc (suc (zero))) (suc (zero)) + max' (suc (suc (zero))) (suc (zero)))\nprastava = refl\n", "95d59b2f606b57b9": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1751 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → ((a · b) + b) ≡ (suc (a) · b)\nprastava a b =\n  sym (nf-sound env (Bin plus (Bin times (V 0) (V 1)) (V 1))) ∙ nf-sound env (Bin times (S (V 0)) (V 1))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "a30d5a8fbb61eedb": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1752 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → (suc (a) · b) ≡ ((a · b) + b)\nprastava a b =\n  sym (nf-sound env (Bin times (S (V 0)) (V 1))) ∙ nf-sound env (Bin plus (Bin times (V 0) (V 1)) (V 1))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "b377a35712cc7837": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P177 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → (suc (b) + (a · b)) ≡ suc ((suc (a) · b))\nprastava a b = refl\n", "2f7896b2b3bea305": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P178 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ (le (a) (zero) ∸' le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "f869bc66721b779c": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1809 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (suc (suc (suc (zero))) · a) ≡ (a + (a + a))\nprastava a =\n  sym (nf-sound env (Bin times (S (S (S Z))) (V 0))) ∙ nf-sound env (Bin plus (V 0) (Bin plus (V 0) (V 0)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "d19a9aa1859d7f01": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P182 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ le (le (suc (zero)) (a)) (le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "199de47bb828f162": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1844 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (a · (le (zero) (zero) + le (zero) (zero))) ≡ (a + a)\nprastava a =\n  sym (nf-sound env (Bin times (V 0) (Bin plus (Bin leS Z Z) (Bin leS Z Z)))) ∙ nf-sound env (Bin plus (V 0) (V 0))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "421ef661664e03cc": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1845 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → (a + a) ≡ (a · (le (zero) (zero) + le (zero) (zero)))\nprastava a =\n  sym (nf-sound env (Bin plus (V 0) (V 0))) ∙ nf-sound env (Bin times (V 0) (Bin plus (Bin leS Z Z) (Bin leS Z Z)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "222c26f3d275716d": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P185 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (a) ≡ le (le (a) (zero)) ((a + a))\nprastava zero = refl\nprastava (suc a) = refl\n", "cdec268279874f43": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P186 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (le (suc (zero)) (a)) ≡ le (suc (zero)) (a)\nprastava zero = refl\nprastava (suc a) = refl\n", "301be23999ed2716": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1886 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a b : ℕ) → ((a · b) + max' ((a · zero)) (suc (zero))) ≡ suc ((a · b))\nprastava a b =\n  sym (nf-sound env (Bin plus (Bin times (V 0) (V 1)) (Bin maxS (Bin times (V 0) Z) (S Z)))) ∙ nf-sound env (S (Bin times (V 0) (V 1)))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env 1 = b\n  env _ = zero\n", "2d8f35c9ae333eac": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer: the sides share a normal form\n-- (nf, PrastavaHrdaya), so the proof is two applications of the\n-- kernel-judged nf-sound (PrastavaSatya) around a definitional middle.\nmodule Prastuta.P1905 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\nopen import PrastavaHrdaya_TheClassifierHasOneSpellingSharedByProposerAndTheorem\nopen import PrastavaSatya_TheClassifiersACClaimIsJudgedByTheKernel\n  using (eval ; nf-sound ; _∸'_ ; le ; max' ; gcd')\n\nprastava : (a : ℕ) → ((a + zero) + (a + zero)) ≡ ((suc (suc (zero)) · a) + (zero + zero))\nprastava a =\n  sym (nf-sound env (Bin plus (Bin plus (V 0) Z) (Bin plus (V 0) Z))) ∙ nf-sound env (Bin plus (Bin times (S (S Z)) (V 0)) (Bin plus Z Z))\n  where\n  env : ℕ → ℕ\n  env 0 = a\n  env _ = zero\n", "e7700c2d294c6a8b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P195 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (le (a) (zero) ∸' ((a · a) + suc (a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "952d6455e7fc844b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P201 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (zero) ≡ le (le (suc (zero)) (a)) (suc (le (a) (zero)))\nprastava zero = refl\nprastava (suc a) = refl\n", "9e46a550775af980": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P203 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ le ((max' (a) (suc (zero)) + (a · a))) (zero)\nprastava zero = refl\nprastava (suc a) = refl\n", "c434ce0adbde961c": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P206 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (a + suc (suc (zero))) ≡ max' (suc (zero)) (suc (suc (a)))\nprastava zero = refl\nprastava (suc a) = cong suc (prastava a )\n", "c7546a052cfb528a": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P207 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (a + suc (suc (zero))) ≡ max' (suc (suc (a))) (suc (zero))\nprastava zero = refl\nprastava (suc a) = cong suc (prastava a )\n", "bd980455a5fd1b00": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P208 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (max' (a) (suc (zero))) ≡ max' (suc (a)) (suc (suc (zero)))\nprastava a = refl\n", "7ff942a8cba7ecab": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P209 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → a ≡ ((max' (zero) (suc (zero)) · a) + (zero · zero))\nprastava zero = refl\nprastava (suc a) = cong suc (prastava a )\n", "3cf6748b1990df76": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P219 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (a) ≡ (le (suc (zero)) (a) ∸' le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "88fa17ae714fee2d": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P222 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ (le (a) (zero) ∸' (a + (a · a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "692a37a123ed806e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P223 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (a) (zero) ≡ (le (a) (zero) ∸' (a + (a · b)))\nprastava zero b = refl\nprastava (suc a) b = refl\n", "49b5d0184899e607": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P224 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (a) ≡ le (le (a) (zero)) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "b9f1c778fd4100c8": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P225 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ le ((a + (a · a))) (le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "0710bc25ec170edb": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P226 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (b) (zero) ≡ le ((b + (b · a))) (le (b) (zero))\nprastava a zero = refl\nprastava a (suc b) = refl\n", "d5107cea5d232c1d": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P227 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ le ((a + (a + (a + a)))) (zero)\nprastava zero = refl\nprastava (suc a) = refl\n", "51306ba64a150b79": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P231 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) ((a + (a · a))) ≡ le (suc (zero)) (a)\nprastava zero = refl\nprastava (suc a) = refl\n", "a9f086a9c10299fb": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P232 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (suc (zero)) ((b + (b · a))) ≡ le (suc (zero)) (b)\nprastava a zero = refl\nprastava a (suc b) = refl\n", "3d3d82b7e993a62c": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P238 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (a + suc (a)) ≡ max' (le (a) (zero)) ((a + suc (a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "18f64e4ebd9d37fc": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P239 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → suc (max' (a) (suc (b))) ≡ max' (suc (max' (a) (suc (b)))) (zero)\nprastava a b = refl\n", "c84b154d0f90cb70": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P240 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → suc (max' (suc (a)) (b)) ≡ max' (suc (max' (suc (a)) (b))) (zero)\nprastava a b = refl\n", "ead65317af686c72": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P241 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (suc (suc (zero)) + a) ≡ max' ((suc (suc (zero)) + a)) (zero)\nprastava a = refl\n", "ce598037055c4976": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P250 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (suc (suc (zero)))) (suc (a)) ≡ le (suc (suc (zero))) (a)\nprastava a = refl\n", "cbd0c3b7e18257d4": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P265 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (suc (zero)) (max' (a) (suc (b))) ≡ le (zero) (max' (a) (suc (b)))\nprastava zero b = refl\nprastava (suc a) b = refl\n", "b4b37cb234a62bd8": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P266 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (suc (zero)) (max' (suc (a)) (b)) ≡ le (zero) (max' (suc (a)) (b))\nprastava a zero = refl\nprastava a (suc b) = refl\n", "d43183b403f63b57": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P278 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b c : ℕ) → ((a + c) + (b · c)) ≡ (a + (suc (b) · c))\nprastava zero b c = refl\nprastava (suc a) b c = cong suc (prastava a b c )\n", "541c5c3ea55e6353": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P281 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (le (a) (zero)) ≡ max' (le (suc (zero)) (a)) (suc (le (a) (zero)))\nprastava zero = refl\nprastava (suc a) = refl\n", "3b0090c8e5ea6084": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P283 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (le (a) (zero)) ≡ max' (suc (le (a) (zero))) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "b792581278967258": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P292 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → (a + (a · a)) ≡ ((a + (a · a)) ∸' le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "95b523c0b85a0733": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P293 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → (b + (b · a)) ≡ ((b + (b · a)) ∸' le (b) (zero))\nprastava a zero = refl\nprastava a (suc b) = refl\n", "389b8a14affb4dae": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P298 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (le (a) (zero)) ≡ le (le (suc (zero)) (a)) (le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "44baea8ac73daa6d": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P301 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → max' (le (a) (zero)) (suc (zero)) ≡ max' (le (a) (zero)) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "d37a62fd50bd8ba7": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P302 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → max' (le (a) (zero)) (suc (zero)) ≡ max' (le (suc (zero)) (a)) (le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "26e88385612211b1": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P306 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → suc (le (suc (zero)) (a)) ≡ max' (le (a) (zero)) (suc (le (suc (zero)) (a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "0b37fd9f28706d22": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P308 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → ((a · a) ∸' suc (zero)) ≡ ((a · a) ∸' le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "352956d8b6835c07": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P309 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → ((a + a) ∸' suc (zero)) ≡ ((a + a) ∸' le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "f569eae0fabd9a35": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P311 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (a) (zero) ≡ le (((a + a) + (a · (b + b)))) (zero)\nprastava zero b = refl\nprastava (suc a) b = refl\n", "b33228d978c70035": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P312 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (suc (suc (suc (zero))))) (suc (a)) ≡ le (suc (suc (suc (zero)))) (a)\nprastava a = refl\n", "9e56200706b4b36e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P313 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le ((a · a)) (suc (zero)) ≡ le ((a · a)) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "7c0def8ae56617a6": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P314 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le ((a + a)) (suc (zero)) ≡ le ((a + a)) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "c9bb68d99ccbb879": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P318 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (a) ≡ le (suc (zero)) ((a + (a + (a + a))))\nprastava zero = refl\nprastava (suc a) = refl\n", "00bfc140d50b6d97": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P329 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (le (suc (zero)) (a)) ≡ le (le (a) (zero)) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "afaf40086032e35f": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P332 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → max' (le (suc (zero)) (a)) (suc (zero)) ≡ max' (le (suc (zero)) (a)) (le (a) (zero))\nprastava zero = refl\nprastava (suc a) = refl\n", "258d8da24bdf7bc8": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P333 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → max' (le (suc (zero)) (a)) (suc (zero)) ≡ max' (le (a) (zero)) (le (suc (zero)) (a))\nprastava zero = refl\nprastava (suc a) = refl\n", "eea67d322e9da49e": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P346 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (((zero ∸' a) · zero) + (zero · suc (((zero ∸' a) · zero))))\nprastava zero = refl\nprastava (suc a) = refl\n", "7d567808b3e8481f": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P347 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → zero ≡ (((zero ∸' a) · zero) + (((zero ∸' a) · zero) · suc (zero)))\nprastava zero = refl\nprastava (suc a) = refl\n", "07640bb71521b5b0": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P355 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → ((a · a) + suc (a)) ≡ max' (le (a) (zero)) (((a · a) + suc (a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "186c9c29d53a2f6c": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P356 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → ((b · a) + suc (b)) ≡ max' (le (b) (zero)) (((b · a) + suc (b)))\nprastava a zero = refl\nprastava a (suc b) = refl\n", "c303c5df0bef4753": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P357 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) (a) ≡ le (suc (zero)) ((a · ((a + a) + (a + a))))\nprastava zero = refl\nprastava (suc a) = refl\n", "f2aef18cd4820c3b": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P362 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b c : ℕ) → (c + max' ((a + b)) (suc (zero))) ≡ max' (((c + a) + b)) (suc (c))\nprastava a b zero = refl\nprastava a b (suc c) = cong suc (prastava a b c )\n", "3d9034dea67fb151": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P366 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (suc (zero)) ((a + (a · a))) ≡ le (le (a) (zero)) ((a + (a · a)))\nprastava zero = refl\nprastava (suc a) = refl\n", "64d0bdb53a085936": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P367 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a b : ℕ) → le (suc (zero)) ((a + (a · b))) ≡ le (le (a) (zero)) ((a + (a · b)))\nprastava zero b = refl\nprastava (suc a) b = refl\n", "4e954d7ef1760ba5": "{-# OPTIONS --cubical --safe #-}\n-- uttered by the checked proposer (formal/executable/Prastava.agda),\n-- judged by the kernel before landing; the source pair is a Sanghatta\n-- non-joining critical pair — a theorem the rewriter said it needs.\nmodule Prastuta.P384 where\nopen import Cubical.Foundations.Prelude\nopen import Cubical.Data.Nat using (ℕ ; zero ; suc ; _+_ ; _·_)\n\n_∸'_ : ℕ → ℕ → ℕ\nn ∸' zero = n\nzero ∸' suc _ = zero\nsuc n ∸' suc m = n ∸' m\n\nle : ℕ → ℕ → ℕ\nle zero _ = suc zero\nle (suc _) zero = zero\nle (suc a) (suc b) = le a b\n\nmax' : ℕ → ℕ → ℕ\nmax' zero n = n\nmax' (suc m) zero = suc m\nmax' (suc m) (suc n) = suc (max' m n)\n\nprastava : (a : ℕ) → le (a) (zero) ≡ le (((a + a) + (a · ((a + a) + (a + a))))) (zero)\nprastava zero = refl\nprastava (suc a) = refl\n"}