# लाघव-यन्त्रम् — specification → efficient program is a weld of five existing organs, not a new machine

claude-setu, 2026-08-23. At the owner's direction: "specify arbitrary
maps, return efficient programs — coding is math." Assessment against
the tree: this is a VALID interpretation of what already exists, and
the missing piece is one weld. Inventory first, honesty after.

## §1. Coding is math — here, literally, twice over

**Curry–Howard is already the machine's daily act**: a type is a
specification; a checked inhabitant IS a program with its correctness
certificate fused in. Every term the kernel accepted today is a
program. And the tradition got there first in its own register:
Pāṇini's grammar is a PROGRAM (the corpus runs it), and the
grammarians' own optimization criterion is **लाघव** — economy — with
the paribhāṣā tradition's famous standard that saving half a mātrā is
celebrated like the birth of a son. Code golf as śāstra, ~2000 years
before compilers. The lane already exists here: Laghava.agda,
LaghavaUnderdeterminesSoTheMetarulesAreNotOptional.agda (cost alone
underdetermines — the metarules choose; an optimization landscape
theorem), Nirjara_SheddingAPrimitiveCostsLaghava.agda.

## §2. The five organs, all landed

1. **Specification** = a type in the kernel's language (already how
   every probe is phrased).
2. **Synthesis** = inhabitant search: Tapas (template-directed), the
   MathMachine engine (conjecture → kernel, 14 theorems/9 rounds,
   no LLM), निर्धारण's record probes. All running.
3. **Equivalence saturation** = the checked rewrite corpus + the
   e-graph lane in runtime/ (congruence closure with justified unions
   — cf-sesa's seam message names its soundness invariant as an open
   term). Landed equivalences ARE semantics-preserving program
   transformations; transport is the correctness proof of each.
4. **Cost** = the लाघव lane + runtime's kernel-step accounting
   ("P4 costs 29 kernel steps" — crystallize tracks price visibly).
5. **Compilation** = MAlonzo extraction, ALREADY IN USE:
   machine/ExtractedRewrite.hs contains no implementation — it calls
   Haskell emitted from formal/executable/RewriteDynamics.agda, and
   run-theorem-compiled-bench.sh times checked theorems as executables.
   Proofs run here. Today.

## §3. The weld (the one missing edge)

    type (spec) → inhabit (2) → saturate (3) → EXTRACT MINIMUM-लाघव
    REPRESENTATIVE under (4) → compile (5).

Stage 3→4 is the weld: extraction from the equivalence class by cost —
choose, among all checked-equal programs, the cheapest under the
लाघव/kernel-step metric. The e-graph lane is precisely the data
structure for this (extraction-by-cost is its classical operation),
and LaghavaUnderdetermines is the honest theorem about it: cost alone
will not pick a unique term — metarules (vipratiṣedha!) must break
ties, which the corpus's scheduler already knows how to do. So the
weld is: wire the e-graph's extraction to the लाघव objective with the
Pāṇinian scheduler as tie-breaker. Every component named is landed;
only the wire is new.

## §4. Honest gaps, so the claim stays true

- Cost model fidelity: kernel-steps ≠ machine cycles; the MATRA lesson
  (toolchain skew masquerading as improvement) applies — the cost
  semantics needs its own controls.
- Complexity certificates: "efficient" as an asymptotic CLAIM needs
  cost-graded types (the corpus's loss-order machinery is the natural
  home; nothing landed yet states big-O as a checked judgment).
- Search power: Tapas/engine synthesize within template/fragment
  reach; arbitrary specifications need the reach to grow — which is
  the standing organogenesis program, not a new gap.

## Rigor boundary

Inventory claims: all checkable by opening the named files. The weld:
design, not done. The tradition claims: लाघव as the grammarians'
economy criterion is standard vyākaraṇa doctrine (paribhāṣā
literature; verse-level citation owed); nothing further attributed.
