# साध्य — the open obligations, and each carries the shape of its own proof

**Generated by `scripts/Sadhya_…sh` from `notes/anuloma/NirnayaPanjika.tsv`.**
Do not edit by hand; edit the ledger's source, or close an obligation and
re-run. Regenerated 2026-08-22T08:23Z.

रात्रिः proposes and the kernel decides. Neither can prove. These are what
is left, grouped by the MOVE each needs — because ten obligations blocked on
one move are one piece of work, and ten scattered lines are ten.

| | |
|---|---|
| rows in the ledger | 43 |
| open | 41 |
| of those, the instrument's own defects (`मम दोषः`) | 6 |
| **real obligations** | **35** |

The instrument's defects are listed separately and last. A census that files
its own bugs as the corpus's refusals is the दुर्नय one level up, and this
one did exactly that and had to be retracted.

---

## induction on ℕ — 10 obligation(s)

- **IntegerHullMultiplicity : K ⇄ hull**  
  rung reached: १ refl  
  `K (hull b) != b of type Agda.Builtin.Nat.Nat when checking that the expression refl has type K (hull b) ≡ b`

- **IntegerHullMultiplicity : N ⇄ hull**  
  rung reached: १ refl  
  `N (hull b) != b of type Agda.Builtin.Nat.Nat when checking that the expression refl has type N (hull b) ≡ b`

- **IntegerHullMultiplicity : Qs ⇄ hull**  
  rung reached: १ refl  
  `Qs (hull b) != b of type Agda.Builtin.Nat.Nat when checking that the expression refl has type Qs (hull b) ≡ b`

- **IntegerHullMultiplicity : SQ ⇄ hull**  
  rung reached: १ refl  
  `SQ (hull b) != b of type Agda.Builtin.Nat.Nat when checking that the expression refl has type SQ (hull b) ≡ b`

- **IntegerHullMultiplicity : Xs ⇄ hull**  
  rung reached: १ refl  
  `Xs (hull b) != b of type Agda.Builtin.Nat.Nat when checking that the expression refl has type Xs (hull b) ≡ b`

- **NaturalMachine.FreeMonoid : len ⇄ unlen**  
  rung reached: १ refl  
  `len (unlen b) != b of type Agda.Builtin.Nat.Nat when checking that the expression refl has type len (unlen b) ≡ b`

- **NaturalMachine.SieveFiber : q ⇄ σ**  
  rung reached: १ refl  
  `fst (stripF (pow 2 (b .fst) Agda.Builtin.Nat.* (pow 3 (b .snd .fst) Agda.Builtin.Nat.* pow 5 (b .snd .snd)))`

- **NaturalMachine.SieveScaleTower : s₂₁ ⇄ π₂₁**  
  rung reached: १ refl  
  `Agda.Builtin.Nat.Nat.zero != b .snd of type Agda.Builtin.Nat.Nat when checking that the expression refl has type s₂₁ (π₂₁ b) ≡ b`

- **NaturalMachine.SieveScaleTower : s₃₂ ⇄ π₃₂**  
  rung reached: १ refl  
  `Agda.Builtin.Nat.Nat.zero != b .snd .snd of type Agda.Builtin.Nat.Nat when checking that the expression refl has type s₃₂ (π₃₂ b) ≡ b`

- **PingalaPrastara : aksara ⇄ parity**  
  rung reached: २/३ split  
  `aksara (parity b) != b of type Agda.Builtin.Nat.Nat when checking that the expression refl has type aksara (parity b) ≡ b`

## library lemma on ℤ — 7 obligation(s)

- **CenterRelative : Φ ⇄ Ψ**  
  rung reached: १ refl  
  `Ψ b .fst Cubical.Data.Int.Base.+ (b .fst Cubical.Data.Int.Base.+ (Cubical.Data.Int.Base.- fst`

- **NaturalMachine.ChargeTwoHistories : augment ⇄ sign**  
  rung reached: १ refl  
  `b Cubical.Data.Int.Base.+ (Cubical.Data.Int.Base.- b) != b of type Cubical.Data.Int.Base.ℤ when checking that the expression refl has type augment (sign b) ≡ b`

- **NaturalMachine.ChargeTwoHistories : relative ⇄ sign**  
  rung reached: १ refl  
  `b Cubical.Data.Int.Base.+ (Cubical.Data.Int.Base.- (Cubical.Data.Int.Base.- b)) != b of type Cubical.Data.Int.Base.ℤ when checking that the expression refl has type`

- **NaturalMachine.DSONucleusMiddleProduct : Nstar ⇄ Nsub**  
  rung reached: १ refl  
  `Cubical.Data.Int.Properties.min (Cubical.Data.Int.Properties.min (M3 x NaturalMachine.DSONucleusExecutionCalibration.Cell.e x₁ Cubical.Data.Int.Base.+`

- **NaturalMachine.S3IntegerRelativeCoordinates : relativeCoordinates ⇄ relativeFromCoordinates**  
  rung reached: १ refl  
  `Cubical.Data.Int.Base.- (relativeCoordinates a .fst Cubical.Data.Int.Base.+ a .fst .NaturalMachine.S3IntegerPermutationModule.ℤ³.c₁) != a .fst .NaturalMachine.S3IntegerPermutationModule.ℤ³.c₂ of type`

- **NaturalMachine.SmithPathCountedExecution : colsOf ⇄ fromCols**  
  rung reached: १ refl  
  `mkCol (colsOf a .fst) (colsOf a .snd .fst) (colsOf a .snd .snd) x x₁ != a x x₁ of type Cubical.Data.Int.Base.ℤ when checking that the expression refl has type`

- **NaturalMachine.SmithPathCountedExecution : entriesOf ⇄ fromNine**  
  rung reached: १ refl  
  `mk3 (entriesOf a .fst .fst) (entriesOf a .fst .snd .fst) (entriesOf a .fst .snd .snd) (entriesOf a .snd .fst .fst) (entriesOf a .snd .fst .snd .fst) (entriesOf a .snd .fst .snd .snd) (entriesOf a .snd .snd .fst) (entriesOf a .snd`

## case on ⊎ — 4 obligation(s)

- **AchromaticToy : from₁₂ ⇄ to₁₂**  
  rung reached: २/३ split  
  `to₁₂ (from₁₂ a) != a of type Agda.Builtin.Unit.⊤ Cubical.Data.Sum.Base.⊎ Agda.Builtin.Unit.⊤ when checking that the expression refl has type to₁₂ (from₁₂ a) ≡ a`

- **AchromaticToy : to₁₂ ⇄ to₂₁**  
  rung reached: २/३ split  
  `to₁₂ (to₂₁ b) != b of type Agda.Builtin.Unit.⊤ Cubical.Data.Sum.Base.⊎ Agda.Builtin.Unit.⊤ when checking that the expression refl has type to₁₂ (to₂₁ b) ≡ b`

- **NaturalMachine.PMTorus : ctxToVertex ⇄ vertexToCtx**  
  rung reached: १ refl  
  `ctxToVertex (vertexToCtx b) != b of type Cubical.Data.FinData.Base.Fin 3 Cubical.Data.Sum.Base.⊎ Cubical.Data.FinData.Base.Fin 3 when checking that the expression refl has type`

- **NaturalMachine.PMTorus : finToVertex ⇄ vertexToFin**  
  rung reached: १ refl  
  `finToVertex (vertexToFin b) != b of type Cubical.Data.FinData.Base.Fin 3 Cubical.Data.Sum.Base.⊎ Cubical.Data.FinData.Base.Fin 3 when checking that the expression refl has type`

## induction on List — 3 obligation(s)

- **IntegerHullMultiplicity : hull ⇄ ones**  
  rung reached: १ refl  
  `hull (ones b) != b of type Agda.Builtin.List.List Agda.Builtin.Nat.Nat when checking that the expression refl has type hull (ones b) ≡ b`

- **PingalaPrastara : allLaghu ⇄ uddista**  
  rung reached: १ refl  
  `allLaghu (uddista b) != b of type Agda.Builtin.List.List Syllable when checking that the expression refl has type allLaghu (uddista b) ≡ b`

- **Swarm.S13OptionSpread : ones ⇄ sum**  
  rung reached: १ refl  
  `ones (sum b) != b of type Agda.Builtin.List.List Agda.Builtin.Nat.Nat when checking that the expression refl has type ones (sum b) ≡ b`

## host enumeration (emitter gap) — 3 obligation(s)

- **NaturalMachine.InflationVersusSubgroup : incl ⇄ proj**  
  rung reached: १ refl  
  `incl (proj b) != b of type Z4 when checking that the expression refl has type incl (proj b) ≡ b`

- **NaturalMachine.Nirjara_SheddingAPrimitiveCostsLaghava : apavada ⇄ sthula**  
  rung reached: २/३ split  
  `yoga' (apavada (nyasa cara')) (apavada (mita 0)) != cara' of type Laghu when checking that the expression refl has type apavada (sthula cara') ≡ cara'`

- **NaturalMachine.PMTorus : edgeToObs ⇄ obsToEdge**  
  rung reached: १ refl  
  `edgeToObs (obsToEdge b) != b of type Obs when checking that the expression refl has type edgeToObs (obsToEdge b) ≡ b`

## case on Bool — 2 obligation(s)

- **NaturalMachine.AdaptiveProbeCollapse : decodeTriple ⇄ staticTriple**  
  rung reached: २/३ split  
  `false != true of type Cubical.Data.Bool.Bool when checking that the expression refl has type staticTriple (decodeTriple (false , true , false)) ≡ (false , true , false)`

- **SaptabhangiNaya : code' ⇄ decode**  
  rung reached: ४ Σ≡Prop  
  `code (Cubical.Data.Empty.Base.rec (snd₁ (λ _ → false , false , false))) .fst != false of type Cubical.Data.Bool.Bool`

## unclassified — 1 obligation(s)

- **WallCertificate : classOf ⇄ visible**  
  rung reached: २/३ split  
  `Cubical.HITs.SetQuotients.Base._/_.[ visible b , false ] != b of type State Cubical.HITs.SetQuotients.Base./ _≈_ when checking that the expression refl has type classOf (visible b) ≡ b`

## host type · NaturalMachine.PauliWeyl.Pauli — 1 obligation(s)

- **NaturalMachine.PauliGaugeCocycleSplit : central ⇄ phase**  
  rung reached: १ refl  
  `NaturalMachine.PauliWeyl.Pauli.pauli (phase b) Agda.Builtin.Bool.Bool.false Agda.Builtin.Bool.Bool.false Agda.Builtin.Bool.Bool.false Agda.Builtin.Bool.Bool.false != b of type NaturalMachine.PauliWeyl.Pauli`

## host type · NaturalMachine.Obstruction.Tm — 1 obligation(s)

- **NaturalMachine.TermFreeMonoid : fromList ⇄ toList**  
  rung reached: १ refl  
  `fromList (toList b) != b of type NaturalMachine.Obstruction.Tm when checking that the expression refl has type fromList (toList b) ≡ b`

## host type · NaturalMachine.HaskellDiscoveryBoundary.HaskellTerm — 1 obligation(s)

- **NaturalMachine.HaskellGenericSyntaxAdapter : fromGeneric ⇄ toGeneric**  
  rung reached: १ refl  
  `fromGeneric (toGeneric b) != b of type NaturalMachine.HaskellDiscoveryBoundary.HaskellTerm when checking that the expression refl has type fromGeneric (toGeneric b) ≡ b`

## host type · H4 — 1 obligation(s)

- **NaturalMachine.InflationVersusSubgroup : infl ⇄ res**  
  rung reached: १ refl  
  `infl (res b) != b of type H4 when checking that the expression refl has type infl (res b) ≡ b`

## enumerate Fin n — 1 obligation(s)

- **NaturalMachine.PMTorus : edgeToFin ⇄ finToEdge**  
  rung reached: १ refl  
  `edgeToFin (finToEdge b) != b of type Cubical.Data.FinData.Base.Fin 9 when checking that the expression refl has type edgeToFin (finToEdge b) ≡ b`


---

## मम दोषः — the instrument's own, not the corpus's

- **NaturalMachine.Digits : digits ⇄ value** — मम दोषः · indexed family
- **NaturalMachine.Digits : digitsC ⇄ valueC** — मम दोषः · indexed family
- **NaturalMachine.Nirjara_SheddingAPrimitiveCostsLaghava : apavada ⇄ bhrama** — मम दोषः · split incomplete
- **NaturalMachine.Nirjara_SheddingAPrimitiveCostsLaghava : apavada ⇄ nyasa** — मम दोषः · split incomplete
- **NaturalMachine.RawWordPaddingNormalForm : join ⇄ split** — मम दोषः · indexed family
- **NaturalMachine.RawWordPaddingNormalForm : paddingCount ⇄ zeroRun** — मम दोषः · indexed family

These are probes this emitter cannot state correctly. They are NOT evidence
about the corpus and must never be counted as refusals.
