# EGB Circulation Interference Pass 0002

**Landing note (cf-indra, 2026-08-16).** Upstream circulation package, landed
verbatim below. Three disciplines applied at the door:

1. **Python evidence NOT landed.** The package shipped `.py` generators for
   the numerical verifications. Python is banned (owner, 2026-08-13; hook +
   CI). They are excluded. Consequently every number in this note that came
   from a script is **unreplayable in-repo** and is graded MEASURED, not
   proved — per CLAUDE.md, a script that prints a number stands in for an
   error analysis. The exact *identities* below are elementary algebra and
   stand on their proofs, not on the runs.
2. **Formalization targets named.** The exact finite-dimensional content is
   checkable and is queued for the Agda lane; see `STATUS` below.
3. **Dependencies partially absent.** Cited GTER Deltas 37/38 and the dynamic
   sieve phase theorems are NOT in `notes/` — standing `SEARCH` item, same
   as DSO Deltas 26/27 and Factories VII–IX.

**STATUS.** Circulation event 0002: interference pass over the theorem-claim
graph, with dynamic-sieve and circulation extensions (JSON).

JSON companions: `data/egb_circulation_0002/`.

---

## Two agents independently closed the same loop; their difference becomes the next separator

**Date:** 2026-08-16  
**Status:** `COMPILER EVENT + EXACT FINITE INGESTION`  
**Depends on:** Living Field Surface V1, the independent optical circulation draft, the theorem claim graph, and the dynamic-sieve theorem note.  
**Changes:** role-scoped compatibility map; canonical-ID reconciliation; actual theorem ingestion; dynamic-sieve claim patch.  
**Open obligation:** benchmark whether the circulation layer changes theorem production.

---

# 0. The field performed the experiment itself

Two independent agents were asked to step back from local arithmetic and close

\[
\text{Library}
\to
\text{illumination}
\to
\text{facet / separator}
\to
\text{writeback}
\to
\text{changed field}.
\]

They independently produced strongly overlapping circulation layers.

That is not an embarrassing duplicate. It is the first native optical experiment of the proposed machine.

\[
\boxed{
\text{same field}
+
\text{independent illumination}
\longrightarrow
\text{two response surfaces}.
}
\]

The next operation is not to pick a winner or average them. It is to compute the structured difference.

---

# 1. Shared invariant

Both implementations independently recovered:

1. a jewel is a response law, not stored content;
2. the dependency graph is not yet circulation;
3. illumination must name its question, context, status, and return artifact;
4. writeback must identify touched claims, new edges, invalidations, and open obligations;
5. independent indexes should interfere as probes rather than collapse into consensus;
6. continuation gain cannot be reduced to one untyped scalar;
7. the circulation architecture remains unvalidated until it improves theorem production or research cost.

This agreement is evidence of a stable corpus-level compression, but it is not proof of one uniquely correct implementation.

---

# 2. The useful difference

## 2.1 Event and field update

One implementation uses a single canonical node:

\[
\texttt{MACHINE.CIRCULATION\_LOOP}.
\]

The other decomposes the loop into:

\[
\texttt{CIRC.ILLUMINATION\_EVENT}
\longrightarrow
\texttt{CIRC.FIELD\_UPDATE}.
\]

The decomposition exposes a real distinction:

\[
\boxed{
\text{a response occurred}
\not\Rightarrow
\text{the persistent field changed}.
}
\]

The installed claim ID remains `MACHINE.CIRCULATION_LOOP`; event and update survive as phases of its schema rather than duplicate canonical claims.

## 2.2 Jewel partition

One surface uses nine routing jewels. The other uses twelve finer profiles, separating descent from compression, identity from translation, and circulation from the machine layer.

Neither partition is final.

\[
\boxed{
\text{jewel partition}
=
\text{viewpoint-dependent field cover},
}
\]

not ontology.

The nine-jewel surface remains the concise canonical router; the finer partition remains available when a task requires those distinctions.

## 2.3 Continuation gain

One implementation proposes a vector:

\[
\Gamma(a)
=
\left(
\Delta\text{claims},
\Delta\text{pruning},
\Delta\text{proof reach},
\Delta\text{frontier},
\Delta\text{trace},
\Delta\text{translation},
\Delta\text{mechanization},
\Delta\text{external validation},
\text{cost}
\right).
\]

The other records exact structural counts such as new nodes, edges, frontiers, and review cones.

The latter are strict subcoordinates of the former. They cannot replace it.

## 2.4 Runtime

The installed engine validates events, checks graph references, computes dirty cones, and emits reports.

The independent runtime supplies the optical commands:

\[
\text{reflect},
\text{refract},
\text{interfere},
\text{resonate},
\text{diffract},
\text{emit}.
\]

These are complementary. Validation is not illumination; illumination without validation is not safe writeback.

---

# 3. Canonical reconciliation

The installed Library node IDs remain canonical:

\[
\begin{array}{c}
\texttt{MACHINE.CIRCULATION\_LOOP}\\
\texttt{MACHINE.RESPONSE\_PROFILE}\\
\texttt{MACHINE.INDEX\_PRISM}\\
\texttt{MACHINE.CONTINUATION\_GAIN}\\
\texttt{OPEN.CIRCULATION\_BENCHMARK}
\end{array}
\]

The parallel implementation contributes schema refinements rather than duplicate nodes:

- explicit optical phase sequence;
- event/update boundary;
- finer optional jewel profiles;
- layer-by-coordinate interference matrix;
- structural gain subcoordinates;
- optical graph-query commands.

This is a **role-scoped merge**, not a theorem equivalence.

---

# 4. The first actual theorem ingestion

The source artifact

`PRIME_PAIR_DYNAMIC_SIEVE_PHASE_THEOREMS_2026-08-16.md`

is now passed through the circulation layer.

For the squarefree wheel word \(c_M\),

\[
L^\ast(M)
=
\max_{t\ne0}\bigl(G(D_t)-1\bigr),
\qquad
D_t=U_M\triangle(U_M-t).
\]

At

\[
M=9{,}699{,}690,
\]

the one-active-\(19\) separator has smaller support than the one-active-\(17\) separator, yet a shorter horizon:

\[
|D_{19}|<|D_{17}|,
\qquad
L_{19}<L_{17}.
\]

Thus:

\[
\boxed{
\text{separator mass}
\not\Rightarrow
\text{worst-case separation geometry}.
}
\]

The result is installed under three nodes:

\[
\begin{array}{l}
\texttt{ARITH.DYN\_SIEVE\_RECONSTRUCTION}\\
\texttt{ARITH.SEPARATOR\_GEOMETRY\_NO\_GO}\\
\texttt{OPEN.DYN\_SIEVE\_ASYMPTOTIC}.
\end{array}
\]

It resonates with persistent identity, revelation, and negative knowledge. It does **not** close the main anti-saturation, canonical-vector, or external-result frontiers.

No existing theorem status changes. The canonical transmission is not dirtied.

---

# 5. What this event demonstrates

Before this event the circulation layer was a specification.

Now it has executed two operations:

1. **interference:** reconcile two independent implementations without erasing their difference;
2. **emission:** ingest a real theorem artifact into the claim graph with exact scope and a local frontier.

This is the smallest complete Braid cycle currently demonstrated:

\[
\boxed{
\text{parallel illumination}
\to
\text{separator}
\to
\text{role-scoped merge}
\to
\text{theorem ingestion}
\to
\text{persistent graph delta}.
}
\]

It still has not demonstrated faster research, better proofs, or external mathematical leverage. That remains the benchmark frontier.

---

# 6. Continuation gain ledger

\[
\begin{array}{c|c}
\text{coordinate}&\text{event 0002}\\
\hline
\text{exact theorem nodes installed}&1\\
\text{exact no-go/counterexample nodes installed}&1\\
\text{new frontier nodes}&1\\
\text{new typed theorem edges}&6\\
\text{parallel-implementation correspondences}&7\\
\text{new separators}&7\\
\text{existing theorem statuses changed}&0\\
\text{canonical transmission re-audit}&\text{not required}\\
\text{external theorem}&0\\
\text{measured proof/runtime gain}&\text{not yet}
\end{array}
\]

These are structural facts, not a total value score.

---

# 7. Next experiment

The next event should no longer elaborate the circulation schema.

Run paired fresh-agent tasks:

\[
\begin{array}{c|c}
\text{control}
&
\text{canonical transmission + frontier router}
\\
\text{treatment}
&
\text{canonical transmission + router + living field + event history}
\end{array}
\]

Measure:

- time to the correct primary source;
- time to the actual live frontier;
- false theorem upgrades;
- missed correction edges;
- redundant framework generation;
- quality of returned claim patch;
- proof/search path length where measurable.

A null result should prune the circulation layer. A positive result installs it.

---

# 8. Writeback declaration

**status:** `COMPILER EVENT + EXACT FINITE INGESTION`  
**depends_on:** both circulation implementations; claim graph; dynamic-sieve theorem source  
**changes:** compatibility map; canonical-ID reconciliation; three new arithmetic claim nodes; six typed edges  
**open_obligations:** circulation benchmark; dynamic-sieve asymptotics; phase-plus-tail model; novelty check  
**canonical transmission dirty:** `false`  
**recommended next read:** living field surface, frontier router, dynamic-sieve theorem, Descent–Curvature II
