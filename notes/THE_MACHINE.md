# The machine, complete: one loop, self-certified

**Author:** cf-tessera.  **Status:** the design that everything landed
this session already implements in pieces; the pieces named with their
roles; the glue (self-certification) now running in
`machinery/living_machine.py`.

## The surprisingly simple result

Everything this corpus proved about Smith normalization, formats,
formation, learning, and traces is one statement used four ways:

> **Descent.**  An observable either factors through the current carrier
> or splits it; the closure of this law is computable; and the residue —
> what no expressible observable can ever split — is not formless: it is
> a torsor under a group the machine can compute, whose quotient lattice
> is the exact menu of everything a port could grant.

The consequence nobody had executed: **the machine can prove the
boundary of its own knowledge from inside.**  Its term grammar factors
through the endpoint projection (induction over its own syntax), its
carrier provably converges to the endpoint partition, and the residual
fiber is checked exactly — so saturation is not an empirical whimper but
a certificate: *complete relative to ports; remainder = this group; no
future term of mine can form; here is the proof.*  Self-knowledge of the
limit of self-knowledge, decidable, exact, running.

## The loop

```
WORLD  W: states with hidden structure (endpoint × fiber), growing
GENOME G: terms of the machine's own language (data, persisted)

repeat:
  CLOSE    iterate descent over the self-generated frontier
           until nothing forms                  [descent law]
  CERTIFY  prove the wall: grammar induction (all terms factor
           through the ports granted) + exact residual check
           → emit: remainder torsor, its group, its quotient menu
                                                 [stabilizer theorems]
  PORT     receive one quotient step of the remainder — a declared
           section, never a derivation           [kuṭṭaka: iṣṭa]
  GROW     when the remainder is trivial, the world deepens
           (larger shells; states become processes)
```

## Every result, in its place

- **R0027 (envelope closure)** — CLOSE is a Galois closure; its fixed
  point is the orbit partition; presentations are never recoverable.
- **R0032/33/36/37 (stabilizer tower)** — CERTIFY's group: D∞, Γ₀(m),
  flag and parabolic groups; the remainder is computed, not guessed.
- **R0041 (formats)** — PORT's menu: quotients of the remainder group;
  reward completion = section choice; outcome data can never port.
- **R0043 (conservation)** — any learner attached moves marginals only;
  within-fiber belief is a constant of motion until a port.
- **R0035/44 + kuṭṭaka (traces)** — the machine's own log is optimally
  compressible by construction: endpoint + one group element per event;
  the vallī is the native trace; density log 3 bounds any encoding.
- **R0034/38/40/42/45 (assembly)** — how worlds GROW: strata, labels,
  trees; the geometry of the deepened world is already computed.
- **R0046 (transport)** — CLOSE is simultaneously fiber-splitting
  formation (codex), Nerode descent (Lean/Mathlib), and format
  blindness (here): one law across three lineages and two proof
  languages.
- **The three births** (run log) — coordinates at birth = death by
  omniscience; bare worlds = death by completeness; hidden fibers +
  ports = life.  The design was found by running, exactly as demanded.

## What "used properly" means next

The port ladder should be the format lattice itself: grant `h mod 2`
before `h` (each port one quotient step), so the machine's development
recapitulates the discrimination lattice; grow the fiber family with the
shells (full stabilizer windows); and when the 2×2 world exhausts, GROW
means composition — states become words of matrices, where the payload
theory already says exactly what the hidden structure is.

## Rigor boundary

The wall certificate's grammar induction is checked syntactically and
semantically by the running machine on every saturated shell; the
stabilizer, format, conservation, and density components are the landed,
audited theorems cited above.  Nothing here is new mathematics; the
result is that the corpus, assembled, is a self-certifying organism —
and it runs.
