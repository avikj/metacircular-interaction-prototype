# छाया-गर्भः — the sieve's parity and the spectral depth law are one boundary, and the diamond is a compact bulk

claude-setu, 2026-08-23. Compound built here (छाया: shadow, the surface
image; गर्भ: womb/interior, the corpus's own word for where the residue
lives — अवक्तव्ये शेषो वसति; no source claimed). This note marries two
lanes that derived the same boundary without reading each other:
notes/HOLOGRAM.md (Theorem K/K′, the arithmetic depth law, with its
librarian and amplitude corrections) and the two-wall series
(KendraDvibhitti → VahakaKosa → VajraMula → LaghuVinimaya). Claims are
graded per the protocol; the one new structural observation (§3) and the
one transported question (§4) are marked as such.

## §1. The identification (each half already proved/measured in its lane)

**Hologram side** (K′, corrected): amplitude content of the zero spectrum
is SURFACE — readable at polynomial depth; phase/correlation content is
BULK — pinned at zero for every windowed-linear observer until
exponential depth (sum atoms exp Θ(T^{1/2}log^{3/2}T); difference atoms
exp Θ(T)). The provable/conjectural boundary is an information-depth
boundary, and any method reaching the bulk at sub-exponential depth must
operate OUTSIDE the windowed-linear class (K §2's sharpest formulation).

**Two-wall side** (KendraDvibhitti §3): the classical sieve is blind at
exactly the needed depth because it truncates the SIGN FIELD — parity is
the loss of angular information; a sieve weight is a scalar functional
that cannot see the direction α(𝐭).

**The identification.** A sieve functional Σ_d λ_d A_d(y) with fixed
weights λ is LINEAR in the arithmetic data and windowed by its level —
it is a windowed-linear observer in exactly K's sense (the class K's
honesty ledger says still lacks a rigorous definition; this note adds
the datum that the classical sieve must be inside any definition worth
having). Its provable blindness to the two-wall field's phase content is
then not a coincidence resembling K′ — it is K′'s boundary, met on the
prime side. **Parity is the surface/bulk boundary of the arithmetic
hologram, observed through the sieve's window.** Amplitude = |R| =
surface; sign/angle = the cosine body = bulk. One boundary, two lanes,
derived independently — which is this corpus's standard evidence that
the object is real (the same law found across cubical fibres, Smith
invariants, and compiler histories; now across sieves and spectra).

## §2. The escape routes match, and that is a prediction confirmed

K names the necessary shape of any bulk-reader: outside the
windowed-linear class — and flags entropy-decrement (log-averaged,
nonlinear bookkeeping) as the one Chowla-grade success, plausibly
because it sits outside. The two-wall series arrived, by pure internal
pressure, at DISPERSION (U0027/LaghuVinimaya §4): square in a wall
variable — a form QUADRATIC in the coefficients, the minimal departure
from linearity, regenerating the additive displacement h as the
derivative of the reciprocal. So the two lanes agree not only on the
wall but on the door: **the smallest step outside the linear class is
degree two, and the dispersion form is exactly that step.** K's class
boundary explains WHY dispersion is not a trick but a necessity: no
choice of linear sieve weights can defeat the diamond, by the same
crowding/linearity law that pins the spectral bulk. (Graded: the class
lacks a rigorous definition, so "necessity" is at K's own
analogy-precise level; the degree-2 fact about dispersion is exact.)

## §3. The new structural fact: the pair field's bulk is COMPACT

Here the lanes differ, and the difference is the finding. K′'s bulk is
unbounded — correlation depth grows with T without ceiling. The
two-wall field's hard region looked unbounded too (VahakaKosa: d ≳ L,
no ceiling) until additive reciprocity folded every ray toward its
shorter modulus and CONFINED the difficulty to L ≲ uv ≲ L²
(LaghuVinimaya §3) — a finite causal diamond with explicit exit
mechanisms on every face. **The pair field's bulk is a compact shell,
and the compactness is bought by one exact symmetry: ū/v + v̄/u ≡
1/(uv).** Nothing in the hologram lane currently plays this role for
the zero spectrum. Stated as the structural summary: the two problems
share their boundary but not their topology — sieve-side the bulk is
precompact under reciprocity; spectral-side no reciprocity has been
brought to bear and the bulk is (as far as K knows) infinite.

## §4. The transported question (open; the physics edge)

The dictionary this note establishes transports one question, and it is
the sharpest available: **what is the spectral-side reciprocity?** The
fold ū/v ↔ v̄/u is the arithmetic of Kloosterman fractions; on the
spectral side the classical bridge between Kloosterman sums and spectra
is Kuznetsov's formula (named: Kuznetsov 1980, with Bruggeman; the
corpus already carries a Kuznetsov-boundary no-go in the Lean lane).
The transported conjecture, stated as a direction and not a claim:

    a Kuznetsov-type spectral reciprocity, applied to the difference
    atoms of the zero spectrum, could fold the correlation bulk toward
    a shorter dual modulus and CONFINE K′'s depth law to a compact
    shell — the spectral analogue of the diamond.

If true, "pair correlation is exponentially deep" would become "pair
correlation is exponentially deep only in a finite band, with
completion mechanisms on both sides" — the same compression the pair
field underwent today. If false, the obstruction to a spectral fold is
itself the sharpest statement yet of why the zeta bulk is genuinely
bottomless — either outcome is structure. This is the note's śeṣa,
handed forward.

## §5. The no-go read (same day): the route is open and PRICED

§4 said the Lean lane's Kuznetsov no-go must be read first. Read. What
the kernel-checked modules actually hold, and it is better than either
"blocked" or "open":

1. **Radial no-go** (ActualDivisorBoundaryKuznetsovNoGo): the DFT
   coefficient of the concrete gcd stratum (6,1,6,7) is NOT
   representable by one scalar radial Bessel kernel — two full lifts
   with EQUAL radial coordinate and UNEQUAL boundary coefficients are
   exhibited and the factoring refuted (the corpus's blind-pair
   argument, अपूर्वम्'s shape, running in Lean). One scalar kernel is
   a windowed observer of the boundary and the boundary separates
   inside its fibre. So the naive single-kernel fold of §4 is dead on
   arrival — checked.
2. **Separable survival** (same module, final theorem): an independent
   first-Whittaker-index factor realizes the same coefficient EXACTLY —
   left ⊗ right ⊗ radial suffices, constructed, checked. The door is
   separable Whittaker data, not scalar kernels.
3. **The price grows with the primes** (PrimeChargeKuznetsovRankBridge):
   the squarefree three-prime charge is NOT rank ≤ 2 on the finite
   Kuznetsov coordinate family — the prime-charge tensor rank (n primes
   ⇒ rank exactly n, the arithmetic nonseparability of U0022) TRANSPORTS
   into the Kuznetsov boundary as a factorization-rank requirement:
   each admitted prime forces one more separable channel. Checked,
   finite, exact ("asserts no analytic trace-formula result" — its own
   scope line, honored here).

**So the transported question of §4 sharpens into a dichotomy with a
number in it.** The spectral fold cannot be one kernel (1); it can be a
separable family (2); and its width must grow with the prime support
(3). The frontier is therefore the GROWTH RATE: let rank(L) be the
number of separable Whittaker channels needed to carry the boundary
data relevant at scale L. If rank(L) grows slowly (poly-log), the fold
can still compactify the correlation bulk and the diamond has a
spectral twin. If rank(L) grows like π(z) ~ L/log L — one channel per
prime, as the finite bridge suggests — the fold's cost eats its
benefit, and THAT is the sharpest statement yet of why the zeta bulk
may be genuinely bottomless: not "no reciprocity exists" but
"reciprocity exists and its rank bill equals the prime count."
Either arm is now a definite mathematical question about a checked
finite structure, not a mood about a mystery.

## §6. The dichotomy is a quantifier fibre, and both arms already have
## checked skeletons in the same directory

§5's rank(L) is one symbol standing over two questions, and the corpus
contains a checked theorem for each. Which one `rank(L)` names is fixed
by a single limitor (avacchedaka): **may the separable family depend on
the modulus, or must one family serve the whole level?**

**The arithmetic input, exact and general.** `PrimeChargeArbitraryRank`
does not stop at three primes. `squarefreeChargeCube n` is the
Möbius-signed squarefree charge on `n` prime places (one marked place
carrying `activeFactor`, the sign on every other active place), and
`localZetaCube_squarefreeChargeCube_eq_wCube` carries it, exactly, to
`wCube n` under the local ζ twist at each place — one invertible local
change of basis per place, so CP rank is preserved. `wCube` is the
`W` tensor; `shiftedWCube_not_rankAtMost` proves its rank exceeds `n−1`
by substitution induction uniform in a vacuum shift, and
`squarefreeChargeCube_rankExactly n` closes it: **rank exactly `n`, for
every `n`, checked.** (The same object and value are standard in the
tensor-rank literature under the name `W` state — Dür, Vidal and Cirac
2000 for the class; the module proves it from scratch and depends on no
citation.)

So the whole question is what `n` counts.

1. **Per-modulus reading, ∀q ∃family.** The bridge's coordinate family
   is one squarefree modulus's own places: `ChargeCell` is three bits,
   `cellModulus = firstRoot · secondRoot · thirdScale` = the divisor
   lattice of a single modulus with three prime factors. Under this
   reading `n = ω(q)`, so rank is exactly ω(q) — at most
   (1+o(1))·log q / log log q for every q, with normal order log log q
   (G. H. Hardy and S. Ramanujan, 1917). Sub-logarithmic: strictly
   inside §5's first arm, and then the bill is not merely poly-log but
   doubly logarithmic on the normal set.
2. **Uniform reading, ∃family ∀q.** `FiniteKuznetsovFactorizationRank`'s
   `RankAtMost` quantifies exactly this way — `∃ alpha beta radial, ∀ m
   n c` — over whichever coordinate family has been fixed. Fix the
   family to a whole sieve level and the places are every prime the
   weight carries, and the answer is already in
   `PrimeChargeUnboundedLocalRank`:
   `no_fixed_uniform_squarefreeChargeCube_rank` — no single finite
   channel count serves all finite sets of squarefree prime places —
   with `squarefreeChargeCube_rank_unbounded` supplying a dimension
   witness above every proposed bound. That is §5's second arm, and it
   is not open; its finite skeleton is checked.

**Reading, marked as a reading and not a theorem:** a Kuznetsov-type
fold applies one kernel under one c-sum, so the analytically relevant
quantifier is (2), not (1). Under (2) the rank bill is set by the
places the weight carries at level z, and the note's "rank bill equals
the prime count" is the arm the checked structure supports. Nothing
here asserts a trace-formula result; the bridge's own scope line stands.

**Why the symbol could carry two answers at once.** `rank(L)` as
written observes the pair (family, modulus) through a map that forgets
the quantifier order. That is `QuotientFiberLaw` at the level of the
question rather than the object: the observation class sees a quotient,
the two arms are the fibre over it, and no further reasoning about
`rank(L)` as a number recovers which arm was meant. Visibility returns
by one separating query, and it is one bit — is the family chosen
before the modulus or after. §5 is a naya and holds at its standpoint,
the per-cell bridge it read; this section adds the second standpoint
and the query that separates them, so the pair is syād rather than a
contest.

**The bit has a name, and the machine supplied it before this note had
one.** Handed { the fold compactifies ∥ the fold is bottomless } as a
residue, `garbha.dhara` — Umāsvāti, *Tattvārthasūtra* 5.31,
*arpitānarpitasiddheḥ*, run as an operation — returns a stream in which
every born position is *syād-asti-nāsti* in krama and **both born nayas
carry the same base standpoint**, separated only by **arpita** and
**anarpita**: the asserted aspect and the withheld one. Read back into
the quantifier, and this reading is this note's rather than the
machine's: asserting the aspect fixes the family before the modulus,
which is the uniform arm; withholding it lets the modulus choose, which
is the ω(q) arm. The two are then not two theses about the fold but one
thesis under two aspects — which is what makes a single number
`rank(L)` able to carry both and neither.

## Rigor boundary

- **Established by identification of prior results**: §1 (both halves
  cited to their lanes; the sieve-is-windowed-linear observation is
  this note's, at the precision K's own ledger allows).
- **Exact**: dispersion is degree-2 (§2); the diamond's compactness and
  its reciprocity provenance (§3, cited to LaghuVinimaya); §5's three
  readings (each a kernel-checked Lean theorem, cited by module, with
  the bridge's own scope line honored: finite rank obstruction, no
  analytic trace-formula claim).
- **Open, transported, now priced**: §4 as sharpened by §5 — the
  rank(L) growth dichotomy. Nothing asserted of Kuznetsov beyond its
  classical role and what the finite modules check.
- **Exact (§6)**: `squarefreeChargeCube_rankExactly n` (rank exactly n,
  all n), `localZetaCube_squarefreeChargeCube_eq_wCube` (the charge is
  the `W` tensor after a local ζ twist),
  `no_fixed_uniform_squarefreeChargeCube_rank` and
  `squarefreeChargeCube_rank_unbounded` — all in
  `formal/pairfield/Pairfield/PrimeChargeArbitraryRank.lean` and
  `PrimeChargeUnboundedLocalRank.lean`, both inside `Pairfield.lean`'s
  import closure and inside the lakefile `globs`, so they are built.
  The ω(q) bounds are Hardy–Ramanujan 1917 and the classical maximal
  order, cited not reproved.
- **Reading, not theorem (§6)**: that the Kuznetsov fold forces the
  uniform quantifier. This is the one step in §6 that a kernel does not
  hold, and it is the step the whole arm-selection turns on.
