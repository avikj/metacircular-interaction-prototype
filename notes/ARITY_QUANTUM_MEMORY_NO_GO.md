# Admitting one higher-arity context can force unbounded predictive quantum memory

**Status:** exact correspondence consuming `HIGHER_ARITY_CANCELLATION_FORMATION`.

Fix a prime `p` and arity `n>=2`. For sufficiently large `r`, let

\[
A_r=(1,\ldots,1,p^r-(n-1)).
\]

The higher-arity cancellation theorem proves that the input valuations and
normalized cancellation residuals of every nonempty proper subset of `A_r`
are independent of `r`, while the full `n`-term residual is exactly `r`.

Choose any `R` distinct admissible exponents. Under the control language
containing every proper-subset addition context, all `R` histories have one
predictive profile. Their exact classical and zero-error quantum predictive
memory dimension is therefore one.

After admitting the single full-arity context, the response `r` separates all
`R` histories. Exact readout of distinct deterministic responses forces their
density operators to have mutually orthogonal supports, so the minimum
Hilbert-space dimension becomes exactly `R`; `R` orthogonal basis states attain
the bound.

Thus for every arity cutoff and every `R` there is a finite process whose
memory jumps

\[
1\longrightarrow R
\]

when one next-arity control is admitted. No dimension bound depending only on
the previous memory or the arity increment exists.

## Changed next move

The arithmetic organism must stop treating “addition” as one fixed action
when its observation language includes subset-sum cancellation. Context arity
is control authority. Adding an `n`-input context can create arbitrarily much
exact predictive state even after the entire lower-arity language has been
compiled.

The constructive next object is therefore either:

1. an explicit maximum admitted arity with a separately compiled quotient at
   each level; or
2. a richer carrier, such as normalized unit-residue data, proved sufficient
   across all admitted arities.

No bounded list of scalar lower-arity residuals can serve as that carrier.
This is a zero-error statement; approximate compression and distributions on
contexts are outside scope.

