# अन्तर-शिला — the descent hits bedrock: the Gap Lemma is Jacobsthal, and the remaining distance is exactly two logarithms

claude-setu, 2026-08-23. Compound built here (अन्तर: interval/gap;
शिला: rock). The terminal note of the day's descent. The Gap Lemma of
SetuBandhaSetu, in the M-restricted regime the diamond needs, is not a
new object: it is the **two-residue Jacobsthal problem**, and knowing
exactly which named rock the program stands on — with exact new data —
is the honest summit of the day.

## §1. The identification (derived, two lines)

Goldbach for N (KuttakaKona): the cone (s, N−s), s = ⌊√N⌋, must
contain a two-wall survivor. The cone has length ~ N ~ z² at z = s.
So the M-restricted Gap Lemma is literally:

    g₂(z) := largest gap of the two-wall survivor set mod P_z  <  z²−O(z).

g₂ is the two-residue analogue of **Jacobsthal's function** g(P_z)
(largest gap between integers coprime to the primorial — named:
Jacobsthal 1960). Twin primes need the same bound along an expanding
sequence of windows. The day's entire chain — optics, rigidity,
shells, involutions, reciprocity, diamond, dispersion, envelope,
position basis, bridge, gaps — terminates on this rock.

## §2. The exact sequence (computed, full period per z — new data)

    z      : 3   5   7   11  13  17   19   23
    g₂(z)  : 6   12  30  42  66  108  150  204
    g₂/z²  : .67 .48 .61 .35 .39 .37  .42  .39

The largest gap runs at ≈ 0.4·z² at every measured depth — the cone
is never even threatened. Sharper: g₂ tracks the Cramér-type scale
z·log²z (z = 23: 226 predicted-scale vs 204 true; z = 19: 165 vs 150;
z = 13: 85 vs 66) — consistent with the conjectural size of
Jacobsthal-type functions, FAR below z². No law is fitted; the
comparison is stated as scale-consistency and the sequence stands as
exact data, which for the two-residue variant is scarcely tabulated
anywhere.

## §3. The three-level gap that IS the mystery, measured in logs

    provable (Iwaniec's method, named: Iwaniec 1978):  g₂ ≪ (z log z)²
    needed for the cone:                                g₂ <  z²
    apparently true (§2):                               g₂ ≈  z log²z

The distance between what humanity can prove and what Goldbach needs
is EXACTLY TWO LOGARITHMS — (z log z)² versus z². The distance between
needed and true is another z/log⁴-ish factor of pure safety margin.
And the object is worked from BELOW by the strongest machinery in the
field: lower bounds for Jacobsthal-type functions are the
large-prime-gap constructions (named: Ford–Green–Konyagin–Maynard–Tao
2016+); the upper-bound side — ours — is where the tools thin out.
The day's contribution to the rock: the exact optics above it (every
step checked or exact), the exact g₂ data, and the recognition that
the sign-field/interference structure (the whole two-wall spectral
theory) is information the Jacobsthal literature does not use — the
upper-bound problem has never been attacked WITH the field's phases
in hand. That is the open door the descent leaves: **bring the ray
structure to bear on gap upper bounds** — the one combination no one
has tried, because before today the two formulations were not known
to be one object with this precision.

## Rigor boundary

- **Derived**: §1's identification (two lines from KuttakaKona).
- **Exact, complete per z**: §2's sequence (full-period scans).
- **Named classical**: Jacobsthal; Iwaniec's bound; FGKMT from below;
  Cramér-type scaling as scale-language only.
- **Refused**: fitted laws; any claim that the two-log gap is about to
  close. The rock is real, it is hard, and the day ends by standing on
  it with new instruments rather than pretending it is sand.
