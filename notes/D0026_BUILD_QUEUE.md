# D0026 → the machine: the build queue

**claude-genius-braid orchestrator, 2026-08-17.** The owner has directed that
`collab/upstream/raw/D0026-owner-egb-core-transmission-v2-2026-08-16.md` (the
owner-side EGB/Math-Research core transmission, V2) be read as **instruction
for building the Natural Machine** — the 2026 math-automation object. This note
compiles that instruction into typed obligations mapped onto assets this
repository already holds, so that energy lands on joins rather than on prose.
Marks follow D0026's own alphabet (⊢ ↳ ☑ ◆ ≃? ? ⊥ Δ) plus the house queue tags
(PROVE / SEARCH / DEMONSTRATE). Consumers are declared per PROTOCOL.

The one-sentence orientation: **D0026 §17 closes by saying the next valuable
artifact "discharges a typed obligation and returns a new theorem, estimate,
compiler guarantee, faithful translation, or durable no-go result." This queue
is that sentence, instantiated against our own inventory.**

---

## 0. Standing constraint honesty

- This session's container has no agda/lean/ghc; every item below that lands
  Agda/Lean/Haskell source lands it **awaiting kernel** — a green is an exit
  code or it is a rumour. The gate loop and toolchain-bearing sessions check.
- No equivalence below is claimed. Items marked ≃? carry the obligation of
  D0026 §1.4 and our no-premature-Rosetta law alike: typed map + preserved
  invariant + round trip + defect + status, or it stays a candidate.

## 1. PROVE — dischargeable now, finite and exact

**Q1 (≃?→⊢, the three candidate identifications, each an afternoon of typed
work).** The strongest cross-corpus convergences from the 16-region
orientation; each needs a comparison map, not new mathematics:

- **Q1a.** D0026 §4.3 projection curvature `(PUP)(PVP)−PUVP=−PUQVP`
  vs our kernel-checked `ExcursionReturn`/`CompressionDefect` (T18.4,
  `K_tK_s−K_{t+s}=−PT_tQT_si`, cubical `--safe`, landed twice independently).
  Obligation: state both in one ambient category, exhibit the map, compute the
  round-trip defect (expected 0 on the shared fragment; the *difference* — our
  version's semigroup/time grading vs their word-insertion grading — is the
  retained residue). Consumer: `STRUCTURED_DEFECT`, the machine's fiber
  discipline, and D0026 §14.5's "tear maps to a specified object" acceptance.
- **Q1b.** D0026 §4.4 `N_obs = ⋂ ker(P∘Tⁿ)` vs our `ObservabilityQuotient` +
  `ExtremalDescription` (maximality PROVED here, only asserted there).
  Obligation: the map is near-identity; the deliverable is the *status
  upgrade* flowing upstream (their ⊢ → our checked term) with scope fence.
  Consumer: D0026 §14.1; our Delta-19 lane.
- **Q1c.** D0026 §8.4 consequence fiber vs our `VERIFIER_BLIND_FIBER_REWARD`
  (fiber = regular Γ₀(m)-torsor; endpoint observables constant on it; log 3
  per letter unrewardable). Obligation: show their qualitative fib_C(p₀)
  claim restricts to our quantitative theorem on the Smith task; declare what
  their general claim adds beyond our instance (expected: genuinely more —
  ours is one task family). Consumer: process-supervision lane; D0026 §14.8
  external-value test.

**Q2 (?→☑, D0026 §14.1's finite core).** Kernel-check, as exhaustive finite
certificates in the house style: the trefoil identity `M(ab,c)+M(a,b) =
M(a,bc)+M(b,c)` (one-line algebra over any monoid with declared p — provable
generally, not just finitely); the 4-element monoid one-sided-closure
associativity failure (§2.4) as an exact table computation; middle-nucleus
associativity on the same carrier. This is the transmission's own named
acceptance test, and it is exactly our proven pattern (finite `refl`
certificates + general lemmas). Consumer: D0026 §14.1 acceptance; the DSO
lane's constitutional standing in the shared corpus.

**Q3 (⊢ import, new exact objects for the notes lane).** D0026 §5 contains
exact identities the 16-region sweep shows nowhere in our corpus:

- the Chen primitive-boundary functional `Δ(E) = Σ_E μ² − Σ_E (ω−1)` with
  `1_P = μ² − (ω−1)` on `1 ≤ Ω ≤ 2` (§5.9) — twin/Goldbach counts as
  positivity of ONE functional on transverse Chen slices;
- the charge-deformed Buchstab identity and convolution semigroup
  `μ_{z1} * μ_{z2} = μ_{z1+z2}` (§5.2);
- the divisor-lattice characteristic polynomial
  `Φ_n(t) = t^{Ω−ω}(t−1)^ω` with κ_r as coefficients (§5.5);
- the fugacity propagator's three facets ([z⁰], value at 1, log-derivative
  moments) and `PU_{h+k}P − PU_hPU_kP = μ_{h,k}(ℕ∖{1})` (§5.11).

Obligation: land each as a note in house form (statement, proof or exact
derivation, scope fence, consumer), with finite instances kernel-checked where
the pattern fits (Φ_n(t) and the Chen projector are ideal refl-certificate
material). These are the *real* corpus-derived candidates the machine's mouth
was starved of. Consumer: the analytic lane; the machine vocabulary program
(see Q6).

## 2. SEARCH — comparisons that must precede any claim

**Q4 (≃?).** D0026 §5.12 item 2 (complete incomplete Kloosterman fractions;
control the Gauss change of basis uniformly) vs our **Hypothesis U**
(incomplete-interval bilinear bound behind M1's O(1)) and the E2/Mertens-floor
critical block at `[d,e] ≍ X`. Both corpora carved the same sub-√X equilibrium
/ balanced-critical-block wall from opposite sides. Obligation: a two-column
formal comparison stating whether the missing estimates coincide, imply, or
are incomparable — with the repo's proved constraints imported: DPP Thm 10 +
DCLOSE (no asymptotic zero-statistics input decides the variance targets;
warning for their route 3), L3/LEVER3 double-positivity (their correction #15,
proved here twice blind), archimedean de-centering (which parts of their
"global spectral effects" row are optics vs arithmetic). Consumer: both
frontiers; whichever session next attacks the one analytic door.

**Q5 (SEARCH).** D0026 §10 vs our Indic-logic lane (`ABHAVA`, `APOHA_*`,
`PRAMANA_*`, `ELSEWHERE_CONDITION`, `ExclusionScope.agda`). Their §10.3 poses
apoha-vs-polarity as open; our `ExclusionScope` already proves a scoped no-go
(no apoha operator on Eq(X); transitivity the single mechanism). Obligation:
check whether that theorem answers, narrows, or misses their question; report
with primary-source status per nalanda-dvara's discipline. Consumer: D0026
§14.6 acceptance ("bidirectional operational transport, not aesthetic
resonance").

## 3. DEMONSTRATE — machine engineering proper

**Q6 (the mouth).** The machine's certificate language speaks 0/suc/add; its
thoughts file already carries residuals demanding div/mod. D0026 §5 needs
Ω, ω, μ², λ. Per `THOUGHT_FORMAT.md`, residual lines demanding vocabulary are
the designed channel. Landed with this note: residual demands for
`Omega(n)`, `omega(n)`, `musq(n)` in `machine/thoughts.math`, so the
vocabulary-widening obligation (already the machine's own queue item) is
driven by a real consumer: expressing Q3's identities natively. Consumer:
MathMachine vocabulary program; codex-noether/yoneda's gate lane.

**Q7 (the join the orientation ranked #1 — offered, not trampled).** The
certificate-carrying induction gate (`MathMachineInductionGate.hs`, built,
mutation-controlled) is not wired into `round1`'s `kernelAccept` (refl-only ⇒
sound and nearly sterile). The gate lane is owned (codex-noether declared its
next enlargement). This queue *offers* the wiring as the D0026-aligned next
join — §8.1's "theorem = installed transformation" is empty until inductive
theorems can install — and stands ready to draft the patch on request.
Consumer: the live machine; F32's seed criterion.

**Q8 (the latch).** ~37 Agda modules are orphans (checked once by author,
never again); `Everything.agda`'s coverage claim was false at commit; no Agda
CI exists. D0026 §11.3's reverse-audit law makes this a constitutional defect,
not a chore. Obligation: extend `Everything.agda` to reach every non-Control
module (including `Swarm/` and `CarryClassNonzero`), regenerate
`AGDA_COVERAGE_INVENTORY.md`, and land the check as a gate-runnable script.
Consumer: every "checked" claim in the corpus.

## 4. What this queue is not

Not a Rosetta. Not a claim that the two corpora are one theory. Both
constitutions say the same thing about that: equivalence by proof, not
resemblance — समता प्रमाणेन, साम्येन न. Every ≃? above dies or lands as a typed
map with its defect retained.
