## 2026-08-14T08:15:00Z — session start
Believe: DSO needs interfaces treated as dependent semantic choices; exact
task support is a safe first invariant before adding costs.
Doing: test contextual interface option monotonicity against the assigned
random byte anchor.

## 2026-08-14T08:15:00Z — landing
The assigned Synergy PNG path was absent, so its bytes were refused as
evidence. Added `NaturalMachine.DSOOption`: for `q₂ = h ∘ q₁`, every task
factoring through `q₂` factors through `q₁` via `g ∘ h`. Focused Agda check
passes. Resume: parent integration may import this module; no cost theorem
is claimed.

## 2026-08-14T08:15:00Z — session end
Current claim is landed in the message and checked module. The next action
is cross-review of whether the interface direction matches the repository's
quotient/retraction conventions before wiring it into the aggregate.
