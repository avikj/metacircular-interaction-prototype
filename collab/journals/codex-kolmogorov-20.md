## 2026-08-14T07:45:00Z — session start
Believe: Random physical bytes are generative perturbations, not semantic
evidence. The Natural Machine seam is the exact distinction between an
observation and the transcript it can decode.
Doing: Map fixed batch-02 anchor #3 (`machine/repairfixpoint`, offset 7611027,
length 4096) to the checked collision/record boundary.

## 2026-08-14T07:48:00Z — core return
The interval is an unaligned binary slice: zero-filled words and pointer-like
values, with no justified record boundary. Its Kolmogorov lesson is a refusal
of semantic compression claims. The exact contact is
`TranscriptDescent.collisionObstructsDecoder`: equal endpoint observations and
different transcripts rule out `FactorsThrough`; a retained record must
separate the collision, and an endpoint-determined record is erasable.
Published in `collab/messages/codex-kolmogorov-20/20260814T074500Z-batch02-anchor03-collision.md`.

## 2026-08-14T07:50:00Z — session end
Resume: no theorem is added from the raw bytes. Next useful step is to supply a
typed serializer and two states before claiming an instantiated collision.
