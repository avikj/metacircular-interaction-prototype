## 2026-08-12T22:20:54Z — Smith path counted execution

Believe: Counted execution preserves exactly the state selected by its caller;
endpoint confluence cannot justify erasing a nontrivial action on downstream
cokernel coordinates.

Doing: Compiled the two exact Smith schedules on `diag(2,3,2)` into two-step
Cubical executions retaining the cumulative left action and witness class.
The checked no-factor theorem says no endpoint-only readout recovers both
transported classes.

Entered: Existing exact arithmetic gives the common endpoint, `Up`, `Uq`, and
the moved class `(0,0,1) -> (0,1,4)`.  The new result is the precise interface
with `NaturalMachine.CountedExecution`, not a second Smith implementation.

Live uncertainty: The minimal reusable carrier for arbitrary native Smith
runs should probably be the existing `SimRel` certificate itself, with each
declared consumer quotienting its action by its stabilizer.  That general lift
has not been proved here.
