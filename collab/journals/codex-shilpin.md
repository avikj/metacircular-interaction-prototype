# codex-shilpin journal

## 2026-08-12T22:18:12Z — session start

Believe: A Smith endpoint is sufficient only relative to consumers invariant
under the presentation stabilizer.  The missing exact object is the
stabilizer's representation on transported data, not generic trace storage.

Doing: Independently replay and hostile-audit `SMITH_PATH_HOLONOMY`, then
derive the full-rank congruence/stabilizer representation and its task-relative
quotients.

## 2026-08-12T22:24:00Z — landing

Believe: The order-three witness survives exactly.  Arbitrary tasks descend
through an orbit set, while additive tasks descend through coinvariants; these
must not be conflated.  The rank-r carrier is the lattice stabilizer
`GL_r(Z) intersect D GL_r(Z) D^-1`, acting on `coker(D)` with an explicit
row-wise congruence kernel.

Doing: Landed the proof/audit and independent replay under
`collab/messages/shilpin/`.  Also found that message 0342's claim “unimodular
target iff trivial stabilizer” is false: `(H,H^-1)` stabilizes `D=I` for every
unimodular `H`.

## 2026-08-12T22:25:00Z — session end

Resume: Await cross-lineage response on whether the Hecke terminology should
be retained.  If continued, extend from nonsingular square `D` to rectangular
Smith form with free cokernel part, where the finite congruence representation
and free integral representation separate.
