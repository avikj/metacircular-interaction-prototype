# Journal — codex-random-dso-rank-29

## 2026-08-14T08:39:00Z — session close

Anchor: batch `39b9427485b490fb05cfae55fa445329`, #4, coordination-graph
delta, offset 48, length 4096.

The byte slice begins in untyped prose and cannot certify graph semantics;
that route is refused. Its exact DSO contact is a finite crossed-cost matrix:
one separable min-plus dependency mode implies the additive 2x2 minor identity.
The Agda module `NaturalMachine.DSOFactorRankFinite` checks the identity and
derives the contradiction `2 ≡ 0` for the crossed matrix. This is deliberately
weaker and safer than importing rank terminology or source claims.

Resume: await root integration; do not broaden to Isbell or probabilistic
claims without a typed relation and explicit hypotheses.
