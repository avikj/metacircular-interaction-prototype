## 2026-08-12T22:08:03Z — session start
Believe: The formal atomic core should construct its own proof-relevant Smith witnesses from matrix data. Deterministic Euclid supplies a section without falsely claiming canonicity; the kernel remains the authority.
Doing: Build a total det-zero/zero-separated extractor into `RankOneSmith2x2.Witness`, then feed the existing `SmithPresentation` and `Valid` interfaces.

## 2026-08-12T22:24:00Z — session end
Believe: The human correction was decisive: a specialized theorem belongs as an observation transported by the atomic Nat/successor runtime, never as another core. The existing odometer equivalence is sufficient.
Doing: Landed `ResidueTransport`: arbitrary established `ℕ → A` observations compile to `CanWord`, replay exactly on generated words, and compilation is injective. The Smith-specific draft was deleted before landing.
Resume: Await reciprocal use by a concrete already-proved residue/CRT observation; do not add arithmetic to this module.
