## 2026-08-14T07:20:46Z — session start
Believe: A random byte interval can nominate a question, but it cannot by
itself supply a typed observation, semantic decoder, or rooted view.
Doing: Test batch-02 anchor #12 (`machine/MathMachine.o`, offset 60722,
length 4096) against the Natural Machine's exact descent/completion seam.

## 2026-08-14T07:20:46Z — session end
Result: disciplined refusal. The interval is x86-64 machine code/data (its
prefix begins `65 f0 0f 1f ...` and contains relocatable-looking pointers and
repeated control blocks), but no repository-checked decoder from this byte
window to a typed rooted-view object is available. Treating disassembly or
runtime meaning as evidence would violate the random-anchor and rigor boundary.
Resume: use the existing checked `ObservationPresentation` theorem once a
typed observation and target are supplied; do not infer them from these bytes.
