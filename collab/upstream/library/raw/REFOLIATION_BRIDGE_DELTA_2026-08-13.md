# Refoliation Bridge Delta
## Curvature, quotient semantics, reconstruction, and the correction to the Coordination Graph

Date: 2026-08-13
Status: cross-program synthesis with exact propositions and explicit conjectural bridges.

# 0. The new contribution

The quantum-gravity refoliation work changes several older library objects.

The strongest correction is:

**Noncommutation is not itself an irreducible coordination obstruction.**

The earlier Coordination Graph used
G_coord = G_noncommute union G_information.
That is exact in the restricted product-action model, but too crude universally.

GR gives the counterexample:
[H[N],H[M]] = D[beta_q(N,M)],
beta_q(N,M)^a = q^{ab}(N partial_b M - M partial_b N).

Two normal deformations do not commute, yet this is not a physical conflict requiring global serialization. Their commutator is another allowed geometric deformation. Different paths can represent the same physical spacetime process.

The correct primitive is **path coherence modulo semantic equivalence**.

Generalized curvature:
F(a,b) = [nabla_a,nabla_b] - nabla_[a,b]_allowed.

Quantum GR:
F(a,b) = (1/i hbar)[G_a,G_b] - G_[a,b]_HDA.

So the corrected candidate is

G_coord = G_curvature union G_information union G_resource/strategic,

where G_curvature records failures of semantic path independence, not every algebraic noncommutation.

# 1. From trace monoids to connections

The old concurrency theorem says FG=GF implies histories differing by swaps of F,G have identical semantics. This is the zero-bracket special case.

More generally two paths can satisfy
G_F F = C_{F,G} F_G G
where C is an allowed internal/gauge comparison transformation.

Define holonomy Omega(F,G) as the mismatch after applying the canonical comparison.

Strict commuting concurrency is C=I and Omega=I.
GR is the case where C is generated by the HDA bracket.
Quantum GR allows ray-level Omega=e^{i theta}I.

Hence:

strict commutation
subset closure modulo allowed transformation
subset projective closure
subset true curvature/conflict.

This hierarchy should replace binary commutes/conflicts.

# 2. Exact quotient-commutation proposition

Let X be a state space, F,G:X->X, q:X->Q an external semantic quotient.

PROPOSITION. If q(FGx)=q(GFx) for all x, then no downstream task factoring through q can distinguish the orders.

Proof. Every such task is h composed with q. Apply h to the equality. QED.

Strict commutation is sufficient but unnecessary. The correct question is whether paths commute in the quotient relevant to the external task family.

# 3. Contextual curvature

For allowed contexts C define p~_C q iff every allowed context gives the same observation.

A path cell is C-flat when its composites are contextually equivalent.

Define Curv_C as the set of elementary path cells not flat modulo ~_C.

Adding more contexts refines equivalence and can reveal more curvature. Thus necessary coordination is observer/capability/task-relative in exactly the same sense as minimal public semantics.

# 4. Materialization gap = foliation overhead

Sufficient Interfaces established a materialization gap:
Delta_mat(R,S)=log kappa_0(R)-log kappa_0(S o R) >= 0.

Forcing an intermediate R-state can expose distinctions irrelevant to the final composite task.

A foliation similarly materializes Sigma_0 -> ... -> Sigma_n inside a spacetime region whose physical boundary semantics should depend only on its actual boundaries.

General covariance demands independence from those intermediate materializations.

Thus refoliation invariance is a **dynamic materialization-independence principle**.

New theorem target: characterize when intermediate realizations of a composite relation are gauge-equivalent and when their mismatch produces curvature.

# 5. Wolfram causal invariance as flat rewrite transport

A multiway rewrite system has states, update edges, and diamonds/relations between rewrite sequences. Assign observer-relevant transport to edges. Each diamond has holonomy Omega_c.

Then the precise bridge is:

causal invariance = flatness of rewrite transport modulo observer equivalence.

This creates a falsifiable comparison to GR: ask whether a causal-invariant rewrite family has a continuum deformation algebroid whose curvature tends to zero and whose bracket approaches HDA structure.

# 6. Rovelli and descent consistency

RQM treats facts as relative to interactions rather than values of one observer-independent universal state.

A geometric implementation is a bundle/groupoid representation Sigma -> H_Sigma, with local relational frames as trivializations.

Evolution and frame change should satisfy
T_{Sigma'} U_F = U_{F'} T_Sigma
or projectively.

The mixed holonomy
Omega_mixed = (U_F')^{-1} T_{Sigma'} U_F T_Sigma^{-1}
is an exact descent obstruction for relational quantum descriptions.

# 7. Physical coarse-graining

The Knowledge Process insists coarse-graining should arise from actual restricted couplings.

If subsystem A couples to world X only through q:X->Y, then q is a physical interface. States in one q-fiber are operationally identical to A until another coupling exists.

The same form occurs in relational frames, capabilities restricting contexts, finite sieve scales, and agent context compression.

The reusable invariant is not "everything is a quotient" but:
**which distinctions survive the physically/computationally available interface?**

# 8. Prime-Pair: when cohomology is legitimate

The arithmetic program correctly warns that missing parity is not automatically a cohomology class.

Quantum gravity supplies a stronger discipline. There cohomology is forced:
F=i alpha I implies d_A alpha=0 by Jacobi/Bianchi, while rephasing gives alpha~alpha+d_A lambda.

So for arithmetic:
1. identify actual scale/place transport;
2. identify paths meant to represent the same reconstructed object;
3. compute their comparison/holonomy;
4. derive a cocycle identity from associativity/refinement;
5. identify legitimate gauge changes;
6. only then take cohomology.

This is a methodological upgrade for Reconstruction/Obstruction.

# 9. Candidate arithmetic curvature from Buchstab transport

Affine Buchstab flow is an exact candidate transport. Peeling one prime modifies one Euler place while globally applying an ax+b transformation.

For peeling operations P_p,P_q, compare the two orders after canonical affine re-identification.

At local Euler-product/KMS equilibrium this transport may be flat. On the positive cone with stopping/boundary conditions, the paths may retain different charge/boundary data.

Concrete target: compute this finite holonomy in the sieve-observability model and classify it as zero, removable/projective, genuine boundary holonomy, or unrelated. This is sharper than saying parity "resembles" gauge charge.

# 10. Positive cone and boundary anomaly pattern

Prime-Pair established:
- Goldbach and gap finite-place densities agree;
- their distinction is archimedean/sign-sector;
- symmetrization removes positive-cone homometry;
- the translation-invariant algebra is a boundary quotient;
- prime conjectures are boundary-sensitive.

The QG calculation independently found low-order bulk central anomalies vanish on closed spatial manifolds under restricted naturality assumptions, while boundary terms can survive.

Shared theorem pattern:

**bulk/local quotient can be flat while boundary/lift data carries obstruction.**

This does not identify arithmetic parity with gravitational anomaly. It points toward relative cohomology, extension theory, and boundary maps as common mathematics.

# 11. Why ordinary K-theory failed and what replaces it

Arithmetic ordinary K-theory did not detect Liouville gauge information. Prime creation is odd but not nilpotent; forcing a differential produces Mobius/squarefree rather than Liouville/bosonic parity.

The QG angle explains why endpoint K-groups may be the wrong invariant. QG's relevant invariant is transport curvature/holonomy, not endpoint topology alone.

Hence Workstream H's candidates gain a reason:
graded transfer holonomy, eta/spectral asymmetry, signed Ruelle/Fredholm determinant, scattering phase, cyclic cocycles, spectral flow.

These retain path/dynamical information endpoint K-groups erase.

# 12. Canonical vs grand-canonical

The arithmetic library has an exact hierarchy:
- grand-canonical fugacity field factorizes over Euler places;
- fixed total charge r is a symmetric-power coefficient;
- primes are the charge-one projection;
- fixing charge destroys naive local tensor factorization.

This is a global constraint creating coupling among locally independent modes.

The QG analogue is structural: local deformation directions are coupled because the HDA bracket depends on the metric being deformed.

Both warn against deriving the hard object from factorized local equilibrium without carrying the global constraint.

# 13. New operator-geometric reading of prime pairs

Let C=Omega(N) be factorization charge on l2(N), with prime projector Pi_1=1_{C=1}. Let additive translation U_h|n>=|n+h> where defined.

Define leakage
L_h=(1-Pi_1) U_h Pi_1.

Then
L_h^* L_h = Pi_1 U_h^*(1-Pi_1)U_h Pi_1.

The complementary compressed operator
Pi_1 U_h^* Pi_1 U_h Pi_1
has finite-volume trace equal to the prime-pair count at shift h, up to boundary conventions.

Therefore prime-pair correlation is exactly the **survival of the canonical multiplicative charge-one subspace under additive translation**.

This is a better physics connection than the earlier loose Lorentz analogies. The hard interaction is the transversality of additive motion to multiplicative charge fibers.

# 14. Projector connection target

For a smooth projector family Pi, the Grassmann connection has curvature Pi(dPi)^2.

The arithmetic setting is discrete, but define a projected path holonomy from additive moves:
Pi_1 U_h^* Pi_1 U_k^* Pi_1 U_h Pi_1 U_k Pi_1
versus reversed order, with finite-volume/boundary normalization.

This measures path dependence of repeatedly returning to the prime sector under additive moves.

It is not claimed analytically powerful yet. It is an exact finite object that can be computed immediately and compared with singular-series structure.

# 15. Sieve as base, charge as fiber

Bundle-language candidate:
- base = finite-scale sieve-visible state;
- fiber = unresolved factorization/charge data;
- Buchstab flow = scale transport;
- prime stopping = boundary condition selecting charge one.

The finite observability model should test whether the charge fiber has trivial product structure, nontrivial monodromy under scale/place loops, a two-sheeted cover, or a more complicated ambiguity action.

This gives Cubical Agda a stronger target than "quotient plus one bit."

# 16. Cubical/HoTT bridge

In HoTT, a dependent family P:B->U has transport along paths in B. A loop gamma:b=b acts on fiber P(b): monodromy.

So the finite arithmetic question becomes:

**Does the observable quotient carry loops whose transport acts nontrivially on the residual charge fiber?**

If yes, the obstruction is genuinely homotopical rather than merely "the fiber has two elements."

Quantum refoliation is the Hilbert-valued version: hypersurface space is the base, Hilbert spaces are fibers, and loop transport is holonomy.

This is a deep common formal language and a concrete extension of the existing Cubical work.

# 17. Coordination Graph correction, stated cleanly

Old:
coordination is required when actions do not commute.

New:
coordination is required when alternative admissible paths fail to be equivalent in the downstream semantic quotient.

Noncommutation may merely generate another internal/gauge direction.

Thus the causal object should be a 2-dimensional structure—groupoid/2-category/event complex with relations among paths—not merely a graph.

Edges encode transformations.
2-cells encode accepted equivalences between paths.
Curvature is failure of those 2-cells to close coherently.
3-cells/Bianchi identities encode coherence among equivalences.

This is a major structural upgrade: the "Coordination Graph" wants to become a **Coordination 2-Complex / higher groupoid**.

# 18. Information and curvature are dual obstructions of different kinds

The earlier work separated causal and informational dependencies.

That separation survives, but causal is now refined.

Information obstruction:
a quotient fiber contains states requiring incompatible downstream actions; no common witness exists.

Curvature obstruction:
two admissible transformation paths with the same intended boundary semantics fail to identify coherently.

One is failure of **state reconstruction/action sufficiency**.
The other is failure of **history/path descent**.

This gives two axes:
\[
oxed{
	ext{state ambiguity}
\quad	ext{vs}\quad
	ext{path ambiguity}.
}
\]

Prime-Pair has both candidate forms: finite-scale observable fibers and possible scale/place transport holonomy.
Quantum gravity is dominated by path ambiguity/refoliation.
Distributed computation can have both.

This distinction was not explicit enough in the library before.


# 19. A new master square across the projects

The same four objects now recur:

\[
\begin{array}{ccc}
\text{local/internal state} & \xrightarrow{\text{transport}} & \text{local/internal state}\\
\downarrow q && \downarrow q\\
\text{observable boundary} & \xrightarrow{\text{effective transport}} & \text{observable boundary}.
\end{array}
\]

There are two independent questions.

**Sufficiency:** does the vertical quotient retain enough information for the downstream task?

**Flatness/naturality:** does transport commute with quotienting, so that effective dynamics is well-defined independently of hidden representative/path?

This is exactly the distinction between the library's static reconstruction work and the new refoliation work.

A quotient can be sufficient at one instant yet dynamically unsound because hidden representatives evolve differently. The Markov lumpability/bisimulation theorems in the coordination corpus already anticipated this. Quantum refoliation gives the geometric continuum form.

Therefore the master object is not merely an observable quotient:
\[
q:X\to Q.
\]

It is a quotient **equipped with transport**, and the core condition is that transport descend:
\[
q\circ U_\gamma
=
\bar U_\gamma\circ q
\]
up to the accepted equivalence.

When this fails, the quotient has dynamical curvature/memory.

# 20. Connection to lumpability and bisimulation

The coordination theorem corpus proved that a Markov quotient is dynamically exact only under lumpability: states in one quotient block must induce the same transition probabilities between quotient blocks.

That is the stochastic analogue of the same square.

Bisimulation is the nondeterministic analogue.

Refoliation flatness is the geometric/gauge analogue.

Thus three apparently separate theories line up:

\[
\boxed{
\begin{array}{c|c}
\text{system} & \text{exact quotient condition}\\
\hline
\text{Markov process} & \text{lumpability}\\
\text{transition system} & \text{bisimulation/contextual equivalence}\\
\text{geometric quantum evolution} & \text{flat/projectively flat transport}
\end{array}
}
\]

The common problem is **descent of dynamics through an observational quotient**.

This may be the correct abstract center of a large fraction of the library.

# 21. The Knowledge Process gets a sharper mathematical question

The physical-process project asks how macroscopic entities—organisms, societies, observers, technologies—can be real patterns without being fundamental substances.

The new formal question is:

> Which coarse-grainings are dynamically closed enough that the microscopic dynamics descends to a coherent effective transport on the coarse variables?

That is stronger than saying a macrovariable is stable or compressive.

A physically meaningful macro-description \(q:X\to Q\) should approximately satisfy a descent condition:
\[
q(U_t x)
\approx
\bar U_t(q(x))
\]
for the task/time scales of interest.

Failure has two forms:
- unresolved hidden-state information affects future macro behavior;
- path/coarse-graining order produces nontrivial holonomy.

This gives a possible mathematical criterion for “real pattern”:
a quotient supporting approximately autonomous, low-curvature effective dynamics over a relevant regime.

That can be studied for thermodynamics, autocatalytic networks, biological organization, and agent societies without declaring information fundamental.

# 22. Computational irreducibility becomes fiber dynamics

Wolfram's irreducibility says microscopic prediction may resist shortcuts, while observers retain coarse variables.

The descent framework separates two possibilities:

1. microscopic dynamics is irreducible but the quotient dynamics \(\bar U_t\) is simple/closed;
2. hidden fiber state feeds back into the quotient, preventing autonomous coarse prediction.

The first is ordinary emergence of reducible macrodynamics from irreducible microdynamics.

The second is a failure of sufficient interface over time.

This gives a rigorous bridge between the Knowledge Process's “physically implemented coarse-graining” and computational irreducibility: ask for complexity of the descended quotient dynamics and information leakage from hidden fibers.

# 23. Agent-swarm context compression

Sufficient Interfaces already proved a context compressor \(q:\Omega\to C\) is safe for relational action selection iff every collapsed research-state fiber shares at least one valid action.

The new dynamic correction is:

Even if \(q\) is safe for the **next** action, it may not be safe for a sequence of actions if different hidden states induce different future compressed states.

So long-running agents need a dynamic sufficiency condition analogous to bisimulation/lumpability.

This is immediately relevant to the ultimate math machine.

A context state should not merely support one valid next action. It should support transport:
\[
(\text{compressed state},\text{certified action})
\mapsto
\text{compressed next state}
\]
independently of hidden details, or explicitly carry the residual fiber information required.

This gives a formal reason chat-summary compression repeatedly destroys research: it may preserve current conclusions while destroying the transport law of the inquiry.

# 24. New theorem target for agent memory

Let full research states be \(\Omega\), compressed states \(C\), action relation \(R\subseteq\Omega\times A\), and transition relation \(T\subseteq\Omega\times A\times\Omega\).

A compressor \(q:\Omega\to C\) is **dynamically sufficient** if there exists a compressed transition relation \(\bar T\) such that every valid full transition projects to \(\bar T\), and every compressed transition required for planning lifts to at least one valid full transition in every compatible hidden state.

This is a relational bisimulation condition over the research process.

Find the coarsest dynamically sufficient context quotient.

That is a serious mathematical/computational target for persistent autonomous research agents.

# 25. The surprising unification: obstruction has degree

The library has been using “obstruction” for many things. The new perspective separates them by categorical degree.

**Degree 0 / state obstruction:** a local observation fiber contains incompatible states; no common output exists.

**Degree 1 / transition obstruction:** a proposed local action cannot be lifted consistently across states/interfaces.

**Degree 2 / path obstruction:** two locally admissible paths fail to glue/identify; curvature/holonomy appears.

**Higher degree:** equivalences between paths themselves fail coherence; Bianchi/higher cocycle conditions appear.

This is not yet a theorem assigning every library problem a cohomological degree. It is a disciplined organizing hypothesis.

It explains why merely finding a nontrivial fiber is not yet cohomology. Cohomology enters only when there is higher coherence data.

# 26. A possible higher-categorical master object

The mathematical architecture may therefore require:

- objects: local semantic states/interfaces;
- 1-morphisms: admissible transformations/processes;
- 2-morphisms: certified equivalences/refinements between transformation paths;
- higher morphisms: coherence certificates.

Information/capability/resource labels decorate this higher process structure.

Then:
- strict concurrency is equality of 1-morphism composites;
- refoliation is a 2-isomorphism between deformation paths;
- projective quantum anomaly is a central phase on the 2-cell;
- Bianchi/Jacobi is 3-cell coherence;
- semantic quotient is a functor to an observer/task category;
- necessary coordination is failure of that functor to identify paths coherently.

This is why a plain DAG or ordinary category has kept feeling insufficient.

# 27. A direct connection to univalence

Cubical/HoTT is not merely a convenient formalization language here.

A dependent family over observable states has transport along paths. Univalence lets equivalences of fibers be represented as paths at the universe level.

So the statement “two internal realizations are semantically equivalent but not definitionally identical” becomes native.

The research target is to encode finite models where:
- quotient observations form the base;
- reconstruction ambiguities are fibers;
- process/refinement steps generate paths;
- loop transport acts on hidden charge/provenance fibers.

Then monodromy is executable.

This could make the Cubical branch the synthetic laboratory not only for Prime-Pair reconstruction, but for the common descent/curvature theory across arithmetic, coordination, and physics.

# 28. The arithmetic additive/multiplicative collision gets a geometric form

The Prime-Pair program repeatedly finds the hard structure at the interaction of addition and multiplication.

The operator statement in Section 13 makes this exact:

multiplicative charge \(C=\Omega(N)\) defines spectral fibers/projectors;
additive translation \(U_h\) does not preserve those fibers.

So define the commutator
\[
[C,U_h]
\]
or bounded/projected variants.

For basis state \(|n\rangle\),
\[
[C,U_h]|n\rangle
=
(\Omega(n+h)-\Omega(n))|n+h\rangle.
\]

This is exact.

Thus the additive/multiplicative interaction is literally measured by a discrete covariant derivative of factorization charge along additive directions.

Prime pairs select the special event
\[
C(n)=C(n+h)=1.
\]

This suggests treating \(C\) as a scalar field on the additive integer graph and studying its level-set geometry, transfer operators, and projected holonomy.

That is a fresh concrete mathematical angle generated by the QG connection viewpoint.

# 29. Discrete charge connection

Let the additive graph have vertices \(n\) and edges \(n\to n+h\). Define charge increment
\[
A_h(n)=\Omega(n+h)-\Omega(n).
\]

For ordinary scalar differences this connection is exact:
\[
A_h=\delta \Omega,
\]
so its abelian loop curvature vanishes identically around additive parallelograms where all vertices exist.

Therefore **raw Omega increments cannot carry a nontrivial cohomological obstruction**.

This is an immediate no-go.

Any nontrivial arithmetic curvature must arise only after:
- projection to restricted observables/charge sectors;
- positive-cone/stopping boundaries;
- nonlinear canonical extraction;
- scale truncation;
- or noncommuting compressed transfer operators.

This is useful: it kills the naive idea that factorization charge itself has mysterious additive curvature.

# 30. Projected curvature is the real candidate

Although full scalar charge transport is exact, projection can create nontrivial effective holonomy.

Let \(\Pi\) project to a restricted sector, such as primes or sieve-visible states. Compressed transports
\[
T_h=\Pi U_h\Pi
\]
need not form a representation:
\[
T_hT_k
\ne
T_{h+k}.
\]

The defect
\[
\boxed{
\mathcal K_{h,k}
=
T_hT_k-T_{h+k}
}
\]
is an exact **compression curvature**.

For the prime projector \(\Pi_1\), matrix elements of \(T_h\) are prime-to-prime transitions. The defect records failure of the prime sector to be closed under additive composition.

This is computable immediately.

For a sieve projector \(E_z\), compare
\[
E_zU_hE_zU_kE_z
\]
with
\[
E_zU_{h+k}E_z.
\]

As \(z\) changes, study whether the normalized compression curvature converges, vanishes, or develops a boundary layer near the prime horizon.

This is a genuinely new object that connects the QG refoliation angle to existing arithmetic operators without pretending they are the same physics.

# 31. Relation to prolate/concentration branch

The arithmetic program seeks a simplex-prolate concentration operator because product cutoffs miss the positive-boundary interaction.

Compression curvature gives another reason.

Whenever one projects dynamics to a constrained region/subspace, the projected transports cease to compose exactly. The defect is controlled by excursions through the complement:
\[
\Pi U_h\Pi U_k\Pi
-
\Pi U_hU_k\Pi
=
-\Pi U_h(1-\Pi)U_k\Pi.
\]

This identity is exact.

Thus the failure of compressed composition is literally the amplitude/weight of paths that leave the allowed region between steps and return.

For the additive simplex or prime charge sector, this “excursion through the complement” is precisely the boundary information product/separable models erase.

This may connect the prolate concentration eigenvalues to a curvature/leakage norm.

# 32. Exact compression-defect identity

Let \(\Pi\) be any projection and \(U,V\) any composable operators. Define compressed maps \(T_U=\Pi U\Pi\), \(T_V=\Pi V\Pi\).

Then
\[
T_UT_V-\Pi UV\Pi
=
-\Pi U(1-\Pi)V\Pi.
\]

Proof:
\[
\Pi U\Pi V\Pi
=
\Pi U[I-(1-\Pi)]V\Pi
=
\Pi UV\Pi-\Pi U(1-\Pi)V\Pi.
\]
QED.

This is one of the cleanest bridges in the entire synthesis.

Interpretation:
**effective dynamics fails to compose exactly because hidden excursions through discarded degrees of freedom matter.**

That sentence simultaneously describes:
- projected quantum dynamics;
- coarse-grained stochastic memory;
- materialization/context loss;
- arithmetic positive-boundary leakage.

# 33. Information version of compression curvature

If hidden excursions affect the final target, then a quotient interface that discards them is dynamically insufficient.

The information lower bound says any correction message must carry conditional information about the unresolved target.

So curvature and information meet:

- operator curvature says the compressed dynamics does not close;
- conditional mutual information lower-bounds how much hidden-fiber information must be restored to repair predictions.

A future theorem should relate a norm of compression defect
\[
\|\Pi U(1-\Pi)V\Pi\|
\]
to a task-specific information requirement for reconstructing the correct composite output.

That would directly unify the two halves of the Coordination Graph program.

# 34. Quantum/classical emergence

Decoherence can be seen as suppressing observable interference between sectors after entanglement with an environment.

The descent perspective asks a sharper question: when does the coarse classical observable algebra become approximately closed under effective dynamics?

Approximate closure means small compression curvature on the observable sector.

Thus classicality may require not only decoherence of off-diagonal terms but approximate **dynamical descent** of the retained algebra.

This is compatible with, but not a replacement for, standard decoherence theory.

It gives the Knowledge Process a concrete mathematical object to investigate rather than a new interpretation slogan.

# 35. The ultimate project's common engine

Across the library, the recurring operation is now:

1. choose/physically inherit an interface or projection \(\Pi\) / quotient \(q\);
2. characterize its ambiguity fiber;
3. ask whether dynamics/processes descend through it;
4. compute compression curvature/holonomy when they do not;
5. determine the minimal supplement restoring closure/reconstruction;
6. classify genuine obstruction modulo gauge;
7. track the object under scale/refinement;
8. formalize/certify it.

This is not a claim that number theory, quantum gravity, AI, and decentralized systems are one theorem.

It is a reusable **mathematical research engine**.

# 36. Concrete next calculations for collaborators

### Physics collaborator
Attack local Lie-algebroid cohomology of HDA. Prove or kill the conjecture that physically local bulk central extensions vanish in 3+1 dimensions.

### Prime-Pair collaborator
Compute compression defect
\[
\mathcal K_{h,k}^{(z)}
=
E_zU_hE_zU_kE_z-E_zU_{h+k}E_z
\]
and the prime-sector version with \(\Pi_1\), first in finite volume. Compare norms/traces with boundary ratios, parity sectors, and singular-series normalization.

### Cubical collaborator
Extend the finite observability model from fibers to dependent families with transport. Compute monodromy action of observable/refinement loops on residual charge fibers. If all loop actions are trivial, record the no-go.

### Coordination collaborator
Replace `G_noncommute` with a path-equivalence/curvature 2-complex. Formalize quotient commutation and contextual flatness. Determine when local noncommutation closes inside an allowed internal groupoid.

### Knowledge-process collaborator
Model a coarse physical observable as a projection/quotient and test approximate dynamic closure. Quantify hidden-state feedback through compression defect or conditional information.

# 37. One result I would put on the wall

The exact identity
\[
\boxed{
\Pi U\Pi V\Pi-\Pi UV\Pi
=
-\Pi U(1-\Pi)V\Pi
}
\]
is elementary, but in this context it is conceptually central.

Every time we compress the world to an observable sector, the failure of effective composition is exactly mediated by trajectories that leave the retained sector and return.

That is:
- hidden state becoming dynamically relevant;
- a coarse theory acquiring memory;
- a projected quantum theory acquiring effective corrections;
- an arithmetic boundary sector contaminating local equilibrium;
- a compressed agent context losing future-valid information.

The next step is to stop treating these as analogies and compute this defect in each canonical finite model.

# 38. Updated master distinction

The library's Reconstruction / Obstruction pair should be expanded:

\[
\boxed{
\text{Reconstruction}
\quad
\text{Descent}
\quad
\text{Obstruction}.
}
\]

**Reconstruction:** can hidden state/target be recovered from observables?

**Descent:** does the dynamics/process law close on those observables?

**Obstruction:** if not, is the failure removable by refinement/gauge, or is there a stable invariant?

Prime-Pair was mostly asking Reconstruction.
Quantum gravity forced Descent to the foreground.
Coordination needs both.
The Knowledge Process needs both to explain autonomous macroscopic patterns.

That is my strongest cross-library update.
