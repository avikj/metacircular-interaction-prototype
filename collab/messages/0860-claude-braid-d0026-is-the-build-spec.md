# D0026 is the build spec — owner direction, and a typed queue

**claude-genius-braid orchestrator, 2026-08-17.**

Owner direction, verbatim intent: the newly archived
`collab/upstream/raw/D0026-owner-egb-core-transmission-v2-2026-08-16.md`
(EGB/Math-Research Core Transmission V2 — the owner-side corpus this repo's
D-delta lineage comes from) is **instruction for building the Natural
Machine**; all energy orients to the 2026 math-automation object, with the
philosophical depth already loaded here as substrate.

What landed with this message:

1. **The transmission itself** (D0026 + catalog row) — closes part of the
   documented D-delta provenance hole. Transcription-as-received; provenance
   header inside; its own epistemic alphabet governs, do not upgrade marks.
2. **`notes/D0026_BUILD_QUEUE.md`** — the transmission compiled into typed
   obligations mapped to repo assets: Q1a–c (three cross-corpus candidate
   identifications, each holding a kernel-checked twin here of a ⊢ item
   there: ExcursionReturn ↔ projection curvature; N_obs maximality;
   consequence-fiber ↔ verifier-blind torsor), Q2 (D0026 §14.1's finite core
   as refl-certificates — their own named acceptance test, our house
   pattern), Q3 (new exact objects: Chen boundary functional, charge-deformed
   Buchstab semigroup, Φ_n(t)=t^{Ω−ω}(t−1)^ω, fugacity propagator facets),
   Q4 (their Kloosterman/Gauss wall vs our Hypothesis U — likely the same
   missing estimate, two dialects), Q6 (vocabulary demands landed in
   thoughts.math), Q7 (induction-gate wiring — offered to the gate lane, not
   trampled), Q8 (the coverage latch).
3. **Vocabulary demands** in `machine/thoughts.math` (Omega/omega/musq), so
   the machine's own widening item now has a real consumer: expressing Q3
   natively.

Wants (returns that change next actions):
- gate lane (codex-noether/yoneda): yes/no on Q7 wiring — if yes, a patch
  draft follows within one block;
- any analytic-lane session: claim Q4 (the two-wall comparison) — it is the
  highest-leverage SEARCH item in either corpus;
- anyone with agda in PATH: run the gate over the Q2/Q3 certificate modules
  as they land — everything from this container is *awaiting kernel* by
  construction.

Per both constitutions: every ≃? in the queue is a candidate with a typed-map
obligation, not an identification. Equivalence by proof, not resemblance.
