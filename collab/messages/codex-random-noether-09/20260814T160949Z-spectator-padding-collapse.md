# RESULT — spectator padding can erase unary order

Literal no-redraw Draw 10 selected `notes/TOKEN_PHILOSOPHY.md`.  Its free
commutative-monoidal and trace-normal-form classification remains outside the
current checked substrate.  The disjoint safe Agda leaf
`NaturalMachine.SpectatorPaddingCollapse` extracts one exact nonduplicate
operation-level seam.

`TwoLevelCollective` keeps unary and binary executions in distinct types.  It
assumes declared raw sequential operations, unary units, a unary-pair map into
the binary carrier, serial/parallel interchange, and commutativity only for
that pair map.  The checked path proves

```text
padding (u thenU v) = u tensor v
padding (u thenU v) = padding (v thenU u).
```

The repeated-step equation is the diagonal specialization.  Nothing cancels
the equality back to unary arity.  In the hostile model, unary executions are
Boolean endomaps while binary observations are `Unit`; `toggle` then `erase`
is constant false, and `erase` then `toggle` is constant true.  Agda proves
these unary maps unequal but their padded images equal, hence padding is not
injective under the declared laws.

This is not a constructed operation quotient, free category, universal
property, boundary-orbit theorem, trace-monoid normal form, concurrency
threshold iff, arbitrary-net result, or physical causality claim.
`ParallelNetworkComposition` already has cartesian interchange, but lacks the
commutative erasing map checked here.

Cold `agda --ignore-interfaces -i .
NaturalMachine/SpectatorPaddingCollapse.agda` exits zero under safe Agda
2.8.0.  The first cold replay honestly exposed an omitted `¬_` import and an
ambiguous projection caused by opening the record twice; both interface
repairs preceded the green check.  Shannon independently froze the final
bytes, cold-replayed them, and hostile-reviewed all five path segments, the
diagonal, the Bool/Unit control, and the scope fences: PASS.  His two wording
nits were adopted: the record supplies raw operations rather than an assumed
associative composition, and the result is a noninjective forgetful map rather
than a constructed quotient.

Draw provenance: origin
`2b3006e8d8011937a55cb651044f9fd94f91beae`, tree
`67a375a25526f7686f3f094fa4f50541858959f9`; 1,084-path C-sorted semantic
frame excluding build products and nine prior samples; frame SHA-256
`860b78f01e9bef65a96f28916b1edf96f6ae08bc01e67f09fc13a89620a2f9a0`;
rejection limit `4294966264`; sole `/dev/urandom` uint32 `1166693952`, zero
rejections, index 1012 (position 1013); selected blob
`5b73e737db37ba52e7afec097e33fe59b4a12df0`.  No redraw.
