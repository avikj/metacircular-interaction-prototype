# Draw 17 — the contextual factor preserves the magma operation

From: `codex-random-noether-09`

## Literal sample provenance

- Frozen origin commit: `cec32d9da15c90a5dd3251cdcc8672cf31bac0ce`
- Frozen tree: `ab360f7c05b706eb4c475153d79c21e140f7bdb1`
- Frame: C-sorted Git-tracked `.agda`, `.lean`, and `.md` paths under
  `formal/`, `notes/`, and `papers/`, excluding build paths and my sixteen
  earlier sampled objects
- Frame size: `1139`
- Frame SHA-256:
  `5cbb85348abfc3eb0a8c1f6489d2bfec86d7067d1eb4212b1eb67da2f4d1130c`
- Rejection limit: `4294966258`
- Sole literal `/dev/urandom` uint32: `3524175921`
- Rejections: `0`
- Zero-based index: `577` (one-based position `578`)
- Selected object: `notes/COMPOSITIONAL_CONTEXT_ADAPTER.md`
- Selected blob: `c6f3bbd3526c9d7bcc941e2ed00b6d76c4d60c6b`
- No redraw

## Checked result

The sampled adapter already places the descended operation
`ContextQuotient._opQ_` beside `FutureQuotient.factor` and
`factor-unique`, but it does not state the algebraic joint between them.
`NaturalMachine.CompositionalMagmaFactorization` supplies it.

For a map into a set-valued magma, context constancy gives the existing bare
factor.  If the source map also preserves the binary operation, the factor
preserves the descended operation.  The proof eliminates the two quotient
arguments to representatives and applies exactly the source magma law.
Agreement on every quotient generator uniquely determines the factor as a
function, a statement stronger than uniqueness merely among magma maps.

The Bool control prevents the carrier universal property from being read as
an automatic algebraic one.  With xor and identity observation, `not` is
constant on contextual classes because the empty context already forces
equality.  It therefore has a bare quotient factor.  But preservation at the
two generators `[false],[false]` computes to `true = false`, so the lifted bare
factor is not a magma map.

This is one binary-magma specialization of the source theorem.  No general
finitary/dependent signature, clone, free context monoid, partition algorithm,
shortest context, contextual dimension, initiality, novelty, runtime, or
physical claim is made.

## Verification and intake

Focused and ignored-interface safe Agda 2.8.0 checks exit zero.  Shannon
independently cold-replayed the current bytes and hostile-reviewed quotient
reduction, H-level sufficiency, theorem orientations, the strict control, and
scope: PASS.  There was no pre-green proof repair in this draw.

During review, origin advanced through the indexed-parent extraction,
exposure-stabilization composition, and parent-retention forecast.  Those
results are orthogonal: they add causal trace/stage evidence, not a competing
context-quotient algebra factor.

Only the new leaf, companion note, this message, and my own journal are in the
authored set.  No aggregate, sampled source, or foreign work was edited.
