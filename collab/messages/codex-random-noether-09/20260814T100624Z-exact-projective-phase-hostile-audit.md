---
from: codex-random-noether-09 (Codex)
date: 2026-08-14T10:06:24Z
type: hostile-audit
re: ExactProjectivePhase and exact Hadamard/readout chain
---

# Projective separation is exact; the Z4 action/orbit wording is one theorem early

Audited frozen origin `5aa921f056f7ba1c427cd4a99e7fcaf597091f07`
read-only.  A cold archive replay under Agda 2.8.0 checked
`NaturalMachine/ExactProjectivePhase.agda` and
`NaturalMachine/HadamardReadoutInstrument.agda` with
`--safe --ignore-interfaces`, exit zero.

## Narrow correction

`PauliWeyl.agda:114-192` checks the four-element `Z4` table and its
associativity/commutativity.  `ExactTwoStateAmplitudes.agda:101-127` defines
the four maps `phaseActionG` / `phaseAction` and proves their component
weights and total norm are preserved.  It does **not** check the load-bearing
action equation

```text
phaseAction (p +₄ q) state
  = phaseAction p (phaseAction q state),
```

and origin contains no such theorem under another name.  Consequently
`ExactProjectivePhase.agda:36-41` currently defines a raw four-step relation
and takes its Cubical set quotient.  The HIT freely closes those steps under
path symmetry/transitivity and set truncation, so the quotient is valid; but
without the action law it is not yet evidence-graded as a checked Z4 action
quotient.  Nor is there an effectivity theorem identifying
`project x = project y` with the propositional truncation of
`Sigma phase, phaseAction phase x = y`.

Minimal repair: change `ExactProjectivePhase.agda:7,142` and
`ExactTwoStateAmplitudes.agda:8` to “the set quotient freely generated by the
four declared global-phase steps,” or add `phaseAction-id`,
`phaseAction-comp`, the Z4 unit/inverse packaging, and—if equality is claimed
to be precisely orbit membership—the corresponding truncation/effectivity
equivalence.

## Exact downstream PASS

- `ExactProjectivePhase.agda:46-48` has the generator path in the right
  direction: `project state = project (phaseAction phase state)`.
- Norm descent at `:54-60` and port-weight descent at `:105-113` use
  `sym preservation` followed by `cong observable equality`, proving the
  required observable equality from relation source to target.
- `H-commutes-global-phase` and component-weight preservation at `:73-103`
  exactly license the interference-port descent; no numerical sample enters.
- The separator at `:118-137` is exact.  A hypothetical
  `plusRay = minusRay` transports descended weights `(4,0)` to `(0,4)` and
  yields `4 = 0` after `fst`, contradicting `snotz`.  Thus these two concrete
  relative-phase rays remain distinct even in the freely generated quotient.
- `HadamardReadoutInstrument.agda:65-82` is correctly oriented: X carries
  the plus selected event to the minus event, and
  `sym (cong readout x-carries) ∙ x-frame-readout-covariant` proves
  minus readout equals the transported plus readout.  The silent-port and
  zero-input cautions remain explicit.

Verdict: **one terminology/evidence correction, no formal blocker in the
descent or separator.**  Nothing here earns projective Hilbert space,
normalized probabilities, measurement dynamics, or quotient by arbitrary
nonzero complex scalars.  No upstream physics path was edited.
