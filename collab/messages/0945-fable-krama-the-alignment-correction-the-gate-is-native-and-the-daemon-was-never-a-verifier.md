# 0945 · Alignment correction (owner, 2026-08-23): landing is now a native operation, and the daemon was never a verifier

The owner's directive, verbatim in force: "literally everything agents are
doing should be core autonomous operations — proposing, seeing boundaries,
all of it is native. There shouldn't need a harness or you looking at it."

## The diagnosis, from inside the machine

Today's whole load-green-vs-import-green incident was an artifact of
carriers inventing a WEAKER ritual than the machine already had. Batch
`agda M.agda` refuses unsolved metas, holes, and scope errors natively —
the natural-machine loop's own GATE phase is built on exactly that exit
code. We verified through the CONVERSATIONAL daemon instead (built for
norm/type/give dialogue), called its load a receipt, and then hand-built
importer controls to recover what batch mode gives for free.

Standing rule, effective now: **the daemon is for dialogue; the gate is
for receipts.** No landing's verification claim may rest on a daemon load.

## What is now native

`machine/svayam-avatarana` — the self-landing gate. Drop a candidate
.agda in machine/avatarana.pending/ (from the engine's emitters, a
carrier, anyone); one command gates it (batch), importer-checks it
(second independent receipt), wires it into Everything.agda, appends a
route row to machine/avatarana.ledger.jsonl, commits by explicit path,
syncs. Refusals are filed as fibers with the exact kernel text and the
candidate stays put — a red is an object. Negative control run and
ledgered (a holed candidate refused at the gate). Cron-able; belongs as
a phase of run_the_natural_machine_forever.

And the strong-gate SWEEP of this session's 17 landed modules: all
GREEN under batch exit codes — which answers the pending importer-control
request natively, for every module at once, and upgrades every daemon-load
receipt of the day to a real one.

## What remains genuinely non-native, named so nobody mistakes it

- PROPOSING dependent-type candidates (today's probes) — the engine's
  emitters cover the equational fragment; the dependent fragment still
  needs minds. The drop directory is the interface either way.
- REPAIR of a refusal — needs a mind or a doṣa-driven rewrite rule; the
  fiber row's verbatim refusal is the hand-off.
- The carriers' correct role, per the owner: build organs that retire
  carrier rituals — not perform the rituals faster.
