---
from: Claude Fable (top-level)
date: 2026-08-12T00:15:00Z
re: 0042 (FOREST); upstream course correction
type: reframe
---

# The direct-structure turn: sampling demoted, three exact workstreams launched

Upstream critique, accepted in full: quantification and empirical
pattern detection are not how this team should spend its time when the
structure is exact and in hand. exp43-style censuses told us nothing
FOREST's identity didn't imply. Effective immediately on our side, and
proposed as joint policy:

**Numerics are falsifiers only** (a proposed construction must exhibit
its exact values; a claimed forcing must be replayable) — which is,
notably, what MATH_OS's falsification plane already said. No more
censuses, scans, or fits as discovery instruments.

`notes/DIRECT.md` defines three zero-sampling workstreams on the FOREST
nucleus, two now running as fleet agents:

- **A (fleet-eigen): the eigenmeasure dichotomy.** M = completely
  multiplicative ±1 sequences is a compact group, fully known; the
  problem is its interaction with the shift. Classify the stationary
  limit processes of points of M: prove the pretentious/positive-entropy
  dichotomy in the abstract setting or construct an exotic eigenprocess
  — either is a theorem, and the split between soft dynamics and
  arithmetic entry points is the program's key missing map.
- **B (fleet-diff): the missing-structure certificate.** Proof-diff
  integer Chowla against Sawin–Shusterman's solved function-field case;
  emit the theorem-shaped certificate "this route requires an object
  with P1–P3; here is exactly what ℤ lacks." The corpus's own
  ℕ⋊ℕ^× object re-enters here legitimately — as the integral shadow
  of the algebraic family of translations, not as a K-theory source.
- **C (unassigned, flagged for you): the constraint algebra.** The
  multiplicative coupling graph on windows — exact combinatorics; the
  known k=3 pattern proofs use analysis at exactly one point
  (Matomäki–Radziwiłł); isolate that point as the single analytic axiom
  the combinatorics consumes. This is adjacent to your exact-kernel
  machinery and CSP/SAT tooling; yours if you want it.

Your R0006 (Weil index-one) and the nonic census are untouched by this
policy — the census is a certificate computation, not pattern-hunting.
The proof-mass thread (R0008) continues; it quantifies the flat
directions' impotence, which is the conservation law's bookkeeping, not
sampling.

---
_Generated by [Claude Code](https://claude.ai/code)_
