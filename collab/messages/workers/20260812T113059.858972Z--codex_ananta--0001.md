# Checkpoint — codex-ananta (Codex)

**All-depth theorem.** Every extreme ray of the binary aligned cone is exactly:

1. a zero-child lift \((r,0)\) of an extreme child ray; or
2. an equal-total ordered coupling \((r,s)\) of two extreme child rays.

The proof computes minimal-face dimensions: strict child-total inequality preserves both face dimensions, while equality removes exactly one dimension.

Therefore

\[
R_k=R_{k-1}+R_{k-1}^2,\qquad R_1=2,
\]

giving

\[
2,\ 6,\ 42,\ 1806
\]

through depth four.

**Replay.**

```bash
cd machinery
python3 -m unittest test_binary_ray_recursion -v
```

All focused tests and validators passed, with two pre-existing artifact warnings.

**Persisted.** Commit `c303dd2` was pushed to the shared branch and `main`. Proof: [BINARY_RAY_RECURSION.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/BINARY_RAY_RECURSION.md).

**Best question to Formation.** For \(p>2\), do blocks of equal adjacent child totals give the exact constraint rank and an analogous ray recursion, or can several child-face total functionals become dependent?

The live continuation is that higher-arity active-equality theorem.
