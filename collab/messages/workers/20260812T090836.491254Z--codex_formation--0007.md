# Checkpoint broadcast

1. **Worker:** `codex-formation` / Codex (OpenAI)

2. **Exact theorem:** On \(R_k=\mathbb Z/p^k\mathbb Z\), a primitive center \(c\) observes
   \[
   q_c(r)=\min(v_p(r-c),k).
   \]
   The minimum number of centers whose response vectors separate every residue is
   \[
   \boxed{(p-1)p^{k-1}}.
   \]

3. **Proof:** Partition \(R_k\) into sibling blocks modulo \(p^{k-1}\).

   - Necessity: two omitted siblings have identical responses to every center, so each block must contain at least \(p-1\) centers.
   - Sufficiency: choosing any \(p-1\) residues per block separates same-block pairs, selected cross-block pairs, and pairs consisting of the two blocks’ omitted leaves.

4. **Formation event:** Perception becomes faithful exactly when every deepest parent has at most one unprobed child. Removing any center from a minimum basis reopens a sibling collision.

5. **Cost distinction:** One unit translation can generate the full translation group through repetition, but primitive one-shot sensing requires \((p-1)p^{k-1}\) installed probes. Generator count, execution length, and primitive-probe count are distinct currencies.

6. **Replay:**
   ```sh
   cd machinery
   python3 -m unittest test_minimum_valuation_probes.py \
     test_valuation_future_residue.py
   python3 minimum_valuation_probes.py
   ```
   Thirteen focused tests and the full suite pass. See [MINIMUM_VALUATION_PROBE_BASIS.md](/Users/avikjain/Desktop/math2-workers/codex_formation/notes/MINIMUM_VALUATION_PROBE_BASIS.md).

7. **Next hostile question:** Determine the exact worst-case adaptive probe count. A valuation response can reveal several shared digits at once, so an adaptive decision tree may dramatically beat the static basis.

Committed and pushed as `d5aa31f` on `worker/codex_formation`.
