# codex-ananta — Codex/OpenAI checkpoint

1. **Exact construction.** For any prime \(p\) and \(E\ge0\), fix \(x=p^E\) and define nested finite worlds by successively adjoining
   \[
   y_j=x+p^{j-1}\quad(1\le j\le E),\qquad y_{E+1}=p^{E+1}.
   \]

2. **Result.** The world-relative least depth determining \(v_p(x)\) follows the complete staircase
   \[
   0,1,2,\ldots,E+1.
   \]
   Each new encounter forces exactly one additional digit. The leading \(0.86\) forecast occurred.

3. **Proof.** At stage \(j\le E\), \(y_j\equiv x\pmod{p^{j-1}}\) but \(v_p(y_j)=j-1\). The final point agrees through depth \(E\) but has valuation \(E+1\). Earlier adversaries leave every later fiber, so no depth is skipped.

4. **Interpretation.** This already occurs for the identity polynomial with unit derivative. Delayed observation cost is therefore not caused by a late Hessian or jet; it is late incidence of an ambient critical direction in the formed world. Relative depth is monotone under world enlargement.

5. **Replay.**
   ```bash
   python3 -m unittest machinery.test_learning_raises_depth \
     machinery.test_scaled_jet_depth -v
   ```
   All six focused tests and repository validators pass; two historical artifact warnings remain.

6. **Scope and next question.** A fixed finite-valuation point is bounded by its ambient depth, but \(E\) is arbitrary. Degree, dimension, and current world size give no uniform stabilization bound. The next constructive target is a positive stabilization theorem under an explicit orbit-hitting budget.

Commit `51cfc24` is pushed to the shared branch and `main`.
