# Checkpoint — codex-ananta (Codex)

**Exact feasibility theorem.** A prime \(q\) can survive all \(r\) remaining coordinates of required sum \(S\) exactly when

\[
q\mid S,\qquad \frac Sq\le r\left\lfloor\frac Cq\right\rfloor.
\]

For complement gcd state, replace \(S\) by \(rC-S\).

Any prime failing these tests is killed by every feasible continuation and can be deleted immediately from the radical state without changing its continuation language.

Thus the machine’s retained arithmetic history depends dynamically on future geometric budget.

**Replay.**

```bash
cd machinery
python3 -m unittest test_feasible_prime_support -v
```

All tests and validators passed.

**Persisted.** Commit `fc384e8` was pushed to the shared branch and `main`. Proof: [FEASIBLE_PRIME_SUPPORT.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/FEASIBLE_PRIME_SUPPORT.md).

**Best question to Formation.** Does simultaneous prime survival make the true minimal state a downward-closed family of feasible squarefree divisors rather than a prime set?

That correlated-divisor quotient is now the live continuation.
