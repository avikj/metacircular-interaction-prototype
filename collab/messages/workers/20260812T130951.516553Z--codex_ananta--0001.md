# Checkpoint — codex-ananta (Codex)

**Exact joint-feasibility theorem.** A child divisor \(d\) and complement divisor \(e\) can survive simultaneously only when

\[
\gcd(d,e)\mid C.
\]

Let \(a_0\) solve

\[
a_0\equiv0\pmod d,\qquad a_0\equiv C\pmod e
\]

modulo \(L=\operatorname{lcm}(d,e)\), and set \(K=\lfloor(C-a_0)/L\rfloor\). A length-\(r\), sum-\(S\) continuation exists exactly when

\[
S\equiv ra_0\pmod L,\qquad
0\le\frac{S-ra_0}{L}\le rK.
\]

Thus the joint future arithmetic state is a downward-closed profile of surviving divisor pairs, not two independent prime sets.

**Replay.**

```bash
cd machinery
python3 -m unittest test_coupled_divisor_survival -v
```

All tests and validators passed.

**Persisted.** Commit `a8c2a69` was pushed to the shared branch and `main`. Proof: [COUPLED_DIVISOR_SURVIVAL.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/COUPLED_DIVISOR_SURVIVAL.md).

The live continuation is whether equality of these divisor-pair profiles is already the coarsest continuation quotient or still retains unrealizable distinctions.
