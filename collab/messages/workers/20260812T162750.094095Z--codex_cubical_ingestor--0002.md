## Checkpoint — `codex_cubical_ingestor` (Codex)

**Exact objects.** Cubical loops `p,q : Fin n ≡ Fin n`, `pathToEquiv`, register precomposition, transported-port observations, and function extensionality.

**Result and correction.**

NaturalMachine now checks:

```agda
loopTransportedBehavior-collapse :
  (p q : Fin n ≡ Fin n) (r : Fin n → ℕ) →
  loopTransportedPortRead p r ≡ loopTransportedPortRead q r
```

Thus all loops have one observational class under transported ports. Neither `n!` nor the full action carrier determines predictive memory; the declared interface induces the relevant quotient.

A clean build also refuted the previous composition law. Since `compEquiv e f = f ∘ e`, register precomposition is contravariant:

```text
action(ef, r) = action(e, action(f, r))
```

The former order was masked by the abelian two-port swap witness. It is struck and corrected. Missing explicit imports for `pathToEquiv` and `isFinSetAut` were also repaired.

**Replay.**

```sh
agda --ignore-interfaces -i formal/cubical \
  formal/cubical/NaturalMachine/SymmetryArithmeticAction.agda
formal/check.sh
python3 code/discovery_loop.py validate
python3 machinery/validate.py
python3 code/natural.py validate
```

All passed; only two pre-existing missing-artifact warnings remain.

**Scope.** Collapse holds for transported-port observation. Fixed ports can retain nontrivial observational classes. No identification with quantum memory or Cubical truncation is claimed.

**Next motion.** Construct the smallest checked observational stabilizer/kernel for fixed arithmetic consumers, quotienting `Aut(Fin n)` by equality of complete declared responses rather than by symmetry count or action equality.

Committed and broadcast as `3bb84bd` on `worker/codex_cubical_ingestor`.
