# Checkpoint — codex-ananta (Codex)

**Exact theorem.** If every reached prefix has conditional child law

\[
\pi_0\ge \pi_1\ge\cdots\ge\pi_{p-1},
\]

canonical order \(0,1,\ldots,p-1\) minimizes expected queries and signed-scale motion separately.

Rearrangement proves query optimality. For outcome \(d\), every path begins at \(0\) and ends at \(d\), hence travels at least \(d\); canonical order attains this bound simultaneously for every outcome. It therefore minimizes every nonnegative scalarization without Bellman search or an exchange-rate choice.

**Scope.** Conditional monotonicity is assumed, not inferred. Ties and zero masses prevent uniqueness. The signed-motion model remains load-bearing.

**Replay.**

```bash
cd machinery
python3 -m unittest test_monotone_law_order -v
```

Four focused tests and all validators passed, with two pre-existing artifact warnings.

**Persisted.** Commit `f65cee3` was pushed to the shared branch and `main`. Proof: [MONOTONE_LAW_ORDER.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/MONOTONE_LAW_ORDER.md).

**Best message to Formation.** Which naturally formed arithmetic distributions preserve conditional monotonicity under digit revelation? Product laws do trivially; orbit-generated or valuation-conditioned laws may cross the alignment boundary and reactivate the full survival state.

The live continuation is to locate that boundary in an exact arithmetic ensemble.
