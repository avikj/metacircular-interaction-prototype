# codex-ananta — Codex/OpenAI checkpoint

1. **Exact theorem.** Let \(S_0\subseteq S_1\subseteq\cdots\) be increasing worlds under nested charts, and suppose the union-relative least depth at \(x\) is finite, \(D\). Then the stage depths stabilize to \(D\) after finitely many encounters.

2. **Sharpening.** The proposed finite witness basis collapses to one element:
   \[
   W_D(x)=\{y:\pi_{D-1}(y)=\pi_{D-1}(x),\ q(y)\ne q(x)\}.
   \]
   Any member of \(W_D(x)\) also occupies every coarser fiber. The exact stabilization time is the first encounter with this critical witness set. The registered \(0.07\) correction branch occurred.

3. **Dynamic interpretation.** An effective stabilization bound is precisely an orbit-hitting bound for \(W_D(x)\). Density, closure, syndeticity, or mixing may supply such a bound, but none is the invariant itself.

4. **Quantum-process correction absorbed.** Completed nested sensing histories reconstruct from their terminal residue record and carry no additional intrinsic memory. The new theorem identifies when that terminal record becomes semantically adequate; it does not make the acquisition route irreducible.

5. **Replay.**
   ```bash
   python3 -m unittest machinery.test_witness_basis_stabilization \
     machinery.test_learning_raises_depth -v
   ```
   All six focused tests pass, including a nonnested false control. Validators pass with two historical artifact warnings.

6. **Next question.** Which actual arithmetic-life formation rule admits a proved first-hitting-time bound for its critical witness set without replacing causal formation by algebraic closure?

Commits through `558f56c` are pushed to the shared branch and `main`.
