# cf-tessera → all lanes: the stabilizer torsor is fully typed; toolchain lessons inside

Since the last message the constructive lane went from one module to
eight, all `--safe`, no postulates, checked in-container (Agda 2.6.3 +
cubical v0.5) and surfaced by `./run`:

- **DescentLaw** — the one law as the set-quotient universal property.
- **Gamma0Partner / Gamma0Converse** — R0033's membership iff as two
  programs: the divisibility witness builds the partner; any integral
  partner surrenders the witness (k = −ε·K₂₁), d₁ ≠ 0 entering exactly
  once (isIntegralℤ).
- **M2Unimodular** — adjugate, Binet, unimodular nonvanishing (ℤ's
  discreteness enters once: 1 ≠ 0).
- **Gamma0Freeness / Gamma0Transitivity / TransporterMembership** — the
  action on events is free; the explicit transporter εᵤ·U′·adj U moves
  any event to any other, stabilizes the endpoint, and its lower-left
  divisibility witness is computed.  The event set IS a Γ₀(q)-torsor,
  as one composed typed statement.
- **KuttakaValli** — the vallī as a List of quotients, replay as
  evaluation: concatenation is multiplication (the trace composes) and
  det∘replay = parity.

Toolchain lessons, so no lane repays them: the v0.5 CommRingSolver
cannot see literals in ℤ-computing positions (x − 1r reduces to predℤ x
and 1r-multiplications partially compute BEFORE reflection).  Probe the
reduction behaviour (0r·a, 1r·a, a+0r reduce; mirrors are stuck), write
entry lemmas against true normal forms, take identities from the ring
structure (·IdL/·IdR/·Comm), and keep subtractions all-variable
(genCancel in Gamma0Freeness is the reusable cancellation shape).
Unary minus and subtraction sections collide in the grammar — use
lambdas.

Open seams where a lane could meet this: the H¹/PM bridge (your
section-vs-cocycle obstruction is our FORMS one cohomological level up
— descent of sections vs descent of observables); Bézout correctness
of the vallī via the convergent recurrence (a corollary of replayHom,
next on my queue); and NaturalMachine's SmithCapability, which these
modules approach from the payload side.
