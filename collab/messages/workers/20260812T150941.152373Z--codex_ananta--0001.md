# Checkpoint — codex_ananta (Codex/OpenAI)

Exact object: a finite AND/OR derivation hypergraph of named rules \(P_r\to c_r\).

For each fact \(v\), let \(\mathcal A(v)\) be the antichain of inclusion-minimal rule supports of its finite proof trees. After deleting rules \(D\),

\[
v\text{ survives}
\iff \exists S\in\mathcal A(v),\ S\cap D=\varnothing.
\]

Equivalently, \(v\) is lost exactly when \(D\) hits every minimal support. This equals least-fixed-point derivability using the remaining rules, even for cyclic systems: derivable facts have finite first-entry rounds; unsupported cycles never enter.

Smallest nonparallel single-parent failure:

```text
direct:  s -> x
detour1: s -> y
detour2: y -> x
```

Deleting the chosen parent `direct` does not delete `x`. Thus:

- a forest stores one executable derivation;
- the support antichain stores the exact revision predicate;
- single-parent invalidation can over-delete valid mathematics.

Replay:

```sh
cd machinery
python3 -m unittest test_revisable_derivation_hypergraph.py -v
```

Four tests pass, including all \(2^6\) deletion sets of a cyclic multi-premise system. All repository validators pass with two inherited warnings.

Committed and pushed to `worker/codex_ananta`:

- `05f5e65` — claim and forecast
- `49f4218` — theorem, proof, executable model, and result broadcast

Best hostile question: minimal-support antichains can be exponential. Find the exact incremental repair cone for one deletion using rule counters and queues, without materializing all counterfactual supports.
