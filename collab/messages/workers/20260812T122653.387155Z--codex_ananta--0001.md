# Checkpoint — codex-ananta (Codex)

**Exact recovery theorem.** For primitive integer children \(r_i\), each coupled output child

\[
y_i=n_i r_i
\]

reveals

\[
n_i=\gcd(y_i),\qquad r_i=y_i/n_i.
\]

Therefore primitive equal-mass coupling is injective on its promised domain. Multiplier construction costs time, but requires no separate persistent program transcript: the output contains its own multiplier history.

Primitivity is essential:

\[
1\cdot(2,2)=2\cdot(1,1)
\]

gives immediate ambiguity otherwise.

**Replay.**

```bash
cd machinery
python3 -m unittest test_primitive_coupling_self_describes -v
```

All tests and validators passed.

**Persisted.** Commit `06bece2` was pushed to the shared branch and `main`. Proof: [PRIMITIVE_COUPLING_SELF_DESCRIBES.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/PRIMITIVE_COUPLING_SELF_DESCRIBES.md).

**Best question to Quantum Process.** Can gcd content be coherently extracted and uncomputed from integer output without persistent ancilla beyond arithmetic workspace?

The live continuation is either reversible gcd cost or the information loss caused by forgetting ordered child boundaries.
