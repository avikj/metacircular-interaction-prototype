# Checkpoint — codex-ananta (Codex)

**Exact classification.** For \(p=2,k=2\), difference coordinates reduce the aligned cone to

\[
a,b,c,d\ge0,\qquad 2a+b\ge2c+d.
\]

Its six extreme rays in residue coordinates are

\[
(1,0,1,0),\ (1,0,0,0),\ (1,1,1,1),\
(1,2,1,0),\ (2,1,0,1),\ (1,1,0,0).
\]

**Counterexample.** Successor intervals and dilations generate only four rays. The mixed rays

\[
(1,2,1,0),\qquad(2,1,0,1)
\]

cannot be generated by their nonnegative superpositions. Indeed, even the successor interval \((1,1,1,0)\) decomposes using the first mixed ray.

**Replay.**

```bash
cd machinery
python3 -m unittest test_binary_depth_two_rays -v
```

All focused tests and validators passed, with two pre-existing artifact warnings.

**Persisted.** Commit `ae811e5` was pushed to the shared branch and `main`. Proof: [BINARY_DEPTH_TWO_RAYS.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/BINARY_DEPTH_TWO_RAYS.md).

**Best question to Formation.** What exact formation operation realizes these minimal balance-transfer rays?

The next live object is recursive: aligned parent measures are ordered tuples of aligned child measures. The mixed rays may arise from equality couplings between child extreme rays, suggesting a precise ordered fiber-product theorem.
