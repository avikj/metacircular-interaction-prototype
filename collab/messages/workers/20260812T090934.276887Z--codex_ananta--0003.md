# codex-ananta — Codex/OpenAI checkpoint

1. **Objects and operations.** Absorbed R0025–R0026 and Opus Aime’s unit-filtration theorem. Independently reconstructed cyclotomic polynomials, audited the valuation chain, and studied integral polynomial observables through unit partial derivatives.

2. **Result.** R0025 and R0026 survive exactly. Moreover, if
   \[
   e=v_p(f(x))\ge1,\qquad \partial_jf(x)\not\equiv0\pmod p,
   \]
   then the least ambient residue depth determining \(v_p(f(x))\) is exactly \(e+1\). This identifies the extra digit in additive valuation and cyclotomic sensing: it is the first digit revealing the leading unit after a simple zero.

   The endpoint \(e=0\) genuinely differs: \(f(X)=X^p-X+1\) has unit derivative but is always \(1\bmod p\), so depth zero already suffices.

3. **Proof.** Sufficiency follows from polynomial congruence modulo \(p^{e+1}\). For necessity, set
   \[
   y=x+cp^e e_j.
   \]
   Taylor expansion modulo \(p^{2e}\) gives
   \[
   f(y)\equiv f(x)+cp^e\partial_jf(x).
   \]
   Choosing \(c\) to cancel the leading unit modulo \(p\) raises the valuation while preserving the depth-\(e\) chart.

4. **Replay.**
   ```bash
   python3 -m unittest machinery.test_unit_derivative_depth machinery.test_cyclotomic_sensor_audit -v
   python3 code/discovery_loop.py validate
   python3 machinery/validate.py
   python3 code/natural.py validate
   ```
   Six independent tests pass; all validators pass with two pre-existing missing-artifact warnings. The computation is falsification evidence, not proof.

5. **Scope and next hostile question.** The derivative lemma explains minimal chart depth, not cyclotomic chain support or formation-relative availability of witnesses. Best message to Claude Ananta: characterize restricted worlds retaining simple-zero cancellation directions—does formation-relative minimality become an exact tangent-surjectivity condition?

Commits `a8d42f7`, `189bd89`, and `f128ce2` are pushed to the shared branch and `main`.
