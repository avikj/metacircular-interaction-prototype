# codex-ananta — Codex/OpenAI checkpoint

1. **Exact objects.** Integrated `LENS_REPAIR`, `WITNESS_GENERATION`, `FORMED_UNIT_FILTRATION_DEPTH`, and its self-deflation. Studied the cyclic world
   \[
   S_g=\{g^n:n\ge0\}
   \]
   through multiplicative orders modulo \(p^m\) and critical valuation fibers.

2. **Result.** For odd \(p\nmid g\),
   \[
   S_g^2\text{ preserves ambient valuation minimality everywhere}
   \iff \operatorname{ord}_p(g)\text{ is even}.
   \]
   The leading \(0.88\) forecast occurred.

3. **Proof.** The quotient
   \[
   \operatorname{ord}_{p^m}(g)/\operatorname{ord}_p(g)
   \]
   is a power of \(p\), so order parity is unchanged under lifting.

   If the order is odd, \(-1\notin\langle g\rangle\bmod p\); every formed sum has valuation zero, making formed depth zero instead of ambient depth one.

   If the order is even, for \(v=v_p(g^i+g^j)\), the halfway power modulo \(p^{v+1}\) equals \(-1\). Choose
   \[
   k\equiv i+\frac{\operatorname{ord}_{p^{v+1}}(g)}2.
   \]
   Then \(g^i+g^k\equiv0\bmod p^{v+1}\), while automatically
   \(g^k\equiv g^j\bmod p^v\). Thus the required witness lies in the critical fiber.

4. **Replay.**
   ```bash
   cd machinery
   python3 -m unittest test_witness_generation test_formed_locus_depth \
     test_unit_derivative_depth test_cyclotomic_sensor_audit -v
   ```
   All 32 tests pass. Repository validators pass with two pre-existing artifact warnings.

5. **Scope and next question.** This classifies product worlds \(S_g^2\), not sparse sets of actually encountered pairs. The sharpened object is neither closure nor density alone: it is incidence between an orbit of available actions and the critical affine fiber.

   Best hostile question: for a non-product pair-world \(E\subset S^2\), can witness transport be characterized effectively by an action groupoid without silently completing \(E\) to \(S^2\)?

Commits through `172f9dc` are pushed to the shared branch and `main`.
