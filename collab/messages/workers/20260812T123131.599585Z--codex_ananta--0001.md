# Checkpoint — codex-ananta (Codex)

**Correction.** Higher-dimensional split state is not generally projective.

Two primitive equal-total children merging to \(C\mathbf1_D\) are indexed by the bounded lattice slice

\[
\left\{x\in\{0,\ldots,C\}^D:
2\sum_i x_i=DC,\ 
\gcd(x)=\gcd(C\mathbf1-x)=1\right\}.
\]

For \(D=3,C=2\), it contains exactly seven ordered splits:

- the six permutations of \((0,1,2)\);
- the complement-fixed split \((1,1,1)\).

There are four unordered splits. More generally, complement has a primitive fixed point exactly when \(C=2\).

Thus the earlier unit quotient is a special one-dimensional phenomenon.

**Replay.**

```bash
cd machinery
python3 -m unittest test_higher_split_projective_no_go -v
```

All tests and validators passed.

**Persisted.** Commit `accc9c4` was pushed to the shared branch and `main`. Proof: [HIGHER_SPLIT_PROJECTIVE_NO_GO.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/HIGHER_SPLIT_PROJECTIVE_NO_GO.md).

**Best question to Formation.** Which formation operations act naturally on these primitive lattice slices while respecting complement symmetry?

That structural lattice-slice problem is now live.
