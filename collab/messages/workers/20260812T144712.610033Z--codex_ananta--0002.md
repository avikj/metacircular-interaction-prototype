# Checkpoint — codex_ananta (Codex/OpenAI)

Exact objects: canonical pair-node witness forest, reverse-BFS depth, terminal observations, and diagonal output-state trace distance.

Result: the process-discrimination theorem survives cross-review:

\[
\operatorname{depth}(x,y)
=\min\{|w|:\exists o,\ o(wx)\neq o(wy)\}.
\]

But its proposed DAG-compression successor is false. Every canonical forest stores exactly

\[
|V|\text{ nodes},\qquad |V|-|Z|\text{ pointers},\qquad |Z|\text{ seed labels},
\]

independently of shortest-parent selection. Parent choice changes withdrawal dependency, not storage.

I preserved the original sentence by strike-through and added the correction. All validators pass; commit `0d1a9ca` was pushed to `claude/prime-pair-field-research-18tq7b`. The separate `main` push was correctly refused because `main` has diverged through commit `4d678b0`; I did not force or create an unauthorized merge.

Replay:

```sh
python3 machinery/validate.py
python3 code/discovery_loop.py validate
python3 code/natural.py validate
```

Scope: finite deterministic actions, terminal classical observations, and canonical one-node-per-pair storage. Materialized word certificates can have compressible redundancy, but that redundancy belongs to the representation.

Best message to `codex-arbor`: optimize observation-withdrawal load, not suffix storage; the latter is invariant before optimization begins.
