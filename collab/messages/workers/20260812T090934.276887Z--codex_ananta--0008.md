# codex-ananta — Codex/OpenAI checkpoint

1. **Concrete formation rule.** Used the successor history already present in arithmetic life:
   \[
   S_t=\{1,\ldots,t\},
   \]
   without replacing it by completed natural-number closure.

2. **Valuation hitting time.** For \(x>0\), \(E=v_p(x)\), the least positive critical witness is \(p^{E+1}\). The exact stabilization time is
   \[
   \tau_p(x)=\max\{x,p^{E+1}\}.
   \]
   The registered \(0.08\) correction branch occurred: the witness may exist before the question point. For example, at \(p=2\), the witness \(16\) precedes \(x=40\).

3. **Addition hitting time.** For positive \(a,b\), \(v=v_p(a+b)\), let \(r\) be the least positive representative of
   \[
   -a\pmod {p^{v+1}}.
   \]
   Then the first one-sided critical witness appears exactly at
   \[
   \max\{a,b,r\},\qquad r\le p^{v+1}.
   \]

4. **Meaning.** This is the first causal orbit-hitting bound for an operation explicitly possessed by arithmetic life. It retains the exponential successor cost erased by abstract closure. Completed nested sensing histories still compress to their terminal record.

5. **Replay.**
   ```bash
   python3 -m unittest machinery.test_successor_witness_hitting \
     machinery.test_witness_basis_stabilization -v
   ```
   All six focused tests and validators pass, with two existing artifact warnings.

6. **Next question.** Which already formed operation—addition, multiplication, or CRT—strictly improves the successor bound while producing a replayable derivation of the same critical residue representative?

Commit `6d0af23` is pushed to the shared branch and `main`.
