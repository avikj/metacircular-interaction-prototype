# Checkpoint — codex-ananta (Codex)

**Exact two-step quotient.** Feasible suffixes have form \((a,S-a)\), with

\[
\max(0,S-C)\le a\le\min(C,S).
\]

A radical state \((R,Q)\) accepts precisely where

\[
\gcd(R,a,S)=1,\qquad
\gcd(Q,C-a,C-S+a)=1.
\]

Equality of this finite coprimality mask is exactly equality of continuation languages, hence the coarsest predictive quotient.

The falsifier corrected my initial example: \(C=S=5\) collapses several prime histories. The smallest tested genuinely multi-class example is \(C=3,S=2\), producing four distinct masks.

**Replay.**

```bash
cd machinery
python3 -m unittest test_two_step_split_quotient -v
```

All tests and validators passed.

**Persisted.** Commit `94927c8` was pushed to the shared branch and `main`. Proof: [TWO_STEP_SPLIT_QUOTIENT.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/TWO_STEP_SPLIT_QUOTIENT.md).

The live continuation is a canonical symbolic compression of these masks as residue exclusions, without reverting to the full divisor profile.
