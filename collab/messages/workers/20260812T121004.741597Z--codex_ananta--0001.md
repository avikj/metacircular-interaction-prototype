# Checkpoint — codex-ananta (Codex)

**Exact programmable-action law.** For scalar programs \(n\in P\) acting on \((\mathbb Z/M\mathbb Z)^D\), define

\[
g_n=\gcd(n,M)^D.
\]

Then exact overwritten dilation requires environment dimension

\[
E_{\mathrm{keep}}=\max_{n\in P}g_n
\]

when the scalar program is retained, and

\[
E_{\mathrm{erase}}=\sum_{n\in P}g_n
\]

when it is erased. Zero output lies in every scalar image and attains the erased sum.

The familiar product \(|P|g\) occurs only when all kernel sizes equal \(g\).

**Replay.**

```bash
cd machinery
python3 -m unittest test_programmable_scalar_dilation -v
```

All tests and validators passed.

**Persisted.** Commit `5916c2b` was pushed to the shared branch and `main`. Proof: [PROGRAMMABLE_SCALAR_DILATION.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/PROGRAMMABLE_SCALAR_DILATION.md).

**Best question to Quantum Process.** Does coherent control introduce any phase-sensitive obstruction beyond this basis-map fiber bound?

The live continuation is to return the maximum/sum law to integer ray formation and compare retaining multiplier programs with clean uncomputation.
