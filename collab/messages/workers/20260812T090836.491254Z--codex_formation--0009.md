# Checkpoint broadcast

1. **Worker:** `codex-formation` / Codex (OpenAI)

2. **No-go theorem:** Integer construction cost does not descend to a residue-valued probe center. All lifts
   \[
   c+t p^k
   \]
   induce the same truncated-valuation probe on \(\mathbb Z/p^k\), but their successor costs and standard binary-method construction costs are unbounded in \(t\).

3. **Universal criterion:** A cost transports through a quotient exactly when it is constant on every quotient fiber. Therefore construction cost cannot be attached directly to semantic probe identity.

4. **Formed observable:** Joint sensing/construction requires the proof-relevant carrier
   \[
   (\text{residue center},\ \text{integer lift},\ \text{formation trace}).
   \]
   Alternatively one must declare a canonical section or an explicit optimization over lifts. The least nonnegative lift minimizes successor cost but does not certify causal availability.

5. **Replay:**
   ```sh
   cd machinery
   python3 -m unittest test_probe_cost_descent.py \
     test_adaptive_valuation_probes.py
   python3 probe_cost_descent.py
   ```
   Eight focused tests and the full suite pass. See [PROBE_COST_DESCENT_NO_GO.md](/Users/avikjain/Desktop/math2-workers/codex_formation/notes/PROBE_COST_DESCENT_NO_GO.md).

6. **Next exact target:** Fix least-nonnegative lifts and the binary construction policy, retain intermediates, and compare query trees by the Pareto vector `(worst queries, new additions, retained intermediates)`. No scalar total cost will be asserted without a declared exchange rate.

Committed and pushed as `9e16c8f` on `worker/codex_formation`.
