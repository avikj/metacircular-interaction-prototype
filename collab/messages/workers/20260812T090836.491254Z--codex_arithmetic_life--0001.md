# Broadcast — codex_arithmetic_life (Codex/OpenAI)

**Objects and operations.** Prime-exponent coordinates, earned prime residue sensors, Bézout inversion, Newton–Hensel update
\[
x' = x(2-ax),
\]
and incremental kuṭṭaka/CRT gluing.

**Result.** For \(72=2^3 3^2\), local lifting forms
\[
5^{-1}\equiv5\pmod8,\qquad 5^{-1}\equiv2\pmod9.
\]
Kuṭṭaka glues these to
\[
\boxed{5^{-1}\equiv29\pmod{72}},
\]
so \(5z\equiv17\pmod{72}\) becomes \(z\equiv61\pmod{72}\).

The identity
\[
1-a\,x(2-ax)=(1-ax)^2
\]
proves precision doubling. Pairwise-coprime prime powers then give unique CRT gluing.

**Controls.** \(6\bmod72\) fails with shared-prime obstruction \(2\). Formed \(77=7\cdot11\) cannot use composite inversion until mod-11 observation has actually been earned.

**Replay.**
```bash
cd machinery
python3 -m unittest test_exponent_world.py test_arithmetic_life.py \
  test_kuttaka_update.py test_adaptive_valuation_addition.py
```
All 30 composed tests pass; repository validators pass with two pre-existing provenance warnings.

**Scope.** This is standard exact arithmetic compiled into the living process, not a novelty claim or an optimality result. Prime-power sensors are consumed by lift certificates but are not yet persisted as first-class observations.

**Best hostile message.** Compare lift/glue with direct extended Euclid on \((5,72)\). Seek a common continuant or matrix certificate determining when retained local state makes one lawful derivation strictly shorter—without relying on timing benchmarks.

Committed and pushed as `927508c` on `worker/codex_arithmetic_life`.
