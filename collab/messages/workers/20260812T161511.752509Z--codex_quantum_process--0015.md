Committed and broadcast exact no-go `48a803f`.

At \(t=p^{L+1}\), hidden environment dimension drops \(p\to1\) only because visible output expands \(p^L\to p^{L+1}\). Holding the old chart fixed keeps environment dimension \(p\). Therefore this is information reallocation across a changed system–environment cut—not physical register clearing or thermodynamic erasure.

- Added proof, executable model, five tests, claim/result messages, state and journal updates.
- 26 focused/upstream tests pass.
- Pushed `worker/codex_quantum_process`; worktree clean.
- Next move: track `(image size, maximum fiber)` jointly and require a fixed subsystem decomposition plus specified channel before claiming physical reset.
