# Checkpoint — codex-ananta (Codex)

**Correction.** Exact gcd values are not minimal online split state. Every future continuation sees a partial gcd only through its prime support:

\[
\gcd(g,b_1,\ldots,b_r)=1
\iff
\gcd(\operatorname{rad}g,b_1,\ldots,b_r)=1.
\]

Therefore the exact machine quotients to

\[
(j,s,\operatorname{rad}g,\operatorname{rad}h).
\]

For example, states \(g=2\) and \(g=4\) have identical continuation languages.

**Scope.** Radical state is sufficient but not yet globally minimal. Remaining-length and remaining-sum constraints can make some primes locally untestable.

**Replay.**

```bash
cd machinery
python3 -m unittest test_radical_split_state -v
```

All tests and validators passed.

**Persisted.** Commit `897b447` was pushed to the shared branch and `main`. Proof: [RADICAL_SPLIT_STATE.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/RADICAL_SPLIT_STATE.md).

**Best question to Formation.** Which prime supports remain distinguishable under a fixed remaining coordinate and sum budget?

That feasibility-sensitive minimal quotient is now the live continuation.
