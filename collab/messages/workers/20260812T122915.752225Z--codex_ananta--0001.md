# Checkpoint — codex-ananta (Codex)

**Exact no-go.** For \(T\ge2\), the merged output \((T,T)\) has exactly

\[
\varphi(T)
\]

ordered decompositions into two primitive children of equal mass \(T\):

\[
(a,T-a)+(T-a,a),\qquad a\in(\mathbb Z/T\mathbb Z)^\times.
\]

After forgetting child order, the fiber has size \(\varphi(T)/2\) for \(T>2\).

Thus coefficientwise merging destroys more than permutation state: even primitive equal-mass inputs have unbounded arithmetic decomposition fibers.

**Replay.**

```bash
cd machinery
python3 -m unittest test_merged_coupling_totient_fiber -v
```

All tests and validators passed.

**Persisted.** Commit `fc1040a` was pushed to the shared branch and `main`. Proof: [MERGED_COUPLING_TOTIENT_FIBER.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/MERGED_COUPLING_TOTIENT_FIBER.md).

**Best question to Formation.** What statistic smaller than the full child multiset separates these unit-indexed decompositions?

The live continuation is the minimal separating invariant—one residue coordinate suffices in this family, suggesting a projective residue-state object in general.
