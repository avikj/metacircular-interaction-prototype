# Smith certificate ingestion checkpoint

**Worker:** `codex_smith_ingestor` — Codex/OpenAI

## Exact objects and operations

The `2 × 2` Smith reducer now emits a complete word over eight elementary unimodular operations: row/column swaps, row negations, and parameterized row/column shears. A strict versioned binary codec supports independent forward and inverse replay.

Cubical Agda’s native theorem

```agda
smith : (M : Mat m n) → Smith M
```

is pinned and typechecked through `NaturalMachine.SmithNativeAudit`.

## Result and no-go

Forward replay reaches the Smith diagonal; reversing the word and inverting each shear reconstructs the source exactly. A mutated coefficient is rejected.

The leading compression forecast failed. On the declared Fibonacci family, transcript sizes were 30–391 bytes versus 8–56 bytes for aggregate `L,R`; on the quotient family they were 14–18 versus 8–16 bytes. The transcript earns streaming auditability and causal reversal, not compression over aggregate witnesses.

The external checker is not a native proof term. A genuine formal bridge still requires an Agda transcript fold producing `Sim M`; the Cubical library also leaves Smith uniqueness up to units as a TODO.

## Replay

```bash
cd machinery
python3 -m unittest \
  test_smith_residual_machine.py \
  test_smith_certificate_codec.py \
  test_smith_quotient_memory_no_go.py \
  test_online_smith_certificate_reversibility.py \
  test_smith_residual_process_memory.py -v
python3 measure_smith_certificates.py

cd ..
agda -i formal/cubical \
  formal/cubical/NaturalMachine/SmithNativeAudit.agda
```

All 23 Smith tests and the Agda audit pass. Repository validators pass with two pre-existing provenance warnings.

Full ledger: [SMITH_CERTIFICATE_INGESTION.md](/Users/avikjain/Desktop/math2-workers/codex_smith_ingestor/notes/SMITH_CERTIFICATE_INGESTION.md).

## Scope and synchronization boundary

This is exact for the installed `2 × 2` operation schema. It does not establish general-dimensional certificate production, minimality, asymptotic bit complexity, or native certification of external bytes.

Commit and push remain blocked: Git metadata lies outside the writable sandbox, and creating `index.lock` was denied. The coherent edits remain uncommitted in the worktree.

**Best message to another worker:** Ask `codex-quantum-process` or `codex-residual` to attack whether any transcript prefix can be safely erased using the live state, beyond cancelling adjacent inverse operations, without losing reverse replay.

— `codex_smith_ingestor` (Codex)
