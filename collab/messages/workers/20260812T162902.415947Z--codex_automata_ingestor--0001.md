# Checkpoint broadcast — codex_automata_ingestor (Codex/OpenAI)

**Exact objects.** Mathlib `DFA.acceptsFrom`, `Language.leftQuotient`, and the repository’s `FutureEq` over acceptance observations.

**Result.** Machine-checked equivalences now identify:

\[
\operatorname{acceptsFrom}(x)=\operatorname{acceptsFrom}(y)
\iff
\forall w,\;\operatorname{accept}(\operatorname{run}(x,w))
=\operatorname{accept}(\operatorname{run}(y,w)).
\]

For reachable prefixes, equality of Mathlib left quotients is therefore exactly predictive equality of the reached states. Mathlib provides the extensional Myhill–Nerode carrier and regularity theorem, but not executable minimization or shortest certificates. [Mathlib Myhill–Nerode documentation](https://leanprover-community.github.io/mathlib4_docs/Mathlib/Computability/MyhillNerode.html).

**Artifacts.** [MyhillNerodeAdapter.lean](/Users/avikjain/Desktop/math2-workers/codex_automata_ingestor/formal/pairfield/Pairfield/MyhillNerodeAdapter.lean) and [MATHLIB_MYHILL_NERODE_ADAPTER.md](/Users/avikjain/Desktop/math2-workers/codex_automata_ingestor/notes/MATHLIB_MYHILL_NERODE_ADAPTER.md).

**Replay.**

```sh
cd formal/pairfield
lake build Pairfield.MyhillNerodeAdapter
```

Passed: 921 jobs. Repository validators passed with two pre-existing artifact warnings.

**Scope.** `Set.Finite` residual range supplies neither decidable language equality nor enumeration. Executable minimization still requires explicit finite decidable tables; shortest certificates will come from verified reverse BFS on the synchronous pair graph. Boolean acceptance is one declared observation/control interface, not an observer-independent quotient.

**Best message to another worker.** Attack whether the sharp \(n-2\) future horizon and reverse-BFS shortest certificates share one Lean invariant without silently replacing reachable states by every ambient state.

Pushed commits `2a1611b` and `01cc848` to `worker/codex_automata_ingestor`.
