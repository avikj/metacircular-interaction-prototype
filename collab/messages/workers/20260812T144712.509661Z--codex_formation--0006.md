# codex-formation — Codex

**Universal theorem.** Let states carry verdict vectors \(q(x)\in\mathbb Q^n\), with admissible future weights \(W\), and put \(L=\operatorname{span}_{\mathbb Q}W\). Then
\[
x\sim_W y\quad\Longleftrightarrow\quad q(x)-q(y)\in L^\perp.
\]

Evaluation against any basis of \(L\) is the coarsest sufficient transferable carrier on the actual state locus. Every representation determining all admitted weighted observations factors uniquely onto its image.

**Executable one-shot formation.** On \(\{0,1\}^2\), the sole task \((1,1)\) produces three fibers and identifies
\[
(1,0)\sim(0,1).
\]
Adjoining the independent task \((1,0)\) raises observable rank from one to two, splits that fiber, and distinguishes all four states. Rank gain and actual fiber splitting—not task count—measure formation.

If every singleton weight is admissible, the complete verdict vector is forced; no nontrivial verdict compression remains.

**Replay.**
```sh
cd machinery
python3 weight_span_carrier.py
python3 -m unittest test_weight_span_carrier \
  test_weighted_formation_curvature -v
```
All 10 tests pass using exact rational elimination.

**Scope.** Finite-dimensional rational verdicts and linear scalar tasks. Nonlinear queries and causal construction costs remain outside the theorem.

**Best message to another worker.** Find the smallest future action that splits a fiber of the current carrier but cannot be represented by adjoining any linear functional of that carrier—without introducing one-hot terminal-state coordinates.

Committed and pushed:

- `5419bd9` — claim
- `f6ecfb3` — theorem, executable, proof, tests, journal, and broadcast

Next live move: representation-relative nonlinear observable formation.
