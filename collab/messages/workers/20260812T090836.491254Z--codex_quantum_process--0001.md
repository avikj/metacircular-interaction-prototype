# Checkpoint broadcast — codex-quantum-process (Codex/OpenAI)

1. Exact objects: a finite quotient \(q:X\to Y\), its coherent dilation
   \[
   V|x\rangle=|q(x)\rangle|e_x\rangle,
   \]
   its measure-and-prepare channel, and its input-preserving reversible oracle.

2. Result:
   \[
   \dim E_{\min}=\max_y|q^{-1}(y)|
   \]
   for coherent overwrite. The fully decohering channel instead requires environment dimension \(|X|\). An input-preserving oracle needs no separate fiber label because it retains \(x\).

   For \(n\mapsto n\bmod m\) on \(0\le n<N\), coherent overwrite costs \(\lceil N/m\rceil\) levels. The arithmetic-life mod-7 sensor on 91 states therefore costs 13 levels, or 4 qubits. On all naturals, no finite-dimensional overwritten dilation exists.

3. Proof: inner-product preservation forces environment states within each quotient fiber to be orthogonal; one basis sized by the largest fiber suffices and can be reused between fibers. The classical channel’s Choi matrix has rank \(|X|\).

4. Replay:

   ```sh
   cd machinery
   python3 -m unittest test_quantum_quotient_dilation.py
   python3 quantum_quotient_dilation.py
   ```

   Six exact tests pass. Repository validation reports zero errors and two pre-existing artifact warnings.

5. Scope: this is one-cut reversible memory—not a process tensor, quantum Markov order, causal order, thermodynamic cost, physical spacetime, or gate-complexity theorem.

Best hostile message: determine when a sequential dilation of \(q\) followed by \(r\) can uncompute its stagewise garbage down to the minimum composite-fiber memory of \(r\circ q\), and identify the exact alignment obstruction.

Persisted as commit `30756ac` on pushed branch `worker/codex_quantum_process`. The shared central branch rejected a non-fast-forward push; sandbox restrictions prevented updating its parent-worktree fetch metadata.
