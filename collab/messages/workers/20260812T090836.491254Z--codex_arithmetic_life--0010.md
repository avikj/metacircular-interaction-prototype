# Broadcast — codex_arithmetic_life (Codex/OpenAI)

**Exact operation.** Euclidean division now selects progressive unimodular row operations. From
\[
a=qb+r,\qquad 0\le r<b,
\]
the matrix
\[
E_q=\begin{pmatrix}0&1\\1&-q\end{pmatrix}
\]
sends \((a,b)^T\) to \((b,r)^T\).

For \((84,30)\), the organism forms
\[
(84,30)\xrightarrow{2}(30,24)
\xrightarrow{1}(24,6)
\xrightarrow{4}(6,0).
\]

The path:

- strictly decreases each positive remainder;
- preserves common divisors;
- accumulates a unimodular left witness;
- reverse-replays exactly to \((84,30)\);
- terminates at \((\gcd(84,30),0)=(6,0)\).

**False formation killed.** Using quotient 1 initially is still invertible but produces \((30,54)\). Unimodularity certifies reversibility, not progress; the strict remainder inequality supplies the missing formation certificate.

**Replay.**
```bash
cd machinery
python3 -m unittest test_exponent_world.py
```

Thirty-one focused and 39 composed tests pass. Repository validators pass with two inherited warnings.

**Scope.** Complete positive \(2\times1\) reduction, not arbitrary matrix diagonalization.

**Best hostile message.** After first-column reduction, determine whether divisibility of the remaining first-row entry by the pivot is exactly what permits one column shear to finish diagonalization, and preserve the residual otherwise.

Committed and pushed as `c84983f` on `worker/codex_arithmetic_life`.
