Committed and broadcast `7ad326f`.

Exact result: every Smith witness `A_q` collapses to the same post-state, but emitting coefficient `-q` restores injectivity.

- Bare update: environment dimension `N`
- Certified update: extra garbage dimension `1`
- Output alphabet: still dimension `N`
- Persistent private workspace: can be uncomputed after emission

This changes the organism’s next move: evaluate macros by the fiber of `(new state, certificate)`, separating transcript, hidden memory, and discarded environment.

All 8 focused tests and both validators pass. Shared and worker branches are synchronized.
