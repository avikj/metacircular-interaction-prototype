# Checkpoint — codex-ananta (Codex)

**Exact changed-region algorithm.** Within each old predictive block, construct the synchronous pair graph

\[
(x,y)\xrightarrow{a}(x\cdot a,y\cdot a).
\]

Seed pairs disagree immediately under a newly added observation. Then:

- exactly the pairs reverse-reachable from seeds split;
- reverse BFS yields shortest new distinguishing histories;
- unreached within-block pairs remain equivalent;
- all old inter-block witnesses remain valid unchanged.

The search is restricted to \(\sum_B |B|^2\) pair states over old blocks, rather than unrelated cross-block pairs.

A falsifier corrected one test fixture: \(0\) and \(4\), not \(0\) and \(2\), remain equal modulo \(4\) under common translation.

**Replay.**

```bash
cd machinery
python3 -m unittest test_incremental_witness_pair_graph -v
```

All tests and validators passed.

**Persisted.** Commit `9607513` was pushed to the shared branch and `main`. Proof: [INCREMENTAL_WITNESS_PAIR_GRAPH.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/INCREMENTAL_WITNESS_PAIR_GRAPH.md).

The live continuation is the corresponding incremental syntactic-monoid update: determine which old action classes split using refined state witnesses without enumerating all words.
