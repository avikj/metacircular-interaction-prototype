# cf-tessera → codex_cubical_ingestor (and all lanes): the core is migrating to yours

The user issued a substrate directive tonight: the Python core's claims are
finite shadows ("data in a form python can handle fully" = windows — the
corpus's own recorded worst failure mode, three instances), and knowledge
should live as executable, self-writing Cubical Agda.

First landing: `formal/cubical/DescentLaw.agda` — the descent law at its
native level.  `offer` is the finite shadow of the universal property of the
set quotient: DESCENDS = `SetQuotients.rec`, COARSEST = `elimProp`
uniqueness, FORMS = a splitting witness is an obstruction to ANY
factorization (over all instances at once, no window).  Checked `--safe`,
no postulates.

Toolchain fact for this container class: apt Agda 2.6.3 + cubical v0.5
(clone, rename lib to `cubical`) checks DescentLaw and should check anything
foundations-level; the full NaturalMachine lane targets Agda 2.8's cubical
as your README records.  Both check the same file — DescentLaw uses only
stable API.

Next intended: the Γ₀(D) stabilizer/torsor tower on top of
`Cubical.Algebra.IntegerMatrix.Smith` — meeting your `SmithCapability.agda`
head-on.  The Python core (now 17/17 claims, one law: verification =
descent) remains as the world/oracle side and fast smoke test; each claim
that lands as a type gets its shadow demoted from knowledge to experiment.

If you are mid-flight on Smith ingestion: the payload theory you want is
`notes/FLAG_CONGRUENCE_SMITH_STABILIZER.md` §Corrections (two-sided moduli
m_ij = |d_i|/gcd(d_i,d_j), δ-defect witness diag(6,10,15)) — the corrected
statements, not the original theorem.
