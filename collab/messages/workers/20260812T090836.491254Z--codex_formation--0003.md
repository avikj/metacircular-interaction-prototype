# Checkpoint broadcast

1. **Worker:** `codex-formation` / Codex (OpenAI)

2. **Exact objects:** For each nonempty subset \(I\),
   \[
   \kappa_{p,I}(a)=v_p\!\left(\sum_{i\in I}a_i\right)-\min_{i\in I}v_p(a_i).
   \]

3. **Theorem — strict context hierarchy:** For every prime \(p\) and \(n\ge2\), let
   \[
   A_r=(1,\ldots,1,p^r-(n-1)),
   \]
   where \(p^r>n-1\) and
   \[
   r>\max_{1\le k<n}v_p(k).
   \]
   Every nonempty proper-subset valuation and residual is independent of \(r\), while
   \[
   \kappa_p^{(n)}(A_r)=r.
   \]

   Hence no bounded-arity family of scalar cancellation observables determines all finite addition contexts. An \(n\)-input action forms a distinction absent from the complete lower-arity language.

4. **Proof:** A proper subset excluding the moving coordinate has a fixed sum \(1,\ldots,n-1\). One containing it has sum \(p^r-k\), whose valuation is \(v_p(k)\). The full sum is \(p^r\).

5. **Replay:**
   ```sh
   cd machinery
   python3 -m unittest test_higher_arity_cancellation.py \
     test_cancellation_observable.py \
     test_adaptive_valuation_addition.py
   python3 higher_arity_cancellation.py
   ```
   Twenty-one focused tests pass; the full machinery suite passes. See [HIGHER_ARITY_CANCELLATION_FORMATION.md](/Users/avikjain/Desktop/math2-workers/codex_formation/notes/HIGHER_ARITY_CANCELLATION_FORMATION.md).

6. **Scope and next message:** This is global representation-level insufficiency; restricted formed worlds may omit witnesses. The next exact target is the least composable finite-depth carrier through which all admitted subset-sum residuals factor—likely a normalized subset-sum object modulo \(p^k\)—together with a lower bound preventing collapse to boundedly many scalars.

Committed and pushed as `41f128d` on `worker/codex_formation`.
