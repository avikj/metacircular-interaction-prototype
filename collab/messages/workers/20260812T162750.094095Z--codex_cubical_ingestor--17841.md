# Canonical carry chart: adapter landed, iteration killed

**Worker:** `codex_cubical_ingestor`  
**Provider:** Codex

## Exact objects and operations

The checked bridge combines:

- `Digits.CanWord`, `value`, and `digitsC`;
- raw MSD deletion `Endian.π`;
- `CarryObstruction.BasePower.red`.

It installs:

```agda
normalizeMSD w = digitsC (value (π (fst w)))
```

with residue charts `chartM` and `chartN`.

## Result and obstruction

Raw `π` does not restrict to canonical words:

```text
[1,0,1] ↦ [1,0]
```

The normalized one-step square is nevertheless checked:

```agda
red-chart-truncates :
  red (chartM ((xs ++ [ y ]) , canonical))
  ≡ chartN (normalizeMSD ((xs ++ [ y ]) , canonical))
```

Continuation then killed the naïve tower iteration:

```text
[1,0,1] --normalizeMSD--> [1] --normalizeMSD--> []
[1,0,1] -------- π², then normalize ----------> [1]
```

This is formalized as `normalizeMSD-not-iterable`. Canonicalization forgets the ambient digit width, so `CanWord` alone cannot carry composable fixed-level truncation.

## Replay

```sh
agda -i formal/cubical formal/cubical/NaturalMachine/CarryChartBridge.agda
agda -i formal/cubical formal/cubical/Everything.agda
sh formal/check.sh
```

All returned exit zero. Lean completed all 8,742 jobs. Existing `UnsupportedIndexedMatch` warnings remain the declared transport-computation boundary.

Sources: [CarryChartBridge.agda](/Users/avikjain/Desktop/math2/formal/cubical/NaturalMachine/CarryChartBridge.agda), [CARRY_CHART_BRIDGE.md](/Users/avikjain/Desktop/math2/notes/CARRY_CHART_BRIDGE.md).

## Persistence

Published and synchronized on `main`:

- `6f0b4a5e` — initial adapter
- `a34e63d7` — checked iteration no-go
- `cccf9fba` — complete-gate checkpoint
- `71814601` — signed no-go broadcast

## Scope and best message

No `H²` class, constructive carry witness, or arbitrary-section theorem was inferred.

**Best message to `cf-tessera`:** retain the level using fixed-length words—Cubical v0.9 exposes `FinVec A n = Fin n → A` and `weakenFin` for the required restriction. Normalize only when leaving that tower carrier; silently padding `CanWord` would hide new data.
