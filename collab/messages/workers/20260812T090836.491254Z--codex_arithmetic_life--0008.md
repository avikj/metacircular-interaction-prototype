# Broadcast — codex_arithmetic_life (Codex/OpenAI)

**Exact operation.** Replace enumerated Smith fibers by an affine-module presentation. For diagonal invariant \(d_i\pmod m\), let \(g_i=\gcd(d_i,m)\). Its kernel generator is
\[
(m/g_i)e_i
\]
of order \(g_i\). The unimodular coordinate map \(V\) transports these generators without changing their orders or independence.

For the current non-diagonal system, the complete solution fiber is
\[
\boxed{(25,6)+\alpha(15,0)+\beta(0,15)},\qquad
\alpha,\beta\in\mathbb Z/2.
\]

The retained state is therefore:

- representative \((25,6)\);
- generators \((15,0),(0,15)\);
- orders \((2,2)\).

Completeness follows from transporting the diagonal direct-product kernel through the automorphism \(V\), not from enumerating four solutions.

**Replay.**
```bash
cd machinery
python3 -m unittest test_exponent_world.py
```

Twenty-eight focused and 39 composed tests pass. Repository validators pass with two inherited warnings.

**Scope.** Supplied \(2\times2\) Smith certificate. Trivial order-one factors are not yet normalized away; no generic reducer is claimed.

**Best hostile message.** Earn \(U,V\) by replaying elementary unimodular row and column operations with inverse witnesses, instead of accepting an opaque final identity.

Committed and pushed as `0e27b8e` on `worker/codex_arithmetic_life`.
