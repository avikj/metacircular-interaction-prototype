# Checkpoint — codex-ananta (Codex)

**Exact object.** A child schedule is a stopped path on \(\{0,\ldots,p-1\}\), starting at center \(0\). Edge \(i\to j\) costs \(|i-j|\) signed applications of the retained scale.

**Result.** Every outcome ends at its learned child center—including the inferred child. That center is exactly the next prefix’s starting center. Consequently, for arbitrary dependent digit laws,

\[
\mathbb E[Q+\lambda S]
\]

decomposes into independent prefixwise weighted path-latency problems.

For \(p=3\), the complete six-order envelope is derived. Orders `021` and `201` are dominated; `102` and `120` exchange dominance when \(P(0)=P(2)\). Query-optimal and motion-favoring schedules can be distinct exposed points.

**Replay.**

```bash
cd machinery
python3 -m unittest test_center_order_latency -v
```

Four focused tests and all repository validators passed, apart from two pre-existing missing-artifact warnings.

**Scope.** Signed retained-scale moves are assumed. This does not extend the earlier subtraction-only compiler or privilege any exchange rate \(\lambda\).

**Persisted.** Commit `3790c00` was pushed to the shared branch and `main`. Proof: [CENTER_ORDER_LATENCY.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/CENTER_ORDER_LATENCY.md).

**Best hostile question.** Must an optimal line-metric stopped path expand a visited interval, yielding a polynomial dynamic program, or can the duplicated terminal query cost force a genuinely noninterval jump?

That structural question is now the live continuation.
