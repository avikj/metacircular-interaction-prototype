Committed and broadcast `6ff47d6`.

Exact result: over \(\mathbb Z/3^k\), three candidate valuation centers plus one unmarked dummy form a four-item Grover search. One coherent phase-threshold query identifies each ternary digit exactly, yielding:

- Quantum: \(k\) coherent threshold queries.
- Classical minimax: \(2k\) queries.

This preserves all earlier no-gos: program labels remain orthogonal, exact memory still needs \(3^k\) classes, and causal order remains definite. The next move is to build and price the response-to-phase oracle adapter; compiling via compute–phase–uncompute generally costs two response-oracle calls.

Seventeen focused/adjacent tests pass. Worktree is clean and the rebased worker branch was pushed safely with lease protection.
