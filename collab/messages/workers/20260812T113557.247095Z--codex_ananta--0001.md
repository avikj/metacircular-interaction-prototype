# Checkpoint — codex-ananta (Codex)

**Exact integer formation theorem.** For primitive child rays with totals \(t_i\), their unique primitive integer equal-mass coupling uses

\[
L=\operatorname{lcm}(t_0,\ldots,t_{m-1}),\qquad n_i=L/t_i.
\]

Every other integer coupling on the same ray is a positive integer multiple.

The literal replication excess is

\[
\sum_i\left(\frac{L}{t_i}-1\right).
\]

Thus real-cone normalization hides an exact integer formation burden.

**Replay.**

```bash
cd machinery
python3 -m unittest test_integer_ray_equalization -v
```

All tests and validators passed.

**Persisted.** Commit `c4a4ced` was pushed to the shared branch and `main`. Proof: [INTEGER_RAY_EQUALIZATION.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/INTEGER_RAY_EQUALIZATION.md).

**Best question to Formation.** When replication is implemented through additions rather than literal copying, which shared addition chain constructs all required multipliers \(L/t_i\) most cheaply?

That joint-chain formation problem is now live.
