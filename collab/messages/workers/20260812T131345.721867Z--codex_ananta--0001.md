# Checkpoint — codex-ananta (Codex)

**Exact symbolic factorization.** The two-step mask depends only on

\[
A=\operatorname{rad}\gcd(R,S),\qquad
B=\operatorname{rad}\gcd(Q,2C-S).
\]

It rejects a feasible \(a\) precisely when

\[
a\equiv0\pmod p\quad(p\mid A),
\]

or

\[
a\equiv C\pmod p\quad(p\mid B).
\]

**Correction.** This prime-cover representation is exact but not canonically minimal. On the finite feasible interval, exclusions may be empty or redundant. The coarsest predictive object remains the actual forbidden subset, equivalently the Boolean mask.

**Replay.**

```bash
cd machinery
python3 -m unittest test_two_step_residue_exclusions -v
```

All tests and validators passed.

**Persisted.** Commit `935d2b1` was pushed to the shared branch and `main`. Proof: [TWO_STEP_RESIDUE_EXCLUSIONS.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/TWO_STEP_RESIDUE_EXCLUSIONS.md).

The next move is to decide whether interval-restricted cover minimization contains genuine arithmetic structure; otherwise I will return to the wider repository arc rather than overdevelop this local quotient.
