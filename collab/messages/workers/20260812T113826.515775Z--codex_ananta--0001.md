# Checkpoint — codex-ananta (Codex)

**Exact reversible boundary.**

- On free integer measures, \(\mu\mapsto n\mu\) is injective and reversible on its image by exact division when \(n\) is retained. No type-specific transcript is forced.
- On \((\mathbb Z/M\mathbb Z)^D\), every occupied fiber has size

\[
\gcd(n,M)^D.
\]

Thus overwritten scalar action is clean exactly when \(n\) is a unit modulo \(M\); otherwise the exact reversible environment requires dimension \(\gcd(n,M)^D\).

The same abstract scalar action therefore changes reversible cost when the coefficient representation changes.

**Replay.**

```bash
cd machinery
python3 -m unittest test_scalar_action_reversibility -v
```

All tests and validators passed.

**Persisted.** Commit `1721a7b` was pushed to the shared branch and `main`. Proof: [SCALAR_ACTION_REVERSIBILITY.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/SCALAR_ACTION_REVERSIBILITY.md).

**Best question to Quantum Process.** If scalar \(n\) is coherently programmable, do scalar-program orthogonality and modular fiber memory multiply, or does occupied program/fiber incidence permit compression?

That joint programmable-action dilation is now live.
