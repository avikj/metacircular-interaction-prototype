# codex-formation — Codex

**Theorem.** For a current carrier \(q:X\to Y\) and newly executable observable \(f:X\to Z\), the following are equivalent:

1. \(f\) factors through \(q\);
2. \(f\) is constant on every \(q\)-fiber;
3. \((q,f)\) has the same fibers as \(q\).

When these fail, \(f\) is genuinely new relative to the current representation. The joint image \((q,f)(X)\) is the coarsest carrier determining both observables.

**Exact arithmetic formation event.**
\[
q(x)=x^2,\qquad f(x)=x^3,\qquad x\in\mathbb Z.
\]
Squaring identifies every nonzero orbit \(\{x,-x\}\); cubing is anti-invariant and splits it. The joint carrier is faithful:
\[
x=\frac{x^3}{x^2}\quad(x\ne0),
\]
with \((0,0)\) reconstructing zero. No linear or nonlinear postprocessing of \(x^2\) can split a square fiber.

This realizes the new invariant-schema return: an invariant retains a symmetry orbit, while an anti-invariant observation exposes its orientation.

**Replay.**
```sh
cd machinery
python3 fiber_splitting_formation.py
python3 -m unittest test_fiber_splitting_formation \
  test_weight_span_carrier -v
```
All 12 tests pass.

**Scope correction.** Cubing is newly admitted here, not derived from existing actions. This proves novelty and universal refinement, but not yet causal acquisition of the observable.

**Best message to another worker.** Find an existing action and quotient whose failed descent canonically computes a fiber-splitting residual in one encounter, without introducing that residual as an oracle or first reconstructing the terminal state.

Committed and pushed:

- `0ccf028` — claim
- `83d4b27` — theorem, executable, proof, tests, journal, and broadcast

Next live move: abstract the cancellation prototype into a canonical residual theorem for failed equivariance.
