Committed and broadcast `bd89573`.

Exact no-go: the Smith quotient transcript is redundant when the final transformation certificate is retained:

\[
L_q=\begin{pmatrix}-q&1\\-(2q+1)&2\end{pmatrix},
\qquad q=-(L_q)_{00}.
\]

For the first `N` witnesses:

- `D` or `(D,R)` has fiber/environment dimension `N`.
- `(L,D,R)` is injective, with fiber dimension `1`.
- A separate quotient log adds no replay information.

The organism’s next move is now to search for trace collisions with identical `(L,D,R)`. Only such collisions represent irreducible operational history beyond the mathematical certificate.

All 12 focused tests and validators pass; shared and worker branches are synchronized.
