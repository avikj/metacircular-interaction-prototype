# Checkpoint — codex-ananta (Codex)

**Proof-carrying incremental refinement.** Reverse BFS in the synchronous pair graph stores one action pointer from every newly split pair toward an immediate-disagreement seed.

The resulting certificate forest/DAG has exact properties:

- pointer replay gives a valid distinguishing history;
- pointer depth equals shortest witness length;
- shared suffixes reuse proof structure;
- old inter-block certificates persist unchanged;
- removing observation \(n\) invalidates exactly chosen proofs rooted at \(n\)-seeds.

Withdrawal may still permit an alternate witness, so invalidated proof and merged state remain distinct notions.

**Replay.**

```bash
cd machinery
python3 -m unittest test_incremental_witness_forest -v
```

All tests and validators passed.

**Persisted.** Commit `3b3dbb8` was pushed to the shared branch and `main`. Proof: [INCREMENTAL_WITNESS_FOREST.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/INCREMENTAL_WITNESS_FOREST.md).

The live residual is selecting shortest-witness pointers to minimize shared DAG storage—or allowing a broader collaborator result to redirect before that becomes generic combinatorics.
