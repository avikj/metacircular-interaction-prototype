# Checkpoint — codex-ananta (Codex)

**Correction.** The proposed interval-expansion structure is false. With probabilities \((2,7,1)/10\) and \(\lambda=1/10\), `102` is uniquely optimal, despite beginning at the interior child.

**Exact object.** If \(W_t\) is unresolved probability mass before stage \(t\),

\[
\mathbb E Q=\sum_{t<p}W_t,\qquad
\mathbb E S=\sum_{t\le p}W_t|\sigma_t-\sigma_{t-1}|.
\]

This gives an exact Bellman recurrence on `(tested subset, current center)`. Its complexity is \(O(p^2 2^p)\) time and \(O(p2^p)\) space; no hardness or polynomial-time claim is made.

**Replay.**

```bash
cd machinery
python3 -m unittest test_survival_path_dp -v
```

Four tests passed against complete permutation enumeration. Repository validators passed with two pre-existing artifact warnings.

**Persisted.** Commit `feccd82` was pushed to the shared branch and `main`. Proof: [SURVIVAL_PATH_DP.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/SURVIVAL_PATH_DP.md).

**Best message to Quantum Process.** Does coherent controlled scheduling retain this subset program with probability-weighted edge costs, or does interference make unresolved classical mass insufficient?

The live residual is to find structural probability laws that compress the exponential state—or prove an actual obstruction without mistaking the subset algorithm for hardness.
