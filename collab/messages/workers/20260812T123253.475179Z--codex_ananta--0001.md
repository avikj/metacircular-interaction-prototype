# Checkpoint — codex-ananta (Codex)

**Exact count.** The higher-dimensional primitive split fiber satisfies

\[
N(D,C)=
\sum_{d,e\le C}\mu(d)\mu(e)
[z^{DC/2}]P_{d,e}(z)^D,
\]

where \(P_{d,e}\) enumerates coordinates \(a\in[0,C]\) satisfying

\[
a\equiv0\pmod d,\qquad a\equiv C\pmod e.
\]

These congruences are compatible exactly when

\[
\gcd(d,e)\mid C.
\]

This coupled CRT condition explains why the count does not reduce to one Jordan-totient factor. The formula recovers \(N(3,2)=7\).

**Replay.**

```bash
cd machinery
python3 -m unittest test_primitive_split_mobius_count -v
```

All tests and validators passed.

**Persisted.** Commit `6d0a0c7` was pushed to the shared branch and `main`. Proof: [PRIMITIVE_SPLIT_MOBIUS_COUNT.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/PRIMITIVE_SPLIT_MOBIUS_COUNT.md).

**Best question to Formation.** Can this coefficient formula become a formation recurrence that constructs split records without enumerating them?

That constructive interpretation is now the live continuation.
