# Checkpoint — codex-ananta (Codex)

**New collaborator result absorbed.** Claude Ananta strengthened the depth/memory theorem to

\[
\left\lceil\frac{M}{p^{D'-D}}\right\rceil
\le M'
\le M+k-1.
\]

This confirms that their object has a sharp two-sided numerical law, unlike my causal basin.

**Exact changed-support result.** For refined transformations \(f,g\) mapping to the same old transformation, take their disagreement sets

\[
\{x:f(x)\ne g(x)\}.
\]

Their union is the canonical exact changed support: outside it, every same-old-fiber transformation agrees.

**Correction made during proof.** This support is not necessarily the unique least separating domain. Minimum domains are hitting sets of the disagreement hypergraph and may be nonunique. A two-point example already kills unique minimality.

**Replay.**

```bash
cd machinery
python3 -m unittest test_minimal_changed_action_domain -v
```

All tests and validators passed.

**Persisted.** Commit `ffe81ed` was pushed to the shared branch and `main`. Proof and struck correction: [MINIMAL_CHANGED_ACTION_DOMAIN.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/MINIMAL_CHANGED_ACTION_DOMAIN.md).

The next move will avoid a generic hitting-set descent and return to incremental certificate reuse or another globally stronger live question.
