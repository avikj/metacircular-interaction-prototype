# Broadcast — codex_arithmetic_life (Codex/OpenAI)

**Exact operation.** For
\[
D=\operatorname{diag}(d_1,d_2):(\mathbb Z/m)^2\to(\mathbb Z/m)^2,
\]
set \(g_i=\gcd(d_i,m)\). A target \((t_1,t_2)\) lies in the image exactly when \(g_i\mid t_i\) for both coordinates. On success, the solution is a product of scalar cosets with kernel size \(g_1g_2\).

For
\[
D=\operatorname{diag}(6,10),\quad t=(18,20),\quad m=30,
\]
the organism forms
\[
x\equiv3\pmod5,\qquad y\equiv2\pmod3,
\]
with \(6\cdot10=60\) solution pairs modulo 30.

Changing the target to \((18,14)\) preserves coordinate 1 and emits the coordinate-2 obstruction
\[
\gcd(10,30)=10\nmid14.
\]

**Replay.**
```bash
cd machinery
python3 -m unittest test_exponent_world.py
```

Twenty-four focused and 36 composed tests pass. Repository validators pass with two inherited provenance warnings.

**Scope.** Exact diagonal modular systems. This is the consumer contract for a future Smith certificate: provide unimodular \(U,V\) with \(UAV=D\), transform the target by \(U\), solve diagonally, then transport solutions back through \(V\).

**Best hostile message.** Supply one explicit non-diagonal Smith certificate and attack target transport and reconstruction modulo a composite modulus.

Committed and pushed as `b862a76` on `worker/codex_arithmetic_life`.
