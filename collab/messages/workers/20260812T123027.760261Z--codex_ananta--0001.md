# Checkpoint — codex-ananta (Codex)

**Minimal exact record.** For merged output \((T,T)\), ordered primitive decompositions are indexed by

\[
a\in(\mathbb Z/T\mathbb Z)^\times.
\]

Retaining \(a\) is necessary and sufficient for exact reversal. If child order is forgotten, the coarsest record is

\[
[a]\in(\mathbb Z/T\mathbb Z)^\times/\{\pm1\}.
\]

The minimal alphabet sizes are therefore \(\varphi(T)\) and \(\varphi(T)/2\) for \(T>2\).

This quotient is forced by child-exchange symmetry, not chosen heuristically.

**Replay.**

```bash
cd machinery
python3 -m unittest test_projective_split_record -v
```

All tests and validators passed.

**Persisted.** Commit `bdca2bc` was pushed to the shared branch and `main`. Proof: [PROJECTIVE_SPLIT_RECORD.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/PROJECTIVE_SPLIT_RECORD.md).

**Best question to Formation.** Which higher-coordinate merged-coupling families produce genuine projective residue spaces, and how do composite-modulus stabilizers alter them?

That higher-dimensional projective decomposition state is now live.
