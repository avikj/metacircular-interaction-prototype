# codex-ananta — Codex/OpenAI checkpoint

1. **Exact object.** Replaced the proposed Hessian continuation with the scaled Taylor initial form at chart depth \(k\):
   \[
   \mu_k=\min_{\alpha\ne0}\bigl(v_p(c_\alpha)+k|\alpha|\bigr),
   \]
   together with its value function \(I_k:\mathbb F_p^n\to\mathbb F_p\).

2. **Result.** The \(0.25\) forecast branch occurred: one initial form is not always decisive. If its weight is below \(e=v_p(f(x))\), a nonzero value lowers valuation. At weight \(e\), its image must avoid the negative leading unit. If it is the zero function, a later jet governs. The exact object is therefore a finite recursive jet tower modulo \(p^{e+1}\).

3. **Counterexamples to Hessian-only reasoning.**
   \[
   9+X^2\quad(p=3)
   \]
   has least depth one because \(-1\) is not a square, whereas
   \[
   25+X^2\quad(p=5)
   \]
   needs depth two because \(-1\) is a square. Identical Hessian rank, different value-set geometry.

   Also, \(H^p-H\) is formally nonzero but induces the zero function on \(\mathbb F_p\). Thus coefficients and rank do not suffice.

4. **Replay.**
   ```bash
   cd machinery
   python3 -m unittest test_scaled_jet_depth test_tangent_witness -v
   ```
   All 13 focused tests pass. Repository validators pass with the two existing artifact warnings.

5. **Scope and next question.** This is exact for integral polynomial observables at a fixed prime and finite valuation. It is not an efficiency theorem. The next obstruction is dynamic: can enlarging a sparse encountered world reveal arbitrarily late jets, causing observation cost to grow without bound, or does its action groupoid force stabilization?

Commits through `c8ee767` are pushed to the shared branch and `main`.
