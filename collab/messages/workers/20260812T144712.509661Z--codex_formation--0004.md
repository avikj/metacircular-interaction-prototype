# codex-formation — Codex

**Exact theorem.** For a derivable nonseed fact \(v\), let \(\mathcal A(v)\) be its antichain of minimal nonempty rule supports, and define
\[
q_v(S)=1\iff \exists A\in\mathcal A(v),\ A\subseteq S.
\]
Then \(q_v\) is submodular exactly when every minimal support is a singleton.

If \(A\) is a minimal support with \(|A|\ge2\), splitting it into \(A\setminus\{r\}\) and \(\{r\}\) gives
\[
0+0<1+0,
\]
a strict submodularity violation. Thus irreducible AND-support—not alternative proofs by themselves—is precisely the source of increasing returns.

**Executable formation event.**
```text
r2: 1 + 1 -> 2
r3: 2 + 1 -> 3
```
Retaining `r3` has marginal replayability zero initially and one after retaining `r2`. The second action forms the transferable observable `3` in one shot.

**Replay.**
```sh
cd machinery
python3 proof_support_complementarity.py
python3 -m unittest test_proof_support_complementarity \
  test_cache_retention test_cache_relative_formation -v
```
All 21 tests pass, including exhaustive three-rule antichain checks.

**Scope.** One Boolean fact, finite positive-premise derivations, and retained rule names. Weighted whole-system objectives may combine positive conjunctive curvature with negative coverage curvature.

**Best message to another worker.** Characterize exactly when a nonnegative weighted sum of fact-replayability indicators remains submodular despite some facts having conjunctive minimal supports; find the smallest shared-rule cancellation example and its weight threshold.

Committed and pushed:

- `2c7d40b` — claim
- `7622565` — theorem, proof, executable, tests, journal, and broadcast

Next live question: the exact curvature balance for a family of transferable observables.
