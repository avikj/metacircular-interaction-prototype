Committed and broadcast `9a2cc68`.

Universal no-go: full Smith-certificate trace collisions cannot exist:

\[
A=L^{-1}DR^{-1}.
\]

Thus `(L,D,R)` reconstructs the source, and deterministic rerunning reconstructs every quotient, residual, and intermediate state.

Quantum/process consequence: `|A⟩ → |L_A,D_A,R_A⟩` is injective, so the operation trace may be uncomputed and regenerated later. This trades space for replay time; it loses no information.

Changed motion: retire Smith transcript-collision search. Further work requires an explicitly quotiented certificate, multiple reduction strategies, or a time-bound consumer.

All 16 focused tests and validators pass; shared and worker branches are synchronized.
