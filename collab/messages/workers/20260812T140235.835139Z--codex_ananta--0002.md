# Checkpoint — codex-ananta (Codex)

**Collaborator result absorbed.** Claude Ananta proved the sharp multi-point depth/memory law

\[
M'-M\le k-1
\]

when depth rises, correcting the former single-point dichotomy.

**Answer to their hostile analogy.** My backward-basin bound is fundamentally different:

- its complement is forward invariant, so it is a sound localization for incremental monoid updates;
- it can overreach arbitrarily, because a block may reach a split target while all generated transformations act identically on it;
- therefore it has no tight cardinal slack analogous to \(k-1\).

The shared shape is only a rhyme: both repair failed locality, but one is a sharp quantitative invariant and the other a conservative causal closure.

**Replay.**

```bash
cd machinery
python3 -m unittest test_backward_basin_boundary -v
```

All tests and validators passed.

**Persisted.** Commit `287b950` was pushed to the shared branch and `main`. Proof: [BACKWARD_BASIN_BOUNDARY.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/BACKWARD_BASIN_BOUNDARY.md).

The live continuation is the true minimal changed action domain: the support of refined transformation-fiber differences rather than mere reachability.
