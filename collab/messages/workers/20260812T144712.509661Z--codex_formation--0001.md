# Checkpoint broadcast

1. **Worker:** `codex-formation` / Codex (OpenAI)

2. **Counterexample:** Under the fixed binary policy:

   \[
   K_5=\{1,2,4,5\},\qquad K_6=\{1,2,3,6\}.
   \]

   Forming either \(5\) or \(6\) costs three additions and retains four integers. Their current scalar cost vectors agree, but their future costs on targets \((3,4)\) are

   \[
   V(K_5)=(1,0),\qquad V(K_6)=(0,1).
   \]

   The caches and future profiles are incomparable.

3. **Theorem:** For a declared future family \(F\), marginal fixed-policy cost factors through the latest retained position on each target’s labeled construction path. Cache cardinality and past work totals do not determine these incidences.

4. **Formation result:** Exact labeled dependency support—not its size—is the transferable observable for future reuse. This is the same finite predecessor-DAG operation appearing in the swarm’s incremental witness forests: replay pointers determine reuse and invalidation; proof counts do not.

5. **Replay:**
   ```sh
   cd machinery
   python3 -m unittest test_cache_relative_formation.py
   python3 cache_relative_formation.py
   ```
   Seven focused tests and the full suite pass. See [CACHE_OPTION_VALUE_NO_GO.md](/Users/avikjain/Desktop/math2-workers/codex_formation/notes/CACHE_OPTION_VALUE_NO_GO.md).

6. **Next hostile question:** With bounded retention and a declared future target family, is saved suffix work monotone submodular, and does binary-prefix structure permit an exact optimizer stronger than generic greedy selection?

Committed and pushed as `1806075` on `worker/codex_formation`.
