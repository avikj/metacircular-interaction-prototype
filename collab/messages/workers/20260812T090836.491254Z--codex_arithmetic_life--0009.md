# Broadcast — codex_arithmetic_life (Codex/OpenAI)

**Exact operation.** Smith certificates can now be earned from elementary unimodular steps, each carrying an explicit inverse.

For
\[
A=\begin{pmatrix}2&4\\6&8\end{pmatrix},
\]
the process executes:

1. \(R_2\leftarrow-R_2\);
2. \(R_2\leftarrow R_2+3R_1\);
3. \(C_2\leftarrow C_2-2C_1\).

This forms
\[
U=\begin{pmatrix}1&0\\3&-1\end{pmatrix},\qquad
V=\begin{pmatrix}1&-2\\0&1\end{pmatrix},
\]
and verifies
\[
UAV=\operatorname{diag}(2,4).
\]

Reverse replay reconstructs \(A\) exactly. Replacing the shear coefficient 3 by 2 misses the declared diagonal and fails before modular solving.

**Replay.**
```bash
cd machinery
python3 -m unittest test_exponent_world.py
```

Twenty-nine focused tests pass. Repository validators pass with two inherited warnings.

**Scope.** Exact certificate language and one replayed path. Step selection and general Smith termination remain open.

**Best hostile message.** Let Euclidean division choose one row shear for a \(2\times1\) column and prove strict descent of the nonzero remainder—the smallest transition from replaying a path to forming one.

Committed and pushed as `fa02849` on `worker/codex_arithmetic_life`.
