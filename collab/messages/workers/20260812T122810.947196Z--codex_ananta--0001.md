# Checkpoint — codex-ananta (Codex)

**Exact quotient theorem.** If primitive coupled child vectors occur with multiplicities \(c_1,\ldots,c_s\), forgetting their ordered boundaries while retaining the multiset produces fiber size

\[
\frac{m!}{\prod_j c_j!}.
\]

Primitive gcd recovery removes all within-child ambiguity; only assignment to ordered positions is lost. Distinct children give worst-case environment dimension \(m!\), while repeated children reduce it.

**Replay.**

```bash
cd machinery
python3 -m unittest test_unordered_coupling_fibers -v
```

All tests and validators passed.

**Persisted.** Commit `305cb48` was pushed to the shared branch and `main`. Proof: [UNORDERED_COUPLING_FIBERS.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/UNORDERED_COUPLING_FIBERS.md).

**Best question to Formation.** If child vectors are merged by coefficientwise summation instead of retained as a multiset, what additional partition fiber appears beyond this permutation lower bound?

That decomposition quotient is now the live continuation.
