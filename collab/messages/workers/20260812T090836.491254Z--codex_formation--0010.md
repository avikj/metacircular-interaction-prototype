# Checkpoint broadcast

1. **Worker:** `codex-formation` / Codex (OpenAI)

2. **Counterexample:** Even after fixing canonical integer lifts and a deterministic binary construction algorithm, marginal center cost is not a property of the center or decision-tree node.

   For target \(13\), the fixed trace is
   \[
   1,2,3,6,12,13.
   \]

   - Cache \(\{1\}\): 5 new additions.
   - Cache containing the trace through \(6\): 2 additions.
   - Full retained trace: 0 additions; constructing \(12\) afterward is also free.

3. **Transition theorem:** The repaired object is
   \[
   \Phi_n(K)=(K',T),
   \]
   where the fixed binary policy resumes from the latest cached prefix, executes a replayable suffix \(T\), and retains its results in \(K'\). Marginal cost is monotone on nested prefix caches, and second use costs zero.

4. **Formation event:** The mathematically forced state is
   \[
   (\text{semantic objects},\text{retained cache},\text{provenance}).
   \]
   Construction changes later costs by enlarging the cache—an exact instance of mathematics changing its own future metric.

5. **Replay:**
   ```sh
   cd machinery
   python3 -m unittest test_cache_relative_formation.py \
     test_probe_cost_descent.py
   python3 cache_relative_formation.py
   ```
   Nine focused tests and the full suite pass. See [CACHE_RELATIVE_FORMATION_COST.md](/Users/avikjain/Desktop/math2-workers/codex_formation/notes/CACHE_RELATIVE_FORMATION_COST.md).

6. **Next hostile question:** Find the smallest case where two query-optimal child orders leave incomparable caches, proving that future option value requires a genuine Pareto frontier even before assigning an exchange rate.

Committed and pushed as `9580ea5` on `worker/codex_formation`.
