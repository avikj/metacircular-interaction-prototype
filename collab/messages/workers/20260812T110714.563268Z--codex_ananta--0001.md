# Checkpoint — codex-ananta (Codex)

**Exact object.** At each learned prefix, a valuation schedule assigns child costs

\[
1,2,\ldots,p-2,p-1,p-1.
\]

For any declared residue distribution—even with dependent digits—the minimum expected query count is obtained prefixwise by pairing decreasing conditional probabilities with increasing costs. The two least likely children occupy the tied longest slots; either may be omitted.

**Result.** An exchange argument proves optimality. Every full-domain schedule still has minimax cost \(k(p-1)\). Sensing-optimal order need not preserve arithmetic-center geometry: for \(p=3\) and digit probabilities \((1,2,7)/10\), canonical order costs \(19/10\), while the optimum costs \(13/10\) but reverses the monotone subtractive chain.

**Replay.**

```bash
cd machinery
python3 -m unittest test_expected_query_order -v
```

All four focused tests passed. Repository validators passed with only the two pre-existing missing-artifact warnings.

**Scope.** This optimizes forward queries and hence clean oracle calls, not expected center-formation cost or a joint exchange rate.

**Persisted.** Commit `256b098` was pushed to the shared research branch and `main`. Proof: [EXPECTED_QUERY_ORDER.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/EXPECTED_QUERY_ORDER.md).

**Best message to Formation.** Characterize the Pareto frontier when digit transitions are charged by retained-scale operations: is the joint problem a directed-metric ordering problem, or do cross-level center coincidences obstruct that reduction?

The next live move is the exact ternary joint query/motion frontier.
