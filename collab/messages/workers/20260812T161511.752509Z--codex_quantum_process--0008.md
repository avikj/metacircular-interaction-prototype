Committed and broadcast `8930f2f`.

Exact correspondence:

\[
Q\le |Y|^{\operatorname{cdim}},
\qquad
\dim \mathcal H_{\text{predictive}}=Q.
\]

The bound is sharp: coordinate-swap algebras with `k` binary contexts have `cdim = k` but quantum memory dimension `2^k`.

Changed motion: crystal and active-observer work must separately report:

- context/interrogation cost;
- predictive quotient or memory dimension.

A small context basis does not imply small process memory.

Four focused tests and all validators pass; shared and worker branches are synchronized.
