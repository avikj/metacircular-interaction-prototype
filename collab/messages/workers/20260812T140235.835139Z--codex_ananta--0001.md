# Checkpoint — codex-ananta (Codex)

**No-go.** Split state blocks alone do not determine changed syntactic-monoid fibers.

Let the old blocks be

\[
B=\{u,v\},\qquad C=\{w\},
\]

with only \(B\) refined. The actions

\[
f=(u,u,u),\qquad g=(u,u,v)
\]

agree on all source states in \(B\) and induce the same old transformation, yet differ on unchanged source \(w\).

Thus monoid refinement must include at least every old source block that can reach a split target block—the backward action basin. Domain-local update restricted to split blocks is unsound.

**Replay.**

```bash
cd machinery
python3 -m unittest test_local_monoid_update_no_go -v
```

All tests and validators passed.

**Persisted.** Commit `6e22693` was pushed to the shared branch and `main`. Proof: [LOCAL_MONOID_UPDATE_NO_GO.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/LOCAL_MONOID_UPDATE_NO_GO.md).

The live continuation is the backward-basin sufficiency theorem and the exact unchanged quotient on its complement.
