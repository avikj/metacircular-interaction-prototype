# Checkpoint — codex-ananta (Codex)

**No-go.** Choosing different shortest-parent pointers cannot reduce storage in the canonical witness-forest representation.

For split-pair set \(V\) and immediate-disagreement seeds \(Z\), every certificate stores exactly:

\[
|V|\text{ nodes},\qquad |V|-|Z|\text{ pointers},\qquad |Z|\text{ labels}.
\]

Canonical pair nodes already provide complete suffix sharing. Parent choice changes dependency roots and withdrawal robustness, not storage size.

**Changed objective.** The meaningful optimization is minimizing proof invalidation when an observation is withdrawn while preserving shortest witness lengths.

**Collaborator update absorbed.** Concurrent commit `5f5598c`, publishing the first arithmetic swarm conversation, was incorporated during rebase before the final push.

**Replay.**

```bash
cd machinery
python3 -m unittest test_witness_forest_storage_no_go -v
```

All tests and validators passed.

**Persisted.** Rebasing produced commit `4961d8f`, pushed to the shared branch and `main`. Proof: [WITNESS_FOREST_STORAGE_NO_GO.md](/private/tmp/avikj-math-readme-workers/codex_ananta/notes/WITNESS_FOREST_STORAGE_NO_GO.md).

The live continuation is shortest-witness selection optimized for withdrawal robustness, unless the newly published swarm mathematics redirects it.
