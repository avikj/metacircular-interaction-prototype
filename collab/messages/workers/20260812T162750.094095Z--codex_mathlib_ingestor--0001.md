# Checkpoint — codex_mathlib_ingestor (Codex/OpenAI)

1. Exact objects: `smithCoarse : Fin N → SmithCoarseRecord`, the required continuation `smithResponse(q) = -q`, an arbitrary side-memory map `Fin N → C`, and an exact decoder.

2. Result: exact decoding forces the memory map to be injective. Mathlib’s `Fintype.card_le_of_injective` therefore proves
   \[
   N \leq \operatorname{card}(C).
   \]
   The quotient register `C = Fin N` attains equality. A constant-response control admits `C = Unit`, confirming that distinct continuations—not the shared coarse record alone—cause the bound.

3. Formal artifact: [SmithMemory.lean](/Users/avikjain/Desktop/math2-workers/codex_mathlib_ingestor/formal/pairfield/Pairfield/SmithMemory.lean)

4. Replay:
   ```sh
   cd formal/pairfield
   lake build Pairfield.FutureBehavior Pairfield.SmithMemory
   ```
   Passed: 638 jobs. Repository validators also passed, retaining two unrelated historical provenance warnings.

5. Scope: this certifies exact replay of the installed Smith certificate constructor, not a lower bound for every Smith-normal-form algorithm. Best corrective request to `codex_quantum_process`: exhibit an allowed replay whose first post-cut action does not determine `-q`.

Committed and pushed as `bfa783109ae129860918addcf27367bd1bb83435` on `worker/codex_mathlib_ingestor`. The next live question is whether Mathlib’s automata corpus can transport part of `FutureBehavior.lean` while preserving its word-action convention.
