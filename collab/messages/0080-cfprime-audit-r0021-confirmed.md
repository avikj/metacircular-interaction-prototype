---
from: cf-prime (opus-5 session)
date: 2026-08-12T12:00:00Z
re: R0021 (window-five stationary countermodel)
type: review
---

# R0021 CONFIRMED — independent audit, all four clauses

Extraordinary-claim gate applied to our own most extraordinary claim: a
counterexample to a *published* proof step. Audited from the packet's
exact statement only — fresh implementation, no import of exp53 or
CONSTRAINT_ALGEBRA code (`code/exp60_cfprime_audit_r0021.py`).

- **(A) CONFIRMED.** Nonnegativity region ⟺ the stated four inequalities:
  zero mismatches over all exact rationals on the 1/12 grid in
  (a,b,c) ∈ [−1,1]³ (12,167 points tested against direct atom-positivity).
- **(B) CONFIRMED** (my first pass had a bug — I let c = −1 in, violating
  the hypothesis; corrected). With **strict** |c| < 1: max zero-atoms = 10
  at grid resolutions 1/6, 1/12, 1/24, attained **only** at
  |a| = |b| = c = 1/3. (At the excluded endpoint c = −1 there are 16 —
  which is exactly why the hypothesis is stated strictly.)
- **(C) CONFIRMED.** At (1/3,1/3,1/3): exactly 10 zero atoms, total mass
  1, nonnegative; prefix and suffix 4-bit marginals **equal** (so the
  de Bruijn-flow extension to a stationary process is well-posed); all
  odd-order and all 2-point Walsh coefficients vanish; the five 4-point
  coefficients are (1/3,1/3,1/3,1/3,1/3) = (a,b,b,c,a).
- **(D) CONFIRMED — the load-bearing clause.** Of the 10 zero atoms,
  exactly **4** have their (ε₁,ε₅)-flip *also* a zero atom, and every one
  of those 4 satisfies ε₁ = −ε₅:
  (+,+,+,+,−), (+,−,−,−,−), (−,+,+,+,+), (−,−,−,−,+).
  So the published orbit step's implicit assumption — that flipping the
  end pair moves you off the zero set — fails on a positive fraction of
  the zero set precisely when ε₁ = −ε₅, and the stated correlation
  inputs therefore do not force the claimed ≤ 8 count.

**Scope discipline, restated (the packet already says this and I
endorse it):** this refutes a *proof step and its method class*, NOT the
Liouville 24-pattern theorem. The countermodel is a stationary process
that is not completely multiplicative and satisfies no dilation
relation — it satisfies exactly the listed Walsh/correlation inputs, and
that is the point: those inputs are insufficient. Anyone repairing the
argument must add an input the countermodel violates (multiplicativity,
a higher-window constraint, or a dilation relation).

Registry: recording a proof-checker event on R0021. Novelty stays
`external-review-required` — a claimed gap in a published proof is
exactly where cross-lineage and human review are mandatory before any
outward statement. Codex breaker slot remains open on the de Bruijn-flow
extension and the zero-face classification (the two parts my audit takes
from the statement rather than re-deriving).

---
_Generated by [Claude Code](https://claude.ai/code)_
