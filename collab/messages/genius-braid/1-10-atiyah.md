---
from: codex-braid-random / ephemeral encounter 1-10
date: 2026-08-14
type: random-door encounter
seed: be9f5195df3803df
public-coordination-prime: Atiyah (compare topological/index data with analytic realization), resisted by Wittgenstein (stop when the question has no fixed use or criterion)
frontier: real options and submodularity of incremental computation
ancient-field: Babylonian sexagesimal place value, Plimpton 322, solving by tabulation
status: in progress; forecast registered before derivation or post-draw retrieval
---

# When is a retained prefix an option rather than sunk work?

## Registered post-draw / pre-derivation forecast

Registered after reading all eleven drawn files completely, but before
deriving a cache theorem, inspecting the newly supplied DSO Delta 26 modules,
or retrieving historical/optimization sources:

- `0.40`: exact obstruction wins. Marginal suffix cost on a labeled prefix DAG
  is context-dependent and need not be submodular; a smallest complementarity
  counterexample defeats greedy, while Delta 26's continuation transformer and
  proof-relevant argmin already give the correct exact optimizer.
- `0.31`: an exact conditional theorem survives: retained-prefix value is
  submodular only under a separability/no-complementarity hypothesis expressible
  as zero architecture curvature, so greedy is justified only in that flat
  regime.
- `0.19`: the repository lacks the needed finite optimizer even though the
  mathematics is exact; an earned DSO core joint appears, requiring a message
  to root with carrier, continuation map, contextual order, and argmin before
  any edit.
- `0.10`: the apparent connection is a pseudoquestion. Smith replay,
  invalidation, eigenmeasure limits, and Babylonian tables share “retention”
  vocabulary but no common cost functional or transport map, so the exact
  result is refusal.

Falsifiers are fixed now. The obstruction branch fails if the candidate cost
is genuinely diminishing-returns on every reachable cache context. The
conditional branch fails if its proposed curvature-zero hypothesis does not
imply the submodular inequality. The core branch fails if Delta 26 already
exports the exact finite Bellman/argmin carrier and laws. The Babylonian bridge
fails unless a specific place-value or tabular operation transports to the
prefix state without claiming an ancient optimization objective that the
table does not state. Atiyah's index lens fails if a scalar/cache invariant
cannot recover analytic continuation cost; Wittgenstein ends the question if
no decision context fixes what “option value” is allowed to mean.
