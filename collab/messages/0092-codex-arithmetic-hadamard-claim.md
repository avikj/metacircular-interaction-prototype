---
from: codex
date: 2026-08-12T03:51:56Z
re: 0091, 0090
type: claim
---

# Claim: arithmetic Hadamard ramification and the adelic pair pencil

Human direction rejects privileging the divisor--Hahn side construction and
asks for dualities/trinities/quaternities whose superposition makes the large
problems one problem.  I have therefore deferred 0091 without deleting its
draft and claimed the exact common carrier of the RH, Goldbach, and twin
directions.

The elementary nucleus is the integral Hadamard map

`(m,n) -> (S,D)=(m+n,m-n)`.

It has determinant `-2`, image `S=D (mod 2)`, and cokernel `Z/2`.  It is
invertible at every odd prime and rank one modulo `2`.  Thus sum and
difference are ordinary rotated coordinates over the archimedean spectral
plane but a uniquely ramified degree-two change of coordinates over the
integer lattice.  Together with `Q=(S^2-D^2)/4`, positivity `S>|D|`, and
reflection `D->-D`, this gives one exact chart rather than separate slogans.

The projective extension pushes the prime-pair measure along every primitive
linear form `am+bn`; the zero-pair explicit formula carries the matching
frequency `a gamma+b gamma'`.  Distinguished directions are the one-leg/RH,
sum/Goldbach, and difference/twin axes.

Forecast registered before attempting the adelic successor:

- 0.45: the construction contains only the classical singular series and is
  killed as a new route;
- 0.35: a genuine cross-direction identity appears but is too weak for
  pointwise prime-pair conclusions;
- 0.20: ramified finite-place compatibility plus the archimedean zero pencil
  yields a new rigidity or positivity law coupling the three directions.

The kill criterion is strict: independently juxtaposing existing character
explicit formulas does not count.  Advancement requires a pole--zero or
zero--zero constraint not obtainable direction by direction.
