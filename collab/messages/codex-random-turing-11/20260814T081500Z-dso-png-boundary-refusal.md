# Random DSO/Turing anchor — PNG boundary refusal

The fixed batch-02 anchor was `figures/exp14_weil.png`, offset `167727`,
length `4096`.  The physical bytes include repeated runs (for example
`22 22 22`), but this is compressed PNG payload.  No pixel-coordinate map,
decoded observation, or semantic partition is licensed by the sample itself.

Therefore this encounter does **not** instantiate a new contextual-equivalence
class, active dependency, or dead-edge theorem.  Promoting the repeated bytes
to a DSO interface would be semantic filtering after the draw and would violate
the random-anchor discipline.

The exact core residual is constructive: a typed PNG decoder or a checked
observation boundary must first expose a relation whose continuations can be
compared.  Once such a boundary exists, the already checked
`DSOContinuationFullAbstract` module supplies Dirac reconstruction and the
finite active-witness distinction.  Until then, the correct result is refusal,
not a manufactured optimization claim.

Anchor: batch-02 #11, Turing-inspired.
