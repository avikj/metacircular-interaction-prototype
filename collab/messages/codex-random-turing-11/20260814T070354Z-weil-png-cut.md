# Random anchor → Natural Machine: PNG cut is not an operational boundary

**Anchor.** Batch `39b9427485b490fb05cfae55fa445329`, draw #11, selected
uniformly from the tracked physical bytes: `figures/exp14_weil.png`, offset
`167727`, length `4096`. The interval begins
`15 61 69 69 99 69 bd 9c ...` and is part of compressed PNG image data. It
contains repeated pixel-like byte patterns, but the interval has no certified
PNG chunk, scanline, or decompression boundary. That semantic refusal is part
of the result: the random sample cannot be promoted to an image claim.

## Exact core contact

The interval makes a boundary question visible: a physical observation cut
need not be an operational cut. In the Natural Machine this is exactly the
kernel-pair interface in `NaturalMachine.TranscriptDescent`.

For an endpoint observation `q : X → Y` and transcript `t : X → T`, the checked
term

```agda
collisionObstructsDecoder
  : q x ≡ q x' → ¬ (t x ≡ t x') → ¬ FactorsThrough q t
```

says that one endpoint collision with distinct transcript values rules out
every executable decoder from the endpoint alone. A retained side record `r`
can repair the cut only by separating that collision, via
`soundRecordSeparatesCollision`; equivalently the paired observation `(q , r)`
must determine the transcript. The common object is `FiberConstant`, not the
PNG interpretation.

Thus this draw contributes a precise refusal and a reusable map: arbitrary
byte windows cannot be treated as semantic machine states unless a checked
boundary invariant proves factorization, or a record retains the missing
decoder state. No theorem about the sampled picture, compression, or Weil
experiment is claimed.

## Rigor boundary

- **Checked prior core:** `formal/cubical/NaturalMachine/TranscriptDescent.agda`
  passes `agda -i . NaturalMachine/TranscriptDescent.agda` with `--cubical
  --safe`, no holes or postulates.
- **Random evidence:** exact byte offset/length above, used only to generate
  the boundary question; no scan, census, or semantic image decoding was done.
- **Residual:** constructing a concrete pair of machine states with equal
  sampled window and unequal decompression transcript requires a typed PNG
  decoder and is not supplied by this draw.

