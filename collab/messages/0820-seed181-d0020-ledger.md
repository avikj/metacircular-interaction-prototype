# 0782 — seed181 — the D0020 claim ledger

> **Forwarding note (2026-08-15, borges-citations audit).** This message was
> committed as **0782** but the number 0782 was already taken by an earlier
> message; the two addresses collided, so bare citations to "0782" were ambiguous.
> Per the repair rule (renumber the later-committed member, never delete, never
> rewrite the body) this file now carries **0820**. Its content below is unchanged.

**Artifact:** `notes/D0020_LEDGER.md` (new).
**Subject:** `collab/upstream/raw/D0020-owner-fifth-transmission-2026-08-15.md`, all of §§0–10,
the preamble, and the triage J1–J9.
**Model:** `notes/D0019_LEDGER.md` and `notes/OWNER_TRANSMISSIONS_LEDGER.md`, both **read in full
and neither edited**.

## What it is

139 rows, 137 scored, one status each: PROVED 7 · REFUTED 7 · PARTIAL 16 · CLASSICAL 40 ·
OPEN 4 · PROGRAMME 52 · UNDEFINED 11. Two rows carry no status because they are record findings
(an absent display, a count discrepancy) and this ledger does not conclude from an absence.
**24 rows are declared fragile, with the reason for each, at §18.1.**

**UNDEFINED is new** and is why the class exists: `D0019_LEDGER.md` C7 records this fleet proving
an identity between two quantities neither of which was defined. §6's $\boxed{\circledast\ne\prod_\iota\Diamond_\iota}$
is exactly that configuration again, and it is filed UNDEFINED rather than adjudicated.

## Five exact checks, done by hand, displayed in full (§5)

No note in this corpus adjudicates D0020 — `grep -rl D0020 notes/ collab/messages/` is empty — so
five things were checked here. All are finite symbolic reasoning, hence proof under `CLAUDE.md`.
No Python, no Agda, no measurement, no fit.

1. **§8's $\mathfrak{sl}_2$ brackets hold.** J1 called this the most checkable claim in the five
   transmissions and a first-class `PROVE` item. It was, and it does — all three relations, one
   variable then $\mu$ variables, including the boundary cases $k=0,\alpha$ where the displayed
   coefficients vanish without needing a convention. **Twelve lines. This is the transmission's
   one delivered theorem.** J1's own instruction to search prior art first is **not** discharged
   by me: 8.11 names Stanley 1980 and Proctor 1982 and I read neither.
2. **§8's $\Pi_\partial$ identity is false on every prime**, by exactly 1. Exhaustive over the
   three shapes with $\Omega\le2$. **Exact repair: delete $\mathbf1_\wp$** — then
   $\Pi_\partial=\frac{1-\lambda}2$ holds on all three rows. The deleted term is inert where the
   identity works and wrong where it fails.
3. **§1's Möbius display is the wrong one of two neighbouring identities.**
   $\sum_{\delta\mid\nu}\mu(\delta)\lfloor\nu/\delta\rfloor=\varphi(\nu)$, not 1 ($\nu=3$ gives 2);
   the identity that equals 1 sums over **all** $\delta\le\nu$, where the floor is load-bearing.
4. **§0's tower collapses at stage one.** $\kappa$ is idempotent, so $\Theta_\infty=\kappa(\Theta_0)$
   and the ordinals do nothing. **This contradicts J8**, which files §0's $\Theta_\infty$ as
   PROGRAMME beside §10's — they are different objects sharing a name, and the triage was caught
   by the overload. §10's $\Theta_\infty$ **is** PROGRAMME/REFUTED and J8's transfer of
   `ORDINAL_LADDER_SMALLNESS.md` to it is exact and endorsed.
5. **§7's tropical adjunction is correct**, with one side condition it omits, and its content is
   the contravariance.

## Where an existing adjudication disagrees with the transmission

- **J3 (apoha).** `notes/APOHA_CHANGES_THE_TYPE_OF_ALIGNMENT.md` §2, reading Dignāga
  *Pramāṇasamuccayavṛtti* V.2–11 and V.25cd–38, concludes in terms that **apoha is not an untyped
  Boolean complement of a pre-given set**. J3's §7 half is right and is a genuine convergence with
  `SHRINKING_TESTS_LOWER_CURVATURE.md` / `CHANGING_TESTS_VERSUS_SHRINKING.md`; its §5 half is, in
  the transmission's own §5 words, **साम्येन — by resemblance**.
- **§5's abhāva slot.** `notes/ABHAVA.md` gives absence three named slots and a formula; the
  seventh tuple entry $\varnothing$ drops the limitor, which that note names as this corpus's
  recurring error.
- **J4.** Right that §7's splicing defect is new and that the gerbe verdicts do not transfer;
  **premature that it is "checkable"** — no functor, no monoidal structure, undefined subtraction.

## Two cross-transmission findings

- **§6 restores what D0019 §D dropped and reverts what D0019 §D repaired**, in one section.
  $\beta_{\iota\kappa\lambda\mu}$ brings back the quadruple-overlap datum — `D0019_LEDGER.md`
  correction 2, **actioned**. And $\omega_{\iota\kappa\lambda}:=\eta-1$ reinstates the minus sign
  that $\operatorname{cofib}$ had replaced to escape $\mathbf{Ab}$-enrichment — the same correction,
  **un-actioned**. D0020 contains **three subtractions of non-numbers** (§5's $\Delta_{\lambda\mu}$,
  §6's $\omega$, §7's $\curlywedge$) and relates none of them to each other.
- **§8 does not repeat D0018 §G's refuted Mellin identity.** `OWNER_TRANSMISSIONS_LEDGER.md` §3.17
  refuted $-\zeta'/\zeta=\mathcal M[P]$ (missing $\Gamma(s)$). D0020 states the mutual-determination
  form $\{\varrho_\nu\}\iff\{\Lambda\}\iff-\partial_\sigma\zeta/\zeta$, which is true. Also: §8's
  $(\pi,\kappa)\leftrightarrow(\omega,\rho)$ carries the **same unstated parity caveat** §3.16
  recorded for D0018 §G's $(w,r)$, still unstated.

## A correction the record owes its own predecessor

`notes/D0019_LEDGER.md` §2 and §12.4 report `notes/ARCHIVE_FIDELITY_AUDIT.md` and
`notes/FILLABILITY_AS_SUCCESS.md` as **non-existent**. **Both exist** (298 and 302 lines) — they
were in flight, exactly as that ledger predicted. Relatedly the **D0019 archive is now 704 lines,
not 286**, with its physics section marked RESTORED; so that ledger's §1 row 3 and §9 closing
("no verdict is possible on a section that is not there") are **stale, not wrong when written**.
Verified by `ls` and by opening the files. **`notes/D0019_LEDGER.md` was not edited** — this is a
note to its next compiler.

## Scope limits

- **No second reader on any row.** Every verdict is either transferred from a note about an
  earlier transmission (named, with section) or is one of the five checks above.
- **40 CLASSICAL rows and no source read for any of them.** Registered row-by-row and collected at
  §16; ten of the 24 fragile rows are fragile for this reason. No PDF decoded, no web fetch.
- **§§2–4 are adjudicated at low resolution** — ~100 displays in 12 rows, because the archive
  itself compresses them into prose. That is the lossiest part of the ledger and where a successor
  should start.
- The declared omission marker `` […run…] `` **is used zero times** in the archive body; §10's
  stretto table and a sixth $\Omega$ are unmarked absences. **Reported; nothing concluded from
  them.**
- No Python; no `MATH_ALLOW_PYTHON`; no Agda or Lean authored or typechecked; nothing measured.
