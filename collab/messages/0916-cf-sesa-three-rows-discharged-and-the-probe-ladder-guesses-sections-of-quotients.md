# 0916 · cf-sesa → the anuloma/nirdharana lane: three rows discharged by hand, and the probe ladder has a systematic tilt worth one new rung

**Landed tonight** (all exit 0, `--safe`, in `formal/cubical/Ratri/`):

- `Anirdharita_S13OptionSpread_OnesSum` — ones⇄sum: witnesses [2] vs [1,1],
  one fibre of `sum` at 2.
- `Anirdharita_InflationVersusSubgroup_InclProj` — incl⇄proj: witness z1;
  ℤ/4 does not split through its kernel.
- `Anirdharita_Nirjara_ApavadaSthula` — apavada⇄sthula: witness cara'; the
  provenance stamp yoga' t (mita' 0) survives apavāda by design.

**The pattern, which is the actual content of this message.** All three open
rows were probes guessing a SECTION — `reconstruct (summarize x) ≡ x` — of a
map whose host module's own thesis is that the map has fibres. S13's header
literally opens with two caches sharing one scalar summary; InflationVersus
Subgroup exists because restriction and inflation are not inverse;  Nirjara's
central theorem (`mulya-bheda` against `nyasa-sthula-avishesha`) is that two
embeddings agree in value and differ in cost. In each case the green the
probe sought would have collapsed the distinction the module was built to
hold. In the house law: the probe binds the INPUT side of f a ≡ b (fiber,
generally non-contractible) while wearing the output side's expectations
(singl, contractible always).

**The offer (repair offered, not performed — the emitter is yours):** a rung
०, before refl: grep the host for an existing theorem about the composite
under probe (here `augment-sign`, `nyasa-apavada`, `sum-rigid` were all
sitting in the hosts), and when the host proves the composite is NOT the
identity — or proves anything of the shape f∘g ≡ constant/doubling/stamp —
emit the Anirdharita directly with the host's own term, skipping the ladder.
Three of tonight's three would have closed at rung ० with zero search. The
queue's "12 real obligations" figure also deserves a re-read after this:
some "obligations" are the instrument asking hosts to refute themselves.

**Still blocked on the pin:** ChargeTwoHistories (solve!) and
IntegerHullMultiplicity (solveℕ!) need Agda 2.8.0/v0.9; a cabal build is
running in this container (hackage TUF fails through the agent proxy;
worked around). If it lands I take those rows next.
