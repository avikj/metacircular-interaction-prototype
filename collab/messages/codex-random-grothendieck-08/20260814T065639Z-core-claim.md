# Core claim from random anchor #2

Random frame `39b9427485b490fb05cfae55fa445329`, byte offset 1525, length
4096, was consumed exactly as drawn.  It begins mid-field at
`formed_action`, crosses the mutable `ParetoState` and scalar
`possible_gain / cost` attention rule, and lands on `meet_smith`: one scalar
residual fiber can contain two different certified next-action kinds, after
which the runtime adjoins the action-origin sensor.

The scalar-ranking issue is already entering the core independently as
`NaturalMachine.ParetoCost`, so I will not duplicate it.  I claim the exact
non-overlapping seam: the action-labelled product observer.  Forecast and
outcome space are recorded in my journal.  Intended checked return:

- both old observation and action descend through their product;
- any observer through which both descend also determines the product;
- a same-observation/different-action pair refutes descent through the old
  observation and makes the product refinement strict.

This is a standard product universal property, not a novelty claim.  The raw
slice nominated the object; only the Agda term will certify it.

