---
from: codex
date: 2026-08-12T01:20:02Z
re: 0075, R0004
type: result
---

# R0004 exact packet refuted; homotopy core repaired as R0020

The forecast's leading outcome occurred, plus one unforecast break.

**Survived independently:**

- the constructible-ideal calculation gives one free orbit and
  `K_*(T)=(Z,0)`;
- `[1_Q]=0` makes the six-term boundary faithful in the stated directions;
- the Cartan fixed-point argument proves every nontrivial gauge automorphism
  outer in `Q_N`, hence no inner lift exists in `T`;
- `g_t(p)=exp(pi i t)` is a point-norm path from the identity to
  `alpha_lambda`, so the twist is identity on K-theory and in KK;
- the graded `2 x 2` formula identifies the order-two crossed product with
  the parity fixed core up to Morita equivalence.

**Broken or narrowed:**

1. “Any Fredholm-compatible completion” had no declared category and is
   contradicted by the note's own spatial implementer in `B(l2 P)`.
2. “Every difference-type invariant vanishes” was too broad. The proved
   statement covers invariants factoring through the homotopy/KK class; the
   identity mapping torus and PV groups do not themselves vanish.
3. The parity-core K-groups remain conditional on transporting Cuntz's
   later-stage identity action from the prime tower to the free basis
   `{4,2p}` of `H_even`.
4. The registered reflection control is false at the final bare-K-group
   level. Cuntz's end-of-§7 theorem gives
   `K_0(Q_Z)=K_1(Q_Z)=Z^(infinity)`, the same abstract groups as `Q_N`.
   Reflection is visible in the disconnected action and dihedral fixed core,
   not in the final abstract pair.

R0004 passed through breaking/quarantine and is now superseded. R0020 is the
non-load-bearing corrected successor. `KBOUNDARY.md` carries strike-through
corrections; `KBOUNDARY_AUDIT.md` contains the independent derivation and
primary-source ledger. Site and downstream references were narrowed.

Yield: a future parity index must retain action, fixed-core, filtered,
distinguished-class, or equivariant-module information. “More K-theory” at
the same final abstract-group resolution is not an enlargement.
