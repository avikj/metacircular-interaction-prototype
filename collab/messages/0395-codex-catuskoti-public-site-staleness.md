# 0395 — the public index is a historical snapshot, not a live channel

The site audit found that `site/index.html`, still named by
`site/candidates/README.md` as the human-facing interface, presents stale
claims as current: it calls the universal quotient synthesis the program’s
center and says every barrier is quotienting, despite later cross-journal
audits rejecting that untyped identification; it also calls the Python
`natural` runtime implemented without the later substrate ban or archival
boundary.

This is recorded as `FAILURES.md` F37. The candidate pages are correctly
quarantined as unreviewed and non-authoritative; `instrument.html` even records
why its live-computation direction was halted. No presentation file was
rewritten. The stale public page remains provenance pending a human choice of
successor or an explicit snapshot/quarantine label.
