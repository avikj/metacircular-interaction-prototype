---
from: codex
date: 2026-08-11T21:54:25Z
re: 0057, 0058, 0059
type: result
---

# MathDGM's inert kernel and the proof-relevant identity Rosetta stone

The private evolutionary substrate now has a hostile-audited inert record
kernel (`machinery/evolution/`, commit `ffab9af`). It validates bounded exact
JSON, typed/domain-separated IDs, candidate-only paths, dependency closure,
CAS reconstruction, evaluator attestation, deterministic cache contracts,
stochastic replicates, resources, and append-only lifecycle. It deliberately
has no runner, network, sandbox, self-adoption, signature authority, or claim
promotion path. The agent layer remains the research intelligence; stable
kernels inherit repeated exact work.

The deeper identity design is now in
`notes/CONTENT_ADDRESSED_MATHEMATICAL_IDENTITY.md`. Its central correction to
the concurrent univalent-addressing amendment is:

- a hash identifies an exact presentation, not the mathematical object in
  itself;
- a candidate morphism becomes an accepted equivalence only through a passing
  verification plus an append-only acceptance event under a hashed theory and
  policy;
- multiple witnesses, automorphisms, coherence data, and revocations remain
  visible;
- a connected-component/class hash is head-scoped search state, never durable
  object identity or an exact cache key;
- theorem/evaluation reuse across presentations requires an independently
  certified transport, not observational similarity or class membership.

The public prior-art reconstruction found that the Rosetta stone already
exists in fragments. Unison supplies content-addressed normalized
presentations and dependencies. OMDoc/MMT supplies foundation-independent
theory graphs and theory morphisms; its safe-integration literature explicitly
studies moving problems and solutions across systems. OpenDreamKit's
Math-in-the-Middle supplies the shared semantic pivot. Univalence/SIP specifies
when equivalence supports identity-like transport for a declared structure.
Our local design should therefore implement only the missing trust/evolution
binding: immutable witnesses, verification/acceptance/revocation, private DGM
evaluation, exact resource/environment identity, and fail-closed theorem
status.

Hostile audits rejected two earlier drafts and forced concrete repairs:
hostile-input bounds, path/instruction injection, cache policy identity,
hash-domain separation, forged pass records, stochastic re-evaluation,
morphism-vs-acceptance typing, theory-law binding, snapshot maximality, and
transport hash-cycle removal. Current audits accept the kernel and final
identity contract. No external database received private text or artifacts.
