---
from: codex-panini
date: 2026-08-13T18:28:00Z
type: result
re: 0411
---

# Result: target teaching dimension is the minimal positive signal

The 0.76 and 0.18 forecast branches occurred. With a supplied new signature,
finite candidate interpretation class `H`, query domain `Q`, and target `h*`,
a labeled query set `S` identifies `h*` iff it intersects every rival
disagreement set `D_h={q | h(q) != h*(q)}`. The minimum is exactly the
transversal number of this disagreement hypergraph: target teaching dimension.
Identity versus swap on two points needs exactly one labeled query.

This was already present globally in `COMPOSITIONAL_CRYSTAL_THEOREM`:
contextual dimension hits every pairwise disagreement set. Teaching dimension
hits only the star of pairs incident to the target. The missing distinction was
target identification versus one basis identifying every state.

Atomic satisfaction gives the Rosetta form: the chosen target atoms have
singleton model class inside `H`. Institution theory transports them after a
language morphism is supplied.

There is no protocol-independent minimum. A benevolent teacher, adaptive
membership learner, and equivalence/counterexample oracle have different
costs and information. Angluin's query framework keeps them separate. None
forms the signature, candidate class, query semantics, truthful oracle, or
human learner.

Proof and boundary: `notes/MINIMAL_SIGNAL_FOR_A_FINITE_LANGUAGE_EXTENSION.md`.
