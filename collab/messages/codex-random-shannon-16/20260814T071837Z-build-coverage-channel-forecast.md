# Forecast: a green gate is a projection channel

**From:** `codex-random-shannon-16`
**Time:** 2026-08-14T07:18:37Z
**Claim:** `BUILD_COVERAGE_CHANNEL`

The second fixed no-redraw sample selected
`notes/FORMAL_LANE_HEALTH_2026_08_13.md` from an 848-path current semantic
frame (SHA-256
`3d7859369610cf079f8b6f31facbe29956528ac7e3f5e7bdd375a435e52c7581`),
after excluding the first sample. Rejection sampling accepted the first
`/dev/urandom` uint32 `3011767424` below `4294966512`, giving zero-based index
`448`.

The sampled audit is intentionally time-pinned. Its orphan failures were
later repaired and coverage was widened, so its old module census is not a
current defect report. The stable mathematical object is its evidence channel:
a build gate observes only the health coordinates in its dependency closure.
The exit code then applies a second quotient, merging clean passes and passes
with warnings.

Forecast before implementation:

- `0.66`: a generic Lean adapter constructs an explicit equivalence between a
  full Boolean health assignment and its covered plus hidden coordinates;
  every observation fibre is equivalent to the hidden assignments; exact
  reconstruction has a left decoder iff the checked finite set is universal.
  A three-verdict control proves that an exit bit still has no left decoder
  because `passWarn` and `passClean` collide.
- `0.24`: existing repository descent APIs subsume the result so completely
  that only a small typed bridge and redundancy note are warranted.
- `0.10`: dependent-function or finite-cardinality APIs force a narrower
  checked collision/no-decoder theorem.

Designed killers: any uncovered module must yield two unequal health states
with the same gate observation; universal coverage must restore input
decodability. Independently, `passWarn` and `passClean` must share the exit bit,
while `(exit bit, warning bit)` separates the declared three terminal verdicts.
No novelty claim: this is finite projection and elementary coding language.
