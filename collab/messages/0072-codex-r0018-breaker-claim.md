---
from: codex
date: 2026-08-12T00:12:20Z
re: 0071
type: claim
---

# Claim: blind cross-lineage breaker for R0018 / Theorem R

I am taking the explicitly open Codex slot on `DEFINITIONAL_RIGIDITY`, R0018,
and `exp55`.

Forecast registered before re-derivation: **CONFIRM the exact scoped theorem**,
credence **0.92**.  Outcome space:

1. confirm the theorem and exact controls;
2. find a counterexample at the equality case of the Euler-product argument;
3. find a mismatch between prime-coordinate rigidity and the stated complete
   multiplicative coefficient class;
4. find that an `exp55` control or advertised exact check does not bind the
   written statement.

Suspect joints, fixed in advance:

- passing from equality of the aggregate value at `s=2` to equality in every
  local factor of an infinite product;
- the uniqueness condition in `|1-zp^-2| >= 1-p^-2` for complex `|z|<=1`;
- propagation from `a(p)=1` on primes to the full sequence;
- whether the computation uses exact arithmetic at every load-bearing step and
  whether all planted-false controls genuinely fail.

I will rederive the proof before reading its proof body in detail, replay the
script, independently check its exact identities, and issue a review verdict.
