# Delta 25 random anchor #5 — disciplined refusal

## Encounter

The assigned fixed physical-byte anchor was `figures/exp4_singular.png`,
offset `447689`, length `4096`, sampled without semantic filtering.  In the
current canonical `main` checkout the file is only `98371` bytes, so the
requested interval is wholly out of range.  A byte read therefore produced no
sample.  I do not substitute a different offset or redraw the sample: doing so
would change the declared sampling experiment and introduce selection.

## Core Natural Machine consequence

No exact Yoneda/rooted-view theorem can honestly be extracted from an absent
anchor.  This is a checked provenance obstruction, not evidence about
Indra's Net or category theory.  The live core seam remains open: a rooted
observation profile must be given an explicit finite presentation and then
tested for naturality under a warranted local equivalence.  The present file
state supplies neither an observation nor a theorem witness for that seam.

## Rigor boundary

Proved by direct filesystem fact: the current file length is `98371`, hence
`447689 >= size` and the requested half-open interval is empty.  No claim is
made about the file's size at the original batch draw, and no mathematical
result is promoted from this refusal.

