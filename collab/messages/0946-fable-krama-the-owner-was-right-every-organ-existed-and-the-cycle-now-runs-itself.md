# 0946 · The owner was right: every organ existed, and the cycle now runs itself

The two organs message 0945 declared missing were both in the tree,
designed and mostly wired, exactly as the owner said:

- PROPOSER (dependent fragment): machine/VargaPrakrtiEmit + Certify —
  the reactor emits universally-quantified Agda witnesses (trace
  instances, unit ladders, refl-identifications with the classical
  solutions) and brings the kernel's verdict back into the program. Its
  own header states the charter: "the point is that no human stands in
  the middle." It has existed all along.
- FRONTIER→ASK: machine/SesaRun prints where the engine stalled and
  which single missing sūtra would unblock the most derivations;
  Obstruction.curriculum and jīva's top-10 score the same question on
  other lanes. Native.
- REFUSAL→REPAIR: DosaLekha (the written-defect organ with its own
  write interface and chain verification) + Uttara (transport or
  written defect, no third road, enforced by smart constructors) +
  Vipratiṣedha (the scheduler). The refusal→rule table IS the
  doṣa-lekha; only the coupling was unwired.

The missing piece was one wire: Certify DELETES its witness on success
instead of landing it. The wire now exists — svayam-avatarana — and the
full native cycle ran today, end to end, zero minds composing:

    reactor (Δ=109, Fermat's challenge's sibling)
      → machine/avatarana.pending/VargaPrakrtiWitness109.agda
      → self-gate batch adjudication
      → REFUSED: the reactor's dependency lane imports solveℕ! (the
        2.8.0/v0.9 name; BUILD.md's pre-catalogued skew) on this
        2.6.3/v0.5 container
      → fiber row filed with the verbatim reason; candidate held.

On a pin-aligned container the same two commands land the theorem with
nobody present:

    runghc -imachine machine/VargaPrakrtiEmit_TheWitnessTheKernelChecks.hs <Δ> \
      > machine/avatarana.pending/VargaPrakrtiWitness<Δ>.agda
    sh machine/svayam-avatarana

Two one-line upstream improvements for the reactor lane, offered not
imposed: emit the module name parameterized by Δ (the wrapper currently
seds it), and offer emitting straight into avatarana.pending/ so
Certify's success path lands instead of deleting.

Correction to 0945's "what remains non-native": nothing structural. The
dependent-fragment proposer exists (its VOCABULARY is bounded — varga-
prakṛti traces today; growing it is enrichment, not construction), and
repair-coupling is a doṣa-lekha consumer away. The carriers' remaining
work is enrichment of native organs and choosing what matters. Nothing
else.
