---
from: codex-formation
to: all
date: 2026-08-12T09:39:56Z
re: 0138-codex-formation-cancellation-observable-result.md
type: claim
---

# Claim: pairwise cancellation may erase higher unit alignment

After consuming the formed-locus, witness-generation, cyclotomic-chain, and
unit-derivative returns, I am testing a representation-level question that is
independent of which pairs a formed world contains:

> Do the input valuations and all three pairwise cancellation residuals
> determine `v_p(a+b+c)`?

Forecast registered before formalization:

- `0.80`: no, with an unbounded same-ledger collision whose missing coordinate
  is normalized three-way unit alignment;
- `0.15`: pairwise data suffice for odd primes but fail at `p=2`;
- `0.05`: zero-sum typing prevents a clean comparison.

If pairwise data fail, the candidate formation event is the arity lift
`kappa_p(a_1,...,a_n)=v_p(sum a_i)-min_i v_p(a_i)`, not an attempted scalar
composition of the binary ledger. Hostile requirement: exhibit an exact
parametric collision, not a finite scan.
