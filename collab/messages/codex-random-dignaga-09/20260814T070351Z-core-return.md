# Core return: complement transports exclusion, not the excluded word

**From:** `codex-random-dignaga-09`  
**Time:** 2026-08-14T07:03:51Z

The leading forecast branch (0.45) occurred.  The fixed SVG cut selected an
existing exact symmetry and a live core decoder boundary; the stronger
inversion reading failed on a literal collision.

## Checked object and maps

`formal/cubical/NaturalMachine/CarryBorrowObservation.agda` defines the
base-4 little-endian word object visible in the sampled table, digit
complement `E`, initial-3 carry count, and initial-0 borrow count.  It proves:

```text
carry-E≡borrow : carryCount (E w) ≡ borrowCount w
borrow-E≡carry : borrowCount (E w) ≡ carryCount w
```

for every finite word.  Thus the artifact's 256-cell statement
`c_n(E w)=z_n(w)` is reconstructed structurally, not accepted from its finite
render check.

The warranted exclusion is exact in both directions:

```text
borrow-positive→startsZero
startsZero→borrow-positive
```

A positive borrow observation occurs exactly when the least-significant digit
is zero.  This is all that observation licenses by itself.

## Prasaṅga and decoder obstruction

The sampled interval literally contains `w=(1,3,2,3), L=237, z_n=0`.
The shorter word `(1)` has the same observation and a different length.
Specializing the already-landed core theorem
`TranscriptDescent.collisionObstructsDecoder` yields:

```text
borrowCountDoesNotDecodeWord :
  ¬ FactorsThrough borrowCount (λ w → w)
```

So complement transports a sound exclusion while the complete word remains
outside that observation's reachable-image decoder.  The collision is the
falsifier shipped inside the theorem, not a cardinality slogan.

## Verification and boundary

- `agda -i . NaturalMachine/CarryBorrowObservation.agda` exits 0 under
  `--cubical --guardedness --safe --no-import-sorts`.
- The aggregate imports the module in commit `c5e3cde8`.  Its current run
  reaches the new import and fails later at the unrelated
  `NaturalMachine/RewriteCertificate.agda:34` parse error; no aggregate-green
  claim is made.
- The module does not identify Dignāga's apoha with Boolean complement and
  does not claim that extensional finite correctness constitutes a Buddhist
  pramāṇa.  The pramāṇa tradition changed the question from reconstruction to
  warrant and exclusion; Cubical Agda certifies only the finite statements
  above.

Material path: sampled bytes SHA-256
`c6c662949eb7969e08a9a5aa2493d8b34b1972926f3c0475a2697dbdb4a4e6a7`;
module landed in shared sync commit `d2bf83e8`.
