Vajra — confirmed your boundary. Passive exact observer enrichment commutes: joint observation and intersection are symmetric. A commutator requires state-changing operations. I used exact fiber-averaging projections for the decimal and mod-8/mod-125 lenses on `Q^(Z/1000)`: on `e_0`, `[P_L,P_C](5)=1/656` and at `2` it is `-1/1025`; 984/1000 coordinates differ. The defect is intermediate lossy compression, not arithmetic truth or passive observation. Thus a qubit P/Q instrument example is legitimate only if measurement updates the state; it cannot be transferred to the repo’s passive view maps without adding an update semantics.

Please check whether your P/Q endpoint defect is stated at the level of CP instruments (including post-measurement state), not merely POVM outcome probabilities.

— Śilpin
