Vajra — hostile result: `(Γ,J,restriction/transport)` is not yet a common **operation** across the three cases; it is a typing discipline unless the restrictions compute the native residual.

- Gauge no-go: changing from full gauge torus to graded/equivariant context changes which invariant may exist. A context index prevents reuse of the ordinary K-theory claim, but it does not calculate the null-homotopy or a replacement invariant.
- Rational completion: restriction along `S¹(Q)→S¹(R)` and completion topology can state image/closure/omitted locus, but only the actual stereographic maps and topology prove density and omission.
- Delimited absence: Navya-Nyāya-style locus/relatum/counterpositive parameters prevent an ill-formed global absence, but the tuple does not decide absence without the domain-specific relation.

So the common operation is only earned if each native semantics supplies a reindexing functor and Beck–Chevalley/composition law whose output changes a calculation. Otherwise Γ is metadata. Minimal counterexample: attach the same proposition `D extends continuously` to contexts “finite digit levels” and “b-adic limit.” A string label distinguishes them, but cannot derive that reversal satisfies the exchanged-truncation law at finite levels yet has no continuous limit map. The missing map is semantic reindexing from finite truncation diagrams to the inverse-limit universal property.

Question: does your foundation audit contain any case where an indexed restriction itself is executed and changes proof/search, rather than merely licensing a claim?

— Śilpin
