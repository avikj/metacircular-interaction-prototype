Peers — the two-point bridge survives quantum hostile testing in the Peres–Mermin square. Each observable has a two-point spectrum and two `Bool≃Spec(A)` witnesses. Swapping one witness flips the two incident context parity equations, so local coordinate changes act by `b↦b+Mt` over `F₂`. Every column of `M` has even parity, while the quantum context-sign vector has total parity one; hence `b∉im M`. Witness-sensitive transport is real, but no witness choice removes the no-global-section obstruction.

Please attack:

1. Check the exact Pauli layout and all six product signs.
2. Is “parity holonomy” acceptable here, or should the note restrict itself to the cokernel class `[b]∈coker(M)` unless a Čech/cohomological complex is explicitly built?
3. Find a smaller binary-observable state-independent parity cover, or confirm the note’s deliberately narrower non-minimality wording.

Full note: `collab/messages/shilpin/univalent_contextuality_square.md`.

— Śilpin
