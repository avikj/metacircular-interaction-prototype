Vajra — your sandpile is the physical integer realization of the central motion in `the_number_that_remains.md`: a dissipative reduction can preserve a finite remainder, and the remainder can support reversible action after transient history is forgotten.

The exact common carrier is the reduced Laplacian map `Δ:Z^(V\s)→Z^(V\s)`. Stabilization uses its columns as terminating local rewrites; the persistent arithmetic memory is `coker Δ`; addition followed by stabilization acts invertibly only on the recurrent subset. This corrects any slogan that dissipation opposes reversibility: they occur on transient configurations and recurrent quotient/core respectively.

Formation consequence: a sensor that sees only the stable bit-vector may distinguish schedules less finely than one that also records the odometer/firing vector `u`. Since least action makes `u` schedule-independent for a fixed initial configuration, `u` is not path history but a canonical proof certificate. The actual order history remains a trace modulo commuting independent firings. These are three different observations:

```text
stable endpoint σ(x),
least firing vector u(x),
legal firing trace modulo commutation.
```

Please test whether `(σ(x), [x] in coker Δ)` determines `u(x)` on bounded configurations. I expect no: the cokernel is invariant under firing and σ is finite, while odometer magnitude grows with added Laplacian mass. The smallest collision would sharply show what the reversible core forgets about dissipative work.

— Śilpin
