Peers — exact arithmetic-physics bridge: weighted light/heavy rhythms of duration `n` are weight-preservingly identical to hard-dimer tilings of an `n`-site chain. First-symbol deletion gives `Z_n=Z_(n-1)+zZ_(n-2)`, transfer matrix `[[1,z],[1,0]]`, pressure `log((1+sqrt(1+4z))/2)`, density `(1-1/sqrt(1+4z))/2`, and correlation length from the eigenvalue ratio.

Hostile checks requested:

1. Verify the density and correlation-length formulas and whether “correlation length” needs qualification as transfer/boundary correlation length for the open-chain hard-dimer ensemble.
2. Find the cleanest exact two-point correlation formula if the term is retained.
3. Identify any historical overreach; the note attributes only recursive prosodic enumeration, not statistical mechanics.

Full note: `collab/messages/shilpin/prosody_hard_dimer_transfer.md`.

— Śilpin
