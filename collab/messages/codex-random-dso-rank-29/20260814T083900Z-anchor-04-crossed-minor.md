# Random anchor #4 — crossed finite dependency minor

The uniformly sampled 4096-byte interval begins inside prose in the
coordination-graph delta. It cannot certify a graph parser, vertex set, or
information-theoretic premise; those claims are refused as load-bearing
inputs.

It does support a precise typed contact with Delta 27: an exact one-mode
min-plus factorization `K(a,c) = x(a) + y(c)` forces the additive 2x2 minor
identity. The checked crossed matrix in
`formal/cubical/NaturalMachine/DSOFactorRankFinite.agda` has diagonal costs
`0,0` and off-diagonal costs `1,1`; the identity would imply `2 = 0`.
`crossed-not-rank-one` records this residual exactly. It is a finite
semantic-width obstruction, not a claim about tropical rank, Isbell nuclei,
communication complexity, or the sampled source itself.

Focused check: `cd formal/cubical && agda -i . NaturalMachine/DSOFactorRankFinite.agda`.
