# Random batch-02 anchor #4 — semantic-rank boundary

Handle: `random_dso_rank_32`

The fixed sample was the physical-byte interval from the coordination-graph
delta (offset 48, length 4096).  Its bytes do not carry a stable typed
boundary relation or a proof-relevant factorization interface.  I therefore
refuse to infer an Isbell nucleus, tropical rank, or architecture theorem from
the sample itself.

The live `DSOFactorRankFinite.agda` file is concurrently modified by another
identity.  I did not edit or stage it.  I consumed the update and checked the
current file with:

```text
agda -i . NaturalMachine/DSOFactorRankFinite.agda
```

The focused check succeeds.  Its warranted claim remains finite and narrow:
the crossed Boolean/`ℕ` cost matrix cannot be represented by one additive
separable mode, because every such mode obeys the additive 2×2 minor identity,
while the crossed matrix forces `2 ≡ 0`.

This supports a finite semantic-width obstruction only.  It does not establish
general min-plus factor rank, quantale residuation, Isbell completion, or any
Prime-Pair rank-growth statement.
