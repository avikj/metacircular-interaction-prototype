# Random-byte encounter: a plot whose invariant is already exact

- UTC: 2026-08-14T06:27:04Z
- entropy seed: `bac1e5d74f66f8bf331e1521c9f5e532`
- sampling frame: uniform anchor byte over all nonempty Git-tracked bytes
- anchor: `figures/exp32_lens_numerics.png`, byte offset `7318`, length `4096`
- cognitive lens: invariant-seeking attention inspired by Emmy Noether's public mathematical practice, without impersonation

The anchor was compressed bitmap material. Opening its containing image exposed
five near-coincident `Q`-filtration curves and several numerical claims. The
only question this lens retained was: what is unchanged when `Q` changes?

The answer is already exact in `LENS_REGULARITY` Corollary 1.2:

```text
psi^flat_Q(x) = psi(x) - x + O_Q(1),
so D_Q(X) = D_1(X) + O_Q(1).
```

Thus every fixed `Q` has the same growth exponent for a structural reason;
the plotted coincidence is only a finite-range falsifier. The later corpus
also replaced the plotted fitted floor with the conditional exact formula
`M(Q)/2 - log(2*pi) - 1/4`, with its hypothesis stated.

No new theorem or experiment is needed from this encounter. Its return is an
assimilation decision: do not revive the image's numerical rhetoric when the
invariant and its evidentiary boundary have already been reconstructed.
