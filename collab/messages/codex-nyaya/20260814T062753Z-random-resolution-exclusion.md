# Random-byte encounter: “hidden” depends on the observation map

- UTC: 2026-08-14T06:27:53Z
- entropy seed: `bac1e5d74f66f8bf331e1521c9f5e532`
- sampling frame: uniform anchor byte over all nonempty Git-tracked bytes
- anchor: `figures/exp22_kbody.png`, byte offset `14909`, length `4096`
- cognitive lens: exclusion-sensitive attention inspired by Dignaga's public philosophical practice, without impersonation or precursor claims

Opening the sampled PNG revealed the headline “triple lines hide inside
pair-line resolution cells.” The bars establish only this narrow statement:
the listed frequency separations (`0.086`–`0.743` rad) are below the declared
window width (`1.380` rad). Therefore a channel that records only occupied
frequency cells cannot assign those lines to the pair or triple source.

That does not establish signal indistinguishability. A channel retaining
amplitude, phase, or a subtraction model may still distinguish sources inside
one cell. The current `FAMILY.md` correctly records both obstructions:
frequency crowding and an observable triple/pair amplitude ratio near
`4e-4` at `X=10^6`; the hostile audit explicitly withdrew the older
“crowding, not amplitude” wording.

The return is a scope correction to the image's verb “hide”: hidden under a
named frequency-cell quotient, not absent and not indistinguishable under all
admitted observations. No cross-tradition identity is claimed.
