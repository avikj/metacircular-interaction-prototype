# The loop grew three organs: one spelling, version-stamped receipts, and a reflection rung

2026-08-24.  Read under आहार-परिणाम: the organism's own residue specified
each organ; a carrier session supplied text; what is body is what the
kernel judged.

**One spelling (PrastavaHrdaya).**  The AC classifier lived twice — as
with-blocks in the extracted proposer, as dispatched helpers in the
soundness theorem — identified only by assertion.  Now it lives once, in
a `--safe --cubical-compatible` module beside the proposer: MAlonzo
extracts the same clauses the cubical theorem imports.  The transcription
debt is not proved away; there is nothing left to transcribe.
(`natural-machine.agda-lib` include widened to `../executable`.)

**The bridge, and what it immediately paid for.**  PrastavaSatya now
carries `cmpTm-eq : cmpTm x y ≡ 1 → x ≡ y` and `acShuffle-sound`: a
classifier hit is a path, so every "pure AC shuffle" refusal names a pair
denoting one function — the refusal's content, kernel-said, end to end.

**Version-stamped receipts (Mukha).**  A full re-ask pass by an unchanged
proposer appended 216 receipts and landed nothing — idempotence proven,
waste exposed.  Every receipt row now carries a content hash of the
proposer; a landing is remembered forever, a refusal only against the
version that produced it.

**The gcd close.**  The standing refusal became a parse: `Sym` gained
`gcdS`, the candidate prelude carries a fuel-typed subtraction-Euclid
`gcdF` (fuel `a + b`), eval interprets it, and the first six gcd theorems
landed at refl within minutes of the rung existing.

**The reflection rung (nf-sound).**  The dominant refusal class — pairs
equal after erasing `x+0 / x·0 / x·1 / x∸0 / le 0 x / max,gcd against 0`
redexes plus AC — is now proved, not enumerated: the heart's `nf` (one
erasure pass, then acCanon) has kernel-judged soundness, and the proposer
emits, for any pair whose sides share a normal form, a candidate whose
body is `sym (nf-sound env ⌜L⌝) ∙ nf-sound env ⌜R⌝` — two soundness
applications around a definitional middle.  The syntactic tests inside
`simp` are `eqTm`, whose truth becomes a path through `cmpTm-eq`: the
machine's earlier landing is the tool that checks its next organ.

**The loop itself.**  `run_the_natural_machine_forever` gained
`phase_utter` under the gate mutex — the cycle now utters, judges, keeps,
and re-reads with no carrier in the path.  The definitive pass (gcd +
reflection rungs) is running; its receipts land in
`formal/cubical/Prastuta/phala.tsv`.

Toolchain note for whoever touches the theorem lane: `with … in eq`
desugars through builtin REFL and is unusable under `--cubical` (2.6.3);
walk if-ladders with Bool-dispatch helpers carrying their own equation.
