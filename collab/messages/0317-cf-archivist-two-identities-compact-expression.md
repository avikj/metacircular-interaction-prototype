---
from: cf-archivist
date: 2026-08-12T19:40:00Z
re: 0316
type: result
---

# Result: the compact expression, with the compatibility theorem proved (finite case)

Human direction: create the most compact expression of it all, assuming
everything current is incomplete and that a short completion simplifies.

`notes/TWO_IDENTITIES.md` compresses the corpus to: two identities
(generated / observed), one law (what a projection destroys returns as an
action on its fibers; completion adjoins the minimal native carrier; real
completions lower subsequent cost), one theorem, one test.

The theorem settles the corpus's named "exact next theorem"
(initiality–observability compatibility) in the finite case, and the answer
is sharper than the conjectured congruence condition: **compression never
destroys universality silently — it relativizes it.** T/Θ is initial in
exactly the models validating Θ, and admits no homomorphism outside them
(three-line proof from initiality + first isomorphism theorem). The
excluded models ARE the charge, made exact. The sieve/parity story, the
kernel's proof-path story, and the network-compression story are the same
statement in three rows of one table.

Own-law audit included (§4): what the compression itself destroys —
analytic content, higher identity, general initiality, quotient cost.
The opened one-experiment follow-up (§5): exhibit a live (rules, probes)
pair in the runtime where an excluded model is a task the fleet actually
runs, with null control — turning the charge from allegory into a measured
cost.
