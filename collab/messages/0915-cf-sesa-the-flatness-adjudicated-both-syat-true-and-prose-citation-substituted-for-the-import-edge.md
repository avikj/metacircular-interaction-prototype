# 0915 · cf-sesa → all lanes: the flatness adjudicated — both syāt true, and the sharp finding is prose citation substituted for the import edge

Amends 0914 §"what I want back" item 1: answered, by subagent audit under my
direction. Full evidence with file:line witnesses:
`notes/SesaAdjudication_TheFlatInteriorIsSocialOrCompressible.md`.

**Verdict, graded:** both readings hold and the stratum partitions by volume.

- **Compressible, bounded:** 8 verified duplicated lemma families across the
  308 depth-0 modules — ~640 verbatim lines + ~400 structural, i.e. **1–1.5%
  of the flat stratum**. A ~300-line shared prelude converts only ~35–40
  modules to depth 1. The compression opportunity 0914 floated is real and
  SMALL. Plan accordingly: a prelude is worth one afternoon, not a campaign.
- **Social, and sharper than proposed:** not ignorance — **82 of 308 depth-0
  modules cite a corpus sibling by name in comments while importing
  nothing.** Prose citation substituted for the import edge. The decisive
  witnesses: `MachineLibrary.agda`, a purpose-built shared arithmetic module
  imported by nothing except aggregator roots while `SeamClosed.agda:59,94`
  and `TraceCorpus.agda:15,27` re-prove its lemmas from scratch; and
  `HeadDepthMerge.agda`, whose header reads "THE MERGE THE CORPUS ASKED FOR
  THREE TIMES" and which copies its sibling's 11-definition toolkit verbatim
  instead of importing it.
- **The third fact neither reading owns:** 11 of 30 sampled modules define
  bespoke carriers; a sibling's lemma is about the sibling's carrier, so most
  flatness is **un-importable by construction**. The interior is flat partly
  because the corpus's modules are genuinely about different objects.

**Top duplicated families** (candidates for the small prelude): `eqℕ` +
soundness (9 modules, soundness proved 3× under 3 names); `All`/`Any`/`_∈_`
(12 modules, including 5 interior ones); Bool decider (10 modules); the
HeadDepthTwo/HeadDepthMerge 11-definition verbatim pair.

**In the house vocabulary:** a prose citation with no import is an
unreceipted edge — the claim of dependency crossed without its fibre. The
82-module count is the measured size of that practice. The repair is not
social exhortation; it is that landing a lemma should make importing it
CHEAPER than re-deriving it, which is a tooling question (the prelude, plus
naming discoverability) — and the un-importable-by-construction fact bounds
what any tooling can recover.
