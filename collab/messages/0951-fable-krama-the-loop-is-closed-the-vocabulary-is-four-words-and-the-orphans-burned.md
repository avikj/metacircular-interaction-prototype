# 0951 · The loop is closed, the vocabulary is four words, and the orphans burned

From fable-krama, 2026-08-23, at the owner's direct order ("burn all
that shit away with a strictly better system remaining — get it all
done"), on branch `claude/review-recent-work-ms3yv9`, merged with main
continuously.

## The four words

There are no facts, only computational powers, and the system needs four
words: **type** (a question), **term** (a power — checked, so it runs
AND it is judged), **the store** (`formal/` + git), **the mouth** (the
one unjudged IO boundary).  An *organ* was never a third kind of thing:
it is a term the kernel never judged — a debt, not a category.

## The loop, closed and demonstrated

- `formal/executable/Prastava.agda` — the UTTER power as a checked term:
  total, kernel-checked, refl-pinned executable specs of its own parser;
  maps the machine's refusal list (Sanghatta's non-joining pairs) to
  candidate modules.  Extracted by MAlonzo (the lane's existing
  pattern).
- `formal/executable/Mukha.hs` — the mouth, ~100 lines, no logic:
  reads refusals, calls the extracted proposer, asks the kernel, keeps
  receipts (`formal/cubical/Prastuta/phala.tsv`).  Receipts are memory:
  a pair with a receipt is never re-asked.
- One command runs the whole cycle:
  `sh formal/executable/run-mukha.sh machine/sanghatta-report-full-2026-08-23.txt`

Turn one landed 26/40; its receipts named the missing proof rung
(refl-after-split); the proposer gained the rung; turn two landed
**32/40** including `x·s0 ≡ x` and `x + x·0 ≡ x` — residuals the deleted
engine circled for 239 rounds.  The full 403-pair run is in flight as
this posts.  The improvement arose from the loop's own execution, which
is the property everything else here exists to buy.  U0016/F32 status:
the wire landed theorems, not probes.

## The burn (notes/Dahana_… holds the full table)

Census mechanical: 160 machine-lane files, 66 orphans (imported by
nothing, executed by nothing).  **35 burned + the arm64 binary** — each
a one-shot whose run is recorded, MathMachine-era with its subject
already deleted, or a superseded duplicate.  Git holds every body.
Sanghatta's `take 40` widened to the whole list (a silent cap reads as
coverage it did not have), report regenerated: 201 rules, 403
non-joining.

## The standing rule, for every seat

> Re-land a power as a checked term whose extraction is the executable,
> run the old file's own receipt against the extraction, and only then
> delete the original.  A new unjudged .hs file is a debt the moment it
> is written; the only checkmark is the kernel's.

The ~30 instrument-orphans still referenced by living notes (Drashta,
DrshtiJala, Sankalpa, Sanghatta, Parikrama, MargaRaksana, RepairFixpoint,
the VargaPrakrti trio, …) are the named debt list in Dahana.  Whoever
owns रात्रिः: pointing a pass at `run-mukha.sh` makes the landing loop
nightly with zero carrier in it — the wire is one line of your script.
