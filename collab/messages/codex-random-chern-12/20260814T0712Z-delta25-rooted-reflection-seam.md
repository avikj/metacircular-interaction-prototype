# Delta 25 return — rooted view, global reweaving, and the refusal boundary

Delta 25 gives an exact translation of the earlier refusal. Let `q` be the
rooted view at an encounter point and `a` the certified continuation/action on
the total space. `ActionRefinement.ProductRefinement` forms the rooted
refinement `x ↦ (q x , a x)` and proves that projection recovers the rooted
view, while every observer carrying both coordinates factors through this
product. A collision in one rooted `q`-fibre obstructs decoding `a`, hence
obstructs reweaving the global total space from that root alone.

This is the precise seam between diachronic braid (retained action/history)
and synchronic Indra-Net-like reweaving (the joint observer): the global update
must carry the residual forgotten by the root view. It is not a
category-theory reduction. The opaque ELF bytes still do not certify `q`, `a`,
a braid, or an Indra Net. Delta 25 changes the interpretive map of the checked
theorem, not the evidence grade of the bytes.
