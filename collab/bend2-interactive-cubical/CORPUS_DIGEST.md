# Corpus digest

*Appended lane by lane as the corpus is read, so that what is learned
survives any one session. Each entry: the module, the theorem it carries
(as its types say it), and what it fixes for the runtime. Companion to
`STATE_OF_THE_WORK.md` Part II, which holds the digest of the first pass
(fibre core, cost, coordination, kernel-flat, abstracts 14–25, lane docs).*

## Batch 1 — the residue, kernel and rope modules the geodesic note cites

**Prasna** (`theorems/residue`). `Interaction X = (Q : X → Type, δ : (x) → Q x → X)`. `IExec x` (now, receipt, answer, rest) ≃ `Answers x` (answer, more): `forgetStates`/`replay`, both round trips corecursive paths, the run side by the ∨-square `here e (~i ∨ j)`. Contractible questions ⇒ `answers-unique` as a `PathP` over a path of states ⇒ `isContr (IExec x)`; no set-ness of `X` used. `closed f` has `Q = Unit`; `utm = closed uStep`; Niyati's one-execution returns as an instance. *Runtime:* a question is a SUP labelled by the question; the answer is the DUP; receipts are `refl` and weigh nothing; the history is the answer stream.

**AnantaVeni, AnantaVeniMatra, SthairyaSutra, CaturamsaBhramana** (`theorems/physics`). The rope `Rajju = Dhārā Sūtra` (a coinductive stream of `Bool × Bool`); crossing `veṇī∞ i` swaps strands i, i+1 with the quarter turn `(a,b) ↦ (not b, a)` on the strand passing over. Every braid relation and every distant commutation holds at every position, by one induction peeling to a definitional base. Each crossing is local (readers below and above the pair unmoved, refl) and of order exactly eight (fourth power refuted on the constant rope). Every crossing is 1-Lipschitz with unit lookahead in the take-metric; a word of length |w| is uniformly continuous with modulus |w|: depth is time, the word's length is its light cone. The quarter turn has order four on the pair, its square is the half turn which descends to each coordinate definitionally, and the quarter turn descends to neither: the order-four coherence lives only on the interdependent pair. *Runtime:* an interaction net's locality and unit lookahead are exactly this; the geodesic `Bring(n) = n` rests on §2 of SthairyaSutra; the coherence a single observable cannot carry is the label correlation.

**Apratikaryatva** (`theorems/grammar`). The type of ways back from the n-truncation, `Σ f. ∀a. f ∣a∣ ≡ a`, is `hasRetract` on the nose, forces `isOfHLevel n A`, is contractible when A is an n-type, hence a proposition always, hence `प्रत्यानयनम् n A ≃ isOfHLevel n A`. Not an n-type ⇒ the type of ways back `≃ ⊥`. Truncation annihilates every path space; preserves exactly maps into propositions (`(∥A∥₁ → P) ≃ (A → P)`). Path two's ledger `A ≃ Σ b. fiber f b` is complete; `isEquiv f ≃ ∀b. isContr (fiber f b)`; and the disjunctive "no third road" for propositions is exactly LEM, both directions.

**TritiyaMarga** (`theorems/grammar`). `Defect f = Σ b. ¬ isContr (fiber f b)` (a site, not a refutation). `Writable` (every non-equivalence has a written defect) implies Markov's principle via `fst : Σ n. ¬(α n ≡ true) → ℕ`; LEM yields only `∥ Defect f ∥₁`, which has no retraction when the defect is two-valued. Writing the defect is an imperative on the author (the kuṭṭaka discipline: keep the remainder and recurse), not a logical principle. *Runtime:* a checker's error report is a `Defect`: it names a site. The mathematics says producing it is search, which is why `-C` collapse and the must-fail registry are the right shape and a boolean verdict is a दुर्नय.

**Nasti** (`theorems/grammar`). `अविशेषः`: every map out of `∥A∥₁` is blind to which. Two moves: transport (`transport (ua e)`, `uaβ`) and truncation; the सूत्र's "no third" is the sentence the two modules above price.

**HidingAndHardness** (`kernel-flat`). With `view : W → V`, `V` a proposition: simulation is perfect (equal, not indistinguishable); `fiber view v ≃ W` (the view shrinks nothing); extraction is an equivalence iff the witness is unique (both hypotheses named: `V → W` is completeness); two distinct witnesses forbid extraction outright. `∣_∣₁` is the maximal hiding view. The kernel's `sound` is a perfect-hiding view and extraction at the seed is refuted by two routes. Binding the output is free (`isContrSingl`); all price sits at the other binding. *Runtime:* zero-knowledge is not a protocol property here, it is the h-level of the codomain.

**RewriteCertificate** (`Kernel`). `Tm` (six variables, zero, suc, add), `Step` with `reverse` as a constructor, `Derivation` as a list of steps, `eval` into ℕ, `step-sound`/`derivation-sound`, `HypStep`/`HypDerivation` with a conclusion-indexed hypothesis usable under contexts, `InductionCertificate` (base + step) sound for every environment by induction. The atom of the kernel.

**Residue_TheDerivationCarriesNoMeaning** (`Kernel`). `eval a ρ ≡ eval b ρ` is a proposition (ℕ a set); soundness factors through `∥ Derivation ∥₁` with the triangle `refl`; direct and detour histories have lengths 2 and 4 so `Derivation seed target₀` is not a proposition; `len` does not factor through the truncation; no function of the meaning, at any type, separates the two. `reverse (reverse p)` is semantically invisible and distinct data. *Runtime:* the route is the whole residue; ranking is extra-semantic; `advance` may not dedup.

**AvrttiResidue** (`historical_proofs`). `GroupoidOver M` = an inverse with both laws over a given `MonoidMachine`. `X ≃ X` fills it (`invEquiv`); the kernel's `Derivation A A` under `⊕` fills the monoid strictly and carries no inverse for any candidate function, because `len-⊕` is additive and the round trip has length two. The kernel keeps what `≃` throws away, invisibly to meaning.

**Vyapti** (`Kernel`). `control-sound : Control t → t ≡ source` makes every installed `NativeOperation` fire at exactly one term with a constant output: the kernel memorises. Coverage of a library is finite over an infinite language (heights). `SchematicOperation` carries a substitution witness `Σ u. t ≡ subVar u lhs` and is sound for free by `eval-subVar`: generalisation is free, memorisation costs, and no native operation covers two contexts. A schema whose lhs has no variable is enabled by every substitution at one context, all emitting one output: the fibre over an emission is a copy of `Tm`.

**Interaction_TheKernel** (`Kernel`). `Control` is an open extension point (`demand R d` requires any evidence `R`), and `enabled-set-is-subsingleton` is its safety theorem. `learn = install ∘ CheckedFuture.derivation`; `Session` (origin, here, trace, library); `step` advances all four; `⊕-sound` costs nothing because the meaning is a proposition; `retire` installs a whole session as one operation; every operation that exists is sound. *Runtime:* the transcript is the library; the library grows by concatenation; nothing false can be taught because a `NativeOperation` cannot be formed without its derivation.

**Varanam** (`theorems/physics`). A section `∀b. fiber f b` is a choice of receipt everywhere; contractible when f is an equivalence (`isContrΠ`); `Bool → Unit` has two distinct sections. Read as spontaneous symmetry breaking: the vacuum chooses a point of a formerly free fibre.

**NKSUnivalence** (`theorems/logic`). Wolfram's four sentences as terms on a unary substitution system with two events: W1 coordinatisation is a path of structured systems (`ua`, `ua→`, `ua-gluePath`), every property transports by `subst`, the emulation is the path's transport and computes by `uaβ`, and the space of coordinatisations is contractible (`EquivContr`); W4 divergence with definitional reconvergence; W2 the merge is `∣_∣₁`, with the branchial pair as the fibre content of the merged point; W3 every consumer of the thread answers equally on both branches and the merge has no section. *Runtime:* the ruliad is the typed point's path structure; observers are maps out; branchial structure is the fibre of collapse.

**Sarvavibhaga** (`theorems/logic`). `A ≃ Σ b. fiber f b` for every f (the isomorphism theorem and rank–nullity as one construction); injective iff every fibre a proposition; surjective iff every fibre inhabited.

**Bahupratyanayana** (`theorems/logic`). A retraction makes every fibre source-thin; two distinct sources over one target kill every undo (no h-level, no decidability); the discrete log `powg` has no left inverse; `Unit → S¹` has a retraction and a fibre `ΩS¹ ≃ ℤ`, so two fibre points are not the obstruction, two sources are; the circle's no-descent is a different theorem, at π₁.

**Avaccheda** (`theorems/number`), **Avaccheda** (`fibre`), **WholePartialDesa**, **LawfulStep**, **TheCarrierIsTheMotive** (`fibre`). The cut's boundary is the base and memory is the fibre failing to be contractible, with three verdicts (empty, contractible, crowded) that `isContr` merges. The fibre of `∣_∣₁` over any point is the whole source. The census `देश f b` is a datatype whose constructors carry evidence; the sequential diagnostic "the first non-contractible fibre is where the information went" is refuted in both directions by `Unit → Bool → Unit`. A lawful step need not be invertible: `collapse : ℕ → ℕ` is lossless as a factorisation because its residue is large; `suc` fails the other way (empty fibre). The Carrier is the motive, `f = realize ∘ to-motive` by `refl`, and n realizations of one motive mediate n² translations through the middle.

## Physics lane (277 modules, headers read in full)

The lane has six families, and every one of them is the fibre law read at a physical object.

**The rope and the braid (≈40 modules: VeniBandha, VeniTraya, VeniCatustaya, AnantaVeni, AnantaVeniMatra, AnantaVinimaya, VibhagaSutra, SuddhaVeni, GhurnaGana, GhurnaPatha, NirupaSutra, VeniSamvahana, ArdhaSamasa, CatuhResidueSiddhanta, PurnaSiddhanta, PurnaAbheda, PurnaModa, AnantaKendra, KendraAtireka, KendraNirvahana, KendraPurnaNirvahana, SimaSutra, SthairyaSutra, PurnataSutra, VestanaSutra, VestanaBheda, CaturekaSutra, MatraSopana, SopanaBheda, SopanaSamyoga, VibhajakaOrder, SamanyaSopana, CihnaRajju, VeniPatha, CaturamsaBhramana, Jyotiryugma).** Strands are interdependent pairs `Bool × Bool`; the quarter turn `(a,b) ↦ (not b, a)` has order four on the pair and descends to neither coordinate (each sees only its square, the half-wave). The twisted swap braids by refl (Yang–Baxter), has exact order eight, inverts, and three of them do not commute: B∞ acts on the coinductive rope with every relation of every Bₙ at once. Every crossing factors as pure swap after pure twist and the swap conjugates the twist by its transposition (a semidirect weave); every word normalises to (permutation, twist word); twists commute totally and four alike cancel; a word acts trivially iff its swap word does and every count vanishes mod four; two words act identically iff their permutations agree and their counts agree mod four. The writhe is the crossing count, conserved by the relations and read by no bounded observer: a gauge charge, a homomorphism onto ℤ/4. Each observable reads the count modulo the order of its own blindness (full: mod 4; xor: mod 2; constant: mod 1); coarser rungs are dashboards of finer ones, climbing is a new sense certified by a blind pair; the ladder is a category. Every crossing is 1-Lipschitz with unit lookahead, a word is uniformly continuous with modulus its length (depth is time, the word's length is its light cone), the rope is complete (coinduction is completeness), the uniform quarter turn is central of order four, the pointwise limit of named finite words, and outside their image; the centraliser on the causal class is exactly ℤ/4. The constant rope is blind to swaps and the marked rope sees them: the theory's detectors are an interdependent pair. *Runtime:* this is the interaction net's own geometry: local rules with unit lookahead, labels as the coordinates of an interdependent pair, the same-label diagonal as the only place order-four coherence can live, and cost as a per-strand count that no local observer reads whole.

**Holonomy, gauge, bundles (Pradakshina, Sarani, Jala, SetuKsetra, AvinimayaSetu, LoopSamyoga, SamanaVarga, Pula, EkaTransport, HolonomyDescent, HolonomyIsInvisibleExactlyToAnInvariantSemantics, LosslessReturnLoop, GaugeOrbitClasses, CakraVarga, ChargeCriterion, ChargeIsStrictRefinement, OracleQueries, OracleSeparation, QuotientFiberLaw, ProjectionChargeAudit2, DescentObstructionUnified, Vestibulum, AbstractSpinNetworkKinematics, JetPravaha, Ekaghata, HolonomyFluxDerivation).** The holonomy of the helix over the circle is the successor function, computed by `uaβ`; the constant bundle returns identically, so holonomy lives in the family, not the loop. A lattice, a connection and a Wilson loop are built; the transformed line telescopes to endpoint conjugation, so on a loop the conjugacy class is the invariant and every class function factors through it; a principal bundle is a family whose fibres are torsors, transport is equivariant by path induction, holonomy is a group-valued homomorphism from loops and changing the fibre point conjugates it (the gauge law derived, not assumed). Holonomy is invisible exactly to consumers invariant under it; caches, provenance and proofs are the identity consumer. A set-valued task descends through an orbit quotient iff invariant, uniquely (`isContr`). Sign assignments are a torsor over the gauge group; the transcript fibres are the cosets of the query annihilator (the class theorem, over any abelian group); a query set separates iff it contains an odd-Ω query; the functional equation carries zero charge and the specialised instance is one charged value query. `QuotientFiberLaw` is the one theorem: a closed observation class sees a quotient, no post-processing manufactures the fibre, visibility returns only by a charged query, twelve proofs in the corpus are its instances. *Runtime:* observation is a map out of the typed point; what a consumer can see is its fibre; a superposition's label is the charge the observer either reads or is blind to.

**Conservation and symmetry (Dhruva, Bhedaka, Kaksya, SamanaKaksya, SvaFiberVasa, YogaDhruva, YogaKsetra, Apratiloma, Vyapti, Agamya, Sunyabhara, Varanam, ApasaranaNiyama, Khahara).** Conservation `f ∘ Φ ≡ f` is exactly "Φ maps every fibre into itself": the conserved quantity is the fibre index, the gauge orbit is the fibre. If f is an embedding, a conserving flow is the identity (surjectivity unused); the conserving flows of any observable are the sections of its own fibre family (`Σ-Π-Iso`, both round trips refl); at a lossless observable the flow space is contractible; at addition the fibres are torsors and the flows are the shear fields; the loss order "g factors through f" is a preorder along which the conserving monoid grows, and it reads unreached points that the flows cannot. Noether's first theorem does not transfer (no dynamics, no variational structure, the flows are a monoid); the structural half does. Every instruction of the groupoid machine has a contractible conserving-flow space; the discrete log sits off it by its period: what an attacker cannot recover and what can move unseen are one fibre. Reversibility conserves distinctions, so erasure displaces a bit into the environment's kept fibre; heat is that fibre's thermodynamic name. A section is a choice of receipt everywhere, contractible for an equivalence, real for `Bool → Unit` (symmetry breaking at the smallest scale).

**Strata and charge (StaraArpana, AyamaArpana, AnantaraArpana, ArpanaSopana, Mauna, SarvaMauna, CurvedLoop, CurvedLoopSanketa, GranthiCarya, CaryaNivrtti, IndrajalaDipa, BahumargaBheda).** Truncation withholds a charge for one stratum and utters it whole at the next: `Ω(∥S¹∥₃) ≃ ℤ`; the n-sphere is silent through stratum n+1 and speaks at n+2; the stratum above silence carries the whole charge, never a partial one; every stratum of a sphere utters a new charge (π₃S² = ℤ) and the charge is whatever the space holds (ℤ×ℤ for the torus, a free group for a bouquet); a groupoid falls silent above its own level, so the torus and Klein bottle are bells struck once, differing only in whether the two successions commute, a difference the carrier cannot see and π₁ and H² each see in its own way. The string carries its integer; windings add.

**Optics and quantum (Mani, Trika, Sphatika, Yamala, Bandha, VargamulaViparyaya, VeniYangBaxtara, UnivalentTensorInteraction, ExactTwoStateAmplitudes, SamaMana, BraidCoherenceBoundary, PMCokernel, PMIncidenceLocalSystem, PMMonodromyDerivationNoGo).** The swap on a two-point set has no square root, so the qubit is forced: enrichment past two points is what holds √NOT; the orb's total-internal-reflection gate is that achromatic quarter-wave; axis quarter-waves are the quaternions, non-abelian, and the qubit is a spinor; one orb is an abelian integrable billiard; two coupled orbs give SWAP, whose half is the entangler; CNOT is lossless and does not factor, and copies the control into the target (the diagonal). Unitarity and Yang–Baxter are logically independent both ways. Peres–Mermin's obstruction is a nonzero class in coker δ ≅ F₂ read by the parity functional, with local sections everywhere and no global one. *Runtime:* every gate is an equivalence; what is given up on the way to universality is commutativity, never reversibility; the entangler is the non-factorable diagonal, which is the same-label correlation.

**Analysis made finite (the RH and Navier–Stokes series: TauRupa, WeilDhanatva, KreinSucika, ParimeyaRupa, Reflection, Vrddhi, Atikrama, Prabala, AnekaVrddhi, Parimana, Nirapeksa, BoundaryBlock, BoundaryBlockGeneral, CyclicParseval, ParaSima, BlockSegment, Shell, SimaResidue, WindowShiftResidual, FiniteExplicitFormula, SarvaGrahi, Pramanika, Sadhaka, SadhakaResidue, Gersgorin, Count, HistoryCompletion; QRClosure, VieillefosseFibre, PrabhavaDhruva, DiscriminantIsTheOnlyFirstIntegral, TriangularPair, TorusFourierLayer, TaranagaGanita, GalerkinJets, GalerkinLookahead, DhanaVibheda, SecantCriterion, SamaChaya, SamyakSopana, SopanaTransport, ScaleFluxContinuity, Sphota, VyayaLekha, VrddhiSima, StrainInvariants, and the derivation-identity modules).** Every analytic claim is reduced to a ring identity, a two-point witness, or a finite certificate: Weil positivity is "every mode fixed by the reflection" on a finite configuration; a mode is bounded iff its ratio is at most one; positivity certificates are integer LDLᵀ plus a diagonally dominant slack, checked at machine speed; the explicit formula for a finite zero set is Newton's identities; the restricted-Euler quotient closes by ring identity and the pressure coupling does not descend through it; energy is a noncoercive marginal whose completion is the signed scale-flux; a blow-up witness is a dependent record that two incompatible inherited readings empty; Galerkin truncation is exact to the window's depth by SthairyaSutra's crossing profile. *Runtime:* the discipline is the one the emitter must keep: nothing analytic is trusted, only what reduces; the trust boundary is the last identity that is not a term, and it is named.

**The Jain arena (DharmaAdharma, Kala, Karma, Gunasthana, MoksaLosslessReturn, Pudgala, Jiva*, Vargana).** Discrete time, a speed cap giving a causal cone, the soul's equation of motion as an affine step on ℕ whose only fixed point is zero, identity through change as a section through a changing family with the biography theorem. The soteriology and the termination measure are one fixed-point theorem.

**Design consequence collected here.** The lane says the same thing at every object: the observable is a map out, the fibre is what it forgets, symmetry lives in the fibre, charge is the fibre index, holonomy is transport around a loop with its receipt, and cost is a Matra on the route, invisible to every semantic reading and read whole by no local one. The runtime that carries this is the one whose values are typed points, whose labels are the coordinates of interdependent pairs, whose collapse enumerates fibres without merging them, and whose count is schedule-invariant.


## Residue lane (156 modules, headers read in full)

The residue lane is the fibre law read at the *machine*: at codes, executions, witnesses, collisions and the one-way function. Where the physics lane asks what an observer forgets, this lane asks what a computation forgets, and answers with the same object.

**The one coin (Nanaka, GterTwoCoordinate, TrtiyoMargoNaVidyate, TrtiyaCidram, EkamChidram).** Gap, route, schedule and count are four readings of one coin: a non-contractible fibre. There is no third road: a fibre is contractible (free, routed, no residue) or it is not (paid, carried, the residue), and the corpus refuses every construction that tries to make a middle case. Two coordinates that do not factor through each other are the minimal non-contractible fibre, and every "hole" in an execution is that pair.

**The census (Nasti, Ekatva, Vyutpatti, Vistara, Avacchedaka, BahuSesa, LinearOrderFinite).** नास्ति (empty), सकलादेश (contractible, whole) and विकलादेश (crowded, partial) are the three verdicts and they are exhaustive. Lossless ⇔ contractible fibres ⇔ the map is an equivalence ⇔ the trace determines the source. Vistara's padding lemma: padding a code changes no fibre census, so size is not a function of meaning (the NaturalMachine/Laghava sentence proved at the residue). A finite linear order is the census sorted.

**Collision and the one-way function (SanghattaBija, Residue_TheOneWayFunction, GodelSeparation, PNeqNPIsNotUniversal, PeqNPHoldsOnTheLosslessUniversalMachine, TheFloorIsAnswerability, WhyTheSitesAreTwo, WitnessNumber*, SaturationAtACut).** A collision is an off-diagonal pair in the kernel; the kernel is the fibre product of f with itself; a one-way function is one whose fibres are crowded and whose crowding no bounded observer reads. The gap between finding and checking is the same fibre read from the two sides of the equivalence (`VerifyIsDecide`). P ≠ NP is a statement about a *projection*, and it holds there; on the completed (lossless) universal machine the gap is refuted because the fibre is carried, not recomputed. The floor of every hardness argument is answerability: whether the question's fibre over the answer is inhabited. There are exactly two sites at which a witness can sit (the input side or the receipt side) and the witness number counts the crowding at a cut; saturation at a cut is when the crowd is the whole source.

**Execution as transport (TransportResidue, Transport_TheMachineIsItsCompletedSelf, YugapatTransport, CenterRelativeExecutable, HeadDepthMerge, Samyoge, Setu*, Parampara).** A machine's execution is transport along a path in its state family; the completed machine is its own state family with every fibre carried; simultaneous transport (Yugapat) along two paths commutes exactly when the fibres are contractible, and otherwise the two orders differ by the residue, which is what a schedule "chooses". Center-relative execution: the executable is a cocycle relative to the centre of the label group, so anything central is free. Head-depth merge: merging at the head is the only merge with no residue. Samyoge: walls (contractible boundaries) cross fords, defects (non-contractible) do not. The Setu (bridge) modules are the transport across a boundary with a receipt; Parampara is the lineage of a value through its transports, a section of the trace family.

**Reflection, code and behaviour (Reflection, Decategorification, Abhedabheda, TermFreeMonoid, PathIsSymmetry, PerspectiveCore, PerspectiveSymmetry, PolarityClosure, Sambandha, Vikarna).** Behaviour is the set-quotient of code; two programs with one behaviour differ by a fibre the behaviour cannot see. Decategorification is truncation to a set and loses exactly the paths. A term algebra is a free monoid and its identities are the paths; path is symmetry, and every symmetry of a value is a loop in its type. Polarity closure: input and output polarities close under the interaction, and the closure is the fibre of the interface. Vikarna (the diagonal) is the DUP-SUP diagonal read at the residue: a pair that is its own copy.

**The self-improving crystal (SelfImprovingCrystal, GenerativeLoop, ObstructionSubstrate, WitnessPolicy, ProgressDefinition, Siddhanta, Pratyanayana, AnulomaViloma, Anveshana).** Progress is defined as a strict decrease of a Matra on the residue; the generative loop searches for the obstruction (the first crowded fibre), installs a witness policy for it, and the substrate grows by concatenation with no reindexing. The crystal is self-improving because installed identities lower every later count without changing any meaning. Pratyanayana (recovery) is the inverse read at a section; Anuloma/Viloma are the two directions of one path. Siddhanta is the fixed point of the loop: a substrate with no crowded fibre at the queries it is asked.

**Prashna and Sakshin (Prashna, Sakshin, TheFibreIsTheSubject, Nasha, CatuhSamskara, IntegerHullMultiplicity, AtlasResiduals, DisclosureDimension, RnaDhana, the rate/threshold quotients, the relational-frame modules).** A question is a map out with a receipt; a witness is a section; the subject of every question is its fibre. Nasha (destruction) is erasure as the loss of a fibre to the environment. The four saṃskāras are the four operations that preserve the census. The integer hull's multiplicity, atlas residuals and disclosure dimension are all the count of the crowded fibres in three coordinate systems; RnaDhana (debt/credit) stratifies the count by sign; the rate and threshold quotients are the census under a coarser observer; the relational-frame modules read the census through a fixed relation (the automata-theoretic frame).

**Design consequence collected here.** The residue lane is the runtime's accounting written as theorems. Every DUP is a fibre census; same-label is the diagonal (Vikarna), free; different-label is the crowd, paid and carried. Schedules differ by residue only where fibres are crowded, and there the difference is the *value* of the residue, not a cost: strong confluence at the level of meaning, a Matra difference at the level of count that the installed identities drive to zero. The one-way function, the P/NP gap and the collision are the same non-contractible fibre, and the lossless machine (the one the emitter is building) refutes the gap by carrying the fibre instead of searching it. What the runtime must therefore never do: merge two crowded fibres (no dedup, Avirodha), invent a middle verdict, or recompute what was carried.

## Cost lane (80 modules, headers read in full)

The cost lane is where the corpus prices things, and the single most important fact in it is negative: **cost is not a graded monoid and has no shortest-path formulation.** Everything else is a consequence or a control.

**Cost composes as a dependent sum, not as addition (DesaAggregate, Vilopa, ReflectionAggregate, Parampara, Lekha).** Fibres compose by Σ: `fibre (g ∘ f) z ≃ Σ (w : fibre g z). fibre f (fst w)`. Lifted to the census this gives three laws: an empty outer fibre is absorbing (loss upstream of an inexpressible point is invisible, so extending a path can hide cost already paid: monotonicity fails); a contractible outer fibre is transparent (the composite census is the inner census on the nose, the one case where a scalar weight would have been right and is not needed); a crowded outer fibre with empty inner fibres except one contractible entry yields a contractible composite (विकलादेश ∘ अवक्तव्यम् = सकलादेश, two defects annihilate). Vilopa names the mechanism: the annihilation is exactly a failure of choice, `isContr (Σ B P) → ¬ isContr B → ¬ ((b : B) → P b)`; a section is global data no pointwise census records, so no census-to-census composition function can exist in any cost model. ReflectionAggregate: cost composes monotonically exactly on the image, which is why typing the decoder on the image is sound and why a router carries a section or has no route ("the receipt economy's unit is a term and not a check"). Parampara instantiates the Σ-law at a hand-written chain of three and shows the additive ledger predicts four where the truth is three, the missing one being an absence in the middle fibre; `Unit → Bool → Unit` cancelling is the same phenomenon. Lekha: the whole audit trail of a chain is contractible, not just one step, so the receipt rides free at every length (Bennett's garbage tape is the carried witness, and it costs nothing).

**Invertible means free (AvarohaNisedha, Machine, Viparyasa, Gunana, WitnessNumberIsInvariant).** In ℕ a sum is zero only when both summands are, so an element with an inverse admits no nonzero additive cost, and no receiver that makes an arrow invertible can retain a faithful cost: the implementation fibre must stay attached beside the motive. The computer is the groupoid of proofs of transport, not the monoid of irreversible steps; the monoid's missing inverse is exactly the non-empty fibre whose kT ln 2 is paid; "erases nothing ⟺ equivalence ⟺ contractible fibre ⟺ zero heat" is one statement. The kernel's `reverse` makes the derivation category a dagger (transpose is contravariant and grade-preserving) and never a groupoid (the grade detects the unit, so nothing inverts `⊕`). The kernel carries a cup product (horizontal composition of derivations) whose grade is additive and whose interchange defect (left-first vs right-first schedule) is real as data and invisible to meaning by one appeal to set-ness: the Eckmann–Hilton defect, priced. Laghava's `size` is not a function of the denotation; the witness number (least list of points on which no decoder survives) is, because it is a quantification over the decoder space rather than a function out of the presentation.

**Counted cost is exact, not fitted (CountedComposition, CountedDigitsEdge, AcceptanceTest, CostGeometryIndexed, TransportCost, TheTransportOverheadIsProvablyRedundant, TransportDivScale, RjuKutila).** Ticks compose additively across checkpoints; the carry cost of one increment satisfies the exact potential identity `digitSum (suc w) + carries·(b−1) ≡ digitSum w + b`, so total carry work over n increments is pinned to within the factor b/(b−1); the acceptance test (one theorem makes a task compile to a strictly better program, replayably) lands at native cost. Cost is a function of the input, not a scalar per edge; a pointwise speedup is strictly weaker than a uniform one. Transport across `ua e` is literally `e⁻¹ ∘ f ∘ (e × e)` by the library lemma `transportUAop₂`, so n chained transported operations carry n provably redundant round trips: the quadratic overhead of the transported term is derived, never measured, and the value-level identity `retEq` is a path, not a reduction. The chart/home frontier scales as linear vs exponential in word length, so the detour wins past a derived threshold. Strict discrete convexity forces once-rising-always-rising, so a cost-minimal route is the only stationary point.

**Second law, counted (BharaGana, GhataLekha, DvitiyaNiyama, ChhayaRig, Chala, PratiphalaAntara).** Mass is conserved by merging, permuted by reversible maps, multiplied under independence; on powers of two the logarithm is exact and entropy is the exponent; downstream a collapsed distinction never returns and upstream the displacement ledger never forgets. The boolean shadow of counting provenance is a homomorphism with no homomorphic section (idempotence kills it). Reward hacking is a theorem: the proxy-optimal policy earns zero true reward and no estimator repairs it; a potential-shaped return telescopes to the endpoints and so cannot see the route.

**Pareto, orders and fuel (the Pareto/RnaDhana/strata modules, TheAdmissibleOrders, CachePathOrder, TheMeetIsMax).** A vector fitness has no best; every scalarisation adds a decision; the stratum is decidable and the filter exact; the strata cover, are disjoint and are ordered; fuel above the archive's length is inert and below it is a real failure; cost coordinates flip soundly but not faithfully, and faithfully below a per-coordinate cap on the vector claimed better. The admissible orders of a happens-before relation are its principal up-set, so "strength" is the relation read upwards and no edge count measures it. Commuting updates have a permutation-invariant fold. The min-plus meet is max, and the profile cut is a Galois connection.

**Audit discipline (Abhijnana, Bhitti, PraksepaFiber, PravesaFiber, LiftingFiberResidue, TheUnstableGround, TheDeflationaryTestWasAlreadyRun, and the h-level audits).** A receipt and an elision agree on the result and differ only in the fibre, so only a checker tells them apart. A wall (a proved ¬(A ≃ B)) retires a merge forever; ℕ ≄ Bool is the largest one. A projection's fibre is the discarded factor; an injection's fibre is contractible on its image and empty off it, all receipt and all wall. "Nonempty lift" at full generality is excluded middle; the repair is a given enumeration. Instability of a ground can never be exhibited (¬¬Stable A for every A). A rediscovery that typechecks as `refl` against prior art is a fact about the process. Two implications upgrade to an equivalence only when both sides are propositions; `isSet` is redundant beside `Discrete` and not beside pointwise stability.

**Design consequence collected here.** The runtime's cost accounting is fixed by this lane, and it contradicts every conventional cost model: there is no per-edge weight, no additive path length, no Dijkstra. Cost is the census of the fibre along the route, composition is Σ, and two defects can annihilate. Same-label DUP/SUP is the transparent case (contractible outer fibre); cross-label is the crowded case, and its price is the section the runtime must *carry* (the DUP node itself is that section). Every equivalence is free, so every installed identity lowers the count without changing the meaning, and the interaction count is the only honest cost because it counts what was executed and nothing else. The interchange defect between schedules is real as data and dead in meaning, which is the exact statement that the schedule is gauge.

## Kernel lane (kernel-flat 41 + Kernel 23 modules, headers read in full)

This is the corpus's own runtime in miniature: 296 lines in three files (RewriteCertificate: terms over six variable coordinates, zero, suc, add; `Step` including `reverse`; `Derivation` as a walk with no relations imposed; `eval : Tm → Env → ℕ`; `derivation-sound`; `induction-sound`. ControlledGrammar: `NativeOperation` with an open `Control` field and `control-sound : Control t → t ≡ source`; `install : Derivation lhs rhs → NativeOperation`; `advance` with `advance-preserves-branch-count`. GenerativeKernel: branches and one worked seed with two histories, direct 2 steps and detour 4). Everything else in the lane is a theorem *about* those 296 lines, and every theorem is the fibre law read at the kernel.

**The one fact and its four readings (Residue/Sesa, Vyapti, Ankapasa, Asesa/TheWholeDerivationTypeIsOneFibre, Syat).** Every soundness field lands in an identity type of a set, hence in a proposition, hence carries zero bits. So: a derivation carries no semantic information whatsoever, all of it is remainder (śeṣa); no semantic criterion at any h-level selects the short proof (selection must be extra-semantic or it does not exist); the installed operation fires at exactly one locus (the kernel memorises; generalisation needs a substitution witness and is then free); the counting semantics is a decategorification and the bit it drops is a transposition (commutativity of addition is a ℤ/2 of holonomy, visible to `⟦_⟧ : Tm → Type` via `uaβ` and invisible to every set-valued readout); the whole derivation type is one fibre of its own soundness, so both bindings of the fibre law coincide and the kernel sits at the maximally lossy end. Syat sharpens: the blindness is a property of the codomain, and the obvious repair (interpret into a groupoid) fails for an independent reason, because cost sends a round trip to twice its length while every functorial semantics sends `reverse` to `sym`: cost is a functor on the category and not on the groupoid, and the obstruction is inversion, not truncation.

**Meaning descends, cost does not (TransportShreni, VyayaResidue, ForgetfulCompressionPricesTheDrop, PvsNPGapLivesInTheForgetfulProjection, WindingCost*, AnswerIsProjectionAtOutputSize, ClosedAdditionCostIsLinear, MultiplicationUnfoldsInLinearPeels, CongruenceLiftsAreGradePreserving).** The Gabriel–Zisman localisation at the meaning-preserving class is the set-quotient by "same meaning"; meaning factors through it definitionally and cost does not (one `eq/` collapses two points that `len` separates). The kernel of the localisation is generated by the round trips `d ⊕ revD d`, which have trivial meaning and cost 2·len d: `revD` is a dagger, not an inverse. The P vs NP gap lives in this projection: carried, the answer is a field of the executed object and the route is data; forgotten, the routes collapse and the gap is what the projection destroyed. Cost is output-sensitive and exact: the winding to standpoint n costs n+1 = the unary size of the output; closed addition costs b+1; multiplication unfolds in b+1 peels and the total tracks the unary magnitude of the *output*; every congruence lift preserves length, so a derivation built by structural recursion has length equal to the number of redexes it eliminates. The classical exponential lives only in the unary→binary compression.

**Initiality and the elucidator (AdiBija, Vivarana, MulaCakraPariksa, MulyaVinimaya, Vyakhya, KernelDhara, KernelShreni, SthapanaVarga).** The kernel is initial: every receiver is read by exactly one fold, so eval, length and every evaluator integral are the same recursor at different receivers and "seeing all paths" is a theorem. A trace is a chain, an evaluator a cochain, value their pairing; exact evaluators (coboundaries of potentials) telescope and integrate to zero on cycles; the depth evaluator has a nonzero cycle integral (pos 3 on the confluence diamond) and so is a class no potential represents; one basis-cycle test certifies path-independence for every antisymmetric evaluator. Filling the diamond with a 2-cell (an explanation, imposed as a set-quotient) is lossless for every exact evaluator and kills the depth class: the choice of protected observers is the caller's, and quotienting before declaring them is the dangerous act. `install` is coning off an obstruction: its applicability locus is `singl lhs`, contractible, one cell not a class; the fee is the non-descending cost one level up; and the tower's rungs are one predicate ("cost is not exact") re-instantiated at successive degrees. Self-extensions are classified by derivation up to control gauge, and installation is the canonical gauge.

**The kernel as a distributed, interactive, reversible system (Avirodha, Interaction/Samvada, TheEncounterOfTwoPeers, AnEquivalenceIdentifiesTheCarriers, HidingAndHardness, IntrinsicRewrite, TheControlCarriesItsInstanceAndLocus, Adesa, Siddhasadhana, TheGenerativeLoopOnTheKernelsOwnTerms).** `⊕` is strictly associative and unital, `rev` is an inverse only up to meaning, and the gap is the śeṣa. The library is a list, the join is `++`, order-independent and idempotent, total with no failure mode; two nodes cannot disagree about what is true because the type a vote would range over is a proposition; what they differ on is the route, and forks are kept, not resolved. A session's transcript is a derivation, hence a theorem, hence one installable operation (`retire`). Two peers' encounter is one trace wearing five hats (execution, provenance, proof, transport, program) and no order-blind scalar projection of it has a section. An equivalence identifies the carriers and settles nothing about provenance (satya ⊊ artha ⊊ mūla), so a transport must carry its provenance separately. Hiding and hardness are one fibre read twice: perfect simulation costs `isProp` and nothing else, and two witnesses forbid extraction outright. A `Run t` is intrinsically typed so an ill-composed motion cannot be built; substitution and context act on derivations structurally, so one theorem fires at a `Tm × Locus`-indexed class with no new soundness obligation. Installing what you can already reach is a plateau; the learner on the kernel's own terms is `learn = install ∘ normalize`, and it computes.

**The strict hierarchy (Naya, TrtiyaSopana, CaturthaSopana, Vyabhicara, Nirapeksa).** rewrite closure ⊊ induction closure ⊊ citation closure ∋ commutativity ⊊ truth in ℕ, each strictness witnessed by a term (0 + x = x; x + y = y + x), and a second model (a non-commutative, non-left-unital W) decides the underivabilities. One environment where the meanings differ forbids a derivation entirely, so the six coordinates are never identified. Neither unqualified assertion nor unqualified denial survives, and the Jaina and Bauddha fourth positions sit at different levels (dependency of a family on an index vs the propositional structure of negation).

**Design consequence collected here.** This lane is the specification of hvm5/bend3's evaluator written as theorems about a 296-line prototype of it. The runtime must: carry the derivation (the route) as data and never quotient it (no dedup of branches; multiplicity is the only place the collapsed information survives); treat `reverse` as a dagger with cost, not a free inverse; measure cost as interaction count, which is initial-fold length and equals output size; expose `Control` as an open, caller-supplied evidence type whose safety is a subsingleton theorem; make `install` the metacircular step (a proved identity becomes a rule, its locus is contractible, its fee is the cost that does not descend); and keep meaning in a set-valued projection while keeping the categorified semantics `⟦_⟧ : Tm → Type` as the place order information lives. HVM4's labels-as-budget, printer-as-collapse and checker-outside are each a violation of one of these.

## NaturalMachine lane (140 modules, headers read in full)

This lane is the corpus building its own machine inside the one language, and watching what each addition costs. Four threads run through it.

**The internal prover, grown organ by organ (EkaBhasha, EkaTantra, AdeshaPositional, Aroha, SvarthaAnumana, ShrutaMatipurva, ShrutaParampara, AptaMimamsa, ArpitaAnarpita, BhavaIndriya, SvayamBhavendriya, SyatWholedesha, YugapadArpana, SamaDrsti, Rashi, SadharanaVishesha, PramanaNaya, PurnaPramana, RatnaTraya, Upayoga, UtpadaVyayaDhrauvya, Samvara, SatyaMahavrata, Alopa, AnulomaShruta).** The one structural move: a store entry carries its proof as a field (`record नियमः = { lhs ; rhs ; साक्षी : ⊨ (lhs , rhs) }`), so an unproven rule is unconstructible and the gate is the type. The prover is norm-and-compare with its soundness theorem, no Bool on the wire; the matcher returns a substitution with the path, not "matched". Scheduler and prover are one contention structure (an assembly of partial standpoints at a site, a joiner that either decides or holds the residue whole) differing only in the joiner parameter; a critical pair is Kātyāyana's द्वौ प्रसङ्गौ एकस्मिन्. Induction enters through the same typed gate (a completed-standpoint hypothesis `(ρ) → ρ k ≡ n → eval l ρ ≡ eval r ρ` travels into nested descent with no machinery; two-coordinate descent for operators that recurse on both arguments). The record feeds the prover: mati mints a rule, it becomes śruta, mati-with-śruta proves commutativity that mati alone could not; the elder Haskell store crosses as raw text, 92 of 102 re-derived alone, more with the lineage behind. The AC frontier falls to a joiner swap: flatten the ⊕-spine to the sequence-free (anarpita) form, and the store that birthed mirror pairs without bound is saturated by refl; the sum read as a heap with su a unit atom lets the hypothesis speak by sub-multiset surgery; max canonicalises by setting aside the common heap. The eye is grown from the body's own attained theorems (mx-commutativity extracted at point environments), then grown *by* the body with no agent (pattern-match the store for commutativity/associativity/unit, instantiate one canonicaliser proved once over an abstract commutative monoid). Five provers were nayas of one pramāṇa parameterised by eye, record, descent, instrument. The store turns itself: critical pairs are born already proven by composing the parents' witnesses through the unified site; influx already reachable is stopped at the door; the whole cycle (digest, self-turn, digest) is one function with every proof a field, and a false utterance passes through in silence. The compiled mouth cannot lie: its Bool test lifts to a path and its false word to a refutation. Sampling forty random assignments is answering a question normalisation answers exactly, and cannot be fixed by sampling more.

**The kernel's semantics, measured from every side (Anupurvi, Samkhyana, Baddha, Ubhayatah, Visranti, Akanksa, Vyapti, Residue, Asesa, Ankapasa, Avirodha, Interaction, MulyaSankranti, ReturnMulyam, TransportPrice, Laghava, Nirjara, TransportResidue).** Every step conserves the variable word, the successor count and the trapped-successor count (a successor in a left operand whose sibling carries a variable can never reach the front because the calculus has no associativity), and the three stand or fall together; the normal form is the complete invariant, so joinability is decidable and still says nothing about the route. `eval` is the abelianisation of the word plus a constant; the calculus preserves the arrangement and the count discards it, and `add-comm` both completes the calculus toward ℕ and introduces the ℤ/2 of holonomy because it is the generator of exactly that quotient. The omitted left rule `add (suc x) y → suc (add x y)` is free (`refl`, since ℕ recurses on the left) while the kept right rule costs a library lemma: the calculus is right-obstinate in the Kahn–Plotkin/Berry–Curien sense, and the trapped count is the cost of the obstinacy. A sound schema is deterministic by meaning-preservation alone (a moving pattern pins its witness; a rigid one cannot pass the variable on). The total composes degenerately (any two inhabitants of the meaning type are equal) and only the trace manufactures (`len (d ⊕ e) ≡ len d + len e` by refl), the trace is non-rival, and recovery from the total returns *a* route, never *the* route: distillation as a theorem. Every additive transport price is the difference of a potential, so loops are free and "price" is the wrong word: it is a coordinate on presentations, invisible to every denotation. Shedding an inert primitive strictly increases the presentation, so a लाघव-steered engine never takes the step; only तपस् does. Three moves between representations are conceivable and only two exist: transport along `ua` (`uaβ` says nothing dropped), or carry the residual `fiber r b` pointwise; copying the unknown does not exist because `∣_∣₁` has no section. The machine takes the retentive (Jaina) reading of `ua` over the emptying (Madhyamaka) one because a transport layer must hold the residual as an object.

**Observers, charge and gauge (ParitySeparator, ChargeCriterion, ChargeBlindnessIsAnOrthogonalComplement, GaugeOrbitClasses, OracleQueries, OracleSeparation, InterfaceSeparation, ChenProjector, ThreeChannels, CornerProjectors, MixedCornerDescent, SuccessorIsNotTropical, PythagoreanTransition, PMTorus, PMCokernel, Prastara, SaranaCatustaya, PraciSadhana, Partialdesa, Pati, CertificateFibration, FiniteInformation, Descent, Vacuity, SufficientInterfaces, HolonomyIsInvisible, Naya_WhichLens).** The sieve parity barrier stripped of its C*-algebra: a completely multiplicative ±1 function is a sign vector on the primes, the gauge flip is one group element, and an observer restricted to even-Ω queries returns literally equal transcripts on σ and its flip, so no post-processing separates them (`cong`). The criterion is an iff on the query set; blindness is the annihilator of the F₂-span of the exponent-parity vectors read, so the barrier has a dimension and k queries see at most 2^k charges; the transcript fibres are the cosets of the annihilator (the class theorem); squares are neutral for every gauge element, so size buys no charge and there is no partial charge. The functional equation carries zero charge (it is the monoid law the character respects, and its closure under multiplication and division conserves neutrality); the specialised instance λ(pn) = −λ(n) is one odd-Ω value query wearing the equation as clothing. On the Chen envelope parity is the charge-one projector and is informationally complete; the primitive projector μ² − π₁ distinguishes p² from pq, which parity cannot. Radius and charge projectors commute and the marginals still say nothing about the joint. The successor has no locality in the multiplicative chart (consecutive integers share no coordinate), while on Brahmagupta's conic at D = −1 translation by a unit is multiplication by a constant and Euclid's parametrisation is squaring: the barrier belongs to the line, not to arithmetic. Peres–Mermin's obstruction is a nonzero class in coker δ ≅ F₂ read by the total-parity functional, with a local section in every context and no global one. A gauge stream costs zero carried bits; a general perturbation flips the bit and is incompressible; the price is the image of the disturbance, not its cardinality (Piṅgala over Ashby). An aggregate readout is phase-blind and the labelled one is not (Ashby over Darwin, on the machine's own flat variant race). One shadow underdetermines and the orbit quotient coincides with the present exactly on a congruence. A domination verdict is a function of the declared family, not of the object; homometric caches are an artefact of the declared family. The certificate is the fibre coordinate: `fiber ⟨f,c⟩ (y,e) ≅ fiber (c ↾ fiber f y) e` by Σ-eta, so the lower bound is a theorem and the attainment is a trivialisation, a datum. `f` factors through `q` iff constant on `q`'s fibres, uniquely when the target is a set. A claim vacuous at every apart-pair is one whose invariant descends. Two individually sufficient interfaces can have an insufficient join (the finite gluing obstruction), and lossy context compression is safe only when every collapsed state shares a valid next action.

**Negation, standpoints and the constructive structure of absence (Abhava, AnyonyaAbhava, Anekanta, Anyapoha, Yugapat, OrderAstiNasti, NonUniquenessAndInexpressibility).** A bare absence is not a proposition; it carries its counterpositive and its delimitor. The negation tower stabilises at three, not two, and collapses to two exactly when the counterpositive is decidable, so the level at which it stabilises measures decidability. Mutual absence and relational absence are interderivable classically and not constructively, by one step of that tower. Affirmation and denial from different standpoints are simultaneously inhabited with no ⊥; the fourth bhaṅga is the theorem that no single standpoint carries both; collapsing the index is sound exactly when every standpoint agrees and impossible when two disagree (the structure identity principle and anekāntavāda are one rule). The exclusion set carries the term only when a decision is supplied; asserting apoha-completeness for every equivalence relation is DNE. The refusal of a joint assertion does not decompose into a pair of refusals without weak excluded middle; the fourth corner at one instance is exactly a failure of the double-negation shift. Inexpressibility and non-uniqueness are independent defects.

**Design consequence collected here.** The lane's engineering conclusions transfer directly. (1) Rules carry their proofs as fields, so the runtime's rule store needs no gate: the type is the gate, and the checker is not outside the machine but the constructor of its rules. (2) The prover, the scheduler and the completion are one contention structure; the joiner is a parameter (syntactic equality, normalisation, AC-normalisation), and a mirror pair under a coarser joiner is not new information. (3) Cost is a coordinate on presentations, never a function of meaning, and loops are free; the runtime must count interactions on the presentation and never try to price a value. (4) A set-valued readout collapses every quantity a policy needs; branches are kept at full multiplicity and the categorified semantics is where order lives. (5) Blindness is a subspace, with a dimension, determined by what is read, never by how much is computed on it: an observer of the runtime (a printer, a profiler, a checker) sees exactly the coset of its own annihilator. (6) The left/right asymmetry of a rewrite rule is a sequentiality index with a counted cost; a runtime that can examine an operator from either side removes that cost, and the interaction net's symmetric rules are exactly that removal.

## Logic lane (121 modules, headers read in full)

The logic lane is where the corpus's epistemology is proved, and it is not decoration: every one of its theorems is a statement about what a *readout* can and cannot hold, and the runtime is nothing but readouts.

**Univalence is anekāntavāda; the fibre law is the isomorphism theorem (Anekantatva, SarvavibhagaH, SamgrahaNaya, Anubandha, PramanaLaksanam, DravyaParyaya, NisvabhavaNet, TwoTruthsCompute, EquivalenceHasNoFloor, PramanaTransport).** The space of presentations of a type is contractible (`EquivContr`): many nayas, each a true view, none privileged, all one vastu. Every map is the sum of its fibres over its codomain, `A ≃ Σ b. fiber f b`: the image is the naya's quotient, the fibre is what it forgot, and this is the first isomorphism theorem, rank–nullity and the dravya/paryāya split at once. The collective standpoint is the set-quotient's universal property; a Galois connection is two nayas mutually adjoint and its closure is what persists; identity is agreement under every standpoint (Leibniz's law as pramāṇa). A mode is a path, the substance is the type it lives in, and persistence is exactly what makes transport possible; across distinct substances there is no free road. `uaβ` is the ultimate reached only through the conventional (MMK 24.10 as a reduction). An equivalence has no identity beyond its action (equivEq, isPropIsEquiv, funExt): the floor is the path, not a thing. A proved equivalence is a receipt: it composes into edges neither party held, is non-rival, owes no counterparty, and crosses back for free; money is a receipt that lost its fibre.

**Standpoints and their blindness, measured (Naya, Paryayarthika, Durnaya ×3, NayaAvarana, OrderSaha ×3, Paryaya, GuhyaNasti, SetBaseNoMonodromy, KulaParasparasraya, KulaStambha, Parasparasraya, SetDrstih, SamacaranaNityam).** Every set-valued observable of the carrier annihilates every loop (`cong F p ≡ refl`), and the loop is still there; the blindness is a property of non-dependent post-composition, not of the h-level of the answer, since the holonomy `transport p : Bool → Bool` is set-valued and sees the loop (a Wilson loop sees Aharonov–Bohm). The dravya census `∥X∥₂` and the paryāya census `∥ΩX∥₂` each conflate a pair the other separates, so only both together see; the two standpoints do not commute and their commutator is exactly ℤ (`∥ΩS¹∥₂ ≃ ℤ` against `Ω∥S¹∥₂` contractible): krama chooses an order and pays or destroys the charge, saha holds the untruncated object, and that is what avaktavyam has meant all along. The pointwise census (empty/contractible/crowded) is exhaustive only for set fibres; over S¹ → Unit the loss hides in the loops and is exactly ℤ, so the sevenfold restratifies by dimension. Over a set base there is no monodromy at all, and the double cover of the circle is the witness that the definition has content. The corpus's fourteen `A ≃ A` loops number eleven charged, one identity, two parameter-dependent; at the hub ℕ the checked automorphism subgroup is ℤ/2. Interdependent families: every single blindness is jointly seen, no member alone is faithful, at arity three every proper subfamily is blind, and joint faithfulness costs a reconstruction (the section again, from the other side). A transitive symmetry flattens the verdict, so an unequal split certifies that no symmetry acts. The collapse of a standpoint-indexed proposition is available iff every pair of fibres is equivalent, the prohibition has content only off the propositional world (Unit and Bool agree in every proposition and cannot be identified), and agreement does not determine the transport (Bool agrees with Bool two inequivalent ways), so the price of a transport is the h-level of its space.

**Absence, its slots, and the constructive tower (Abhava, AbhavaAvacchedaka, TheAnuyogitaAvacchedaka, Abhava_MamaAdarsanam, Anupalabdhi ×2, FitnessIsNecessaryUpToDoubleNegation, DeflationaryTest, TheDomainThatIsAnAbsence, AnyonyaAbhava, Apoha, Anyapoha, TheDelimitorNeedsOnlyStability, Vaidharmya).** A bare absence is not a proposition: it carries its counterpositive, its locus and two limitors, and changing a limitor changes the absence. My not-seeing is not its absence: absence is a Π over the whole field, and "I searched and did not find" has no internal statement; the fitness condition (it would have been seen) is the second hypothesis and it is necessary up to double negation. The negation tower is two-tall for every absence and decidability governs the counterpositive's recoverability, not the absence; stability is closed under ¬, →, ×, Π and not under ⊎, so every either/or in the corpus draws on a decidable source. Witnessed exclusion and negated indistinguishability coincide at every finite level and differ at the horizon by exactly Markov's principle. The general obstruction theorem was never classical: replace Bool by any answer type with an irreflexive apartness and the negative half survives verbatim; separation costs a point.

**The sevenfold, made exact (Saptabhangi, Bhanga, Yugapat, OrderSaha_TheFourthCorner, OrderAstiNasti, SamayikaAndNityaAreIndependent, NonUniquenessAndInexpressibility, Khahara, Satyayantra, AmshaSatyayantra, Setu, Sthairya, Lekha, Fiberjala, GananaSaptabhangi, PurnaSaptabhangi, Kosthanyaya, Kosthabhitti, BhittiSaptabhangi, TrayoNirnaya, EkatvaNirnaya, Vacuity).** Sequential both-ness is not simultaneous both-ness (स्यात्-अस्ति-नास्ति ≢ स्यात्-अवक्तव्यम्), so a two-valued verdict is a durnaya by pigeonhole, and the pigeonhole is unconditional while the loss needs three distinct points; the positions over two atoms form a three-step chain whose first gap is an example and whose second is a constructive taboo (WLEM); the fourth corner at one instance is exactly a failure of DNS. Inexpressibility (avaktavya) and non-uniqueness (0÷0) are independent defects, and Bhāskara's khahara (n÷0) is a third. The honest machine has three laws (sound, stable, complete) and a genuine third position, the un-said, which is neither falsity nor ⊥; the partial honest machine drops completeness for an un-said that is permanent (ananta); the honest machine's two positions {asti, avaktavya} are distinct bhaṅgas and it never asserts nāsti. The append-only ledger's fold absorbs refutation and a Boolean cannot carry it (refuted claims would come back to life). The fibre census is the codomain of every "is this determined?" question, its three verdicts are exactly the three seeds, and the map-level classification is the sevenfold itself (2³−1 = 7), all seven positions now witnessed. A wall (¬(A ≃ B)) transports across every ford and retires a whole component pair; a defect (a property of a map) does not, and the difference is the term/type distinction. The two vows (decide on unanimity, take nothing on disagreement) determine the decision rule uniquely. A vacuity verdict carries its witness, and "carries no information" and "descends" are one statement.

**Wolfram, held to terms (NKSUnivalence, NKSRuliology, ForStephenWolfram).** Coordinatisation is a path of structured systems whose transport computes; the merge is a truncation with no section; the bounded observer separates no co-terminal runs; divergence reconverges definitionally; the advantage of proof is the quantifier (`(n : ℕ) → Evolve zero n` by one induction, where a run is one point of the family); the merged state is one eme (the set quotient contracts to a point with no property but distinctness, and still has no section); purpose ranks nothing inside the ensemble it selects. One term inhabits the conjunction of the seven sentences, and checking the file is reading the letter.

**Design consequence collected here.** The runtime's printer, profiler and type checker are standpoints. A set-valued readout of the net (a normal form, a count) annihilates every loop, and the loops are where the labels live; the only observable that sees them is transport along the path (the holonomy), which is what a DUP/SUP interaction *is*. Two readouts that do not commute cannot be uttered as one map, and the runtime must hold the untruncated object rather than pick an order. A rule store, a ledger and a merge must never carry a Boolean where the question is three-valued. Absence claims (this redex does not exist, this branch is dead) are Π over the whole field and must be produced as such, not read off a failed search.

## Automata lane (68 modules, headers read in full)

**Myhill–Nerode is the one central construction (MyhillNerodeMinimalMachine, NerodeMachine, SensorNerode, Cakra, ObservabilityQuotient, ExtremalDescription, DynamicDescent, ExcursionReturn, CompositionalContextAdapter, ContextCloneEquivalence, AdaptiveResidualAdapter, ObservableHorizon, SingletonAction*, Productive*, TranscriptComposition, TheReachableLaw).** Two states are future-equivalent when every finite action word yields the same observation; the quotient by that relation is the minimal fully abstract machine, effective (a path of meanings is future equality), terminal among behaviour-preserving quotients, and the greatest behavioural congruence. When the next observation factors through the current (`out ∘ δ = g ∘ out`), the whole Nerode relation collapses to the kernel of one observation: factoring kills separation across time as it does across instruments. A sensor family sees a pair only through its lcm, the relation determines the lcm, and the minimal acceptor is the cyclic group of order the lcm (the residue bridge holds at every modulus once `res` replaces the library's `mod`). The safe quotient is N_obs = ⋂ ker(P Tⁿ), strictly finer than ker P (three states, two instantaneously equal, separated next step); the greatest safe relation is unique, so the invariance constant is 0, not "small". The compression defect `K_t K_s − K_{t+s} = −P T_t Q T_s i` is *leave the sector, evolve outside, return*; at the smallest size it is the single number bc, so closure holds iff there is not both a channel in and a channel back; pure leakage is free and only return is memory. Contextual closure is load-bearing (equal observations need not survive one context). The stagewise transcript test is sound for all transcripts exactly when the second stage is injective. The reachable migration law loses a free composition and needs a preservation component.

**Where cost and information actually sit (AnswerIsProjection-style modules here: InvarianceConstant, AssemblyGrading, Samagri, SamaCount, ParivartaYoga, PairwiseCommutationGivesEveryOrder, Shell, Stream, GeneratedCapability, Kausalya, Nirmana, MatchingPenniesSeparator, AdhikaraKara, AddictionChainPredictiveMemory, ActionResidual*).** Kolmogorov's invariance theorem as a term: a complexity function is a class, absolute description length is not a statement about x, and a strict comparison transfers exactly when the gap exceeds 2c (sharp). Āryabhaṭa's vallī lives entirely in the sign-graded part of a three-character free monoid and can produce neither the content nor the primitive index of the Smith assembly: Riemann's lens is blind to `pulv` and Nārāyaṇa's sees only it. Reassociation moves preserve every additive weight, so the axiom multiset is a proof invariant, and cancellation cannot be added (a detour's receipt is consumed twice). The corpus's `_~_` is same-multiset on a discrete step type. The double sum interchanges spending exactly assoc and comm. Pairwise commutation of steps gives agreement of every order, compressed and uncompressed, and disagreement under permutation is off the image; a working tree with line-located patches has commutation as a theorem from distinctness, and blame explains the tree. Under at-least-once delivery with arbitrary duplication and reordering the consumer's state depends only on the set delivered, so exactly-once is algebra and the dedup store disappears; below the watermark the state is final; the store is monotone. A generated capability changes executable future behaviour: same answers, strictly smaller counted cost, forever. A memorising skill fires at exactly one state, so a library's coverage grows by at most one state per skill and no finite library covers ℕ. Pruning by observational equivalence is complete, the collapsed class contains ℕ, and any choice inside it is extra-semantic. Social welfare and the pure-Nash predicate each have one fibre, so best-response structure descends through neither: von Neumann's oldest example and the verifier-blind-reward theorem are one theorem. Robust actions (valid in every hidden state of the fibre) grow with perception. The endpoint cannot predict the cache bit, endpoint plus bit predicts the whole table, and garbage collection makes the table constant.

**Barriers as two-witness problems (BarrierIsTwoWitnesses, Asanna, ChidraDosa, Ekasutra, Lawvere, EndObstruction, QuestionMachine, FutureSeparation, TheDomainThatIsAnAbsence).** No single configuration establishes a barrier (the constant post-processing answers it); a pair is the entire content and the witness number is exactly 2. Near is not equal: the exact obstruction law needs equality of transcripts for `cong`, an analytic barrier delivers agreement within ε, and near-blindness with arbitrary post-processing implies nothing; the lemma must supply either exact agreement or a modulus on Φ, and the latter contradicts "Φ arbitrary". Pointwise invariance with no coherent descent is a term (the double cover of S¹, fibre-constant halving that fails to descend by parity), so `isSet T` in the descent theorem is the exact price of monodromy and set-truncation is the repair. A section of the mapping torus is a fixed point of the monodromy, so Lawvere gives sections and a free monodromy refutes every point-surjection; Cantor is `e = not`, the Möbius band. The end is never among the things the machine can say about the end, and halting does not close.

**Rule 30, decided exactly (Apunaravrtti, Sarvapada, Navapada, Ganana, Sankirnata, Jen, Karna).** Every rational's binary column repeats within its denominator by pigeonhole on remainders; every nine-bit word occurs in the first 4096 bits of the middle column, so by Morse–Hedlund no period and preperiod sum below 512 and no denominator up to 256 is that column; the complexity function is 2ⁿ through n = 9; Jen's theorem (no two adjacent columns both eventually periodic) is proved by left-permutivity carrying periodicity into the light cone; every right diagonal is purely periodic with period dividing 2ᵏ and the centre column is the diagonal of the diagonals, which is the exact shape of the absence of a closed recurrence.

**Peres–Mermin, counted (SandarbhaGanana, NiyantranaGanana).** Over all 512 assignments the satisfied-context counts are 0/96/0/320/0/96/0: even counts are forbidden (a parity superselection rule from each observable lying in two contexts), and the whole landscape is 16·C(6,v) over the coset the obstruction selects, so contextuality degree = coset leader weight and the obstruction displaces by one stratum, never destroys.

**Design consequence collected here.** The runtime's semantics is a Nerode quotient by future observation, and the only safe compression is by distinctions invisible forever, which is a congruence for the step; quotienting by what the printer sees now is unsound (return is memory). Exactly-once, order-independence and blame are algebra given distinct locations, which is what interaction-net locality supplies. Cost comparisons across presentations need a margin of 2c and are otherwise meaningless. Every barrier claim is a pair of configurations with equal transcripts, and the runtime's own confluence proofs are that shape: two schedules, one transcript.

## Grammar lane (55 modules, headers read in full)

Pāṇini's devices are read as information-retention and cost devices, and the reading is checked.

**The tower of presentations and where cost lives (TheTower, Laghava ×3, Anuvrtti ×2, Pratyahara ×2, TheSecondNa, Antya, Sivasutra, Apavada, ElsewhereCondition, Panini, Nirjara in NaturalMachine).** Five levels, each strictly finer, each separation a term: cardinality < denotation < rule set < ordered text < alphabet order. Univalence sits at level one and is blind to the rest by construction, which is why लाघव had to be invented separately. `size` is not a function of the denotation (two presentations, one meaning, different sizes); the master theorem is that a cost and an inverse cannot coexist: a merely graded structure (a Matra) cannot be inverted, and an invertible structure has no cost (so no nontrivial group is graded and transport has no price; there is no cost function on the transports of Bool). Anuvṛtti cost is not a function of the rule set (reorder the same rules and each has to state something different), so the Aṣṭādhyāyī is a text in an order and an unordered grammar has lost something measurable. A pratyāhāra names an interval of the alphabet order in two symbols; not every family of sets can be made intervals at once (over three letters the three pairs cannot), so repetition is forced, and Pāṇini recites ह twice; the repetition buys totality at the cost of locality (अण् is two sets, computed from the śivasūtras), and one anubandha carries every fresh-start suffix of its stretch so a ⊆-chain costs one marker (Dilworth: chain cover = width). The four devices split by type: अपवाद and प्रत्याहार local, अनुवृत्ति and असिद्धत्व non-local; the non-local ones must be shown necessary or they are idle. Brevity does not pick a presentation but a level set (`plus var (lit 1)` and `plus (lit 1) var`), so the metarules are structurally required; any measure blind to a symmetry of the denotation attains its minima non-uniquely. The Elsewhere Condition decides uniquely when it decides, exists on chains and on directed families (strictly weaker than laminar), and fails irreparably at a proper crossing where no equivariant tiebreak exists: 1.4.2 is logically independent of apavāda on either reading of the dispute.

**Asiddhatva is the deliberate construction of a mutual absence (AsiddhatvaBreaksFactoring, AnuvrttiIsTheSameTrade, PratyaharaBuysTotalityWithLocality).** A siddha rule's output factors through the current form; an asiddha rule's provably does not, by the collision lemma: 8.2.1 buys back a distinction 8.2.30 destroyed and pays with the statelessness of the later rules. Anuvṛtti pays non-locality to shorten; asiddhatva pays non-locality to recover; neither is an ordering convention.

**Verdicts, absences and the third position (Nirnaya, TrayoNirnayah, Nasti, TritiyaMarga, Apratikaryatva, Vacuity in logic, Setu in logic).** The verdict type has no place for a bare label: सिद्ध carries its derivation, खण्डित the assignment, अविरुद्ध the domain searched, अवक्तव्यम् both standpoints whole. Survival under retraction has three verdicts because a partial class enumeration cannot decide, and any Boolean computed from the seen prefix destroys a real distinction. The retraction type at truncation level n IS the h-level hypothesis (an equivalence of types), so "no way back" is equivalent to ⊥ at Bool, and "every map is an equivalence or has a nameable defect" is exactly excluded middle; writing the defect (naming its site) costs at least Markov's principle, and LEM yields only the truncated existence, so दोषो लिख्यते is an imperative.

**Certificates and the machine's own seams (ACertifiedRewriteComposes, MigrationNeedsALaw, TheLawBelongsInTheRecord, CertifiedRewritesFormASemicategory, ProvenanceIsCarried, Alopa, Alopasetu, MachineFiber, MachineLoop, SelfImprovement, WallCertificate, SeamClosed in logic, ResiduePurti, ClosureTowerCollapse, Vyapti, UpakaranaVrddhi, ApurvaIndriyam, EkaVidhih, EkaSutra).** A certified rewrite has five components (semantics path, strict cost improvement, migration, the law that migration preserves observation, provenance); three compose for free, cost needs transitivity of strict Pareto domination and an h-level (it is the only component with proof content to disagree about), the law is a real constraint independent of the other four, and provenance is vacuously free (it may be replaced by the empty list). Certified rewrites form a semicategory, not a category, because strict improvement removes identities. The engine never touches the meaning and its invariance is the orbit law instantiated; sampling forty environments cannot be fixed by sampling more because samples are not on the axis; the engine lives in the बहु fibre of its denotation, and inventing a symbol is the only move at an empty fibre. Genotype is not phenotype: no store edit computes a genotype from scores, fitness transports only with an invariance witness, and a gamed evaluator is quarantinable. The wall certificate: congruence implies no term separates (a mutual structural induction over the signature, needing no reflexivity of the relation), the relation "no term distinguishes" IS the partition, a non-congruence port immediately forms the hidden coordinate, and the world splits as visible × hidden with the hidden a torsor under the annihilator. The flagship residual `x ≡ x + 0·x` was proved by the machine inside every replayed module and discarded. The transfinite closure tower is constant from stage one, and the impredicative form does not typecheck. A derived sense adds no separation as an equality of types by univalence; an interdependent pair is never source-and-dashboard; the least congruence containing a pair is built (the closure under the rule) and is the minimal separating query. The one law is descent; the free direction binds b and the costly one needs a section. J, the graph decomposition and the fundamental theorem of identity types are instances of singleton contraction, so encode–decode authors retire into instantiation.

**Design consequence collected here.** The runtime's rule text is at level four of the tower and its cost lives there; nothing at the level of meaning (normal forms, types) can see the interaction count, and the checker must not try to. Rewrite rules are certified records with a migration law, not bare functions. A rule that reads the pre-collapse form (asiddha) is a memory device with a stated price, and the runtime's DUP nodes are exactly that: they retain the distinction a projection destroyed. Order among rules is information; a rule set handed over unordered has lost it. Verdicts are three-valued and carry their witnesses.

## Primes, metre, walks, order, lattices, homotopy lanes (50 + 41 + 57 + 44 + 46 + 40 modules, headers read in full)

Six smaller lanes. Each reads the fibre law at one classical object and pays for the reading in exact finite identities.

**Primes.** The open constellation (Goldbach, twin primes, RH via Davis–Matiyasevich–Robinson, Collatz) enters as types over one computable toolkit; every fibre is decided by a sound and complete Boolean, so each conjecture is exactly "this computable Boolean is true at every stage" and the only openness is the section. The typechecker inhabits the first RH fibres by running δ and the harmonic sum; the reach is bounded by unary arithmetic, not by the question. Goldbach and twin primes are the centre and radius marginals of one pair kernel 𝒦(w,r) = a(w−r)·a(w+r); the sequence of ordered pair counts at every N determines the sequence (odd N load-bearing) and so the Goldbach counts are the prime indicator; the midpoint and sum forms are one question. Two primes above the threshold make an unseen composite, so the square root is forced and no finite divisibility observer decides primality (growth of the sufficient observer with task scale), and centre/radius is Vieta. The Mertens gate |M(k)|² ≤ k is the disproved conjecture, not the hypothesis, and a walk returning true carries no information because the upādhi is known to exist beyond reach. The DMR walk volume is a lossless encoding of the local prime-power field by a multiplicative second difference over ℕ, and any running product is log-convex exactly where its step grows. The kuṭṭaka terminates without fuel. Analytic identities of the receiver (Hardy/Bergman overlaps, Weil matrix positivity, damping ladders as Weyl pairs, Abel normal forms, two-term residuals of truncated inverses, the reflection cycle as a multiplicative holonomy trivial exactly on the fixed pair) are reduced to commutative-ring identities; the only place an ordered structure enters is positivity of a scale factor. A Bool carries no warrant and a Dec does, so the Goldbach sweep is rebuilt to return the 49 certified decompositions themselves.

**Metre.** Piṅgala's 2ⁿ, meru-prastāra (Pascal), Virahāṅka's mātrāmeru (Fibonacci, five centuries before Leonardo), Nārāyaṇa's samāsa-bhāvanā over arbitrary part-sets, Halāyudha's diagonals: all proved from recurrences alone with no factorials, and the counts are the *fibres* of the counting maps (the fibre of `length` over suc n is X × the fibre over n, and that is why the count doubles). Piṅgala's śūnya-dvi method is binary exponentiation two millennia early, and it computes the naive power. The Fibonacci anyon fusion dimension is Virahāṅka's metre count. The symmetric two-outcome Born weight is forced to ½ exactly over a uniquely halving carrier (characteristic 0 is where the interior opens; Gleason is the wall), and transitive symmetry plus normalisation plus unique division forces the uniform measure with the group laws never consumed. The discrete log is the fibre of Piṅgala's power and Shor's period query is the separating query that reads it: the public value is `singl`, contractible and free; the secret is the fibre, a whole coset, and that asymmetry is all of public-key cryptography read at one theorem. Two circuits of the corpus's own ford graph are pure gauge because their closing edge is defined as the composite of the other two; charge lives only where edges are independent constructions.

**Walks.** Descent is the ability to come back down; among the walk's three laws (join over ℕ, ⊕ over ℕ, ⊞ over ℤ) only the group forgets: the join fails by idempotence, ⊕ by positivity (ℕ is a cone), and descent costs the integers, which is the same price the conic charges (the cakravāla's descent is scaling to the rational points, not inversion; the invariant is the norm modulo squares). Along its own trajectory the walk's join is never a join but an absorption: the machine pays for a lattice of dimension π(k) to move along a chain. The join is a compression of the sum whose ratio is exactly the meet (lcm·gcd = ab is max + min = x + y tropically). Every divisor of the capacity is the lcm of a sensor family below the frontier, so the reachable states are the divisor lattice. Set-level descent for the identity is exactly `isSet A` as an equivalence of types, and the C₃ apparatus was never the boundary; descent along a surjection of sets is representable and surjectivity is equivalent to effective descent; the set hypothesis is necessary (the loop of S¹ is the witness). The action type does not descend and the blindness has a depth (Unit/⊥, Bool/Unit, S¹/Unit), and for every dimension the last silent stratum descends while the next does not. The absence tower is three tall unconditionally, collapses to two exactly on stability, and the middle collapse is impossible; the deflationary test is vacuous because every theorem and every negation is stable and stability separates no types. Merging a separated pair breaks at the separating continuation, and which state is wrong is not determined (¬(A × B), not ¬A ⊎ ¬B). The Fubini identity on the Type carrier is an equivalence of types (residuals compose), and every finite Fubini is its shadow. The `next 8` blow-up in the walk was the conversion checker elaborating one term twice, and one `let` is the whole fix.

**Order.** Certification as an append-only chain is Burley's positio: order-independence, satisfiability and actuality cannot all hold (the trilemma is tight, every pair realised), so a manifest must be prefix-blind and needs a separate global consistency check. Admissible modes over a meet-semilattice are exactly the unary ACUI-polynomials plus thresholds, thresholds are a proper subfamily at four elements, the vocabulary must be a generating family under pointwise meet, and generation needs distributivity (M3 and N5 are the boundaries). Adjunction and unit/counit are interderivable over a preorder and not over a bare transitive relation. The thresholds form a chain, both claim families are antitone along it, and the strict and non-strict families differ only at the boundary population; `Rate` carries a strict total order whose trichotomy is an honest sum. The house-robber DP is optimal (attained and unbeatable) as a checked term; the join is the least upper bound. The root/weight lattice index of A₁ is the corpus's parity obstruction. Two boundaries negotiate to a strictly finer meet exactly when each has a named refusal the other admits (the saṃvara-dual of the interdependent pair). Homometric pairs of diameter ≤ 10 do not exist (a 1024×1024 kernel sweep) and the diameter-11 pair is exactly the classical one.

**Lattices.** The Smith normalisation events of a 2×2 matrix form a Γ₀(q)-torsor with every clause a program: partner from witness, witness from partner, uniqueness (the partner type is contractible), freeness, transitivity; unimodularity is a consequence of the stabilisation equation and the determinant hypothesis is a theorem off the degenerate stratum; the witness composes as a crossed homomorphism, not additively. Repairs of a defect form a torsor under the automorphisms of the repaired object, so a repair is canonical iff that group is trivial, and an initial object has trivial automorphisms (Proposition 9). A port is the same constraint at a bigger point (the product action), a redundant port certifies nothing, and a port into the regular action trivialises every stabiliser. Any loss embeds a bit into a fibre, so two is the smallest veil and the first obstruction any observation meets is binary. The Peres–Mermin sign vector is derived from the Weyl/symplectic Pauli algebra by `refl` (the 2-cocycle identity is distributivity of ∧ over ⊕, sixteen cases per qubit, not 4096), the refinements of a fixed alternating form are a torsor under Hom(V, F₂), and the naive dependent family over contexts has a global section (the obstruction needs an incidence base). The interface hierarchy r_e < d_e < raw is strict (2 < 3 < 4) on a four-row cut matrix; a fooling pair forces two rectangles for arbitrary row and column types.

**Homotopy.** H²(ℤ/m; A) ≅ A/mA by the carry cocycle, constructively, so carrying cannot be removed by any choice of digit set: a carry-free positional system would be a group-theoretic splitting, and the exponent obstruction (splitting cannot raise the exponent) kills it with no cohomology. The fibre law IS the object classifier (the corpus's decomposition and HoTT 4.8.2 are the same equivalence by `equivEq refl`), the universal fibration's fibre over X is X, every family is a pullback of it, transport is a connection whose holonomy at X is exactly Aut X (univalence read as a statement about one fibration), and the classifier does not classify itself: the universe is blind to its own total space, and the tower is forced. The residue structure exhausts the symmetries over the base: every endomap over the base is an assembled family of fibre endomaps, so the level above is made of the fibres. Orbit descent and coinvariants on raw HIT quotients need no closure lemma. The arithmetic circle over ℤ is four-periodic where the topological circle's π₁ is ℤ, and the gap is exactly the passage to ratios. The truncation error of a geometric series is exactly rⁿ at every finite stage. The stagewise defect determines the composite exactly over two-valued codomains, where the decoder is xor. Two of the three sevenfolds are one type and the third is a different question (its constructors carry witnesses). A refuted round trip refutes the pair and says nothing about the types.

**Design consequence collected here.** The runtime's memory (descent) requires a group where the walk has only a cone or a join, and the price is admitting inverses: reversible computation is the group completion, and interaction nets with `reverse` as a constructor pay it. Every carry is a nonzero cohomology class, so a positional numeral representation inside the runtime will carry, and the carry is where the work is. The Smith-style normal form of a value is unique only up to a torsor of the stabiliser, so "canonical" means a chosen section, and a printer that prints one representative has chosen. Two is the smallest fibre, so the first thing any observer of the net loses is one bit, and that bit is a label. The universe classifies every family but not itself, so the runtime's type of types is a tower and `Type : Type` is the mistake the corpus never makes.

## Unplaced, Mula, must_fail, fibre lanes (122 + 16 + 11 + 27 modules)

**Unplaced: the descent schema, the frontier as one proposition, and the fences.** `SamanaDescent` is the single most compressing module in the lane: a `DescentObstruction` is a type of configurations with an ℕ-valued measure and a step to a strictly smaller configuration, `emptied : DStep → ¬ Config` is proved once from `no-infinite-descent` (RenormalizedObserverTower: a tower of finite-rank kernels is automatically Mittag-Leffler, so the Clay difficulty is not a lim¹ class and lives in the renormalised transport along the tower), and RH, Navier-Stokes and Fermat's cube are three fields of one record `OneDescent` differing only in whether the step is arithmetic (ℤ[ω] cube-split, closes, `Descent_TheCubeSurfaceIsEmpty…`) or analytic (NS scale-critical depletion `excludeII`, RH boundedness `bo`, both unsupplied). The positivity that makes each measure fall is one spine: Weil sum-of-squares, enstrophy, the Eisenstein norm x²−xy+y². `RHReducesToBoundedness` / `ScaleTransportCriticality` / `ScaleTransportZ` / `ReceiverExponentFaithful` chain the RH face: functional equation sends exponent to its negation, positive exponent unbounds the orbit (Archimedean), receiver is a constant offset that changes amplitudes and not the growth exponent, so bounded received signal on every mode forces every exponent to 0; the one mountain D is "every orbit bounded from the arithmetic side", exactly Θ ≤ ½. `WeilPositivityRealization` puts RH into a type: PSD of the size-t Weil Gram form for every t, with entries reconstructed losslessly and triangularly from Goldbach pair-counts R(N) = Σ_{a+b=N} Λ(a)Λ(b) and no analytic continuation. `NSReducesToDepletion` and `GeometricBudgetZeno` do the NS face: finite dissipation budget Σ 2^{−j} < 2 is NOT an exclusion (suc (Σ_{i<n} 2^i) ≡ 2^n), so excludeII must be genuine depletion; `YugmaVyaya`: two budgets with a fixed difference diverge together, so no single sector carries the blowup (needs `m + m ≤ n + n → m ≤ n`, which the library lacks). `Sima` then applies the corpus to itself: with the received conditional theorems as hypotheses, all six RH routes' unknowns (Bounded, OneSided, Lower, Lift, Dyadic, Goldbach) are propositions equivalent to RH, hence by univalence ONE point of hProp; resolving any resolves all, and a strategy for one reading transports to every other by `subst`. The frontier is one proposition with six readings, not six problems.

Three fences, each catching a shortcut before it is taken. `VyarthaCakra`: for an involution θ and ANY feature F, `defect F z = F(θ z) − F z` satisfies its cocycle identity automatically (it is a coboundary by construction), so "the class vanishes" separates nothing; descent is pointwise vanishing and nothing weaker; a positively weighted sum of squares vanishes iff every entry does, so one number can stand for the family. `Samamsa`: the quadratic diagonal `b x x` determines the symmetrisation `b x y + b y x` (no division by two needed, so over ℕ-graded rings too) and NOT the frozen action `b x₀ −`, which can be shifted by any alternating map; the polarisation is intrinsic, the frozen sector is not, so two sectors that each reproduce the field on their own source still differ off it and only their sum is the variation. `TryaSresthaDosa`: the triadic production defect `F x·(y−z) + F y·(z−x) + F z·(x−y)` is a collinearity determinant, so affine weights (energy, signed helicity) are exactly its kernel and the magnitude weight |σ| is not, with the obstruction 2c(a−b) computed at a mixed-sign triad. `Vyatikrama` completes the parity table of `LeakageCommutator`: self-adjoint with skew-adjoint has self-adjoint commutator, proved in a bare ring with involution and no i to multiply by. `DhruvaMula` and `KatalanaSima` supply the finite ladders (shifted derivative kills degree below m and keeps the top coefficient; a quadratically majorised sequence is bounded by bⁿ·Catₙ·gⁿ⁺¹).

**Unplaced: fibre law applied to towers, networks, addresses, continua.** `EkagraSreni`: a compatible history is a chain of singletons, `Chain (suc n) t = Σ (p : singl (r n t)) Chain n (fst p)`, each rung contractible by `isContrSingl` with no hypothesis, so the compatible tower IS its top stage and the ambient product of readings counts something else; a cardinality from the product counts records before the compatibility equations. `Pratyabhijna` (recognitive cognition, सोऽयं देवदत्तः): a decentralised network is a list of finite-query observers and its pooled transcript is their concatenation; blindness is closed under append, so if every validator is blind on (x, y) then no post-processing whatsoever of the pool (majority, stake, reputation, appeal, any `List Bool → Bool`) separates them; `sees-exactly` makes the relation a network can see exactly the kernel of the pooled observation. Its §3 is the address split again: `Σ b. f a ≡ b` contractible (address of this object, no consensus needed), `Σ a. f a ≡ b` arbitrary (preimage), and content addressing = the assertion that the preimage is contractible, which decomposes into AVAILABILITY (inhabited) and COLLISION-FREEDOM (isProp), independent halves. `AbhilekhaAnirdhara`: an example-based specification does not determine the program, and the fibre over the spec carries an injection of ℕ (n ↦ multiply-by-n), so dedup keyed on observed behaviour collapses a copy of ℕ. `PatraLekha`: distinct-line patches commute (hypothesis of abstract 14 discharged on the model), and blame beyond the last writer is not a function of the working tree. `PunahPrapti`: redelivery is idempotent and deliveries at any two offsets commute, so at-least-once already has exactly-once's effect with no dedup store. `VitaranaYugma`: two senses jointly faithful pointwise are NOT jointly faithful on distributions, and what escapes is exactly correlation (the perfectly correlated and anti-correlated joints share both marginals). `StagewiseCompositeB`: determination of a composite by its stages, relativised to realised spans; both cells realised ⟹ no decoder; a two-valued codomain is always determined; supports compose iff |G| ≤ 2, so only the passage to supports costs and the G-valued ledger never does. `OrderAstiNasti` (four modules): the fourth corner (both सामयिक and नित्य denied) at one instance IS a counterexample to the double-negation shift `(∀x ¬¬A x) → ¬¬(∀x A x)`; enumerability of the INSTANCES is inert (one instance suffices), enumerability of the REMEDIES kills the corner outright (DNS holds over a list by induction); over enumerable decidable instance sets pointwise stability is a theorem; so the corner lives only over a non-enumerable remedy set with non-stable badness. `WhatTheSubstrateArgumentCovers`: in a `--safe` postulate-free development every inhabited type is a decision (`A → Dec A` by `yes`, not a fact about ⊎), strictly stronger than stability (`Stable ⊥` holds while ⊥ is empty), and the two deflations range over what is proved versus what is ¬¬-provable.

`SantataDhara` builds ℤ, ℚ, ℚ⁺, closeness and the HoTT §11.3 HIIT continuum from the prelude alone, limits as constructors so Cauchy completeness needs no choice; `PrecisionIsARepresentation` finds its defect by building on it: closeness is indexed by a REPRESENTATION (1+a)/(1+b) rather than a value, the limit constructors only produce closeness at indices `ε +⁺ δ` whose numerator is a successor, so at 1/4 closeness holds only between rationals and `rat q ≢ lim y` for every q, y: a limit is a fresh point nothing equals and the type is not the completion it says it is. The book's construction survives because its ℚ⁺ is a set of values. `ContinuumBridge`, `TranslationInvariance`, `TranslationOnTheContinuum` stack the analysis tower (translation by a rational is the first ℝ → ℝ map, 1-Lipschitz so no ε rescales) on that continuum with the defect still standing. `FinCardinality` (injection between finite sets of equal card is an equivalence, CRT as an equivalence of types, multiplicativity read off it; five controls, three negations) and `SubgroupIndex` (coset space, index, Lagrange with no choice of representatives because `card (fibre c) ≡ |H|` is a proposition; negation derived: no group of order 3 has a subgroup of order 2) are the counting layer the Γ₀(N) lane needs. `TheCardinalFormOfTheFoolingBound…`: the contrapositive "n rectangles cannot cover suc n fooling cells" is the WEAK sense of "the whole content"; the cardinal form `suc n ≤ m` for covers of arbitrary size needs a trichotomy plus an injection `Fin m ↪ Fin n`, and only the `gt` branch does work. `TheExchangeLemmaIsTheWholeOfPermTransitivity`: `Perm`'s choice of building transitivity into `pcons` costs exactly `insertSwap` (three clauses) and costs transport-computability (ten clause warnings, constructor injectivity of `_∷_` in index position), the same price twice. `Tantrayukti`: Agda refused a cyclic import between a claim and its refutation, and the refusal is a property of the medium (one direction of reference, no way to put the objection inside the thing objected to); the module supplies a type whose inhabitant is a completed पूर्वपक्ष/उत्तरपक्ष/निर्णय. `AbhihitanvayaAnvitabhidhana`: writing `C : Tm → D` at all already takes the Bhāṭṭa side (a term has a meaning in isolation); on the Prābhākara account only `Ctx → Tm → D` exists and `Compositional` is unstatable, not false; soundness `C p ≡ C q → CtxEq p q` is `cong` three times and the separating-context direction is free, while the converse pays enumeration plus discreteness plus full abstraction: one direction is a congruence, the other a search. `TheSecondQualifierConditionDoesAllTheWork` (Nyāya upādhi): the first condition is always satisfiable, the bare existential "some upādhi exists" equals "the pervasion fails", so only a NAMED upādhi is informative; provoked by an engine sampler that declares terms equal on 40 values mod 9. `TheSeparationQuestionIsVacuousUntilGeneralisationIsRequired`: a lookup table satisfies any "fires on these, not on those" predicate, so no enlargement of the evidence list separates a rule from a table; the content is off-list behaviour and base rate (Vapnik–Chervonenkis, and before that the Nyāya requirement of vyāpti over sapakṣa lists). `Vyapti`: the installer's G3 license (identity in the free commutative ring survives every substitution) is the substitution lemma `eval (subst t σ) ρ ≡ eval t (eval∘σ at ρ)`; `Utthapana`: G7's grid license is "a polynomial with more distinct roots than coefficients is the zero function", by Horner quotient and ℤ an integral domain. `Window5Walsh` replaces a retired Python script with 32-pattern exhaustive `refl`s: exactly ten masses vanish, the de Bruijn flow is conserved (planted-false control breaks it), all odd-order Walsh coefficients vanish, and the endpoint flip that the printed argument said changes a zero probability fixes it. `ThueMorseSat` closes the existence half: `tm n = tmFuel n n` by structural recursion on fuel, `solutions ≃ Bool` via `ε ↦ ε 0`. `PairCoordinates`: centre, product and gap are coefficient/discriminant coordinates of one quadratic over an arbitrary commutative ring, so the "striking" reappearance at three levels is one theorem instantiated twice. `WhereTheCircleSplits`: if −1 is a square the circle is two crossing lines and the additive law was multiplication all along; excluded over ℤ, and this split/inert question is the two-squares problem and the cakravāla's whole labour. `KFlowWF` removes fuel: termination of a contracting flow depends only on `WellFounded _⊏_`, and the sharp converse is about the one orbit (some step strictly decreases) not the machine. `Prastuti`: BARRIER's three independent presentations of the Liouville problem generate 2³−1 = 7 combinations by `Saptabhangi`'s equivalence, and its table lists three; Tao's entropy decrement escaped by changing the interface (reading λ(pn) = −λ(n)), never by computing harder inside one. The exact quantum modules (`ExactHadamardInterference`, `ExactProjectivePhase`, `ExactTwoStateInstrument`, `HadamardReadoutInstrument`, `SequentialHadamardReadout`, `PauliJointPhaseRealization`, `ResponseCharacterKickback`, `PhysicalLearningCore`) keep everything in Gaussian integers with unnormalised Hadamard (norm scales by exactly 2), outcome selection as explicit input data rather than a stochastic sample, Z4 global phase as a checked quotient, and the Bool joint-phase seam realised as the central sign sector of the two-qubit Weyl presentation; sign characters of ℤ/3 are all trivial, so additive-trit kickback has no one-query realisation. Historical arithmetic (`Dvikarani`, `Trikarani`, `GhanaBaddha`, `Vargacitighana`, `Shredhi`, `SamasaDvi/EkAmsa/Nyuna`): Baudhāyana's 577/408 for √2 solves x²−2y²=1 and is generated from (3,2) by Brahmagupta's bhāvanā eleven centuries later; Āryabhaṭa's three saṅkalitas in closed subtraction-free form; Nārāyaṇa's part-set generating recurrences.

**Mula: the machine as a groupoid, kevalajñāna as isEquiv, and the three no-decoder theorems tight at ℤ/2.** `Machine`: the computer is the groupoid of proofs-of-transport `A ≃ B`, five laws by `equivEq refl`, running a program IS transport along its path, every run has an inverse run, composition then running equals running each in turn (a functor to functions); a classical computer's operations form a monoid and the missing inverse is the heat, but nothing physical is derived here and the typal content of "erases nothing" is that the collision type is empty for an equivalence. `AvrttiResidue` is the exact counterweight: the kernel's derivations fill the monoid interface strictly and REFUSE any inverse whatsoever (`(inv : Derivation A A → Derivation A A) → ((d) → d ⊕ inv d ≡ done A) → ⊥`), on the same `MonoidMachine` record that `A ≃ A` fills as a groupoid; the difference is invisible to meaning because `Meaning a b` is a proposition; and an invertible groupoid is not thereby braided (two involutive self-equivalences of Bool³ fail Yang-Baxter). `Kevalajnana`: a mind's grasp is `f : A → B`; loses nothing = every fibre a proposition (injective); misses nothing = every fibre inhabited (surjective); both = every fibre contractible = `isEquiv f` on the nose; kevalajñāna is the one position that may assert "all अस्ति" without being a durnaya because it is pramāṇa, not a naya. `Jiva`: karma is the fibre defect (ज्ञानावरण = empty fibre, मोहनीय = crowded fibre), liberation is the equivalence, and by univalence the freed holding and the world are one path `धारणा ≡ विषयः`. `Khahara`: the constant map's fibre is the whole domain (`fiber शून्य-हरः अनन्त ≃ ℤ`), and every endomorphism conserves f ⟺ f is constant, so the loss-symmetry scale runs from trivial monoid at zero loss (Dhruva) through `Fin (suc n)` (Lopa) to the full endomorphism monoid at total loss, the two ends meeting only on a proposition; `SamraksakaGana` makes it a scale of MONOIDS with the section identification a `MonoidEquiv` by refl, the convolution of sections `(s ⋆ t) a = (s (t a .fst) .fst, s (t a .fst) .snd ∙ t a .snd)` carried onto flow composition. `Returnm`: the step is the conjugation `अवतरण ∘ Φ ∘ उत्थान`, Φ is dummy, the whole infinite run is one transport and no fibre is collapsed. `Apunaragamana`: composing with Brahmagupta's (3,2) strictly increases the denominator forever, so the bhāvanā orbit never returns; an irrational rotation's coding is Sturmian, aperiodic at minimum complexity, finite rule with a provably non-returning orbit; a system with nothing left unreturned is dead (isEquiv ⟹ every conserving symmetry is the identity), and an instrument that treats one-way edges as debt reports exhaustion as progress. `YugmaPurana`: three no-decoder theorems in two languages by three unrelated arguments are all sharp at exactly ℤ/2, because every step matrix has determinant −1 (the vallī's endpoint matrix recovers length mod 2 and no more), so their +2 paddings HAD to be even; the surviving THAT is ℤ/2 precisely. `Pata`: receipt (`A ≃ Carrier f`, unconditional) versus address (needs the map to be an identification); मात्रा is carriable but not invertible (लघु लघु and गुरु both weigh 2) while उद्दिष्ट/नष्ट exchange base and carried; every content-addressing scheme is a bet that its naming map is an address and not a receipt. `NastaUddista` / `Pingala` / `PingalaPrastara`: generate rather than store (2ⁿ patterns at zero space, log n each direction), extended to per-place radices as the machine's own rank/unrank; one `Pattern` type with three gradings gives Piṅgala's 2ⁿ, Virahāṅka's additive sequence and Halāyudha's triangle, each recurrence FORCED for any counting function rather than read off a definition. `SaptabhangiNaya` / `SaptabhangiSamyoga` / `AnuktaAvaktavya`: the kernel log records the SAME claim in the SAME round both denied and affirmed under two nayas (`refl` vs `induction on x`), which is the third bhaṅga in krama; the joint content is well-defined, decidable, realised and NOT the denotation of any single standpointed utterance (avaktavyam); succession composes associatively, simultaneity does not, avaktavya is not reachable by succession (a positive fourth position, supplied not derived), asti and nāsti have no meet (it lands on the eighth profile, no predication); and the un-said of the honest machine is सामयिक (∀ instance ∃ remedy) while avaktavya is नित्य (∀ remedy ∃ surviving instance), a swapped quantifier one word had hidden.

**must_fail: the controls that pin dropped quantifiers.** Eleven files that MUST fail to typecheck, excluded from the root aggregate so they may, each recording its expected error verbatim with line and reason. `QuantifierDrop` (line-world corollary stated for every observable instead of `f = X+Y`: the checker holds `c₁ f + c₂ f · s` apart from `1 + s`), `InflationFlattened` ("enlargement" without "along a quotient": `k0 != kι`, restriction along the subgroup kills the class), `MaximizerWithoutNonvanishing` (the checker names `NonVanishing W` in the position the summary deleted it), `ReachabilityWithoutStart` (`st != s0`), `SatisfactionWithoutCodomainAgreement` (`Y q !=< Y′ q`, a TYPE failure at the equation, not a failed proof), `FunctionBoundFromConstant` (two Mersenne witnesses bound the constant, not every function: `2 != 1` is Φ₁₁(2) = 2047 = 23·89), `InjectivityNecessary` (sufficient promoted to necessary: `one != two`), `UnguardedParasparasraya` (the vicious circle with no guarding constructor; the distance between defect and definition is one guarded constructor), `WrongEquivalence` (ℕ ≃ raw digit words without canonicity), `WrongFirstStep` and its tactic-free twin `WrongFirstStepNoTactic` (asserting `refl` for `ResidualIs tickCap` gives `0 != 1`; the twin carries a positive guard so it must fail at §Z2 and NOT at §Z1, because a control that fails for the wrong reason is not being tested, and an exit code cannot tell a control doing its job from a control that never reached its own line). The common sentence across all eleven: the defect has NO LEXICAL SIGNATURE, the flattened sentence contains no wrong word, only a missing one, so grep cannot see it and a type can.

**fibre: the law and its library.** `Carrier`: `A ≃ Carrier f` for every f because `Σ b. f a ≡ b = singl (f a)` is contractible; carried and witness are "syntactically present, informationally determined"; two implementation facts not to regress: `Cubical.Data.Sigma` not `Prod` (eta), and `descend` must not pattern-match. `Residue`: Carrier and the residual are the two projections of one graph `Γ f = Σ a. Σ b. f a ≡ b`; source projection always an equivalence, target projection's fibre is `शेष b`, contractible exactly when f is; which end you stand at decides which theorem you get; `Bool → Unit` prices the residue at exactly one bit. `Trace`: any conservative factorisation `A ≃ Σ b. Trace b` whose visible map is read OFF the equivalence has `Trace b ≃ fiber run b`, so the fibre is the ONLY residue up to equivalence and "keep a smaller trace" was a factorisation of a different computation (the `Bool → Bool` constant-true counterexample). `LawfulStep`: a lawful step is not an invertible step; it is `next`, `Residue`, `materialises : A ≃ Σ a'. Residue a'`, and `visible` tying the projection to `next` (without which the record asserts nothing); `collapse` (n ↦ 0) is lossless BECAUSE its residue is large; `suc` is the empty fibre and `isContr` cannot tell the two apart. `WholePartialDesa`: the census `देश f b` with constructors carrying evidence (empty / contractible / crowded), a diagnosis being `B → देश f b` pointwise rather than a verdict about the map; refutes in both directions the sequential diagnostic "the first non-contractible fibre is where the information went". `Adharmin`: the fourth outcome (no subject) is not a value of `देश`, which is indexed by f and b and so presupposes both; it lives one level up on the QUESTION, matching `Sthana = Position Bhanga | ADharmin`; a boolean verdict over four names merges "no subject" with "no witness" or "nothing lost" with "the loss". `Universal`: the classifier `(Σ E. E → A) ≃ (A → Type)` re-exported with its `rightInv` being `ua`; `invisible ≃ contractible` OVER THE BASE as a path between propositions (an abstract `Σ Bool Br ≃ Bool` can hold with one empty and one crowded fibre cancelling numerically); a tower of families of any finite height flattens to one family by a computed `flatten`; `fiber π X` lives one universe up, which is the size of the classifier and not a defect. `Orbit` / `Nucleus` / `JivitaSmrti`: path equality of orbits IS bisimulation (`(x ≡ y) ≡ (x ≈ y)`, four coinductive definitions); carrier commutes with the whole infinite trajectory, the transport version needing `uaβ` because transport along `ua` does not reduce on a neutral variable; the carried observable is recomputed at every depth, never a stale payload; `--guardedness` alone is safe. `Interaction`: the ISC coalgebra `react : (q : Q w) → Σ w'. Σ o. E w q w' o × ISC w'`; the orbit is the one-query case, all strategies get the same answers there, and `counter` separates strategies at the first step, so the generalisation is proper and the demand is what differs. `Order`: a commutation certificate turns any interleaving into `iterate f (countL w) (iterate g (countR w) a)` (Mazurkiewicz quotient), and its failure is computed (`suc` and `double` give 2 and 1 at equal counts), so the sequence is kept with a proof rather than an assumption. `KuttakaValli`: all three slots (side, magnitude, remainder) are base, three theorems exhibiting the non-determination; what is determined is the pair; keeping the side is what makes the subtractive step decision-free (no `Dec`, no `Bool`). `Composition`: Brahmagupta's क्षेप is carried by the roots and multiplies under bhāvanā, the identity proved from twelve ring lemmas present identically in v0.5 and later so the file pins to neither. `Sthanivadbhava` / `PositionalCount` / `BhittiPositional`: 1.1.56 read as an arity count (the substituted form is the only free slot; sthānin and designations ride carried), the अल्/अनल् split IS the base/carried split (`वाहक-अन्धः` by refl; `एच्-न-वाह्यम्` provably no carried function agrees with a form-reading rule), the law derives blindness across substitutions definitionally and NOT blindness between a substitute and its original (extra hypothesis `स्थानी v ≡ रूपम् v` named), designation constancy along the whole orbit as one bisimulation path; the state space is exactly 81 not 81·9 because the carried datum adds no cardinality; and the wall `¬(आधार ≃ Bool)` crosses its own ford. `TheCarrierIsTheMotive`: `f = realize ∘ to-motive` by refl, so every map is its own realisation precomposed with a lossless lift, and n realisations of one motive mediate the n² translations through the middle. `CorpusNorm` / `CorpusRefs`: normalisation of every declaration's checked type is an observation whose Nerode kernel (group by normal form) is the greatest congruence for that observation, one normalise per declaration and no pairwise work; the total reference relation is a linear syntax traversal.

**Design consequence collected here.** The runtime's step is a conjugation `descend ∘ Φ ∘ ascend` and its memory is the residue family, which is forced to be the fibre; there is no smaller trace to keep, and any "compressed" trace is the trace of a different computation. Every step record carries four fields, and the fourth (the projection IS the visible step) is what stops the record from being vacuous. The census over each output point is a term with three constructors, and the unposed question is a fourth outcome on the question, not the answer: the runtime's verdict type is `Position Bhanga | ADharmin`, never Bool. Content addressing is two independent assertions (available, collision-free) that the runtime must carry as separate proofs, and hashing is the bet that a receipt is an address. A network of validators sees exactly the kernel of its pooled queries, so consensus never adds a separating query; only a new probe does. Every kernel in the tower plateaus, so descent lives in transport along the tower, and the three Clay-shaped faces are one record with three unsupplied step fields. The controls directory is part of the build: eleven files that must fail, each with its expected error verbatim, and a control that stops failing is itself the defect. Everything invertible is a groupoid and costs nothing; everything the kernel keeps is a monoid that refuses every inverse; meaning cannot see the difference and cost is nothing else.
