# Compiler-generated conductive entry smoke test.
#
# There is intentionally NO import and NO source-level fibre call.
# Target.HVM4Full must emit a canonical @__whole_main automatically.

def main() -> Nat:
  2n
